# nginx converter status

## CURRENT

- Pin exactly one nginx 1.29.7 oracle for #305: the accepted root-only proxy
  fragment receiving one fresh depth-zero cleartext H1.1 origin-form
  explicit-close header-absent `TRACE /`. Prove the fixed response/log/EOF and
  live/settled zero upstream before changing converter activation.

## NEXT

1. If the #305 oracle matches the closed generic capability, extend the closed
   nginx semantic profile activation/lowering to the root-only model with golden
   and ownership regressions.
2. Independently review and run targeted converter tests.
3. Add one converter-generated pinned-nginx differential before claiming support.

## BACKLOG

- Account for the reproducible one-byte-per-verification LLVM
  `LLVMVerifyModule` LeakSanitizer allocation in JIT loader tests: #304. Existing
  and new loader cases reproduce it; it is tooling evidence, not a #303 semantic
  blocker or owned-policy leak.
- Provision the exact pinned nginx image for required-mode standard CI, or move
  those tests to a dedicated provisioned job: #297. Missing-image required-mode
  failures must remain explicit and must not be treated as semantic success.
- General prefix, exact, `^~`, regex, and nested location selection.
- Multiple servers, listen/default-server selection, and `server_name`.
- Pinned nginx semantics for absolute-form targets; generated programs now route
  a miss to the bounded ANY fail-closed 400, which is not an equivalence claim.
- Variables, rewrite phases, internal redirects, upstream groups, TLS, static
  files, error pages, and logging compatibility.
- Enforce the declared generic HTTP/1 pipeline depth limit: #285. The runtime
  constant is currently not enforced by `pipeline_shift()`; this does not block
  #278 phase 1 because its staged predicates admit only depth exactly 1.

## BLOCKED

- broader nginx default upstream request behavior: #252.
- broader nginx default proxied response behavior and differential evidence: #253.
- upstream invalid-status/HTTP/0.9 fallback semantics: #266.
- timeout-cause-specific forward failure policy selection: #267. The discovered
  SocketCreate, ConnectSubmit, and asynchronous ConnectCompletion configured-502
  paths and sequential ordinary-RUT unavailable-upstream production wire are
  implemented; broader issue scope remains tracked separately.
- exact escaped-byte syntax for ordinary local-response bodies: #279. String
  response bodies retain `\\n` as two literal bytes while failure-policy byte
  strings decode escapes. This does not block #276, whose static body is arbitrary.
- ordinary local-response HEAD body suppression: #281. This is separate from
  #280 persistence and does not block the current H1.1 GET production slice.
- ordinary local-response HTTP/1.0 status-line fidelity: #282. #280 may correct
  H1.0 connection lifecycle, but full HTTP/1.0 wire equivalence remains blocked.
- ordinary local-response 205 no-content framing: #283. This is separate from
  #280 and excluded from the current 200 GET slice.
- global proxy request Transfer-Encoding validation: #284. #280 classifies
  ambiguous framing conservatively for local-response reuse only; faithful proxy
  rejection/forwarding and nginx differential evidence remain separate.
- per-forward upstream read-timeout timing semantics: #268.
- proxied response buffering and downstream commit semantics: #271, including
  initial coalesced complete-response plus same-batch clean-EOF settlement.
- nginx `proxy_read_timeout` parser/lowering/differential support: #270,
  lowering remains blocked by #268's missing all-method/body domain and #271's
  default-buffering mismatch; bounded parser recognition and fail-closed
  converter rejection are complete.
- broader reusable keep-alive `HEAD` failures: timeout behavior and the
  remaining #253 response semantics still depend on #265; invalid-status
  HTTP/0.9 fallback is separately #266. The epoll prerequisite #262 and
  recv-retirement capability #264 are closed. The incomplete-EOF converter
  differential is complete.

## RECENTLY COMPLETED

- #302 converter-generated pinned-nginx differential landed in `bc70e2b3` after
  one bounded provenance-guard `REQUEST_CHANGES` round and final independent
  `APPROVE`. One shared accepted exact-root fragment feeds nginx and only the
  independent parser/model/lowering-generated ordinary RUT. Distinct fresh TRACE
  clients independently match the fixed Date-only-normalized 405/CL157/full-body/
  close/EOF wire, then cross-equal; both live 500ms and settled recorders prove
  zero upstream. Sol passed the exact-local baseline plus required converter
  differential 2/2 in 12.41s and full pinned-Docker differential 1/1 in 49.42s.
  #302 is closed and only its explicit bounded row is `SUPPORTED`; broader
  root-only TRACE compatibility is separately tracked by #305.
- #302 parser/model/lowering/golden increment landed in `27f3e09e` after one
  four-finding `REQUEST_CHANGES` round and final independent `APPROVE`. A closed
  nginx semantic profile records the implicit pre-location TRACE policy and
  lowers only the accepted exact-root fragment to ordinary `pre_route TRACE`.
  Unknown/missing/out-of-scope profiles and full-span forgeries fail closed;
  measured max source capacity covers ports 65535, IPv4 255.255.255.255 and the
  64-byte body. Compiler/config evidence proves post-lifetime owned TRACE 405
  policy bytes and semantic dedup with CONNECT. Sol passed the lightweight full
  nginx parser/converter binary 31/31 with 1040 checks. Behavioral differential
  evidence was still absent at that increment.
- #303 ordinary-source production evidence landed in `cc6b4381` after independent
  `APPROVE`. One hand-authored `.rut` is loaded only through the public compiler/
  loader/JIT/RouteConfig path and executes the generic pre-route TRACE 405 in the
  real io_uring frontend before an exact local 200 and live root forward. The
  exact nginx-shaped CL157 body/close/EOF wire, exactly-once lifecycle and a live
  500ms zero-upstream window are proven. Sol passed the focused TRACE test 1/1
  with 190 checks, adjacent PATCH 1/1 with 168 checks, and full Integration 1/1
  in 201.52s. The bounded generic RUT capability is complete; #302 is unblocked,
  but converter lowering and nginx-vs-generated-RUT evidence remain absent.
- #303 ordinary RUT source/compiler/loader exposure landed in `edc7346a` after
  independent `APPROVE`. Contextual concrete-only `pre_route METHOD` crosses
  AST/HIR/MIR/RIR/verifier/printer without a JIT route and atomically installs
  the Phase 1 table. Combined source exactly-once validation, parser rollback,
  import/ANY/HEAD/forgery rejection, copy/move/rebase, safe printing, semantic
  dedup/remap, deep ownership and loader rollback are covered. Sol fresh
  sanitizer frontend/RIR passes 5/5 with 115 checks; loader ASan/UBSan passes
  2/2 with 69 checks under the existing CI leak-scan policy. Full RIR/frontend/
  loader passes 3/3 in 73.54s. LSan separately reproduces the external LLVM
  allocation in old and new loader cases; #304 records it. Production was still
  absent at that increment.
- #303 internal RouteConfig/runtime Phase 1 landed in `77f19b7d` after one
  `REQUEST_CHANGES` round and final independent `APPROVE`. A generic concrete
  method table reuses owned strict policies and executes, after bounded admission
  plus existing firewall/metrics precedence, before exact/prefix/unmatched. H2
  matches fail closed with zero frames, including prepared-forward ownership;
  nonmatches retain legacy behavior. Cross-table dedup/remap/copy/rollback,
  owner/method forgeries, fixed TRACE 405 and non-TRACE OPTIONS, metrics and
  independent unmatched precedence are deterministic. Sol fresh H2 sanitizer
  passes 1/1 with 93 checks, adjacent RouteConfig/H2 passes 2/2, and full Network
  passes 1/1 in 18.05s. Source/compiler/loader and production were separate later
  increments.
- #303 design received independent `APPROVE_DESIGN`. The selected generic Option
  A adds a concrete-method `pre_route` policy table that reuses strict response
  policies/bytes/serializer and executes after bounded admission plus existing
  firewall/metrics precedence but before exact/prefix/unmatched selection. ANY
  is forbidden; all-zero metadata preserves legacy behavior. A pre-route-only
  config must not masquerade as unmatched merely because the policy pool is
  shared. Option B's phased replacement of unmatched was rejected as needless
  migration. No new prerequisite issue was found; implementation is split into
  internal runtime activation, source/compiler exposure, production, converter,
  and differential increments.
- #302 pinned-nginx-only TRACE baseline landed in `3b328912` after independent
  `APPROVE`. Under exact-before-root and root-before-exact declarations, distinct
  fresh clients independently receive the fixed ordered 405/CL157/full nginx
  HTML body/close/EOF wire, with Date-only normalization and cross-order equality.
  Each order has exactly one scoped TRACE 405 access record and no TRACE upstream
  connect; only the two existing `/static/` fallback requests connect. Sol
  required mode passes 5/5 and full pinned-Docker differential 1/1 in 48.15s.
  This fixes only the nginx oracle; #302 remains blocked by #303.
- TRACE research established that this is not another exact-local 200 method
  increment. nginx 1.29.7 source rejects TRACE globally with 405 during request
  processing before location selection, while current RUT selects the matched
  exact ANY route before unmatched handling and then rejects TRACE with no
  response. #302 and generic capability dependency #303 now preserve the gap;
  exact pinned-nginx wire/log evidence remains CURRENT and no workaround landed.
- #300 converter-generated exact PATCH differential landed in `8b9a380d` after
  independent `APPROVE`. One shared accepted fragment drives pinned nginx and
  only parse/model/lower-generated ordinary RUT. Distinct fresh clients on both
  sides independently match the fixed ordered Date-only-normalized 200/CL16/
  full-body/close/EOF wire, live/settled zero-upstream state, then cross-equal.
  Generated source retains the method-omitted exact route/root forward and rejects
  PATCH-specific route/token/workaround. Sol required mode passes 3/3, adjacent
  baselines 2/2, and full pinned-Docker differential 1/1 in 48.38s. Only this
  bounded row is `SUPPORTED`.
- #301 ordinary-source production evidence landed in `1fe624c5` after independent
  `APPROVE`. One hand-authored ordinary `.rut` traverses loader/HIR/MIR/RIR/JIT/
  RouteConfig and real io_uring, retains a live root forward fallback, and serves
  the bounded fresh exact PATCH with fixed wire/EOF, exactly-once lifecycle, deep
  config ownership, and a live/settled zero-upstream recorder. Sol focused PATCH+
  PUT production passes 2/2 with 336 checks; full Integration passes 1/1 in
  202.91s. #301 is closed and #300 is unblocked; converter/nginx equivalence is
  still absent, so the row remains `PARTIAL`.
- #301 atomic PATCH activation landed in `5796ed78` after independent `APPROVE`.
  The closed proof adds only Patch/PATCH behind explicit wrappers and evaluates
  PATCH after PUT. All six DELETE/PUT/PATCH cross-pairs, unsupported methods,
  query/framing/body/tail/persistence/depth/protocol/target and owner forgeries
  remain fail-closed; existing method branches remain unchanged. Sol normal and
  ASan/UBSan focused coverage passes 9/9 with 2710 checks each; full Network
  passes 1/1 in 17.60s. Ordinary-source production and converter equivalence
  remain open.
- #301 design selected an exact extension of the existing closed fresh-method
  truth table and received independent `APPROVE_DESIGN`: accept only Delete/
  DELETE, Put/PUT, and Patch/PATCH diagonals; retain explicit wrappers and branch
  order common, CL0 POST, DELETE, PUT, PATCH, reject. All cross/unsupported pairs
  and every shared fence remain closed. No state/ABI/parser/source/compiler/
  config/simulation/serializer/converter change is needed.
- #300/#301 pinned-nginx-only PATCH baseline landed in `149aaafe` after
  independent `APPROVE`. Distinct fresh PATCH clients under both declaration
  orders independently match the fixed Date-only-normalized ordered 200/CL16/
  full-`successor-static`/close/EOF oracle and cross-equal. Exactly two upstream
  failures remain solely `/static/` and `/static/child`. Sol focused required
  mode passes 5/5. The first monolithic run failed closed in the pre-existing
  coalesced io_uring gate with hook error 6 and zero wire/semantic observation;
  no fail-close rule changed, and a second full run passes 1/1 in 47.46s. This is
  nginx-only evidence; #300 remains blocked by #301.
- #298 converter-generated exact PUT differential landed in `e334c84d` after
  independent `APPROVE`. One shared accepted root-before-exact nginx fragment
  drives pinned nginx and only parse/model/lower-generated ordinary RUT. Both
  sides use distinct fresh clients and independently match the fixed Date-only-
  normalized ordered 200/CL16/full-`successor-static`/close/EOF wire through a
  live/settled zero-upstream recorder and final cross-equality. Generated source
  retains the method-omitted exact route/root forward and rejects PUT-specific
  route/token/workaround. Sol required mode passes 3/3, adjacent baselines 2/2,
  and full pinned-Docker differential 1/1 in 47.34s. Only this row is `SUPPORTED`.
- #299 ordinary-source production evidence landed in `a1e8c2fc` after
  independent `APPROVE`. One hand-authored ordinary `.rut` traverses loader/HIR/
  MIR/RIR/JIT/RouteConfig and real io_uring, retains a live root forward fallback,
  and serves the bounded fresh exact PUT with fixed wire/EOF, exactly-once
  lifecycle, deep config ownership, and a live/settled zero-upstream recorder.
  Sol focused PUT+DELETE production passes 2/2 with 336 checks; full Integration
  passes 1/1 in 212.48s. #299 is closed and #298 is unblocked; this is not yet
  converter/nginx equivalence, so the row remains `PARTIAL`.
- #299 atomic PUT activation landed in `f24c2157` after independent `APPROVE`.
  A closed shared proof accepts only exact Delete/DELETE and Put/PUT pairs behind
  explicit wrappers; common and CL0 POST branches remain unchanged and PUT is
  evaluated last. Cross-pairs, PATCH, query, all framing including CL0, body/tail,
  persistence/reuse/depth-one, protocol/target and forged owner states remain
  fail-closed. Sol normal and ASan/UBSan focused matrices pass 13/13 with 2169
  checks each; full Network passes 1/1 in 17.73s. This is deterministic runtime
  evidence only; ordinary-source production and converter equivalence remain open.
- #299 design selected closed-pair Option A and received independent
  `APPROVE_DESIGN`. The complete DELETE proof is shared only for exact
  Delete/DELETE and Put/PUT metadata/raw pairs behind explicit wrappers; branch
  order remains common GET/HEAD/POST/OPTIONS, CL0 POST, DELETE, PUT, reject.
  PATCH and forged cross-pairs remain closed. No new state, ABI, parser metadata,
  compiler/config/source/serializer/simulation/converter change is needed.
- #298/#299 pinned-nginx-only PUT baseline landed in `c16256b4` after
  independent `APPROVE`. Distinct fresh header-absent explicit-close PUT clients
  under exact-before-root and root-before-exact declarations independently match
  the fixed Date-only-normalized ordered 200/CL16/full-`successor-static`/close/
  EOF oracle and cross-equal. Exactly two scoped upstream failures remain solely
  `/static/` and `/static/child`, proving PUT has zero upstream effects. Sol
  focused required mode passes 5/5 and full pinned-Docker differential 1/1 in
  46.38s. This is nginx-only evidence; #298 remains blocked by #299.
- #296 converter-generated exact DELETE differential landed in `c1a60ed6` after
  independent `APPROVE`. One shared accepted root-before-exact nginx fragment
  drives pinned nginx and only parse/model/lower-generated ordinary RUT. Both
  sides use distinct fresh clients and independently match the fixed Date-only-
  normalized ordered 200/CL16/full-`successor-static`/close/EOF wire through a
  live 500ms zero-upstream window, settled empty recorder state, and final cross-
  equality. Generated source retains the method-omitted exact route and root
  forward and rejects any DELETE-specific route/workaround. Sol focused required
  mode passes 3/3, pinned baseline 1/1, generic exact regression 1/1, and full
  pinned-Docker differential 1/1 in 46.47s. Only this bounded row is `SUPPORTED`.
- #295 closed after CI supplied the deferred full-suite production gate for
  `edac3281`: `test_integration` passes in gcc Debug (178.00s), clang Debug
  (170.28s), and gcc Release (166.73s). The red general Build & Test jobs are not
  a DELETE regression: required-mode pinned-nginx differentials report missing
  local image as failure, and the first such failure cancels remaining matrix
  work. Together with focused 5/5, the adjacent CL0 regression, and independent
  `APPROVE`, the generic capability is complete; #296 is now unblocked.
- #295 Phase 3 tests-only ordinary-source production evidence landed in
  `edac3281` after independent `APPROVE`. One hand-authored ordinary `.rut`
  traverses loader/HIR/MIR/RIR/JIT/RouteConfig and a real io_uring shard, retains
  a live root forward fallback, and serves only the bounded fresh exact DELETE
  with fixed wire/EOF, exactly-once lifecycle, deep config ownership, and a live
  zero-upstream window. Luna focused stability passes 5/5 with 168 checks each;
  the adjacent CL0 POST production regression passes 1/1. CI subsequently ran
  full Integration successfully in three compiler/build configurations.
- #295 Phase 2 atomic DELETE activation landed in `ff57d977` after independent
  `APPROVE`. A dedicated last-evaluated branch admits only fresh completed-
  count-zero cleartext H1.1 origin-form header-absent DELETE with no query,
  framing, body, or tail and exactly one normalized `Connection: close`, under
  all existing strict owner fences. Sequential/depth-one, query, CL including
  CL0, TE/Expect/Upgrade, body/tail, persistence, non-origin, H1.0/TLS/H2, and
  forged states remain fail-closed; existing GET/HEAD/POST/OPTIONS and CL0 POST
  branches remain unchanged. Sol normal and ASan/UBSan focused matrices pass
  9/9 with 1216 checks each; full Network passes 1/1 in 17.53s. This is
  deterministic runtime evidence only: ordinary-source production and
  converter/nginx equivalence remain open.
- #295 Phase 1 generic fresh-transport witness landed in `63fc5704` after
  independent `APPROVE`. A loop-thread-owned saturating downstream completed-
  request count resets on new allocation/slot reuse, survives keep-alive request
  boundaries, increments only once under live completion ownership, does not
  advance on partial Send/EPIPE/duplicate completion, and copies coherently for
  same-transport simulation state. Fresh, sequential, and query DELETE remain
  fail-closed before Send/Connect/success accounting, so no capability is active.
  Sol normal and ASan/UBSan focused matrices pass 7/7 with 613 checks each; full
  Network passes 1/1 in 18.32s. No source/compiler/config/converter behavior or
  ABI changed.
- #295 independent design audit selected corrected Option A and
  `IMPLEMENT_NOW`. Depth-zero/generation-zero is not a fresh-transport witness:
  it also describes later sequential requests, while `handler_gen` persists
  across slot reuse. Phase 1 therefore adds only a generic saturating completed-
  request counter reset per downstream transport. Phase 2 will evaluate a
  dedicated DELETE branch last, requiring counter zero, exact close, absent
  framing/query/body/tail, and every existing strict owner fence. Existing
  GET/HEAD/POST/OPTIONS, depth-one GET, and CL0 POST branches remain unchanged.
  Policy admission metadata was rejected as disproportionate source/compiler/
  config/ABI surface. The selected design is recorded on #295.
- #296 pinned-nginx-only DELETE baseline landed in `fd94ec1b` after one
  wording-only `REQUEST_CHANGES` round and final `APPROVE`. Under exact-before-
  root and root-before-exact declarations, distinct fresh explicit-close
  header-absent `DELETE /static` requests independently match the fixed Date-
  only-normalized 200/CL16/full-`successor-static`/close oracle, require zero
  tail and real EOF, then cross-equal. Exactly two scoped proxy failures remain
  attributable only to `/static/` and `/static/child`, proving DELETE is local
  with zero upstream work. Sol focused required mode passes 5/5 and full pinned-
  Docker differential 1/1 in 45.62s. This is nginx semantic evidence only;
  RUT/converter equivalence remains blocked by #295 and #296 stays open.
- #294 converter-generated exact OPTIONS differential landed in `5552b9ea`
  after independent `APPROVE`. The shared accepted fragment drives pinned nginx
  and only parse/model/lower-generated ordinary RUT; generated source retains
  one method-omitted exact route and root forward with no OPTIONS-specific or
  generic-response workaround. Distinct fresh header-absent explicit-close
  `OPTIONS /static` observations independently match the fixed Date-only-
  normalized 200/CL16/full-`successor-static`/close oracle, exclude `Allow` or
  other extra headers by exact equality, require real EOF, remain live through
  separate 500ms zero-upstream windows, then cross-equal. Sol focused required
  mode passes 5/5, pinned baseline 1/1, generic exact differential 1/1, and full
  pinned-Docker differential 1/1 in 45.32s. #294 is closed and only this exact
  OPTIONS row is `SUPPORTED`; OPTIONS-star/CORS/Allow/proxy/query/framing/reuse/
  broader-method semantics and direct nginx.conf runtime remain excluded.
- #293 converter-generated literal query GET differential landed in `35b52e19`
  after one wording-only `REQUEST_CHANGES` round and final `APPROVE`. One shared
  accepted nginx fragment drives pinned nginx and only parse/model/lower output;
  generated source retains the method-omitted exact route and root forward with
  no query-specific or generic-response workaround. Distinct fresh
  `GET /static?x=1` observations on each side independently match the fixed
  Date-only-normalized 200/CL16/`successor-static`/close wire, real EOF, and a
  500ms live zero-upstream window, then cross-equal. Sol rebuilt after the
  wording fix, passed focused required mode 5/5, pinned baseline 1/1, generic
  exact-route differential 1/1, and full pinned-Docker differential 1/1 in
  44.28s. #293 is closed and only the literal query row is `SUPPORTED`; broader
  query/URI normalization, variables, rewrites, forwarding, reuse/pipeline,
  other methods/paths, TLS/H2, and direct nginx.conf runtime remain excluded.
- #292 converter-generated single-CL0 POST differential landed in `7f890688`
  after independent `APPROVE`. One shared accepted root-before-exact nginx
  fragment drives pinned nginx and only parse/model/lower-generated ordinary
  RUT. Each side independently serves a fresh depth-zero cleartext H1.1
  origin-form explicit-close POST with exactly one literal CL0, emits the
  Date-only-normalized exact 200/CL16/`successor-static`/close wire plus real
  EOF, and remains live through its own 500ms zero-upstream window with empty
  settled recorder state. Distinct reset observations, per-side fixed oracles,
  and final cross-equality prevent stale evidence. Sol focused passes 5/5,
  pinned baseline 1/1, causal E3 1/1, and full pinned-Docker differential 1/1 in
  43.27s. #292 is closed and only this explicitly bounded CL0 row is
  `SUPPORTED`; alternate CL, bodies, reuse/pipeline, other methods/paths, TLS/H2,
  broader locations, and direct nginx.conf runtime support remain excluded.
- #291 Phase 3 ordinary-source production evidence landed in `15abe91a` after
  independent `APPROVE`. One hand-authored ordinary `.rut` traverses the public
  loader/frontend/HIR/MIR/RIR/JIT/config path, retains a live executable root
  fallback, and serves a fresh exact `/static` POST with one literal CL0 through
  the production io_uring shard. It emits the Date-only-normalized exact
  200/CL16/`successor-static`/close wire plus real EOF; a 500ms live recorder
  window and settled history prove zero upstream effects. Exactly one
  connection/request/latency/access/capture episode, epoch 2, deep config
  ownership, and neutral connection/upstream/pool state are asserted. Sol
  focused passes 5/5 with 168 checks each, adjacent D1 1/1 with 98 checks, and
  full Integration 1/1 in 198.88s. The generic capability issue is closed and
  converter differential continues in #292. This is not converter/nginx
  equivalence; the compatibility row is only `PARTIAL`.
- #291 Phase 2 atomic activation landed in `d9ea49ea` after independent
  `APPROVE`. The generic strict-local path admits only POST with exactly one
  validated numeric-zero Content-Length, an exact single `Connection: close`,
  the depth-zero legacy boundary, and every existing cleartext H1.1 origin-form
  framing/body/tail fence. Header-absent behavior remains separate and
  unchanged; duplicate CL0, positive/malformed/conflicting CL, CL+TE, other
  methods, keep-alive, fragments, non-origin targets, Expect/TE/Upgrade, bodies,
  and successor bytes remain fail-closed. Lifecycle, partial Send, EPIPE,
  metrics, access, capture, epoch, teardown, and zero-upstream negatives are
  covered. Sol targeted normal and ASan/UBSan tests pass 4/4 with 538 checks
  each; full Network passes 1/1 in 18.04s. This is runtime capability only:
  ordinary-source production and converter differential evidence remain open.
- #291 Phase 1 generic Content-Length field-count identity landed in `eb9c0bb6`
  after independent `APPROVE`. H1 preserves 0/1/duplicate counts without changing
  existing duplicate-CL parser behavior; H2 bridges, connection capture/reset,
  pipeline reparse, and simulation same-request copies keep coherent identity.
  Forged half-states fail closed, and single/duplicate CL0 remain rejected before
  Send/Connect/upstream/accounting, so no capability is active. Sol normal parser
  29/29, H2 4/4, network 4/4 and ASan/UBSan parser 29/29, network 4/4 pass.
- #291 generic CL0 design selected explicit Content-Length field-count proof.
  Existing parser state loses identical-duplicate identity, so a `CL == 0`
  guard change would be unsafe. Phase 1 adds only parsed/current-request count
  ownership with all CL still rejected; Phase 2 may admit exactly one validated
  CL0 POST under the existing strict H1.1/close/depth/boundary fences. No source,
  compiler, config, reload ABI, or nginx-specific runtime mode is introduced.
- Pinned-nginx-only CL0 semantic baseline landed in `d809b4e5` after independent
  `APPROVE`. Under exact-before-root and root-before-exact declarations, one fresh
  explicit-close POST carrying exactly one valid `Content-Length: 0` emits the
  same Date-only-normalized exact 200/CL16/`successor-static`/close/EOF wire as
  the adjacent header-absent POST. `/static/` and `/static/child` each retain one
  scoped dead-port connect failure and total exactly two, proving all local
  vectors have zero upstream effects. Sol focused pinned-Docker baseline passes
  5/5. This is nginx semantic evidence only, confirms the current RUT gap, and
  opens generic capability issue #291; no converter equivalence is claimed.
- #290 tests-only converter-generated POST evidence landed in `0748c536` after
  one wording-only `REQUEST_CHANGES` round and final `APPROVE`. The existing
  GET/HEAD helper adds one fresh depth-zero cleartext H1.1 origin-form
  explicit-close `POST /static` whose byte-exact request has no Content-Length
  including CL0, Transfer-Encoding/TE, Expect, Upgrade, body, or tail. Both
  pinned nginx and parse/model/lower-generated ordinary RUT independently match
  the Date-only-normalized ordered 200/CL16/16-byte-body wire, real EOF, and a
  500ms live zero-upstream window with empty settled recorder state. Sol focused
  passes 5/5, E3 regression 1/1, and full pinned-Docker differential 1/1 in
  42.51s. This does not claim pipeline, TLS/H2, other methods, broader locations,
  or any explicitly framed request body. #290 is closed and draft PR #269 links
  the evidence explicitly.
- #289 tests-only converter-generated HEAD evidence landed in `19333625` after
  independent `APPROVE`. The existing shared-fragment GET differential now adds
  one fresh explicit-close `HEAD /static` on each side. Pinned nginx and only
  parse/model/lower-generated ordinary RUT independently match the exact ordered
  status-200 wire after Date-only normalization, retain `Content-Length: 16`,
  emit zero body bytes, then real EOF, and remain live through 500ms zero-upstream
  windows with completely empty settled recorder state. Source-shape assertions
  require method-omitted exact strict-local `SuppressBody`, the unchanged root
  forward, and no generic `response(...)` workaround. Sol focused passes 5/5,
  E3 regression 1/1, and full pinned-Docker differential 1/1 in 41.42s. This
  extends only the bounded exact row to GET plus fresh explicit-close HEAD. #289
  is closed; draft PR #269 links its evidence, and #281 remains open.
- #277-E3 converter-generated causal differential landed in `414601c2` after
  independent `APPROVE`. One shared accepted root-proxy plus exact-local-return
  fragment and one shared complete `R1||R2` wire feed both reviewed ingress
  helpers. RUT executes only parse/model/lower output as ordinary source. Before
  parser/dispatch each side independently retains the exact coalesced wire with
  zero downstream/upstream effects; release produces Date-only-equal nginx 502
  keep-alive then exact-local 200 close/EOF, zero tail, and exactly one scoped
  upstream attempt per side. Sol focused E3 passes 10/10, adjacent regressions
  4/4, and the full pinned-Docker differential 1/1 in 40.18s. One earlier raw-CQ
  `ERROR_CQ` failed closed without a false positive; recurrence remains a
  harness diagnostic risk. The independent close audit mapped #277 7/7 and #286
  4/4 acceptance criteria to committed evidence; both issues are closed, the
  bounded rows are `SUPPORTED`, and draft PR #269 links the evidence explicitly.
- #277-E2 generated-RUT coalesced-ingress gate landed in `7a6c5d03` after
  independent `APPROVE`. The converter-generated ordinary RUT production binary
  publishes one target multishot Recv owner before the controller's only full
  `send(R1||R2)`. The preload executes the real io_uring_enter, reconstructs the
  exact wire from unconsumed raw CQ/provided buffers, and blocks before runtime
  dispatch with zero Send, Connect, or downstream bytes. Release yields exact
  Date-normalized 502 keep-alive then exact-local 200 close/EOF. A live window
  and post-reap journal both prove exactly one correct dead-port Connect episode
  with no duplicate/overflow. No CQE, generation, provenance, or runtime state
  is forged. Sol new/legacy/negative RUT gate tests pass 6/6.
- #277-E1 pinned-nginx coalesced-ingress gate landed in `eb910ca9` after
  independent `APPROVE`. A pre-bound unique peer performs exactly one full
  `send(R1||R2)`. The nginx worker's first target recv proves the entire wire by
  non-consuming MSG_PEEK and independent real recv, then blocks before returning
  bytes to nginx parsing. At HIT, downstream bytes and dead-port connect failures
  are zero. Release produces exact Date-normalized 502 keep-alive then exact
  local 200 close/EOF with no tail and exactly one scoped connect failure at both
  live and post-reap boundaries. Legacy gate mode remains isolated. Sol focused
  nginx coalesced, legacy gate, and exact-local baseline tests pass 3/3. This is
  nginx-only causal infrastructure; E2 and E3 now compose it into the bounded
  converter differential.
- #286 single-request converter differential landed in `72b233ce` after
  independent `APPROVE`. One root-before-exact nginx fragment feeds both pinned
  nginx 1.29.7 and parser/model/lowering; RUT runs only the converter-owned
  ordinary source. A fresh explicit-close GET `/static` produces byte-identical
  status/header/body and real EOF after Date-only normalization. Independent
  live 500ms recorder/frontend windows and exact empty joined state prove zero
  upstream effects on both sides. Sol required-mode focused and full
  pinned-Docker differentials pass 1/1 in 1.67s and 39.10s. This is translation
  evidence, not direct nginx.conf runtime support. Its required #277 coalesced
  successor evidence is now committed in `414601c2`; the independently audited,
  explicitly bounded GET row is `SUPPORTED`.
- #286 exact-route lowering landed in `acc7fc67` after independent `APPROVE`.
  The converter validates the full exact inventory before output, rejects all
  partial/forged shapes and `/api/` fallback composition, and appends one
  method-omitted ordinary RUT `route exact "/static"` strict status-200 policy
  without changing legacy root bytes. Both nginx declaration orders generate
  identical source. The source traverses AST/HIR/MIR/RIR into a deep-owned ANY
  exact binding and policy in RouteConfig after nginx input, generated source,
  and compiler intermediates are destroyed. The 64-byte maximum model measures
  5200 bytes inside the new bounded 5440-byte converter buffer; legacy maximum
  remains 4899. Sol parser/compiler/config suite passes 1/1. No behavioral
  equivalence is claimed yet, so the row is only `PARTIAL`.
- #286 parser/model foundation landed in `728dffa3` after independent
  `APPROVE`. The model keeps the proxy fallback and optional exact local-return
  action in separate semantic roles, independent of declaration order. The
  accepted second location is bounded to exact `/static`, status 200, and a
  borrowed nonempty 1..64-byte token-safe quoted ASCII body. Explicit tests
  reject missing root fallback, third/duplicate/ambiguous locations, unsupported
  actions/modifiers/status/body forms, and every declared exclusion. Parsed and
  partially forged exact inventories fail converter lowering before legacy
  output, so no accepted directive is ignored. Sol parser/converter suite passes
  1/1. Lowering and behavior evidence remain CURRENT; the compatibility row is
  still `NOT_IMPLEMENTED`.
- #288 closed after final independent `CLOSE_APPROVED`. D3 `db6119aa` compares
  pinned nginx 1.29.7 with hand-authored ordinary RUT across exact
  GET/HEAD/POST/OPTIONS/query hits and `/static/`, child, and `/other` root
  fallbacks. Each side proves full Date-normalized wire/body/EOF, a live local
  zero-upstream window, exact backend method/target/Host/header effects, exact
  three-episode settlement, and a live no-fourth window. Sol required-mode
  focused and full pinned-Docker differentials pass 1/1 in 2.69s and 37.70s.
  `065c9229` additionally proves exact-active OPTIONS-star, CONNECT authority,
  and absolute-form targets fail closed before Send, Connect, accounting,
  access, or capture effects; Sol full Network passes 1009/1009 with 122,470
  checks. This closes only the bounded generic hand-authored RUT capability.
  nginx.conf parser/model/lowering remains CURRENT in #286, and #277 remains
  blocked on converter-generated evidence.
- #288 request-boundary production evidence D2 landed in `f5d93d12` after one
  bounded evidence-only `REQUEST_CHANGES` round and final independent
  `APPROVE`. One ordinary `.rut` source and real io_uring scheduler witness a
  complete coalesced R1+R2 before dispatch: R1 performs the only dead-port
  Connect and emits configured 502 keep-alive; R2 is the current-generation
  depth-one exact-close GET `/static?d2=two`, consumes the prior-owner proof,
  creates no JIT/upstream episode, and emits exact strict 200 close plus EOF.
  The negative completion is bound to the persistent retiring tombstone, the
  current token advances exactly once for R1 retirement, and both tokens remain
  stable through R2 settlement. Exact SQE, health, metrics, epoch, access,
  capture, pool, deadline, prebuilt, and retirement assertions exclude a second
  Connect or request3. Missing ingress evidence hard-fails rather than skips.
  Focused D2 passes repeatedly with 160 checks; Sol full Integration passes 1/1
  in 208.10s. This is still hand-authored ordinary RUT, not converter/nginx
  equivalence.
- #288 ordinary-source production evidence D1 landed in `7417a299` after one
  bounded harness-proof `REQUEST_CHANGES` round and final independent
  `APPROVE`. A real `.rut` source traverses frontend, JIT, transactional config,
  and an io_uring production shard. An earlier-declared root forward fallback,
  exact ANY policy, and exact GET policy prove exact-over-prefix,
  method-specific-over-ANY, query-boundary selection, and slash/child fallback.
  Local hits emit exact Date-normalized close wires and real EOF with a live
  500ms zero-upstream witness; fallbacks emit their explicit bounded-policy wire
  and EOF, record exactly two method/target/Host/header-normalized upstream
  requests, and survive a live no-third window. Recorder thread/listener
  liveness is explicitly witnessed. Sol full Integration passes 1/1 in 205.70s.
  This is hand-authored ordinary RUT, not converter output or nginx equivalence.
- PR CI's GCC 13.3 Debug ICE from #288-A's enlarged test `rir::Module` was
  repaired in `4f9d708c` after independent `APPROVE`. Two forged-metadata
  printer cases now use fresh heap-owned zeroed modules instead of large
  aggregate reassignments; all assertions and production semantics are
  unchanged. Focused GCC/Clang printer and full RIR tests pass; the pushed CI
  matrix is the final GCC 13.3 validation.
- #288 atomic runtime activation C landed in `35433c4e` after independent
  `APPROVE`. The loader now publishes a fully validated combined strict table;
  H1 selects raw exact path by method then ANY, ignores query only at the `?`
  boundary, and reuses the owned strict serializer. A full-target fragment
  witness prevents bounded-path false matches. Depth zero admits only one
  complete request, while the depth-one slice admits one current-generation,
  exact-close GET successor only after all prior owners settle. H2 exact matches
  discard the complete batch and close with zero bytes; clear nonmatches retain
  legacy prefix routing. Invalid/partial metadata fails before application
  effects. Owner-path, provenance-forgery, parser, H2, loader, and RouteConfig
  tests pass normal and focused sanitizers; Sol full affected suites pass 5/5 in
  20.31s. Production wire and nginx differential evidence remain in D, so #288
  and #286 stay open and the compatibility row is not upgraded.
- #288 runtime ownership foundation B landed in `d24971e3` after independent
  `APPROVE`. RouteConfig now owns the fixed exact table and installs policies,
  unmatched mappings, and exact bindings through one staged deep-copy,
  semantic-dedup, ID-remap, runtime-validation, and fieldwise commit transaction;
  the old unmatched API is an exact-empty wrapper. Loader rejection remains.
  Any hand-built valid, invalid, partial, over-capacity, or count-zero-tail exact
  inventory closes H1/H2 before firewall, built-in responses, routing, frame
  publication, or upstream effects; accounting and epoch settle exactly. Normal
  and sanitizer focused tests pass; Sol full Route ART, Network, HTTP/2, and
  Serve Loader suites pass 4/4 in 19.81s. Behavior remains inactive until C.
- #288 compiler metadata foundation A landed in `7b101957` after one
  tests-only `REQUEST_CHANGES` round and final independent `APPROVE`.
  `route exact [METHOD]` now parses one direct strict local-response into a
  padding-safe 72-byte binding carried losslessly through AST/HIR/MIR/RIR,
  with no executable route, function, or JIT artifact. The combined source
  validator enforces exact selector uniqueness, unmatched+exact policy
  exactly-once references, method/body compatibility, and completely neutral
  inactive inventory. Forged metadata prints safely; the production loader
  rejects any exact inventory before config mutation, so behavior remains
  inactive. Normal and sanitizer focused tests pass; Sol full affected RIR,
  Frontend, and Serve Loader suites pass 3/3 in 73.05s.
- #288's generic exact strict-response design received independent
  `APPROVE_DESIGN` after two `REQUEST_SPLIT` rounds. A padding-safe inline
  binding, separate source/runtime reference invariants, combined transactional
  install, mandatory full-target fragment witness, raw H1/H2 classification,
  and early invalid-metadata fences avoid prefix leakage and truncated-target
  false matches. Work is split into compiler metadata A, runtime ownership/fence
  B, atomic selector plus bounded depth-one exact-GET successor activation C,
  and production/differential evidence D. Broader pipelines and nginx location
  semantics remain excluded.
- PR #269's GCC Debug io_uring gate startup crash was repaired in `80f372c0`
  after direct SIGSEGV/GDB/disassembly evidence and independent `APPROVE`.
  GCC O0 had emitted variadic register-save writes in a naked C interposer with
  no frame, corrupting its caller. A fixed file-scope x86-64 assembly entry now
  preserves the exact syscall/errno ABI across optimization levels; reaped-child
  diagnostics report exact exit/signal and safely settled gate state. Focused
  GCC Debug, GCC Release, and Clang gate matrices pass 5/5; PR runner validation
  is pending.
- #287 closed after the pinned-nginx vs ordinary-RUT comparison landed in
  `e7b8acbc` with independent `APPROVE`. One shared explicit-close GET selects
  nginx's exact `/static` local return and RUT's top-level unmatched policy;
  the test explicitly treats this as a response-selection precondition, not a
  #288 routing claim. Exact status, ordered headers, CL16 body, close, and real
  EOF are byte-identical after Date-only normalization. Independent live 500ms
  absence windows plus settled counters/history prove zero upstream work on
  both sides. Sol full pinned-Docker differential passes 1/1 in 35.18s. This is
  a generic RUT response capability only; converter support stays blocked.
- PR #269's older-header CI compile regression from `a4370349` was repaired in
  `2314c25d` after independent `APPROVE`. Stable io_uring ABI bit constants and
  exact bytewise validation of the 24-byte `io_uring_buf_reg` reserved tail now
  compile against both Linux 6.8 and newer UAPI layouts without weakening the
  gate. Current-header clang/GCC, official-v6.8-header emulation, and five
  focused gate tests pass; the pushed PR matrix is the final runner check.
- #287 ordinary-source production io_uring evidence landed in `ab2182e8` after
  independent `APPROVE`. `load_rut_program` proves lexer/parser through
  HIR/MIR/RIR, JIT, and owned `RouteConfig`; a nonmatching compiled sentinel
  leaves `/static` to the top-level strict Representation200 policy with zero
  declared upstreams. Exact Date-normalized nginx-shaped wires cover GET
  keep-alive followed by HEAD close/EOF on the same connection, plus separate
  POST and origin-form OPTIONS close/EOF, with exact metrics, access/capture,
  epoch, buffer, deadline, and ownership settlement. Sol full Integration passes
  1/1 in 193.52s. This does not claim #288 exact-route placement or converter
  support.
- #287 validator/compiler/deterministic foundation landed in `cbf4dc32` after
  independent `APPROVE`. A derived, non-stored profile admits exact status 200
  only with HTTP/1.1, `OK`, Date current, `text/plain`, request-bound connection,
  HEAD suppression, and a nonempty bounded body; existing 400..599 LegacyError
  behavior, empty bodies, Reject/Suppress, ForwardFailure, layouts, and ABI stay
  unchanged. Parser/compiler/RIR/RouteConfig ownership, reload, forged metadata,
  GET/HEAD/POST/OPTIONS, partial Send, exact accounting, zero-upstream, H1.0/H2,
  and normal/ASan rejection matrices pass. Sol full affected Network, RIR,
  Frontend, and Serve Loader suites pass 4/4 in 90.50s. #288 stays separate.
- Pinned nginx 1.29.7 exact-local-return semantics landed in `6b38bc60` after
  independent `APPROVE`. Exact `/static` and query variants emit 200/text/plain/
  CL16 with body `successor-static`; HEAD keeps CL16 and suppresses the body;
  default keep-alive and normalized close/EOF are pinned across GET, POST, and
  origin-form OPTIONS. `/static/` and `/static/child` fall through to the root
  proxy with exactly one scoped attempt each, local vectors have zero attempts,
  and two declaration orders produce byte-identical normalized wires. Focused
  evidence passes and Sol full pinned-Docker differential passes 1/1 in 33.76s.
  This is semantic evidence for #287/#288, not converter support.
- #278 acceptance-8 converter-generated pinned-nginx differential landed in
  `160372a9`, built on reviewed test-only nginx and RUT publication gates
  `6515848c` and `a4370349`. One shared accepted nginx fragment feeds pinned
  nginx and the parser/model/converter ordinary-RUT path. Both sides pause after
  request 1's 502 is fully staged but before its first downstream publication,
  prove exact request 2 arrival without consuming it, then emit byte-identical
  Date-normalized 502 keep-alive and 502 close followed by real EOF. Nginx has
  exactly two scoped dead-port connect failures; RUT has exactly two distinct
  nonzero-episode target Connect SQEs and no journal overflow/third attempt.
  Independent review closed cleanup/diagnostic races before final `APPROVE`.
  Focused gate/differential tests pass 7/7 and Sol full pinned-Docker differential
  passes 1/1 in 32.07s. The exact bounded late-successor row is now `SUPPORTED`;
  broader buffering and pipeline domains remain blocked or unplanned.
- #278 acceptance-7 ordinary-source production wire landed in `f303af6e` after
  independent `APPROVE`. A raw-CQ production-scheduler barrier proves request 1
  has dispatched its negative ConnectCompletion and staged but not submitted its
  exact configured 502 Send before a distinct complete request 2 appears in the
  original multishot Recv CQ. Unchanged production scheduling emits configured
  502 keep-alive, shifts/dispatches request 2 as a depth-one current-generation
  strict successor, emits configured 502 close, and causes server EOF before
  teardown. Two distinct ConnectCompletion episodes/tombstones, health 1→2,
  requests 2/0, two latencies/access/captures, epoch 4, no retry/third request,
  and fully settled ownership are required. The target passes seven consecutive
  runs without SKIP; Sol full Integration passes 1/1 in 194.47s. #278 remains
  open for the converter-generated pinned-nginx differential and no compatibility
  row is upgraded yet.
- #278 acceptance-7 exact-close persistence landed in `6d877f2a` after the first
  tests-only production-wire handoff correctly stopped at `BLOCKED_BY_RUT`, an
  independent `APPROVE_DESIGN`, implementation by a different Luna, and final
  independent `APPROVE`. The existing generic immutable `downstream_close`
  proof now pins exactly one parser-normalized HTTP/1.1 close field through
  provisional/JIT materialization, selected/preconnect, upload, active/retired-
  postcommit, timeout/D1, and copied-prebuilt D2. FixedStrip origin persistence
  remains orthogonal to downstream lifetime. Configured preconnect/async failure,
  CL0/positive-CL success, timeout, both Recv owner shapes, D2 rendezvous orders,
  exact-once accounting, and forgeries are covered; duplicate/compound close,
  explicit keep-alive, and every broader protocol/method/body/depth domain still
  fail closed. Normal and ASan generation suites pass 18/18 with 1,916 checks,
  and Sol full Network passes 1/1 in 17.50s. The exact production wire is now
  unblocked; #278 and converter compatibility remain open.
- #278 nine-site atomic activation landed in `afae1cc3` after an independent
  `REQUEST_CHANGES` for missing retired-postcommit evidence, a Sol full-Network
  regression discovery, and final independent `APPROVE`. The exact depth-one
  cleartext H1.1 bodyless exact-GET, CompleteContentLength, Http11FixedStrip,
  single fresh IPv4 endpoint domain now revalidates one generation-bound
  successor at preflight, JIT materialization, positive and negative Connect,
  upload, active/postcommit, io_uring deadline identity, timeout/D2, and copied-
  prebuilt boundaries. Positive-CL retirement pins the proof to the retiring
  episode through HeaderSend/BodySend and rejects post-retirement forgery before
  further body bytes. A full Network run exposed that the generation token is a
  generic freshness token rather than a strict-activation bit; exhaustive live
  and copied neutral inventories now preserve ordinary token-only pipeline,
  timeout, and prebuilt behavior while every half-activated owner fails closed.
  Focused normal generation tests pass 17/17 with 1,332 checks, focused sanitizer
  tests pass, and Sol full Network passes 1/1 in 17.54s. #278 stays open pending
  the ordinary-source production wire and converter-generated differential; no
  converter compatibility row is upgraded.
- #278 behavior-neutral foundation landed in `83e7632b` after one independent
  `REQUEST_CHANGES` round and final `APPROVE`. A generation-bound token now
  publishes only for a complete, strict, non-malformed depth-one HTTP/1 request;
  incomplete/error/deeper/wrapped states remain neutral, reset clears it, and
  simulation copies it. Pure provisional, JIT, preconnect, connected, upload-
  active, and copied-prebuilt predicates preserve depth-zero legacy behavior and
  have zero production consumers. Preconnect keeps #278 raw/upload identity
  neutral, upload-active never sets the #277 raw-length discriminator, and
  copied proof binds current request lengths and deadline generation. Real
  validated-failure-to-successor tests plus forgery matrices pass normal and
  ASan 13/13 with 558 checks; Sol full Network passes 1/1 in 16.38s. #278 stays
  open and behavior remains blocked until the nine-site atomic activation.
- #278 generation-token design received independent `APPROVE_DESIGN` after
  three `REQUEST_CHANGES` rounds. The final design keeps pipeline depth as chain/
  fragmentation state, binds a separate token to the current nonzero handler
  generation, never fabricates an upstream episode, and stages provisional,
  JIT, established, connected, and copied-prebuilt identity checks. Positive
  Connect must revalidate before passive health or origin bytes; copied prebuilt
  validation precedes every layout fallback. Foundation is behavior-neutral;
  later activation must wire all nine owner sites atomically. #278 stays open.
- The first drain-fixture hardening attempts `8fc65ea6` and `9d0ed7d0` changed
  only the deadline, but later gcc CI failures disproved that diagnosis. Client
  receipt of warm bytes can precede server-side Send completion; entering drain
  before that callback correctly closes the connection. A test-only production-
  scheduler completion barrier is now under independent review; no runtime or
  converter behavior is implicated.
- #277 narrow ordinary-source production wire landed in `ba8c6968` after one
  independent `REQUEST_CHANGES` cleanup round and final `APPROVE`. One exact
  client `send` precedes accepting; a real `backend.wait()`/`dispatch_batch()`
  barrier proves byte-exact request1 plus ordinary-static request2 in the first
  receive batch while request 1, policy, upstream, response, metrics, and epoch
  state remain unadmitted. Unchanged production scheduling emits the exact
  configured 502 keep-alive, ordinary JIT 200 close/body, and server EOF against
  a bound-but-not-listening endpoint, then settles one connection, two requests/
  latencies, one health failure, pool/slot ownership, and an even epoch. The
  cleanup review additionally guarantees runner join before forced live-client
  teardown on every normal, failed-check, and absent-witness SKIP path. Targeted
  runs observed no absent-witness skips; Sol full Integration passes 1/1 in
  201.70s. Broader #277 stays open, #278 remains blocked, and no nginx
  differential or converter compatibility row is upgraded.
- #277 narrow runtime phase 1 landed in `394d8bdb` after one independent
  `REQUEST_CHANGES` round and final `APPROVE`. Exact prefix-only admission and
  initial JIT visibility, transactional `Http11FixedStrip` successor
  preservation, fresh-upstream proof, exact stash, active/postcommit plus all
  D1/D2/prebuilt boundaries, early-response fail-close, and successor recovery
  activate atomically only for the approved bodyless exact-GET domain. Expanded
  tests exposed and fixed a missing retirement-completion revalidation before
  prebuilt readiness. Dedicated evidence covers preconnect failures, live and
  terminal Recv, complete/fragmented successors, CL0/positive-CL/timeout/EOF,
  capacity, forgeries, both early orders, exactly-once accounting, and a 15-case
  exclusion matrix. Sol full Network passes 1/1 in 15.40s; final ASan/UBSan
  affected matrix passes 92/92 with 3,946 checks. Its production wire is now
  complete; broader #277 and #278 stay open.
- PR #269 CI regression repair landed in `ec9af076` after independent
  `APPROVE`. Five legacy synthetic keep-alive fixtures now traverse a real,
  parser-proven exact HTTP/1.1 request boundary instead of relying on fabricated
  receive lengths; the ASan-only idle-close fixture proves its slot remains
  allocated before testing invalid-fd cancel suppression. The runtime proof is
  unchanged. Sol normal affected suites pass 4/4, including full Network, and
  ASan affected suites pass 4/4; clang-format-20 and diff checks pass.
- #277 phase-1 design received independent `APPROVE_DESIGN` after one
  `REQUEST_SPLIT` round. The selected atomic domain is cleartext H1.1 bodyless
  exact GET with no CL/TE/explicit Connection, default keep-alive,
  `Http11FixedStrip`, `CompleteContentLength`, one fresh IPv4 endpoint, no
  retry/reuse/mutations, `pipeline_depth == 0`, and an ordinary static
  successor. Prefix admission and initial JIT visibility, byte-exact rewrite,
  exact stash, active/postcommit plus all D1/D2/prebuilt boundaries, and final
  recovery must activate together. Early origin response before request Send
  completion fails closed with zero downstream bytes. Broader #277 stays open
  and same-strict-policy request 2 remains #278.
- #276 production raw-SQ/CQ wire landed in `5b5ce98e`, hardened by `ce3c0a39`
  and `23cd5b2f` after two independent `REQUEST_CHANGES` rounds and final
  `APPROVE`. Request 1 alone is admitted by its real live multishot Recv; a real
  negative Connect CQE stages but does not submit the configured 502 Send; only
  then a distinct request 2 is proven byte-exact in non-consumed kernel CQEs
  before Send submission. Production harvests/preserves it and emits exact 502
  keep-alive, ordinary JIT 200 `successor-static` close, and server-caused EOF.
  Continuous SQ ownership, strict raw-CQ allowlisting/fragment order, production
  scheduler order, metrics/epoch/pool/health settlement, and #277/#278 exclusions
  are pinned. Exact tests pass repeatedly; Sol full Integration passes 1/1 in
  197.35s. #276 is complete; #277 design becomes CURRENT.
- #280 ordinary-source production wire landed in `d014d91d`, with fixed-startup-
  sleep removal `05ec40c6` after one independent `REQUEST_CHANGES` round and
  final `APPROVE`. One compiled/JIT/configured ordinary `/static` source handles
  two strictly sequential requests on one production io_uring downstream: exact
  200/body keep-alive, a 200ms live byte-quiet window, then exact 200/body close
  and real server-caused EOF before drain. Post-join ownership, 1/2 connection/
  request metrics, two latencies, and epoch 4 settle. Sol full Integration passes
  1/1 in 183.89s. #280 is complete; #276 is unblocked and becomes CURRENT.
- #280 deterministic runtime implementation landed in `2be4b304`, with exact
  protocol/final-chunked boundary and JIT close ownership proof in `d6732821`
  after one independent `REQUEST_CHANGES` round and final `APPROVE`. Ordinary
  local publishers now share one request-persistence decision; ambiguous framing
  closes instead of reusing. ASan stack-fixture hardening `e7999b23` and six
  legacy valid-request fixture corrections `f0550372` received independent
  approval. Focused normal/ASan pass; Sol full Network passes 1/1 in 22.73s and
  full HTTP parser passes 1/1.
- Revised #280 runtime design received independent `APPROVE_DESIGN` after one
  `REQUEST_CHANGES` round. It adds a generic request Transfer-Encoding framing
  classification, then permits ordinary local-response persistence only when
  strict H1 metadata, client intent, drain state, and the exact None/CL/final-
  chunked message boundary all agree. The identical decision drives wire framing
  and connection lifecycle across JIT ReturnStatus, Static, default 200, metrics,
  and configured-unmatched; completion rechecks drain before pipeline/rearm.
  Proxy/failure and forced-close behavior remain unchanged. HEAD body, HTTP/1.0
  status-line, 205 framing, and global proxy TE validation remain #281-#284.
- The first #276 production-wire attempt stopped without a commit after its
  trustworthy raw-CQ barrier succeeded but exposed #280. A tests-only runner
  used unchanged `backend.wait`/`dispatch_batch`: request 1 alone entered strict
  forward, a real negative ConnectCompletion staged an unsubmitted configured
  502 Send, then a distinct request-2 write was proven byte-exact in kernel Recv
  CQEs before that Send submission. Production harvested/preserved it and routed
  `/static` to exact 200/body. The final close/EOF failed because ordinary JIT
  local responses overwrite parsed close intent with keep-alive. The draft diff
  was removed; no partial/flaky evidence or workaround remains. The same attempt
  also discovered non-blocking escaped-body issue #279.
- #276 narrow runtime implementation landed in `9c87909f`, with tests-only
  ownership completion in `6fe3dfc0` after one independent `REQUEST_CHANGES`
  round and final `APPROVE`. The production delta is existing `pipeline_shift`
  plus `pipeline_dispatch` after exact validated-failure request completion and
  keep-alive/draining checks, and fail-close on an unsuccessful fragmented-
  successor Recv rearm. Six focused tests prove SocketCreate/ConnectSubmit/async
  completion across GET/HEAD/fixed upload, complete and fragmented live/terminal
  Recv ownership, exact Send-versus-Recv SQEs, metrics/access/capture/epoch order,
  zero-target tombstone/deadline neutrality, stale duplicate immutability,
  close/drain/SQ-full/Send failure. Normal and ASan matrices pass; Sol full
  Network passes 1/1 in 14.62s. Its production wire is now complete; #277/#278
  remain explicit separate gaps.
- #276 design review returned `REQUEST_SPLIT`. The approved narrow runtime
  increment is request-1 completion followed, only for keep-alive/non-draining
  connections, by existing `pipeline_shift` and `pipeline_dispatch`; it needs no
  Connection field, ABI, compiler, config, or converter change. It must also
  fail-close an unsuccessful Recv rearm for an incomplete successor. Live
  multishot ownership stays armed; a terminal-positive late fragment rearms once.
  Review proved that same-forward strict-policy request 2 is separately blocked
  by `pipeline_depth == 1`, now #278, while request1+request2 coalesced before
  admission is separately #277. #276 therefore targets 502-to-static successor
  preservation first, with a trustworthy late-arrival barrier.
- The bounded exact-root sequential bodyless-GET default-buffering row is now
  `SUPPORTED`. Converter-generated gateway commit `a7cb1cad` received independent
  `APPROVE`: pinned nginx and generated ordinary RUT each use one downstream
  connection against a bound-but-not-listening upstream, return an exact strict-
  Date-normalized keep-alive 502, remain live and byte-quiet for 500ms, then only
  after the distinct second request return the exact close 502 and real EOF.
  Pinned nginx records exactly two endpoint-scoped connect failures; existing
  deterministic and ordinary-RUT wires prove two runtime failure/health episodes.
  Sol full Docker differential passes 1/1 in 31.94s. The claim excludes
  pipelining/#276, HTTP/1.0/H2, request bodies, transformed targets, non-strict or
  unbounded responses, and broader nginx configuration semantics.
- Converter-generated bounded completion evidence landed in `60741e24`, with
  recorder-liveness hardening in `eb4374dd` after one independent
  `REQUEST_CHANGES` round and final `APPROVE`. The accepted nginx fragment is
  parsed and lowered to ordinary RUT source; pinned nginx and production RUT each
  receive one exact request, the origin performs one complete fixed-CL `send_all`
  then immediate clean shutdown/close, and the client observes the exact strict-
  Date-normalized 200 wire plus real EOF before cleanup. A live 500ms window
  proves the frontend, listener thread, and exact 1/1/1 origin episode remain
  stable with no retry; explicit listener failure and all thread exits are
  witnessed independently from the stop command. Sol full Docker differential
  passes 1/1 in 30.83s. This covers only the bounded sequential explicit-close
  completion vector; the separate sequential unavailable-upstream differential
  remains current and pipelining remains excluded by #276.
- Bounded exact-root bodyless-GET lowering was restored in `caa9fce3` after
  independent `APPROVE`. Production header/source are byte-identical to the
  originally reviewed `d5fc3d51`: HEAD remains streaming/suppress-body, exact GET
  owns fixed H1.1 request plus strict 502/504, nginx-default 60s and complete-
  content-length buffering, while ANY and `/api/` remain unbuffered. OPTIONS/
  CONNECT unmatched behavior and explicit/forged `proxy_read_timeout` fail-closed
  rejection remain intact. Normal/ASan golden/parser/RIR/JIT/RouteConfig tests
  pass; maximum output is 4899 bytes/530 tokens under 5120/640. This is lowering
  evidence only; converter-generated behavior differentials remain current.
- #267/#275 ordinary-RUT sequential unavailable-upstream production wire landed
  in `4476b2af` after independent `APPROVE`. One policy-1 complete-buffering GET
  source crosses lexer/parser/HIR/MIR/RIR/JIT/RouteConfig and unchanged production
  io_uring against a bound-but-not-listening endpoint. Request 1 emits the exact
  configured 502 body with keep-alive; only after the complete quiet response,
  request 2 emits the exact close 502 and real EOF. Worker-thread health is exactly
  two with no ejection, completed metrics are two, and all active/pending/pool/
  slot/epoch ownership settles. Repeated focused and adjacent wires pass; Sol full
  Integration passes 1/1 in 188.46s. #275 acceptance is complete; #276 pipelined
  successors remain explicitly excluded.
- #275 canonical response-accounting reset landed in `c6ea1a3c` after independent
  `APPROVE`. `Connection::clear_response_accounting()` contains exactly status,
  body mode/remaining/chunk parser/sent, and `upstream_send_len`; full reset and
  the admitted HTTP/1 request boundary share it. The boundary call is after H2/
  preface and pipeline-incomplete returns and before new capture/generation/epoch/
  route. Strict validation and all transport/deadline/health/episode/tombstone/
  buffer ownership remain unchanged. Deterministic same-forward 502→502 covers
  live and terminal-consumed downstream Recv, two metrics/health episodes,
  separate tombstones, duplicate fencing, and explicit-close reclaim. Normal and
  ASan review matrices pass; Sol full Network passes 1/1 in 13.89s. #276 remains
  excluded and the sequential ordinary-RUT production wire is next.
- #275 design selected canonical Option A after independent `APPROVE_DESIGN`.
  The admitted HTTP/1 request boundary will reset only `resp_status`, body mode/
  remaining/chunk parser/sent, and `upstream_send_len`, after pipeline-incomplete
  return and before new capture/generation/epoch/route. Old metrics/log/capture/
  transport ownership are consumed before this boundary. Strict validation and
  all send/Recv/deadline/health/episode/tombstone/buffer ownership remain visible.
  Specialized callback cleanup was rejected as incomplete. Pipelined successor
  byte loss is split into #276 and is not part of #275's sequential claim.
- The first #267 ordinary-RUT unavailable-upstream wire attempt stopped without
  a commit after exposing #275. Ordinary policy-1 complete-buffering GET crosses
  source/compiler/JIT/RouteConfig and a real bound-but-not-listening endpoint:
  request 1 emits exact configured 502 keep-alive, but request 2 reaches the async
  callback and closes with zero bytes because completed response fields leaked
  across the request boundary. Worker health remains one and completed metrics
  remain one, while final transport ownership settles. The diagnostic test diff
  was removed; no converter workaround or false production evidence was kept.
- #267 phase-3 asynchronous ConnectCompletion handling landed in `8d4e58d4`,
  with the stale-lifetime ownership proof corrected in `d9996745` after one
  independent `REQUEST_CHANGES` round and final `APPROVE`. Exact negative current
  Connect CQEs are validated after generic pending accounting and before legacy
  health accounting; pinned default 502 serialization precedes a zero-target
  old-episode tombstone and episode advance, then health is recorded exactly once
  before deadline/transport neutralization and bounded staged send. `ECONNREFUSED`
  and kernel `ETIMEDOUT` select 502, not read-timeout 504. Both downstream Recv
  ownership shapes, HEAD/GET/fixed upload, reload, keep-alive successor, explicit
  close, stale/duplicate/malformed CQEs, send failure, and the phase-2 forgery
  matrix pass normal and ASan review. Sol full Network passes 1/1 in 13.42s. No
  source/compiler/config/ABI/Connection layout changed; the ordinary-RUT real-
  socket unavailable-upstream wire remains next.
- #267 phase-2 pre-CQE configured-502 handling landed in `94dc5aad`, hardened by
  `5290aace` and `78871ce1` after two independent `REQUEST_CHANGES` rounds and
  final `APPROVE`. A new exact pre-connect predicate and transactional pinned
  serializer admit SocketCreate/ConnectSubmit for HEAD, bodyless GET, and fixed
  uploads, preserve reload/persistence, leave health/episode/tombstone unchanged,
  and use the bounded phase-1 staged send. Production downstream ownership is
  proven in both exact forms: live multishot Recv true/1 and positive-terminal
  consumed false/0, with precise per-site callbacks. Keep-alive successor,
  explicit-close reclaim/cancel behavior, fixed-upload re-entry, distinct 502/504,
  capacity/policy/submit failure, and 46 mutation-free one-field forgeries pass.
  Normal/ASan review matrices pass 36/36 (1948 checks each); Sol full Network
  passes 1/1. Async ConnectCompletion remains the only discovered #267 runtime
  sub-gap before its public wire and converter restoration.
- #267 phase-1 staged local-response submit helper landed in `81595536`, hardened
  by `c223b99b` after one independent `REQUEST_CHANGES` round and final `APPROVE`.
  It accepts only a complete `response_header_buf` with canonical neutral deadline,
  request, upstream-send, retry/pipeline, mutation, and send ownership; submission
  is one attempt, one nonblocking pending-SQ flush, and exactly one retry. Failure
  remains mutation-free and closeable; persistent saturation has no hidden deferred
  marker. Production and test paths share full flush accounting, while the test
  seam injects only enter result/kernel SQ-head consumption. Normal/ASan targeted
  checks pass and Sol full Network passes 1/1. No production caller, behavior,
  Connection layout, generation namespace, compiler/config/source/ABI changed.
- #267 validated connect-failure design received independent `REQUEST_SPLIT`,
  which Sol accepted. Existing later-phase `ValidatedBeforeArm` cannot validate
  pre-connect failures; SocketCreate also has an exact fixed-upload downstream
  callback/Recv exception. ConnectSubmit SQ-full can make immediate local-response
  send fail too, while nonzero downstream Send generations already belong to the
  response-deadline consumer, so a hidden deferred marker would conflict with
  ownership. The approved phases are: narrow nonblocking flush+one retry helper;
  pre-CQE SocketCreate/ConnectSubmit 502; async completion tombstone/health/CQE
  fencing; public wire and converter restoration. Persistent saturation remains
  explicit fail-close and would require a separate generic deferred-response issue.
- #271 ordinary-RUT initial-complete/immediate-origin-EOF production wire landed
  in `813d3eab`, with close-witness synchronization in `fa098498`, after one
  independent `REQUEST_CHANGES` round and final `APPROVE`. One policy-1 source
  crosses lexer/parser/HIR/MIR/RIR/JIT/RouteConfig and production io_uring. A
  keep-alive request and same-downstream explicit-close request each use a fresh
  origin episode that performs one observed application write then clean close;
  exact rewritten requests, Date-normalized complete responses, quiet reuse,
  final EOF, chronology, joined history, and settled shard/pool metrics are
  proven. Bounded acquire waits observe timestamps published only after real
  close. The wire passes repeated focused runs; Sol full Integration passes 1/1
  in 187.61s. Kernel CQE order remains proven only by the deterministic Network
  seam. This closes the discovered #271 sub-gap; #267 still blocks converter
  restoration.
- #271 initial-complete/same-batch-clean-EOF runtime correction landed in
  `082f20bd` with strict Date-test hardening in `802660b0` after one independent
  `REQUEST_CHANGES` round and final `APPROVE`. Production changes only the exact
  retained initial clean-EOF predicate to admit `BodyComplete` alongside
  `RefreshPending`; no compiler/config/ABI state changed. The deterministic
  keep-alive/explicit-close matrix covers all six complete/EOF/due-timeout orders,
  exact response and pending/deadline/retirement/episode/transport ownership, and
  stale duplicate immutability. Strict transactional IMF-fixdate validation has
  malformed fixed-width, duplicate-header, and body-only probes. Normal/ASan
  targeted checks and neighbor regressions pass; Sol full Network passes 1/1.
  Its separate ordinary-RUT production wire is now committed and approved.
- #271 initial-complete/same-batch-clean-EOF runtime design selected Option A and
  received independent `APPROVE_DESIGN`. A valid initial buffering owner may
  retain clean EOF in `RefreshPending` or `BodyComplete`; the latter then reuses
  existing settlement/send/retirement. Batch preparation already removes the
  timer, so all positive/EOF/due-timeout permutations select the complete body,
  while reset/error/surplus/stale/forged cases stay fail-closed. The change is
  runtime-only with no new state, ABI, compiler, config, or nginx-specific mode.
- Unsafe #271 exact-GET lowering was disabled in `efefc901` after a rejected
  converter-generated completion differential exposed two zero-byte runtime
  paths. Independent diagnosis and review assign initial complete+same-batch EOF
  to #271 and validated non-timeout create/connect failures to #267; both issue
  comments carry minimal reproducers and acceptance tests. The rollback is an
  exact inverse of `d5fc3d51` in its three files, preserves #274 and unmatched
  OPTIONS/CONNECT/ANY behavior, and received independent `APPROVE`. Parser passes
  23/23 (615 checks) and Sol's real nginx differential passes 1/1 in 29.73s,
  restoring the prior success/gateway evidence. The bounded GET row returns to
  `BLOCKED_BY_RUT` and no rejected test draft was committed.
- PR static-analysis fix `6dae156a` changes the lexer stack-budget multiplication
  to C++23 `size_t` literals. Independent review is `APPROVE`; clang-format 20,
  clang-tidy, Clang/GCC syntax, and frontend 986/986 pass without changing the
  64 KiB budget or #274 lexer semantics.
- #271 bounded root converter lowering landed in `d5fc3d51` after one
  `REQUEST_CHANGES` round and final `APPROVE`. Root output now has exact HEAD
  streaming, exact GET direct complete buffering with policy-1 rewrite/60s/
  strict 502+504, and ANY streaming fallback. Full golden plus AST/HIR/MIR/RIR/
  RouteConfig checks prove no timeout/buffering bleed; `/api/` stays unbuffered,
  and explicit/forged `proxy_read_timeout` still rejects before output. Maximum
  source is 4899 bytes/530 tokens under bounded 5120/640 capacities. Sol passed
  normal and ASan parser 24/24 (750 checks each) and route-trie 31/31 (4407).
  This lowering was later disabled by `efefc901` after runtime regressions were
  proven; the historical measurements remain useful, but it is not active and
  the bounded row is `BLOCKED_BY_RUT` on #271 and #267.
- Generic RUT capability #274 landed in `86b629fa` after approved design and
  independent `APPROVE`. `LexedTokens` now has a bounded 640 slots (639 non-EOF
  plus EOF), growing LP64 storage by 5120 bytes while remaining below its 64KiB
  object budget. Exact 639/640/641 token boundaries preserve precise
  transactional `TooManyTokens` spans. A generic 561-token/80-route source
  reaches verified RIR and JIT RouteConfig with all routes owned. Sol passed
  frontend 986/986 (8226 checks), serve-loader 23/23 (142), and ASan/no-JIT
  targeted 2/2 (189 each). #274 is closed and #271 converter work resumes.
- #274 design selected bounded static capacity 640 after independent
  `APPROVE_DESIGN`. On LP64 `Token` is 40 bytes and `LexedTokens` grows from
  20488 to 25608 bytes (+5120); 640 admits 639 non-EOF tokens plus EOF and leaves
  126 slots beyond the measured 514-slot converter source. Bounded heap/arena
  ownership is disproportionate now; 768/1024 add unnecessary stack. Boundary
  tests must distinguish full-at-EOF from a rejected non-EOF token, and a
  generic 80-route program must reach frontend and JIT RouteConfig. A hard 64KB
  thread-stack requirement would force a future re-plan to bounded heap storage.
- The first #271 root converter implementation attempt was stopped before commit
  when it exposed RUT gap #274. The smallest truthful generated source measures
  4518 bytes (4520 at maximum ports), stores exactly 512 tokens, then fails on
  token 513 before EOF; at least 514 slots are required. Inline policy syntax
  cannot be compacted without dropping request rewrite or response semantics,
  and deduplication happens after lexing. A draft global 512→768 workaround and
  all converter edits were removed; the worktree returned clean before filing
  the generic capability issue.
- #271 post-#273 lowering re-audit was independently reviewed with
  `REQUEST_REPLAN`. The whole accepted config/request cross-product remains
  `BLOCKED_BY_RUT`: HTTP/1.0/H2, CL0/chunked/ambiguous bodies, several methods,
  broader responses, and `/api/` target transforms exceed complete-buffering
  admission. The smallest truthful slice needs no RUT change: root exact HEAD
  remains streaming, exact GET becomes direct complete buffering with the nginx
  60s default, and ANY remains streaming so fixed-CL POST does not regress.
  `/api/` and all excluded cross-products remain unclaimed; #271 stays open.
- #273 converter-generated authority-form CONNECT differential landed in
  `2e4d1b87` after one independent `REQUEST_CHANGES` round and final `APPROVE`.
  It uses the accepted minimal model and exact converter output, then proves
  pinned nginx and production RUT emit the byte-identical Date-normalized
  405/CL157/body/close wire and EOF with backend 0/0/0 after a live 500ms window.
  Review exposed a shared permissive Date masker affecting OPTIONS too; the fix
  now requires one header-block Date, validates exact IMF-fixdate tokens,
  punctuation, ranges, and GMT, mutates last, and runs malformed/duplicate/body
  self-checks before Docker. Implementer, reviewer, and Sol final serial runs
  pass; Sol full Integration passes 412/412 (41404 checks, one benchmark skip).
  Both exact rows are `SUPPORTED`; #273 is closed without claiming CONNECT
  tunneling, absolute-form, or generic ANY equivalence.
- #273 converter-generated OPTIONS-star differential landed in `7104b97f`
  after independent `APPROVE`. It uses the accepted minimal nginx model and
  exact `lower_to_rut` output, then runs pinned nginx and production RUT for one
  explicit-close `OPTIONS *` request. Both sides emit the byte-identical
  Date-normalized nginx-shaped 400/CL157/body/close wire and real EOF, remain live
  for 500ms, and finish with backend accept/request/history 0/0/0. Implementer,
  reviewer, and Sol serial real-Docker runs passed in 29.05s, 29.46s, and 29.13s.
  The exact OPTIONS-star row is now `SUPPORTED`; this does not claim CONNECT,
  absolute-form, or generic ANY equivalence.
- #273 converter lowering landed in `2db05a2d` after independent `APPROVE`.
  Every accepted root and `/api/` output now emits exactly three ordinary-RUT
  declarations before routes: OPTIONS 400/reject, CONNECT 405/reject, and ANY
  bounded fail-closed 400/suppress-body. Golden tests and AST/HIR/MIR/RIR/
  RouteConfig ownership/mapping assertions prove the three slots and decoded
  157-byte bodies; present or forged `proxy_read_timeout` still rejects before
  output. Normal and ASan/UBSan targeted suites pass 23/23 (615 checks). Both
  compatibility rows remain `NOT_IMPLEMENTED` until separate generated-RUT
  differentials land.
- CI-only commit `b9f4b117` replaces two large `rir::Module` aggregate resets
  with fresh scoped modules after GCC 13.3 deterministically ICEd before tests in
  no-JIT and no-WebSocket jobs. Independent review confirms all forged-module
  vectors and 22 checks remain unchanged; CI rerun is the GCC 13.3 acceptance.
- #273 ordinary-RUT authority-form CONNECT public wire landed in `f38cc448`
  after independent `APPROVE`. A distinct ordinary source declares a live
  backend/root ANY forward and CONNECT-only unmatched policy, proving all ten
  method slots at AST/HIR/MIR/RIR/config layers before JIT/config registration.
  Exact `CONNECT example.com:443 HTTP/1.1` with a distinct valid Host and one
  close receives the byte-exact nginx-shaped 405/current-valid-Date/CL157/body/
  close response and real EOF. Shard/recorder remain live 500ms, then join before
  history; backend accept/request/history stay 0/0/0 and shard counters settle.
  Luna passed the wire 3x (184 checks each), reviewer repeated it 3x with adjacent
  runtime/config/close evidence, and Sol passed full Integration 412/412 (41403
  checks, one benchmark skip). This is separate ordinary-RUT evidence only, not
  converter-generated equivalence. The CONNECT row moves to `NOT_IMPLEMENTED`;
  converter lowering is now complete and #273 remains open for two differentials.
- #273 ordinary-RUT OPTIONS-star public wire landed in `b8a9dd18` after one
  independent `REQUEST_CHANGES` and final `APPROVE`. Ordinary source declares a
  live backend/root ANY forward plus exact OPTIONS unmatched policy, then passes
  through lexer/AST/HIR/MIR/RIR/JIT/config ownership into production cleartext
  H1 io_uring. One strict explicit-close `OPTIONS *` receives the byte-exact
  nginx-shaped 400/Server/current-Date/Content-Type/CL157/body/close response
  and real EOF. The shard and recorder remain live for 500ms after EOF, then
  join before accept/request/history inspection, which remains 0/0/0; exact
  policy IDs/bytes are proven at every compiler/config layer. Review found the
  shared Date normalizer accepted arbitrary 29-byte values; the fix validates a
  unique full IMF-fixdate token/ranges/GMT transactionally before masking only
  its value and adds ten malformed fixed-width probes. Luna/reviewer passed the
  wire repeatedly and adjacent close/unmatched tests; Sol passed full Integration
  411/411 (41216 checks, one benchmark skip). This is separate ordinary-RUT
  evidence, not converter-generated equivalence. The OPTIONS row moves only to
  `NOT_IMPLEMENTED`; CONNECT now also has separate RUT evidence, and #273 stays
  open for the two converter-generated differentials.
- #273 atomic RouteConfig ownership/runtime activation landed in `aafa6744`
  after one independent `REQUEST_CHANGES` and final `APPROVE`. Bounded generic
  policies are deep-owned with semantic dedup/remap and transactional production
  install; configured/partial hand-built and reload metadata can no longer be
  silently ignored. Strict-parsed HTTP/1.1 route misses select exact method then
  ANY after normal routes; malformed/recovered/HTTP1.0/invalid states close with
  zero bytes. The shared strict serializer preserves failure-policy bytes,
  supports HEAD representation suppression, forces close for unread CL/chunked
  bodies, and pins config bytes through reload/send retirement. Configured H2
  misses discard same-batch staged response/control frames and close/reclaim
  with no send/upstream/state leak; omitted H1/H2 legacy 200 and matched routes
  remain unchanged. Review found two blockers: source-pointer rebase still used
  native subtraction after integer validation, and H2 tests stopped before the
  outer close path. Fixes use checked integer offsets before any write and a real
  `on_h2_data` hit→miss regression proving zero sends and balanced cleanup. Both
  implementation and review passed normal plus ASan/UBSan focused coverage; Sol
  passed full Network, Http2Conn, RouteArt, and serve-loader 4/4. No public wire
  or converter differential landed, so #273/#271/#270 remain open and the matrix
  rows stay `BLOCKED_BY_RUT`.
- #273 guarded ownership-only attempt was rejected before commit and fully
  removed. Although its deep-copy/dedup/atomic-table tests passed, independent
  review found a blocking activation bypass: public RouteConfig builders or
  fields could be installed through hand-built config/`Shard::reload_config`
  without passing `populate_route_config`; runtime would ignore the successful
  metadata and silently return legacy unmatched 200. The compiler population
  guard alone was therefore insufficient. Sol chose the already approved design
  fallback: do not scatter publication guards or expose half-active APIs; merge
  ownership with H1 dispatch, configured-H2 fail-close, and guard removal as one
  atomic increment. Luna removed only the uncommitted phase-2 patch via
  `apply_patch`; status/diff are clean and no phase-3 code was introduced.
- #273 compiler foundation landed in `1fe16907` after one independent
  `REQUEST_CHANGES` round and final `APPROVE`. Generic bounded 400..599 strict
  local-response policies and main-source `unmatched [METHOD]` declarations now
  parse with contextual CONNECT/TRACE, enforce exact fields/bytes/HEAD rules,
  and propagate through AST/HIR/MIR/RIR with owned bytes, deterministic safe
  printing, and forged table/ID/pointer/enum/capacity rejection. Review found
  that the first printer trusted forged counts/pointers and that copy/move and
  transaction coverage was incomplete; the fix validates before any byte read,
  emits one invalid-table marker, rebases empty/binary bodies across all four
  copy/move forms, and rolls back body/policy/method IDs on malformed escapes or
  later parse failure. Most importantly, `populate_route_config` rejects a
  nonzero count or any nonzero fixed method ID before verification or mutation;
  valid source fails loader at Register and omitted source remains unchanged.
  Sol passed full RIR, Frontend, and serve-loader suites 3/3 in 70.77s after the
  independent review. No RouteConfig ownership/runtime selection/converter
  behavior landed, so #273/#271/#270 remain open and both rows stay blocked.
- #273 general RUT capability design completed after two independent
  `REQUEST_CHANGES` rounds and final `APPROVE`. The selected bounded primitive is
  a per-program method-keyed unmatched table referencing generic strict local-
  response policies; matched routes win, then strict-parsed HTTP/1.1 misses use
  exact method before ANY. Policies are bounded, deep-owned, status 400..599,
  and carry current Date, request-derived persistence, HEAD disposition, and
  body bytes. Malformed/recovered and HTTP/1.0 configured misses close with zero
  bytes; unread bodies force close; configured H2 misses protocol-close without
  legacy 200 or H1 bytes. Omitted config preserves legacy H1/H2 200. CONNECT and
  TRACE are contextual selectors; declarations are main-source/program scoped.
  CONNECT origin-form and OPTIONS absolute-form remain outside compatibility
  claims. Most importantly, compiler/config foundation phases retain a
  production activation guard rejecting nonzero tables before mutation; it may
  be removed only atomically with H1 dispatch and H2 fail-close. Issue #273 body
  now contains the reviewed acceptance criteria. No implementation landed yet.
- Pinned nginx 1.29.7 authority-form CONNECT baseline landed in `606c42db`
  after independent `APPROVE`. One raw `CONNECT example.com:443 HTTP/1.1`
  request uses a deliberately distinct valid Host and exactly one
  `Connection: close` against the unchanged minimal root proxy config. nginx
  emits the byte-exact Date-normalized local 405 with its 157-byte HTML body,
  close framing, no trailing bytes, and real EOF. The generalized local-
  rejection helper preserves the OPTIONS-star vector and keeps the backend
  recorder live through EOF plus 500ms; join precedes accept/request/history
  inspection, which settles 0/0/0. Luna passed the final complete Docker suite
  twice, reviewer passed once in 28.62s, and Sol passed once in 28.52s. Together
  with `ebce24e0`, this resolves the expected nginx behavior needed to open the
  generic capability issue #273. It remains nginx-only evidence; #271/#270 stay
  open and converter lowering remains rejected.
- Pinned nginx 1.29.7 `OPTIONS * HTTP/1.1` baseline landed in `ebce24e0`
  after independent `APPROVE`. One raw request with valid Host and exactly one
  `Connection: close` uses the unchanged minimal root-location/no-URI proxy
  config. nginx rejects it before `location /`, emitting the byte-exact Date-
  normalized 400 response with its 157-byte HTML body, `Connection: close`, no
  trailing bytes, and real downstream EOF. The backend recorder remains live
  through EOF plus a 500ms observation window, then joins before history
  inspection; accept/request/history remain 0/0/0. Luna passed the complete
  real-Docker serial suite twice, reviewer passed it once, and Sol passed it
  once in 27.65s. This is nginx semantic evidence only: current RUT route miss
  still emits unmatched 200, no converter equivalence exists, and #271 remains
  open. Authority-form CONNECT is the next separate baseline.
- #271 bounded converter-lowering audit completed read-only and received an
  independent `APPROVE`. A converter-generated default-buffering differential
  cannot land truthfully yet: the accepted nginx model has no method, target-
  form, HTTP-version, framing, or persistence bound; future lowering also must
  split the current `If`-based HEAD branch into a method-specific HEAD route and
  a direct method-omitted route before the buffering analyzer can accept it.
  Unsupported matched origin-form requests can fail closed in preflight, but
  `OPTIONS *`, authority-form CONNECT, and absolute-form targets receive no
  canonical path, miss `/`, bypass preflight, and hit RUT's fixed unmatched
  `200 OK`. No configurable unmatched action exists. A one-second omitted-
  directive test would also be invalid because nginx defaults
  `proxy_read_timeout` to 60s. The reviewed smallest next increment is one real-
  nginx `OPTIONS *` baseline; CONNECT remains separate, and no capability issue
  will be filed until both expected nginx behaviors are pinned. The matrix
  remains `BLOCKED_BY_RUT` and no files changed during either audit.
- #271 matching ordinary-RUT explicit-close premature-clean-origin-EOF public
  wire landed in `8b9cf992` after independent `APPROVE`. Ordinary source selects
  exact GET, `Http11FixedStrip`, the response/failure/timeout policy triple, a
  one-second read timeout, and complete buffering, then runs through parser,
  compiler/JIT, and production cleartext H1 io_uring. One downstream close
  request produces one exact rewritten upstream request with dynamic Host and
  stripped client Host/Connection. Origin sends strict 200/CL12 + `hello`, then
  remains open for about 400ms while downstream is open/byte-quiet. Authorization
  precedes a successful recorder shutdown acknowledgement; the acknowledged EOF
  promptly publishes the exact normalized 200/CL12/close header plus `hello` and
  closes downstream, all before the one-second timeout. No failure/trailing bytes,
  retry, or successor appear; joined accept/request/history settle 1/1/1. Luna
  passed the wire repeatedly and its exact-close/EOF neighbors; Sol passed the
  exact-close family 3/3 (307 checks) and full Integration 410/410 (41108 checks,
  one benchmark skip). This is ordinary-RUT evidence, not converter-generated
  equivalence; #271/#270 remain open.
- #271 pinned nginx 1.29.7 exact-close premature-clean-origin-EOF baseline landed
  in `fc841486` after independent `APPROVE`. With response buffering and upstream
  HTTP version omitted, one bodyless GET with exactly one downstream close
  produces one exact upstream request with dynamic Host and no Connection.
  Origin sends strict 200/CL12 + `hello`, then stays application-open behind a
  test-owned close gate. About 400ms proves downstream open/byte-quiet and origin
  unchanged. Authorization precedes recorder shutdown acknowledgement; release
  then causes the exact Date-normalized 200/CL12/close header plus `hello` and
  downstream EOF in about 1ms, well before the 1s timeout. No failure/trailing
  bytes or retry appear; joined accept/request/history settle 1/1/1. Luna passed
  two required Docker runs, reviewer passed one, and Sol passed the full serial
  differential in 27.2s. This is nginx semantic evidence only; the matching
  ordinary-RUT wire is recorded separately and the converter differential remains
  absent.
- #271 matching ordinary-RUT exact-close incomplete-inactivity public wire landed
  in `ea845ca3` after independent `APPROVE`. Exact ordinary source selects GET,
  `Http11FixedStrip`, the existing response/failure/timeout triple, one-second
  read timeout, and complete buffering, then runs through compiler/JIT and
  production cleartext H1 io_uring. One downstream `Connection: close` yields
  one exact rewritten upstream request with dynamic Host and stripped client
  Host/Connection. Origin sends strict 200/CL12 + `hello` and stays open; for at
  least 500ms downstream is open/byte-quiet and origin remains open with no
  retry. First response byte, downstream EOF, and origin peer-close must all
  occur 0.8–3s after the send; output is exactly the pinned header-only
  200/CL12/close response with no prefix/failure/trailing bytes. EOF and peer
  close precede teardown; joined history settles 1/1/1 with unused slots empty.
  Sol passed the wire 1/1 (106), both neighbors 2/2 (186), and full Integration
  409/409 (41004 checks, one benchmark skip). This remains separate ordinary-RUT
  evidence, not converter-generated equivalence; #271/#270 stay open.
- #271 pinned nginx 1.29.7 exact-close incomplete-inactivity baseline landed in
  `28c7a184` after independent `APPROVE`. With `proxy_buffering` and
  `proxy_http_version` omitted and `proxy_read_timeout 1s`, one bodyless GET with
  exactly one downstream `Connection: close` creates one exact upstream HTTP/1.1
  request with dynamic Host and no Connection header. Origin successfully sends
  strict 200/CL12 plus `hello` and stays application-open; for at least 500ms,
  downstream stays open/byte-quiet and origin remains open with no retry. At
  about 1.001s nginx emits only the Date-normalized 200/CL12/close header,
  suppresses `hello`/502/504/trailing bytes, EOFs downstream, and closes origin
  before cleanup. Joined accept/request/history settle 1/1/1. Luna passed two
  required real-Docker runs, reviewer passed one, and Sol passed the full serial
  differential in 25.9s. This is nginx semantic evidence only; the matching
  ordinary-RUT public wire and converter differential remain absent.
- #271 accepted-domain gap audit completed read-only and received an independent
  `REQUEST_CHANGES` that corrected two boundaries. The accepted nginx config
  model does not restrict downstream method, request body/framing, persistence,
  HTTP version, or upstream response shape, while complete buffering remains a
  bounded HTTP/1.1 strict-response subdomain. Explicit request CL0 is not the
  same as header-absent bodyless; HEAD, fixed-upload close, chunked/oversized
  requests, non-200/no-body/chunked/close-delimited/HTTP0.9 responses, and HTTP/1.0
  remain uncovered. More importantly, `OPTIONS *` and ordinary authority-form
  CONNECT have no canonical path, miss the method-omitted `/` route, and reach
  current unmatched default 200 before buffering preflight; focused internal
  preflight rejections do not prove production fail-close. No new issue is filed
  until pinned nginx semantics determine whether location routing or configurable
  unmatched rejection is required. The reviewed next task remains an exact-close
  incomplete-inactivity nginx baseline before a matching RUT public wire.
- #271 matching ordinary-RUT explicit-close fragmented-completion public wire
  landed in `90ed0500` after independent `APPROVE`. Exact ordinary source selects
  GET, `Http11FixedStrip`, the existing response/failure/timeout triple, one-
  second read timeout, and `complete_content_length`, then runs through compiler,
  JIT, and production cleartext H1 io_uring. One parser-derived downstream
  `Connection: close` produces exactly one rewritten upstream GET with preserved
  query, dynamic Host, and stripped client Host/Connection. Four permit-gated
  origin writes have 0.35–0.65s gaps and span 1–2s; three post-progress probes
  prove downstream open and byte-quiet. The final CL14 byte promptly releases
  the exact pinned RUT 200/close header plus `close-complete`, then real EOF;
  origin peer-close precedes teardown, and joined history settles 1/1/1 with
  unused slots empty. Luna and reviewer passed the wire repeatedly; Sol passed
  it 1/1 (93 checks), its keep-alive neighbor 1/1 (102), and full Integration
  408/408 (40894 checks, one benchmark skip). This is public RUT evidence, not a
  converter-generated differential; #271/#270 remain open.
- #271 pinned nginx 1.29.7 explicit-close default-buffering completion baseline
  landed in `25ed8e93` after independent `APPROVE`. A distinct bodyless GET with
  exactly one downstream `Connection: close` reaches one byte-exact HTTP/1.1
  upstream request with dynamic Host and no Connection header. Four
  permit-gated origin writes about 0.401s apart span 1.204s; all three pre-final
  checkpoints prove downstream open and byte-quiet. The final declared CL14
  body byte releases the exact Date-normalized 200/`Connection: close` response
  plus `close-complete` in under 1ms, followed by downstream EOF and origin peer
  close before cleanup. Accept/request/history settle exactly 1/1 with no retry.
  Luna passed the real-Docker serial differential twice; Sol passed it once in
  24.2s together with all existing vectors. This is nginx semantic evidence
  only: no ordinary-RUT public wire, converter lowering, or SUPPORTED upgrade
  landed, so #271/#270 remain open.
- #271 parser-proven explicit-close complete buffering landed in `7bd84154`
  after an independent `REQUEST_CHANGES` and final `APPROVE`. The existing
  cleartext H1 `CompleteContentLength` + bodyless known-non-HEAD domain now
  admits exactly one OWS-trimmed, case-insensitive `Connection: close` field;
  duplicates, token lists, keep-alive, malformed values, and unpinned states
  reject before effects. One generic close-intent bit joins the canonical
  upload-proof visitor/equality/reset inventory and preserves policy-0 versus
  `Http11FixedStrip` identity across active, post-commit, prebuilt, retirement,
  and D2 phases. Completion, zero-progress inactivity, and premature EOF send
  only the existing disposition and then close exactly once; HeaderSend-first,
  retirement-first, and owner-free completion all suppress already-buffered
  request-2 parse/route/rearm/timer/callback effects. Review caught the legacy
  keep-alive-only zero-progress timeout gate; the correction reuses the pinned
  persistence predicate, and a real timer-expiry test proves exact configured
  binary 504/close output. Sol linked production `rut`, passed explicit-close
  6/6 (305 checks), adjacent runtime/D2 22/22, full Network 920/920 (115494),
  and ASan/UBSan explicit-close plus D2 8/8. No converter/config ABI/public wire
  changed, so #271/#270 remain open and lowering stays rejected.
- #263 io_uring downstream-TLS large-response preservation landed in
  `57caf3b5` after separate implementation and test reviews returned `APPROVE`
  and `APPROVE_TESTS`. GDB evidence identified a generic fixed-capacity harvest
  loss: multishot provided-buffer CQEs could deliver more than the 16 KiB
  upstream staging buffer before callbacks ran, and the partial fifth copy
  correctly fail-closed with `-ENOBUFS`. The bounded ordinary bodyless H1
  downstream-TLS proxy episode now selects token-scoped one-shot upstream Recv;
  excluded health, H2, WebSocket, upgrade, retry, deadline-owner, stale,
  retirement, and boundary domains remain multishot. All rearm failures are
  checked, header-stage `-ENOBUFS` rejects before publication, and the shared
  66-field deadline-owner visitor makes selector neutrality, reset, and future
  ownership additions fail closed together. A production real-ring test proves
  actual high-water pause and low-water resume while comparing all 262144 body
  bytes; focused dispatch, terminal accounting, ring-full, reuse, drain, and
  exhaustive-owner regressions cover the selection boundary. Sol passed Network
  914/914 (115185 checks), five repeated production wires, adjacent TLS
  keep-alive tests, and ASan/UBSan builds plus the main/guard/keep-alive wires.
  This fixes the tracked generic runtime capability; it does not add nginx.conf
  parsing, converter lowering, or HTTPS-listener compatibility.
- #271 pinned nginx 1.29.7 combined default-buffering semantic baseline landed
  in `5cbdccff` after one race-safety `REQUEST_CHANGES` round and final
  `APPROVE`. With both buffering directives and `proxy_http_version` omitted, a
  binary CL12 POST prefix remains downstream-open/quiet for at least 1.2s with
  zero origin accepts/requests; only the exact suffix causes one byte-exact
  HTTP/1.1 upstream upload with dynamic Host and no Connection header. Four
  permit-gated origin application writes about 0.400s apart span more than one
  second while nginx keeps downstream byte-quiet, then the final declared body
  byte promptly releases the exact normalized response. Origin retires 1/1 and
  downstream remains keep-alive/quiet; request-2 reuse is not claimed. Sol
  passed the required pinned differential 1/1 in 22.08s plus format/diff checks.
  This is semantic evidence only: no converter lowering or positive-body
  nginx-vs-generated-RUT differential exists, so #271/#270 remain open.
- #272 immediate bounded CI-timeout mitigation landed in `59a1ec3d` after
  independent `APPROVE_DESIGN` and `APPROVE`. Only the monolithic
  `test_integration` CTest registration receives an explicit finite 300-second
  timeout, overriding workflow defaults of 120 without changing other tests or
  reducing sanitizer's existing 300-second budget. CTest JSON confirms only
  `test_integration`=300 and the pre-existing nginx differential=120 properties.
  #272 remains open for pre-case RUN diagnostics and scalable deterministic
  sharding; this commit does not claim to fix unrelated CI assertions.
- #271 positive-body fixed-upload premature-clean-EOF public wire landed in
  `b962d28d` after independent `APPROVE`. One CL12 binary POST prefix is held
  before the exact upload boundary with zero origin/downstream effects; the
  completed request is byte-exact after dynamic-Host rewriting. Origin sends a
  strict 200/CL12 header plus `hello`, stays application-open behind an explicit
  close gate, and downstream remains byte-quiet. Gate-triggered clean EOF then
  promptly publishes exactly the pinned Date-normalized header plus `hello`,
  suppresses 502/504/failure/trailing bytes, and EOFs downstream with settled 1/1
  history. Sol passed the new wire 1/1 (83 checks), positive-body completion 1/1
  (362), positive-body inactivity 1/1 (330), and bodyless policy-1 EOF 1/1 (91),
  plus format/diff checks. All three bounded positive-body dispositions now have
  public wires; nginx POST baseline and converter equivalence remain absent, so
  #271/#270 stay open.
- #271 positive-body fixed-upload inactivity public wire landed in `58c36b06`
  after independent `APPROVE`. A CL12 binary POST prefix remains incomplete for
  at least 1.2s after successful submission with zero origin accepts/requests and
  downstream bytes; the exact boundary then produces one byte-exact dynamic-Host
  rewritten upload. Origin sends two permit-gated progress fragments about
  0.400s apart and remains application-open. Each progress point is byte-quiet;
  final progress to publication exceeds one second and first progress to
  publication exceeds 1.25s, proving refresh. Expiry emits exactly the pinned
  Date-normalized 200/CL12 keep-alive header, discards `hello`, suppresses
  502/504/failure bytes, EOFs downstream, and retires origin with settled 1/1
  history and no retry. Sol passed the new wire 1/1 (330 checks), positive-body
  completion 1/1 (361), bodyless policy-1 inactivity 1/1 (93), and mode=None
  fixed-upload stall 1/1 (75), plus format/diff checks. Positive-body clean EOF,
  nginx POST baseline, and converter equivalence remain absent; #271/#270 stay
  open.
- #271 positive-body fixed-upload fragmented-completion public wire landed in
  `03385731` after one evidence-only `REQUEST_CHANGES` round and final `APPROVE`.
  Ordinary RUT selects exact POST, `Http11FixedStrip`, the strict policy triple,
  one-second deadline, and complete-content-length buffering. Request 1 sends a
  CL12 binary prefix containing NUL/non-ASCII bytes, then remains incomplete for
  at least 1.2s after successful submission with zero origin accepts/requests and
  zero downstream bytes; the exact remainder then produces a byte-exact dynamic-
  Host rewritten upload. Four permit-gated response sends are about 0.400s apart
  across more than one second, with three byte-quiet probes before exact full
  commit. RUT retires origin 1 before same-downstream request 2 sends a distinct
  CL5 binary body through a fresh, byte-exact origin episode; history settles
  exactly 2/2 with unused slots empty. Sol passed the new wire 1/1 (362 checks),
  bodyless policy-1 POST completion 1/1 (102), and fixed-upload boundary 1/1
  (310), plus format/diff checks. Inactivity/EOF positive-body wires and converter
  equivalence remain absent, so #271/#270 stay open.
- #271 fixed-upload/complete-response-buffering composition landed in
  `04533827` after one `REQUEST_CHANGES` round and final `APPROVE`. Exact
  POST/PUT/PATCH and matching ANY may combine a positive bounded fixed request
  body, exactly `Http11FixedStrip`, and `CompleteContentLength`; the timer and
  first response Recv still arm only after exact upstream upload-send completion.
  Phase-aware gates retain the full upload proof through active, first-batch,
  buffering, post-commit, retirement, and D2, switching from active to retiring
  episode identity only after origin transfer. Review restored the handler-before
  preflight policy-zero trust boundary and required a real combined-mode
  zero-progress configured-timeout D2 control plus current/captured-policy and
  captured-length forgery regressions. All forgeries reject transactionally with
  zero peer bytes/effects. Mode=None fixed-upload positive responses remain
  fail-closed. No profile, enum, metadata, layout, ABI, compiler, converter, or
  public-wire change landed. Sol linked production `rut` and passed Frontend
  977/977 (7,825 checks), JIT 487/487 (6,026), and Network 907/907 (114,830),
  plus format/diff checks. Public positive-body buffering wires and converter
  equivalence remain absent, so #268/#271/#270 stay open.
- #271 next-increment design selected the bounded composition of
  `CompleteContentLength` with the existing fixed-Content-Length upload profile
  and received independent `APPROVE_DESIGN`. It admits exact POST/PUT/PATCH or
  matching ANY only with a positive bounded fixed request body, exactly
  `Http11FixedStrip`, complete upstream upload before the first response Recv and
  timer arm, and the existing strict 200/unique-CL/bounded response domain. No
  profile, metadata, storage, config layout, or ABI change is required: the full
  upload proof already pins lengths, route/function, generation, upstream,
  policy, upload episode, and exact send length, while D2 already supports the
  retired proof. The implementation must use phase-aware composition gates,
  preserve proof identity through active/first-batch/post-commit/retirement/D2,
  and keep omitted-buffering fixed-upload positive responses fail-closed. HEAD
  and broader response-domain options were rejected as the next increment; they
  remain separate blockers. Completion will not enable converter lowering or
  close #268/#271/#270.
- #271 policy-1 ANY-route TRACE incomplete-inactivity public wire landed in
  `e41e628d` after independent `APPROVE`. A method-omitted ordinary RUT route
  compiles as ANY and admits one bodyless origin-form TRACE. The byte-exact
  rewritten request preserves target/query and X-Test, synthesizes backend Host,
  and strips client Host/Keep-Alive. Two permit-gated progress sends are about
  0.400s apart and each is followed by an open/zero-byte downstream probe; final
  progress to first downstream byte exceeds one second and first progress to
  publication exceeds 1.25s, proving refresh. Expiry emits exactly the pinned
  Date-normalized 200/CL12 keep-alive header, discards `hello`, emits no
  502/504/failure bytes, EOFs downstream, and retires the application-open origin
  with exactly one accept/request and no unused history. Sol passed TRACE 1/1
  (89 checks) and unchanged GET inactivity 1/1 (93); format/diff checks passed.
  The POST/TRACE public method evidence is complete, but converter equivalence
  remains absent and #268/#271/#270 stay open.
- CI found the closed route-method predicate added in `2d7d3157` lacked an
  explicit switch default under `bugprone-switch-missing-default-case`.
  `d216dd3c` added a fail-closed default; CI then exposed duplicated explicit
  false branches under `bugprone-branch-clone`, and `1a6b51fe` consolidated every
  non-admitted/future value into that default. The admitted set remains exactly
  ANY/GET/POST/PUT/DELETE/PATCH/OPTIONS while all other values reject. Sol passed
  both targeted tidy rules, the frontend closed-method test 1/1 (187 checks),
  format, and diff checks.
- #271 policy-1 exact-POST fragmented-completion public wire landed in
  `d64b005b` after independent `APPROVE`. Ordinary RUT selects an exact POST
  route, `Http11FixedStrip`, the strict policy triple, one-second deadline, and
  complete-content-length buffering. Two byte-exact origin requests preserve
  POST target/query and X-Test, synthesize the dynamic backend Host, and strip
  client Host/Keep-Alive with no Content-Length or body. Origin 1 emits four
  permit-gated fragments at about 0.400s gaps across more than one second; three
  probes prove downstream open and byte-quiet before exact full completion.
  Origin retirement is observed before same-downstream request 2 opens a fresh
  origin episode; accept/request history settles exactly at 2/2 with unused
  slots empty. Sol passed the new wire 1/1 (102 checks) and the unchanged GET
  policy-1 neighbor 1/1 (102); format and diff checks passed. ANY-TRACE public
  evidence and converter equivalence remain absent, so #271/#270 stay open.
- #271 bodyless known non-HEAD complete buffering landed in `2d7d3157` after
  independent `APPROVE`. A shared closed route-method predicate admits exact
  GET/POST/PUT/DELETE/PATCH/OPTIONS and ANY, with TRACE admitted only through
  ANY; HEAD/CONNECT/Other/OPTIONS-star reject before upstream effects. Analyzer,
  lowering, verifier, preflight, active ownership, first-batch, post-commit, and
  D2 identities all revalidate the domain. Positive-CL classification remains
  deliberately split: only `CompleteContentLength` uses the broadened identity,
  while omitted-buffering streaming and its body pump remain GET-only. No ABI,
  config-layout, converter, or nginx-specific runtime field changed. Sol linked
  production `rut` and passed Frontend 977/977 (7,825 checks), JIT 487/487
  (6,026), and Network 903/903 (114,503); format and diff checks passed. Public
  POST/TRACE method wires and converter equivalence remain absent, so
  #268/#271/#270 stay open.
- #271 bodyless known non-HEAD buffering design received independent
  `APPROVE_DESIGN`. It reuses `BodylessNonHeadContentLengthZero` and existing
  parsed/route-method proofs: exact GET/POST/PUT/DELETE/PATCH/OPTIONS plus ANY,
  with TRACE admitted only through ANY; HEAD/CONNECT/Other/OPTIONS-star reject.
  No AST/HIR/MIR/RIR, HandlerResult, bundle, RouteConfig/RouteEntry, or Connection
  field is needed. One shared closed static-route predicate guards analyzer,
  lowering, verifier, preflight, and complete-buffering ownership. Positive-CL
  classification must remain split: buffering mode may use the closed non-HEAD
  identity, while omitted-buffering streaming and its body pump stay GET-only.
  Existing first-batch, ledger, expiry, retirement, D2, config, policy, and reset
  identities remain authoritative and require adversarial regression coverage.
  Converter behavior is unchanged and #268/#271/#270 stay open.
- Post-wire feasibility review concluded `NO`: no currently accepted nginx.conf
  slice can truthfully lower default buffering for its entire admitted domain.
  The converter emits one method-omitted route with a HEAD conditional; exact GET
  plus ANY fallback splitting is mechanically possible but would silently stream
  or fail closed for body-bearing GET, HEAD, write/other methods, and broader
  response shapes. Omitted nginx timeout also means 60s, whereas current lowering
  emits neither deadline nor buffering. No new issue is needed: #271 covers the
  buffering method/body/response gap, #268 the inactivity lifecycle breadth, and
  #270 the dependent explicit/default lowering. The selected next design extends
  the existing barrier to #268's bodyless known non-HEAD exact/ANY method identity;
  request bodies, HEAD, broader framing/status/size, and converter differential
  remain blocked afterward.
- #271 fixed-request-policy premature-clean-EOF public wire landed in `1b668c65`
  after independent `APPROVE`. One byte-exact rewritten origin request carries
  dynamic backend Host and X-Test with client Host/Keep-Alive stripped. Origin
  sends valid 200/CL12 plus `hello` and waits application-open behind an explicit
  close gate. A pre-gate probe and state recheck prove downstream open and byte-
  quiet; after the timestamped clean close, RUT promptly emits the exact Date-
  normalized pinned keep-alive header plus `hello`, no failure/502/504 bytes, then
  downstream EOF. Cleanup occurs only after gate close, tested EOF, and settled
  one accept/request with zero unused history. Sol passed policy-1 EOF 1/1 (91
  checks), policy-0 neighbor 1/1 (89), policy-1 completion 1/1 (102), and policy-1
  inactivity 1/1 (93). All three bounded policy-1 dispositions now have public
  wires; converter equivalence is still unproven and #271/#270 remain open.
- #271 fixed-request-policy incomplete-inactivity public wire landed in
  `3bc63a32` after independent `APPROVE`. Ordinary RUT reuses the policy-1 fixture;
  one byte-exact rewritten origin request carries dynamic backend Host and X-Test
  while stripping client Host/Keep-Alive. Origin sends valid 200/CL12 plus `hello`
  and remains open. A 100ms probe proves downstream open and byte-quiet; after
  more than one second of inactivity RUT emits exactly the Date-normalized pinned
  keep-alive success header, discards `hello`, emits no default/configured failure
  or 502/504 bytes, then EOFs downstream and retires origin before cleanup. A
  settle interval preserves exactly one accept/request with zero unused history.
  Sol passed policy-1 inactivity 1/1 (93 checks), policy-0 neighbor 1/1 (89), and
  policy-1 completion 1/1 (102). Clean-EOF policy-1 evidence remains; #271/#270
  stay open.
- #271 fixed-request-policy fragmented-completion public wire landed in
  `125de278` after independent `APPROVE`. Distinct ordinary RUT keeps the policy-0
  fixture byte-unchanged and selects exact GET, `Http11FixedStrip`, one-second
  deadline, strict policy triple, and complete-content-length buffering. Client
  Host/Keep-Alive/X-Test become two byte-exact origin wires with dynamic backend
  Host, preserved target/query and X-Test, and stripped client Host/Keep-Alive.
  Origin 1 emits four permit-gated fragments with about 0.400s gaps and a 1.201s
  span; three probes prove downstream open and byte-quiet. Final completion emits
  the exact full response, RUT retires origin 1, and same-downstream request 2
  uses a fresh exact rewritten origin episode. Sol passed the new wire 1/1 (102
  checks) and policy-0 neighbor 1/1 (95). Inactivity and clean-EOF policy-1 wires
  remain; #271/#270 stay open.
- #271 request-policy-compatible bounded buffering landed in `c4566ea6` after
  one `REQUEST_CHANGES` round and final `APPROVE`. A closed common predicate
  admits only omission or `Http11FixedStrip`; analyzer presence/value coherence,
  lowering, full-width RIR verification, pre-effect dispatch, active owner,
  first-batch, post-commit, and final D2 publication all revalidate it. The review
  caught a missing prebuilt D2 identity check; configured-timeout and CL0-success
  adversarial tests now prove forged current and stale captured policies reject
  transactionally with zero peer bytes or send/resource effects. No packed ABI,
  ForwardPolicyBundle, RIR/config/route layout, method/body domain, or converter
  behavior changed. Sol linked production `rut` and passed Frontend 976/976
  (7,628 checks), JIT 487/487 (5,992), and Network 901/901 (114,138). Policy-1
  public wires remain absent; #271/#270 stay open.
- #271 request-policy-compatible buffering design received independent
  `APPROVE_DESIGN`. The selected Option A adds a closed profile predicate that
  accepts only omission or `Http11FixedStrip`, then applies it at analyzer,
  lowering, verifier, pre-effect runtime dispatch, deadline-owner, and post-
  commit stability boundaries. Existing HandlerResult fields already carry the
  request-policy and bundle IDs separately, request rewrite completes before
  upstream/resource effects, and response buffering owns independent storage;
  no RIR/config/route/ForwardPolicyBundle layout change or preflight identity pin
  is needed. Implementation must reject presence/value mismatches, dynamic or
  forged IDs, and stale runtime policy state before copy/commit. Public policy-1
  wires for all three dispositions are a separate next increment. #271/#270
  remain open and converter rejection is unchanged.
- Pinned nginx 1.29.7 default-buffering premature-clean-EOF baseline landed in
  `6449ecce` after independent `APPROVE`. One origin emits a valid 200/CL12
  header plus `hello`, then remains application-open behind an explicit close
  gate. A 400ms probe proves downstream open and byte-quiet. The gate timestamps
  clean origin shutdown; nginx then publishes exactly the Date-normalized pinned
  keep-alive header plus `hello` and downstream EOF in about 0.001s, with no
  502/504, retry, or reconnect. Sol passed the fixed-Docker monolithic
  differential executable with 18 PASS markers. Together with `6ea51e0c` and
  `4980446b`, all three bounded nginx dispositions now have committed semantic
  baselines; converter equivalence is still absent and #271 stays open.
- Read-only feasibility review found no honest converter-generated default-
  buffering differential in the current admitted domain. The converter emits a
  method-omitted route with HEAD handling and a mandatory nginx-compatible
  upstream request policy, while `complete_content_length` currently requires an
  effect-free exact GET with no request policy. Runtime is additionally limited
  to bodyless GET and bounded positive CL. A GET-only test cannot prove the
  method/body domain of the nginx location, and absence of an explicit directive
  still carries nginx's implicit timeout and buffering defaults. Primary blockers
  remain #271/#268; #270 correctly rejects before RUT emission. The smallest
  useful RUT increment is request-policy-compatible buffering for exact bodyless
  GET, but it alone must not unblock converter lowering.
- Pinned nginx 1.29.7 default-buffering fragmented-completion baseline landed in
  `4980446b` after independent `APPROVE`. A minimal root proxy deliberately omits
  `proxy_buffering` and uses `proxy_read_timeout 1s`. One origin emits an exact
  200/CL12 response in four permit-gated fragments with observed gaps of about
  0.401s and a total span of 1.204s. Three pre-final probes prove downstream
  open and byte-quiet. The final byte publishes the exact Date-normalized full
  response in about 0.001s; nginx promptly retires origin, keeps downstream
  reusable, and performs exactly one origin request with no reconnect. Sol
  passed the fixed-Docker monolithic differential executable with 17 PASS
  markers. This is semantic evidence, not converter equivalence; #271 stays open.
- Pinned nginx 1.29.7 default-buffering incomplete-inactivity baseline landed in
  `6ea51e0c` after independent `APPROVE`. Minimal root proxy config contains
  `proxy_read_timeout 1s` and deliberately omits `proxy_buffering`. One origin
  sends a valid 200/CL12 header plus `hello`, remains open, and receives the exact
  dynamic-Host request once. A 500ms capture proves downstream open and byte-
  quiet; first downstream byte, EOF, and nginx-attributed origin close occur at
  about 1.001s. The Date-normalized wire is exactly the pinned keep-alive 200
  header, with no prefix, 502, or 504, and a further settle excludes retry. Sol
  passed the fixed-Docker monolithic differential executable with 16 PASS markers.
  This is a semantic baseline, not converter equivalence; #271 stays open.
- #271 premature-EOF public wire landed in `ecfe2431` after independent
  `APPROVE`. One exact origin sends a valid 200/CL12 header plus `hello`, then
  waits behind a close gate. Gate state plus a 100ms probe prove origin and
  downstream remain open while downstream is byte-quiet. The explicit monotonic
  close timestamp precedes clean origin EOF; RUT then emits the byte-exact pinned
  keep-alive 200 header plus exactly `hello`, no 502/504 bytes, and downstream
  EOF within one second. Exactly one accept/request and settled history exclude
  reconnect; cleanup occurs after the tested EOF. Sol passed the wire and its
  inactivity neighbor 2/2 (178 checks). This is not nginx equivalence evidence;
  #271 remains open.
- #271 incomplete-inactivity public wire landed in `83565177` after independent
  `APPROVE`. Ordinary RUT pins exact GET/strict policy/one-second timeout/
  CompleteContentLength metadata. One origin sends a valid 200/CL12 header plus
  `hello`, remains open and silent, and receives no reconnect. A 100ms probe
  proves downstream open with zero bytes; more than one second after origin
  progress, RUT emits only the byte-exact pinned 200 header retaining
  `Connection: keep-alive`, discards `hello`, emits no 502/504 bytes, then EOFs
  downstream and retires origin before cleanup. Sol passed the wire 1/1 (89
  checks) and completed-buffer neighbor 1/1 (95). This is not yet nginx
  differential evidence; #271 stays open.
- #271 complete-response public wire landed in `eb19ff10` after independent
  `APPROVE`. Ordinary RUT carries `CompleteContentLength` plus one-second timeout
  through RIR/JIT/RouteConfig. Origin 1 sends an exact positive-CL response in
  four permit-gated application fragments; every gap is 350--650ms and total
  exceeds one second. Three pre-final probes prove downstream remains open with
  zero bytes. Final completion emits the exact pinned header/body, then RUT
  retires the still-open origin. Request 2 reuses the same downstream socket,
  waits for a fresh origin episode, and returns a second exact positive-CL
  success; exactly two accepts/requests exclude retry. Sol passed the new wire
  1/1 (95 checks) and immediate-streaming neighbor 1/1 (94). This does not claim
  kernel-CQE boundaries or nginx equivalence; #271 remains open.
- #271 bounded runtime commit barrier landed in `74d7c37f` after two
  `REQUEST_CHANGES` findings and final `APPROVE`. For the exact cleartext
  io_uring/bodyless GET/strict 200-positive-CL/16 KiB profile, a valid response
  pins the serialized success header and commits zero downstream bytes until a
  terminal disposition. Full completion sends the exact header/body and gates
  request 2 on send drain plus pre-send origin retirement; inactivity sends
  pinned header only and discards prefix; clean EOF sends pinned header plus
  prefix; other errors and unsupported/surplus/capacity shapes fail before
  commit. Batch progress/completion wins matching Timeout, and late old-episode
  CQEs are fenced. Sol built production `rut`, passed focused 78/78 (3,056
  checks), Network 896/896 (114,010), and Integration 394 pass / one known #263
  failure / one skip. Public wires and converter differential remain absent;
  #271 stays open and the matrix remains `BLOCKED_BY_RUT`.
- #271 compiler/config metadata landed in `2aece7ca` after independent
  `APPROVE`. Ordinary RUT accepts only
  `response_buffering: "complete_content_length"`; the enum propagates through
  AST/HIR/MIR/RIR bundle dedup, verifier, RouteConfig, registration, and JIT
  without changing the 64-bit HandlerResult or 8-byte bundle ABI. Exact GET,
  positive timeout, and the complete strict policy triple are statically
  required; forged values fail at every trust boundary. Until runtime support
  lands, nonzero mode closes at route preflight before handler, body wait,
  connect, or output, so there is no streaming fallback. Sol built the actual
  `rut` executable and passed Frontend 976/976 (7,593 checks), JIT 487/487
  (5,989), and Network 882/882 (113,595). #271 and #270 remain blocked.
- Production executable link coverage exposed the template visibility gap from
  `d2db00ed`; `943f59d1` adds one explicit io_uring body-pump instantiation plus
  an extern declaration. Independent review is `APPROVE`; default JIT and
  no-JIT `rut` plus `test_network` all link, with no runtime behavior change.
- #271 selected `response_buffering: "complete_content_length"` and received
  independent `APPROVE_DESIGN`. The enum lives in Forward policy metadata;
  omission preserves immediate streaming and the packed handler ABI. The first
  bounded runtime contract is an entire-response commit barrier for cleartext
  io_uring, exact bodyless GET, strict 200/positive CL within 16 KiB. Header
  acceptance snapshots strict success bytes once. Full completion sends the
  pinned header and full body; incomplete inactivity sends pinned header only
  and discards prefix; observed premature EOF sends pinned header plus prefix;
  other recv errors stay explicit zero-byte unsupported. Progress/completion
  wins same-batch Timeout, and surplus rejects before commit. Implementation is
  staged metadata first, then runtime; #271 and #270 remain blocked.
- Pinned nginx 1.29.7 post-commit investigation established two distinct
  baselines for one 200/CL12 header plus `hello` followed by origin silence.
  Default `proxy_buffering on` waits about 1.002s, emits only the 117-byte 200
  header, omits the buffered prefix, then closes downstream and origin without
  502/504. Explicit `proxy_buffering off` immediately emits the 122-byte header
  plus prefix, then closes both sides at about 1.001s. Both use one exact origin
  request/accept; only Date needs normalization. Current RUT matches only the
  explicit-off control, so using it as evidence for the default config would be
  a silent semantic downgrade. #271 now tracks the required general buffering/
  commit primitive and blocks #270 alongside #268. The experiment is not yet a
  committed regression.
- #268 public bounded positive-CL GET progress-refresh wire landed in
  `cdd91d43`. Ordinary RUT source carries exact GET, strict policy bundle, and
  one-second timeout through RIR/JIT/RouteConfig into production cleartext
  io_uring. One origin sends a 140-byte exact HTTP/1.1 200/CL48 response in nine
  recorded 16-byte application fragments; every adjacent gap is 100--650ms,
  while first fragment to exact downstream completion exceeds 1.10s. The
  origin remains open after its last send, then only RUT retirement supplies
  peer close; exactly one accept/request and a settle interval exclude retry.
  The wire exposed a fragmented-header transition bug: retained header-prefix
  progress conflicted with the new body owner when the completing batch also
  carried body bytes. `8e729400` now atomically retires only the exact current
  pre-header proof and seeds post-commit progress from authoritative coalesced
  body bytes. Independent reviews approved both changes. Sol passed deadline
  63/63 (2,627 checks), the new public wire 1/1 (94), and its CL0 neighbor 1/1
  (64). This is application-send evidence, not a kernel-CQE fragmentation claim
  or nginx differential; #270 remains blocked and `NOT_IMPLEMENTED`.
- #268 bounded positive-CL GET response streaming landed in `d2db00ed` after
  two independent `REQUEST_CHANGES` rounds and final `APPROVE`. The internal
  runtime profile admits only cleartext io_uring bodyless exact/ANY GET and an
  exact HTTP/1.1 200 with one positive Content-Length whose header plus body fit
  the existing 16 KiB origin slice. Exact generation/episode/send/cancel owners,
  tombstones, whole-batch progress settlement, deferred pumping, and origin-
  retirement rendezvous make progress/Timeout and completion order independent.
  Pre-commit timeout may emit the configured 504; post-commit timeout, invalid
  framing, short/error, surplus, or SQ pressure closes/truncates without a new
  502/504. Sol passed deadline 62/62 (2,497 checks), Network 880/880 (113,435),
  Frontend 974/974, JIT 486/486, and HTTP/2 75/75. Integration was 393 pass,
  one known #263 failure, and one skip. Public wires, pinned-nginx evidence,
  converter lowering, other methods, larger/chunked/close-delimited bodies, and
  other transports remain open; #270 stays blocked and `NOT_IMPLEMENTED`.
- #268 bounded positive-CL response-streaming design received independent
  `APPROVE_DESIGN`. It generalizes the existing bodyless exact/ANY GET request
  profile and selects a post-commit phase only after validating exact HTTP/1.1
  200 with one positive CL, no TE, and overflow-safe total response size within
  the existing 16 KiB origin slice. Existing streaming callbacks remain the
  data plane; a same-generation post-commit proof, monotonic origin-body count,
  whole-batch ledger, and deferred body pump make progress/Timeout and Send/
  Timeout order-independent across buffer compaction and send-inflight callback
  gaps. Pre-header inactivity retains configured 504; post-commit inactivity
  retires origin and closes/truncates downstream without any replacement
  failure response. Full origin-body receipt removes the timer immediately but
  retains ownership until downstream drain and origin retirement. Review also
  requires overflow-safe capacity checks, full-body-plus-terminal same-batch
  success, and explicit proof that a stalled committed wire contains one 200/
  body prefix followed by EOF with no 502/504 bytes. No compiler/source/API/ABI
  change is planned; #270 stays blocked.
- #268 public fixed-CL POST progress-then-stall wire in `24368e13`: ordinary
  RUT source preserves exact POST, fixed request-policy, strict bundle, and
  one-second timeout metadata through RIR/JIT/RouteConfig into production
  cleartext io_uring. One fresh origin records the exact rewritten request and
  byte-identical 12-byte binary body, sends one nonempty incomplete response-
  header fragment, then stays open and silent. Fragment-to-client completion
  exceeds one second but remains below the unrelated global timeout; the client
  receives only the exact configured full 504 including its 20-byte body, with
  no leaked origin fragment or extra bytes. RUT retires the origin after the
  stall; one accept/request plus a settle interval exclude reconnect.
  Independent review is `APPROVE`. Sol passed progress-stall 1/1 (75 checks),
  fragmented success 1/1 (75), and upload-boundary 1/1 (310). Successful
  response bodies, converter lowering, and nginx differential evidence remain
  open; #270 stays blocked.
- #268 public fixed-CL POST fragmented-success wire in `2e34ffc1`: ordinary
  RUT source preserves exact POST, fixed request-policy, strict bundle, and
  one-second timeout metadata through RIR/JIT/RouteConfig into production
  cleartext io_uring. One fresh origin receives the exact rewritten request
  with a byte-identical 12-byte binary body, then emits exact HTTP/1.1 200 with
  unique CL0 in seven positive fragments. Six recorded adjacent gaps are
  constrained to 100--650ms while first-fragment-to-client completion exceeds
  1.10s; the client receives the exact configured CL0 success rather than the
  timeout response. The origin stays open after its final fragment, and only
  RUT retirement supplies the later close; one accept/request and a settle
  interval exclude reconnect. Independent review is `APPROVE`. Sol passed the
  new wire 1/1 (75 checks), upload-boundary neighbor 1/1 (310), and fragmented
  GET baseline 1/1 (64). Fixed-upload progress-stall, successful response
  bodies, converter lowering, and nginx differential evidence remain open;
  #270 stays blocked.
- #268 public fixed-CL POST upload-boundary wire in `29b47e9c`: ordinary RUT
  source preserves exact POST, request-policy, policy-bundle, and one-second
  timeout metadata through RIR/JIT/RouteConfig into a production cleartext
  io_uring shard. A 12-byte binary body is split 5/7; the incomplete upload is
  held for at least 1.2 seconds plus a 100ms downstream quiet/open probe while
  origin accepts and requests remain exactly zero. After the final fragment,
  one fresh origin records the exact rewritten header and byte-identical body,
  stays open, and sends zero bytes. The exact configured full 504 completes
  more than one second after final-body send and after origin request
  observation, below the unrelated global timeout, then RUT retires the
  origin. Review added a second origin-zero assertion after the quiet probe;
  final review is `APPROVE`. Sol passed the wire 1/1 (310 checks) and its
  bodyless POST neighbor 1/1 (77 checks). Fragmented response progress,
  progress-stall, successful response bodies, converter lowering, and nginx
  differential evidence remain open; #270 stays blocked.
- #268 bounded fixed-CL request-upload runtime in `54545566`: cleartext
  io_uring HTTP/1.1 exact/ANY POST/PUT/PATCH routes now admit one positive
  unique Content-Length body within the existing 16 KiB request-policy slice.
  The handler runs once; pinned config, route/function, bundle, raw/rewritten
  body boundaries, binary bytes, upstream identity, episode, and full-send
  proof are revalidated across body continuation, upload, first-batch copy,
  expiry, D1/D2, and reset. No explicit response-read timer exists during body
  wait, connect, or upload; only exact full upload plus a successfully queued
  first response Recv arms it. Surplus, forged/stale ownership, short/error/
  over-reported sends, and every excluded shape fail closed. Success remains
  exact HTTP/1.1 200/unique-CL0. Two review rounds corrected initial handler
  identity admission and isolated short/error/stale tests; final review is
  `APPROVE`. Sol passed deadline 48/48 (1,854 checks), Network 871/871
  (113,088), Frontend 974/974, JIT 486/486, and Http2Conn 75/75. Integration
  passed 390 tests and failed only the independent #263 baseline (38,705
  checks, one skip). Public ordinary-source upload timing, response bodies,
  converter lowering, and nginx differential evidence remain open; #270 stays
  blocked.
- #268 fixed-CL request-upload design received independent `APPROVE_DESIGN`.
  The selected general-RUT slice admits bounded positive unique-CL POST/PUT/
  PATCH on exact or ANY routes, reuses the existing request-policy body
  continuation, invokes the handler once, pins the authoritative policy bundle
  and exact raw/rewritten body boundaries, and rejects surplus or unsupported
  framing before upstream effects. The explicit response-read timer remains
  absent during client-body wait, connect, and upload; only an exact full-send
  terminal followed by a successfully queued first response Recv may arm it.
  Success remains exact HTTP/1.1 200/unique-CL0. Buffered positive-CL responses
  were rejected as the next slice because nginx may already commit response
  bytes before a later inactivity close, so private buffering plus a replacement
  504 would not be behavior-equivalent. #270 stays blocked.
- #268 public ANY-route TRACE fragmented-success wire in `c6edca67`: ordinary
  method-omitted RUT source preserves exact `kRouteMethodAny` through RIR and
  registered RouteConfig, then a production cleartext io_uring shard receives
  a strict bodyless TRACE. One fresh origin records the exact rewritten TRACE
  wire and emits exact HTTP/1.1 200/unique-CL0 in eight positive fragments; all
  adjacent gaps are 100--650ms while first-fragment-to-client completion
  exceeds 1.10s. The exact downstream CL0 response has no extra bytes, the
  fixture keeps origin open, and RUT-attributed retirement occurs after the
  final fragment with one accept/request only. Independent review is `APPROVE`;
  Sol passed TRACE plus its GET fragmented baseline 2/2 with 130 checks.
  Request/response bodies, converter lowering, and nginx differential evidence
  remain open; #270 stays blocked.
- #268 public bodyless POST wire in `7092cbfd`: an explicit POST ordinary-RUT
  route preserves exact `kRouteMethodPost` through RIR and registered
  RouteConfig, then runs on a production cleartext io_uring shard. Origin 1
  receives an exact POST request with no CL/TE/body, sends zero bytes while
  staying open, and observes RUT retire it; the client receives the exact full
  configured 504 including its 20-byte body after the one-second interval and
  below the unrelated global timeout. Only afterward, the same downstream
  socket sends an exact bodyless POST through a fresh origin connection; origin
  2 stays open after exact HTTP/1.1 200/unique-CL0 and the exact downstream CL0
  response remains quiet/open. An initial review correctly rejected missing
  exact-route assertions; the minimal correction was re-reviewed `APPROVE`.
  Sol passed POST plus its GET baseline 2/2 with 151 checks. TRACE public
  evidence, request/response bodies, converter lowering, and nginx differential
  evidence remain open; #270 stays blocked.
- #268 generic bodyless non-HEAD/CL0 runtime in `54dd54ad`: the deadline
  profile now admits exact known GET, POST, PUT, DELETE, PATCH, OPTIONS, and
  TRACE methods, with TRACE restricted to a method-omitted/ANY route; HEAD
  remains separate and CONNECT/extension methods reject before effects. Every
  admitted request is strict origin-form with exactly one Host and no CL/TE/
  body/pipeline. Exact method identity is captured and revalidated through the
  active owner, first-batch snapshot, backend copy witness, batch ledger,
  expiry, retained D2, send completion, disarm, and reset. Existing progress,
  race, exact configured full-timeout, exact 200/unique-CL0 success, and origin
  retirement machinery is reused. Independent review is `APPROVE`. Sol passed
  Network 863/863 (112,790 checks), Frontend/JIT/Http2Conn, while Integration
  passed 388 tests and failed only the independent #263 baseline (38,562
  checks, one skip). Public POST/TRACE wires, request/response bodies, converter
  lowering, and nginx differential evidence remain open; #270 stays blocked.
- #268 next-increment design comparison selected a generic bodyless origin-form
  non-HEAD/CL0 profile over bounded buffered CL>0 responses and received
  independent `APPROVE_DESIGN`. The selected runtime domain admits known GET,
  POST, PUT, DELETE, PATCH, OPTIONS, and TRACE methods with no CL/TE/body;
  TRACE enters only through a method-omitted/ANY route. HEAD remains on its
  separate suppress profile; CONNECT, extension methods, `OPTIONS *`, explicit
  request CL0, and every other excluded shape reject before upstream effects.
  Exact parsed method identity must be captured and revalidated through active
  ownership, copy witness, batch ledger, expiry, retained D2, and reset. Timer,
  progress, race, full-timeout D1/D2, CL0 success, and retirement machinery are
  otherwise reused. Buffered response bodies remain a later, materially larger
  design; #270 stays blocked.
- #268 public GET/CL0 positive-progress-then-stall wire in `5876280e`: ordinary
  RUT source reaches production cleartext io_uring; origin 1 receives the exact
  bodyless GET, sends one recorded non-empty incomplete header fragment, then
  stays open and silent. Both fragment-to-client timeout completion and
  fragment-to-origin-retirement exceed one second while staying below the
  unrelated global-timeout class. The client receives the exact configured full
  504 including its 20-byte body. Only after RUT-attributed origin retirement,
  the same downstream socket sends request 2 to a fresh origin, which stays
  open after an exact HTTP/1.1 200/unique-CL0 response; the exact downstream CL0
  success remains quiet/open. Independent review is `APPROVE`; Sol passed this
  vector and its HEAD neighbor 2/2 with 138 checks. Broader methods/bodies,
  converter lowering, and nginx differential evidence remain open; #270 stays
  blocked.
- #268 public fragmented GET/CL0 success wire in `34c7e156`: ordinary RUT
  source reaches a production cleartext io_uring shard and one fresh origin
  receives the exact bodyless GET. The exact HTTP/1.1 200/unique-CL0 header is
  emitted in eight recorded positive fragments; every adjacent send gap is
  100--650ms while first-fragment-to-client completion exceeds 1.10s. The
  client receives the exact configured CL0 response, the origin stays open
  after sending, and only RUT retirement supplies the observed close; no
  reconnect occurs. Independent review is `APPROVE`; Sol passed this vector and
  its neighboring fragmented HEAD regression 2/2 with 115 checks. GET
  progress-then-stall, broader methods/bodies, converter lowering, and nginx
  differential evidence remain open; #270 stays blocked.
- #268 public bodyless-GET/CL0 wire in `045412ab`: ordinary RUT source compiles
  through lexer/parser, HIR/MIR/RIR, JIT, RouteConfig, and a production
  cleartext io_uring shard. Origin 1 receives the exact bodyless GET, sends zero
  bytes while staying open, then observes RUT retire it; the client receives the
  exact configured full 504 response including its 20-byte body below the
  unrelated global-timeout class. Only after retirement, the same downstream
  socket sends request 2 through a fresh origin connection; origin 2 returns
  exact HTTP/1.1 200 with one `Content-Length: 0` while staying open, and the
  client receives the exact configured CL0 success and remains quiet/open.
  Independent review is `APPROVE`; Sol passed this vector plus three neighboring
  deadline wires 4/4 with 241 checks. Fragmented GET progress, broader methods/
  bodies, converter lowering, and nginx differential evidence remain open;
  #270 stays blocked.
- #268 bodyless-GET/CL0 runtime slice in `0e58e97b`: one authoritative captured
  deadline profile and shared owner validator now cover the existing strict
  HEAD domain plus cleartext io_uring H1.1 bodyless default-keepalive GET with a
  fresh single IPv4 upstream. GET success accepts only exact HTTP/1.1 200 with
  one `Content-Length: 0`; every other status/framing/parser shape retires the
  origin and zero-byte closes before downstream mutation. A distinct immutable
  full-Content-Length prebuilt purpose validates the complete configured timeout
  body, including empty, binary, and exact-capacity bodies. Both timeout and
  success retire the fresh origin, and a two-owner send/retirement rendezvous
  gates request 2. Independent review is `APPROVE`. Sol passed Network 856/856
  (112,329 checks), Frontend, JIT, and Http2Conn; Integration passed 385 tests
  and failed only the independent #263 baseline (38,343 checks, one skip).
  Public ordinary-source GET wire evidence, broader methods/bodies, converter
  lowering, and nginx differential evidence remain open; #270 stays blocked.
- #270 bounded parser/model recognition in `1e3b1477`: the independent nginx
  parser accepts exactly one explicit raw root-location `proxy_read_timeout Ns`,
  `N=1..63`, stores nginx-semantic milliseconds plus directive/value spans, and
  gives source-located diagnostics for duplicate/arity/context errors, invalid
  time values, and nginx-valid but excluded aliases/quoted/escaped forms. The
  converter independently validates present/absent values, root path, and span
  invariants before any output allocation; every valid present directive returns
  `lowering is not implemented`, while forged models fail closed. Absence keeps
  legacy generated RUT byte-identical. Independent review is `APPROVE`; Sol
  passed `test_nginx_parser` 23/23 with 518 checks. No RUT lowering, golden
  timeout output, differential, or support claim exists; #270 remains
  `NOT_IMPLEMENTED` and its lowering is blocked by #268's broader method/body
  domain.
- #268 public fragmented timing evidence in `4b40e2cb`: ordinary RUT source
  compiles through lexer/parser, HIR/MIR/RIR, JIT, RouteConfig, and a production
  cleartext io_uring shard. Six actual positive header fragments have measured
  100--650ms gaps while first-fragment-to-client-completion exceeds 1.10s, and
  the exact strict HEAD success completes without reconnect or EOF. A separate
  vector sends one positive incomplete fragment, stays open/quiet beyond 1s,
  receives the exact configured header-only 504 below the global-timeout class,
  observes RUT close origin 1, then reuses the same downstream socket against a
  fresh exact origin request. Independent review is `APPROVE`; Sol passed the
  two vectors plus the zero-byte deadline wire 3/3 with 167 checks. This proves
  only the narrow general-RUT domain, not nginx equivalence; #268 remains open
  for broader lifecycle/domain acceptance while #270 can resume bounded work.
- #268 fragmented strict-HEAD runtime progress in `60abda7b`: the io_uring
  provided-buffer producer emits all-or-nothing copy witnesses, and a bounded
  pointer-free whole-batch ledger validates generation, episode, byte ranges,
  every matching Recv terminal, downstream EOF/error vetoes, and slot pinning
  before mutation. Matching positive progress wins a same-batch Timeout; live
  `F_MORE` refreshes the explicit per-forward deadline without another SQE;
  terminal incomplete progress queues exactly one continuation Recv before
  refresh. Later expiry authoritatively revalidates the complete admitted domain
  and retained generation/episode/exact-prefix proof before consuming private
  bytes and entering the configured D1/D2 response. Three review rounds ended
  in `APPROVE`. Sol passed Network 846/846 (111,904 checks), Frontend, JIT, and
  Http2Conn, while Integration passed 383 tests and failed only the independent
  #263 baseline (38,235 checks, one skip). The admitted domain remains narrow;
  public fragmented timing evidence, converter lowering, and nginx differential
  evidence remain open, so #270 stays blocked.
- #268 fragmented strict-HEAD progress design has independent
  `APPROVE_DESIGN` after one `REQUEST_CHANGES` round. The selected general-RUT
  primitive adds all-or-nothing backend copy witnesses and a bounded batch
  ledger keyed only by connection ID, deadline generation, and upstream
  episode. Every owner is validated before timer/state mutation; matching
  progress wins same-batch Timeout, earlier fragments are accounting-only, and
  the parser runs once at the final relevant CQE. Live `F_MORE` refreshes
  without another SQE, while terminal incomplete progress must queue one exact
  continuation before refresh. Invalid copy/cancel/terminal ordering or same-
  batch client close zero-byte closes and pins slot reuse until batch-end
  reclaim. The admitted source/request/upstream/policy domain remains unchanged;
  nginx issue #270 stays blocked by #268.
- #268 public ordinary-RUT deadline wire in `e30cbe47`: an effect-free direct
  HEAD Forward with `response_read_timeout: 1s` compiles through lexer/parser,
  HIR/MIR/RIR, JIT, RouteConfig, and production cleartext io_uring serving.
  A real origin receives the complete request, sends zero response bytes, and
  observes RUT retire it; the client receives the exact configured header-only
  `504 First Response Deadline` within eight seconds while the global upstream
  timeout remains its default greater than eight. The same downstream
  connection then completes a second HEAD against a fresh origin. Independent
  review is `APPROVE`; Sol passed the new and nearest #267 wires 2/2 with 116
  checks. This still does not prove positive-progress refresh, general
  `proxy_read_timeout`, converter lowering, or nginx differential equivalence.
- #268 narrow first-response runtime deadline in `7228a7b4`: cleartext io_uring
  H1.1 bodyless default-keepalive HEAD with one fresh IPv4 upstream and the
  complete paired strict policy bundle now pins the immutable route bundle,
  submits one exact fresh response Recv after upload, owns the TimerWheel node,
  and resolves same-batch Timeout/Recv deterministically before reclamation.
  An unresolved expiry can call only the proven #267 configured timeout path;
  every unsupported or forged shape closes with zero bytes. A review found and
  a different Luna fixed the sole local-400 materialization leak; final review
  is `APPROVE`. Sol passed network 835/835 (111,501 checks), frontend 974/974
  (7,511), JIT 486/486 (5,965), and Http2Conn 75/75 (232). Integration passed
  382 tests and failed only the independent #263 TLS watermark baseline. No
  public ordinary-RUT timing wire, progress rearm/refresh, converter lowering,
  or nginx differential exists yet.
- #268 narrow first-response deadline design has independent
  `APPROVE_DESIGN`. It admits only cleartext io_uring H1.1 bodyless HEAD, one
  IPv4 fresh upstream, and the complete paired strict policy bundle. A
  dedicated fresh-Recv primitive replaces ambiguous idempotent submission;
  the pinned route bundle remains authoritative; Timer expiry latches as
  `ExpiryPending` until the whole CQE batch is accounted, with a matching Recv
  deterministically winning. Batch-end expiry calls only the proven #267
  strict timeout path and otherwise closes with zero bytes. Body/body-wait,
  reuse/retry, TLS/H2/epoll, transforms/mutations/decorators, incomplete first
  response batches, and unpaired policies remain explicitly rejected. This
  slice still excludes positive-progress rearm/refresh, partial responses,
  converter lowering, and nginx differential evidence.
- #268 ordinary-RUT metadata and preflight foundation in `8b541a5a`:
  `forward(..., response_read_timeout: <duration>)` accepts only exact whole
  seconds 1..63 and carries the scalar through AST/HIR/MIR/RIR, the immutable
  policy bundle, RouteConfig, and JIT dispatch without widening the packed
  handler ABI. This first slice deliberately admits only an effect-free static
  direct Forward; branch/yield/body-dependent/effectful shapes and request/
  response mutation combinations reject explicitly. A route stores only the
  authoritative bundle ID, so H1 closes before rate limiting, body wait,
  handler execution, or upstream work, while H2 uses a sticky process-batch
  close fence and reclaims only after `Http2Conn::process()` returns. RIR
  verification is bidirectional, and JIT route registration validates and
  stages the complete fixed registration before commit. Two final reviews are
  `APPROVE`. Sol's full affected suites passed frontend 974/974 (7,511 checks),
  JIT 486/486 (5,965), network 823/823 (111,234), and Http2Conn 75/75 (232).
  This is metadata/preflight evidence only: no timer arm, refresh, timeout
  response, converter lowering, or nginx differential exists yet.
- PR #269 CI hygiene in `12581aeb` and `c5a87303`: the simulator reconstructs
  its non-owning RIR module in place instead of triggering GNU 13's aggregate-
  assignment ICE, and the held-epoll invalid-output regression now uses the
  defined nonnull/`maxevents == 0` EINVAL path instead of violating glibc's
  nonnull contract under UBSan. Both changes have independent `APPROVE`
  reviews and focused local tests. PR CI run `32539599547` confirmed GNU 13
  compilation and the sanitizer job; remaining integration failures are the
  independent #263 TLS watermark baseline.
- #268 general RUT design investigation and independent `APPROVE_DESIGN`:
  `response_read_timeout` is a per-forward upstream response-read inactivity
  interval, not a total request deadline. Exact duration literals representing
  1..63 whole seconds fit the current wheel; zero is absence and explicit
  values never inherit the global 30-second timeout. The scalar extends the
  existing policy bundle without changing the packed handler ABI. Runtime work
  must pin request/config/episode ownership, refresh only on exact positive
  upstream progress, settle same-batch Recv/Timeout races explicitly, and keep
  every unproven phase zero-byte fail-closed. Converter lowering remains later.
- #267 public ordinary-RUT Recv-timeout wire in `a1cd9745`: the source declares
  distinct response, default-502, and timeout-504 policies, compiles through
  HIR/MIR/RIR/JIT/RouteConfig, and runs on a real production io_uring shard.
  A test-only pre-spawn global clock acceleration triggers the real
  timerfd/TimerWheel path without fabricating request policy IDs, transport
  ownership, pending counts, callbacks, timeout events, or CQEs. The origin
  sends zero response bytes and proves RUT closes it; the same downstream
  connection then reaches a fresh origin and completes an exact header-only
  200/close response. The first response is the exact configured header-only
  504/CL167. Independent review is `APPROVE`; Sol's affected public set passed
  6 tests with 241 checks. This proves policy selection, not #268/source-level
  duration semantics, converter lowering, or nginx differential equivalence.
- PR #269 GNU 13.3 build compatibility repair in `f4346acc`: the RIR module
  reset now uses an explicitly typed value, preserving destroy/init/source-name
  semantics while avoiding the compiler's untyped-brace assignment failure.
  Independent review is `APPROVE`; Sol's focused frontend set passed 5 tests
  with 82 checks. PR CI remains responsible for the exact GNU 13.3 rerun.
- #267 bounded Recv-only runtime selection in `77542eeb`: a configured timeout
  policy now serializes transactionally only for cleartext H1.1, bodyless,
  default-keepalive paired `HEAD` with the exact live upstream Recv owner and
  composes with #265 D1/D2. Every unsupported phase/layout remains zero-byte
  fail-closed; epoll stays explicitly unsupported, the default policy ID is not
  mutated, and the successful timeout-origin rendezvous rearms a bounded timer.
  Production-transition evidence reaches admission through the real JIT,
  Connect and full Send callbacks, backend completion dispatch, and TimerWheel;
  fake CQEs replace only kernel delivery. Two independent reviews are
  `APPROVE`. Sol's focused network set passed 54 tests with 2,257 checks.
  Public ordinary-source wire behavior, #268 duration semantics, converter
  lowering, and pinned differential evidence remain open.
- #267 ordinary-RUT timeout-policy metadata/compiler foundation in `b41b1aac`:
  additive `timeout_failure_policy` carries a complete 400..599 immutable policy
  beside the exact-502 default policy through AST/HIR/MIR/RIR, a triple policy
  bundle, RouteConfig/JIT, fixed-body pending state, current request metadata,
  and reset without widening the packed handler ABI. Invalid roles/IDs, missing
  peers, HEAD mismatches, caps, forged outcomes, and config swaps are covered
  before upstream effects. Until runtime selection lands, an explicit timeout
  policy zero-byte fails closed rather than falling into legacy 504/default 502;
  no-policy behavior is unchanged. Two independent reviews are `APPROVE`.
  Sol's focused frontend/JIT/network set passed 8 tests with 194 checks. Runtime
  serialization/D1-D2, public-source behavior, #268 duration semantics,
  converter lowering, and pinned differential remain open.
- Converter-generated pinned incomplete-header/EOF differential in `61bedb27`:
  the independent converter parses/lowers the same minimal nginx fragment to
  ordinary RUT. Fresh pinned-nginx 1.29.7 and generated-RUT recorders both prove
  that exact positive incomplete header bytes stay quiet/open before the
  test-authorized origin EOF, then yield the exact reusable header-only
  502/CL157. Request 2 uses the same downstream connection, a fresh origin
  connection, the exact byte-identical upstream wire, and ends with the exact
  200 response plus downstream EOF. All request-wire/connection assertions run
  after recorder join; failure cleanup waits for recorder-thread Abort
  acknowledgement and cannot manufacture EOF. Two independent reviews are
  `APPROVE`; required serial Docker CTest passed 1/1 in 13.65s with no skip or
  residue. This proves observable EOF behavior, while focused runtime tests
  retain zero-owner/CQE evidence. Timeout/504 remains open under #265 and
  invalid-status HTTP/0.9 fallback remains #266.
- Converter-generated pinned malformed-header differential in `4c844be2`: the
  independent converter parses/lowers the same minimal nginx fragment to
  ordinary RUT, and pinned nginx 1.29.7 matches production generated RUT for a
  valid HTTP/1.1 status followed by the empty-name header `:`. Both yield the
  exact header-only reusable 502/CL157, quiet window, proxy-attributed origin-1
  close, fresh exact request-2 origin, exact 200 close response, and EOF. Fresh
  recorder instances prove two exact accepts/connection IDs and byte-identical
  origin wires. Independent review is `APPROVE`; required serial Docker CTest
  passed 1/1 in 10.33s with no skip or residue. The initially proposed
  `not-http` wire was correctly rejected: nginx treats it as HTTP/0.9 fallback,
  a real broader gap now tracked in #266. At this checkpoint incomplete EOF and
  timeout remained open; the former is now completed by `61bedb27`.
- #265 public incomplete-header/EOF wire evidence in `5c7668dc`: an atomic
  origin-close gate proves non-empty incomplete response bytes alone leave the
  downstream quiet/open, then authorized EOF yields the exact configured
  header-only keep-alive 502/CL157. The same downstream socket sends request 2
  only afterward, using a fresh exact origin wire and ending with the exact
  header-only 200 close response and EOF. Failure, premature-close, unexpected-
  data, authorized-close, and teardown states cannot manufacture the terminal
  event needed to pass. Independent review is `APPROVE`. Sol's public parser-
  failure pair passed 83 checks; full integration retained only #263 (381
  passed, 1 failed, 1 skipped; 38,115 checks). Exact zero-owner accounting stays
  in focused tests, and converter-generated differential evidence is still
  absent.
- #265 public RUT invalid-status-prefix/open-peer wire evidence in `fecec54a`:
  ordinary public
  RUT source compiles through HIR/MIR/RIR and JIT into the production io_uring
  loop. The RUT-specific `not-http` parser error yields the exact configured
  header-only keep-alive 502/CL157 and a quiet reusable boundary; the origin
  observes RUT close request 1 before the same downstream socket sends request
  2, which uses a fresh exact upstream wire and ends with the exact header-only
  200 close response and EOF. The test deliberately does not claim to observe
  kernel `CQE_F_MORE`; focused tests separately prove the exact live-Recv mask.
  Independent review is `APPROVE`. Sol's targeted pair passed 69 checks and full
  integration retained only #263 (380 passed, 1 failed, 1 skipped; 38,077
  checks). This wire is not nginx-equivalent because nginx falls back to
  HTTP/0.9; #266 now records that distinction.
- #265 parser-only production admission in `ba39832a`: non-empty malformed
  positive reads now rendezvous the configured transactional header-only 502
  with exact live-Recv retirement, while buffered incomplete-header terminal
  EOF/error uses the zero-owner handoff. Cause, policy, request, upload, and
  ownership predicates remain exhaustive; every excluded cause fails closed
  without bytes or fallback. Two independent reviews are `APPROVE`. Sol's
  focused suite passed 70 tests with 2,772 checks, the full network suite passed
  807 tests with 110,654 checks, and fresh integration retained only #263 (379
  passed, 1 failed, 1 skipped; 38,032 checks). Public wire and converter
  differential evidence are still absent, so #265 remains open.
- #265 D2 internal prebuilt-header/request-epoch rendezvous in `cf22e100`:
  HeaderSend and exact upstream retirement now race under separate wait owners,
  but request 2 is published only at batch end after phase-bound buffer
  normalization and exact-once epoch release. Zero-owner terminal handoff,
  max-token quarantine, real single-wait composition, duplicate HeaderSend
  fencing, close/reset slice pinning, and reachable Connect/Send/Recv layouts are
  covered. Two review rounds found and closed three HIGH/phase blockers; final
  reviews are `APPROVE`. Sol's full network suite passed 804 tests with 110,534
  checks, and fresh integration retained only #263 (379 passed, 1 failed, 1
  skipped; 38,032 checks). At that checkpoint there was no production failure
  caller.
- #265 D2 reconciled design: a transactional prebuilt header may race exact
  upstream retirement, but both sides publish request-2 readiness only at batch
  end. Compact wait/layout state preserves Connect/fresh-Send prefixes,
  retry-Send snapshots, existing pipeline ordering, and the old config epoch;
  a zero-owner terminal path advances the episode without inventing ownership.
  An independent adversarial re-review is `APPROVE_DESIGN`. No code or public
  behavior changed; the selected implementation remains internal-only.
- #265 D1 exact-owner transport foundation in `2bdbfe1d`: the io_uring
  retirement ledger now owns Connect/Send/Recv target, cancel, and retry bits
  independently, atomically detaches selected live authority, fences retired
  Send mutation, and preserves masks/tombstones through close/reset to exact-one
  reclaim. The production strict entry remains recv-only and no public failure
  response was enabled. Two independent reviews are `APPROVE`; Sol's full
  network suite passed 794 tests with 110,246 checks, and fresh integration kept
  only the #263 baseline (379 passed, 1 failed, 1 skipped; 38,032 checks). #265
  remains open for the header/epoch rendezvous and public behavior.
- Converter-generated reusable paired `HEAD` differential in `5a334df6`: the
  same minimal nginx fragment is parsed/lowered to ordinary RUT source, and
  pinned nginx 1.29.7 matches production RUT for two-request header-only success
  and connect-establishment 502 behavior, including quiet windows, final EOF,
  fresh exact upstream wires, and request-derived persistence. Independent
  reviews are `APPROVE`; Sol's required serial Docker CTest passed 1/1 in 7.89s
  without skips or resource residue. Broader failures remain #265/`PARTIAL`.
- #264 bounded public reusable paired `HEAD` admission in `67411a67`: default
  HTTP/1.1 keep-alive success and connect-establishment 502 now preserve body
  suppression independently from request-derived persistence, while broader
  failures terminate before bytes under #265. Public source-to-real-io_uring
  evidence proves two header-only responses, fresh exact upstream wires, C1-C3
  retirement, and final EOF. Independent reviews are `APPROVE`; Sol's full
  network suite passed 789 tests with 109,994 checks, and full integration had
  only the #263 baseline (379 passed, 1 failed, 1 skipped; 38,032 checks). #264
  is closed; converter-generated differential evidence remains CURRENT.
- A three-way public-admission audit found the next bounded split. #264 may close
  after real reusable success and connect-establishment failure evidence, while
  timeout, send/recv failure, and malformed/incomplete/excess response paths must
  emit zero bytes and terminate until #265 supplies generic all-upstream-I/O
  abandonment. Pinned nginx 1.29.7 proves those broader paths instead emit
  reusable header-only 502/504 responses, so #265 tracks the real compatibility
  gap and #253 remains `PARTIAL`.
- #264 C3 repeated strict recv retirement in `ef6e5d73`: a persistent latest
  tombstone is atomically replaced only after exact prior ownership drains, so
  two production strict callbacks can retire E1 and E2 before request 3 resumes.
  Old records remain inert across reset/reuse, close/reclaim composes with the
  current successor ledger, and exhaustion quarantines. Independent reviews are
  `APPROVE`; Sol's full network suite passed 786 tests with 109,913 checks, and
  the freshly rebuilt integration suite retained only the #263 baseline
  (378 passed, 1 failed, 1 skipped; 37,991 checks). Public reusable `HEAD`
  remains disabled, so #264 stays open.
- #264 C2 generic io_uring HTTP/1 request-boundary rendezvous in `3bb907ac`:
  request-1 bookkeeping completes once, request-2 bytes remain inert until C1
  retirement drains, and batch-end resume occurs exactly once. Exact successor
  ownership now fences backend recv/send mutation and survives close through a
  per-type target/cancel ledger. Independent reviews are `APPROVE`; Sol's full
  network suite passed 779 tests with 109,339 checks, while the freshly rebuilt
  full integration suite had only the independently tracked #263 failure
  (378 passed, 1 failed, 1 skipped; 37,991 checks). Reusable `HEAD` and a second
  strict retirement remain disabled, so #264 stays open.
- Repository architecture and RUT capability reconnaissance.
- Existing test infrastructure and differential harness reconnaissance.
- Official nginx semantic analysis for the minimal proxy example.
- Compatibility matrix v0 and converter architecture proposal.
- Minimal nginx fragment lexer/parser, semantic model, and targeted tests.
- Any-method route design investigation for #251; method omission was selected
  over an `ANY` keyword.
- Any-method source route implementation for #251, including compiler/runtime
  registration and precedence tests.
- Independent review and production registration validation for #251; the RUT
  capability issue is closed.
- First general source listener capability for #250, including source/CLI
  resolution, owned loader metadata, independent review, targeted tests, and
  production request-through-route acceptance. #250 is closed.
- Production io_uring cross-thread submission repair for #255, including fatal
  control-plane propagation, two review rounds, and real one/two-shard HTTP
  process acceptance. #255 is closed.
- Upstream request-policy design investigation for #252; the recommended source
  surface is per-forward opt-in backed by an immutable general policy table.
- First strict #252 request-policy slice, including exact H1 request
  reconstruction, u16 policy ABI, compiler/runtime conflict guards, independent
  review, and 10 Sol-run targeted tests (139 checks). The capability remains
  `PARTIAL` and #252 remains open.
- Proxied response-policy design investigation for #253; the existing transparent
  response path is explicitly insufficient for nginx equivalence.
- Response-policy metadata/ABI foundation and bounded strict H1 serializer for
  #253, including config-owned strings, independent request/response policy IDs,
  exact Content-Length output, H1/H2 fail-closed limits, and targeted tests.
  Broader response compatibility remains `PARTIAL`.
- Independent strict-serializer reviews and Sol validation: 19 targeted tests
  passed with 507 checks. A real RUT process matched the pinned nginx 1.29.7
  downstream response after normalizing only dynamic `Date`, and emitted the
  exact expected upstream request. #253 remains open and `PARTIAL`.
- Deterministic minimal-model lowering in `e6e1ff9b`, followed by independent
  review and the RIR-depth test correction in `4508e306`. Sol reran the focused
  target: 13 tests, 125 checks.
- First converter-generated process loop: the same fragment was included by
  pinned nginx 1.29.7 and parsed/lowered through the converter API for RUT.
  Both matched the bounded GET response vector after only Date normalization,
  and the recording backend received byte-identical requests. #254 remains
  open because its POST/body and gateway-error acceptance cases are unmet.
- Pinned nginx POST baseline: one fixed-CL binary body is forwarded byte for
  byte with the expected rebuilt headers. A 15-second half-body timing probe
  showed zero backend accepts until the final body bytes arrived, rejecting an
  early-connect streaming design. A reviewed Option-B design uses bounded
  pre-upstream staging and keeps #253 body admission as a separate dependency.
- Bounded #252 request-body staging in `0e13413a`, exact-cap/fail-closed
  hardening in `1b051cb1`, and the header-only semantic correction in
  `b0ac05cf`. Independent review is `APPROVE`; Sol reran 7 targeted tests with
  16,495 checks. Complete body arrival now precedes upstream slot/connect, and
  full upload completion is tracked separately from local buffering.
- Bounded #253 fixed-body admission in `642a6085`, its non-vacuous slot test in
  `ce226557`, and response-before-send-completion repairs in `501db5ba` and
  `8e023bfa`. The strict serializer consumes one nginx-compatible upstream
  `Connection` field via `1193a043`, with token/duplicate evidence in
  `303e9a22`; all reviews are `APPROVE`.
- Pinned converter-generated POST process loop: a split four-byte binary
  fixed-CL request produced byte-identical upstream bytes and exact normalized
  downstream bytes versus nginx 1.29.7. Neither implementation connected to
  the backend during the 350 ms half-body window. Sol reran 5 integration tests
  with 319 checks and 2 network tests with 49 checks.
- Pinned unavailable-upstream evidence: nginx emits a 157-byte HTML 502 with
  Server/Date/Content-Type and request-derived keep-alive after the complete
  request body; current RUT emits its hard-coded 11-byte close response. This is
  classified as a RUT capability gap and tracked in #256.
- Request-derived downstream connection mode in `216672d2`, followed by the
  drain/header lifecycle correction and syntax documentation in `45df3f41`.
  Independent review is `APPROVE`; Sol reran 12 targeted tests with 346 checks.
  A pinned converter-generated `Connection: close` success differential matched
  normalized downstream bytes, exact upstream bytes, and EOF after the body.
- The #256 immutable failure-policy/bundle/JIT-RIR foundation in `13877fa1` and
  `5d1bb789`, with fail-closed simulator/runtime regressions in `165c4a2b` and a
  clean-build test correction in `1105897c`. Independent review is `APPROVE`;
  Sol ran 8 targeted tests with 143 checks. Runtime serialization remains open.
- The bounded #256 runtime serializer and connection-establishment failure wiring
  in `32d05244`, with deterministic endpoint/injection and combined-success
  evidence in `7cb6a883`. Independent review is `APPROVE`; Sol ran 11 targeted
  tests with 202 checks. Converter lowering and the real differential remain.
- Converter failure-policy lowering in `f45c767c`, corrected to the pinned
  157-byte CRLF body in `63f635c9`; independent review is `APPROVE` and Sol ran
  4 converter tests with 90 checks.
- Pinned unavailable-upstream process differential: converter-generated RUT and
  nginx 1.29.7 matched complete Date-normalized bytes for two keep-alive GETs,
  explicit close plus EOF, and a split fixed-CL POST. Neither emitted the POST
  502 during the 350 ms half-body window. #256 acceptance is complete.
- Pinned percent-encoded success differential: `/encoded/%7Euser?tag=unreserved`
  produced identical Date-normalized downstream bytes and byte-identical
  upstream requests; both preserved `%7E` in the raw forwarded target. The
  explicit #254 vector set is complete for the bounded minimal fragment.
- The first committed serial differential harness case in `b6b1814c`, hardened
  by `3257feda` and `58c12bc6`: it exercises the converter API, production RUT,
  and the pinned no-pull nginx image for the `%7E` success vector. Independent
  review is `APPROVE`; Sol's real Docker CTest passed with no resource residue.
- The second committed harness case in `989a969f` reserves a non-listening
  upstream port and proves the explicit-close 502 response and EOF against the
  complete pinned CRLF baseline. Independent review is `APPROVE`; Sol's real
  two-case Docker CTest passed and #257 acceptance is complete.
- Pinned `proxy_pass` URI-replacement reconnaissance for #258: `/api/`,
  `/api/x`, and `/api/x?y=1` become `/`, `/x`, and `/x?y=1`; `/api` redirects;
  repeated slash, dot segment, and `%7E` inputs are normalized. Current RUT has
  only literal full-path `set_path`, so #259 tracks the required general
  transform and #258 is `BLOCKED_BY_RUT`.
- #259 design selection: use an independent immutable `target_transform` policy
  recorded by a pre-return RIR effect and resolved through pinned config before
  all upstream side effects. Preserve old literal `set_path`; the first slice is
  cleartext header-only H1 and explicitly rejects normalization-sensitive input.
- #259 metadata-only foundation in `17d3a1e9`, corrected by `e1fc2ee4`: validated
  borrowed RIR specs are atomically deep-copied into RouteConfig-owned storage.
  Independent review is `APPROVE`; Sol built JIT production RUT and ran 9 tests
  with 88 checks. The metadata is intentionally not executable yet.
- #259 executable fail-closed effect plumbing in `9c07a91f`, with full printer,
  H1 keep-alive, and real H2 initial/timer-resume evidence through `9c3a94c5`,
  `97f51929`, `c6a57343`, `2ac9dda0`, and `2bfbed2e` (intermediate review
  corrections retained in history). Independent reviews are `APPROVE`; Sol
  ran 19 targeted tests with 262 checks. Recorded transforms now fail before
  request-policy and upstream side effects, while the simulator reports
  `Unsupported`; target materialisation and source syntax were absent at that
  checkpoint.
- #259 bounded clean H1 materialisation in `05a1e655`, hardened by `54e48807`,
  with real compiled-JIT/request-policy/backend-wire evidence in `99c362e9`.
  The reviewed slice performs an atomic prefix splice before upstream side
  effects and rejects normalization-sensitive or out-of-domain inputs. Sol ran
  26 targeted tests with 654 checks.
- #259 public compiler syntax in `3351f7a8`, with adversarial coverage corrected
  in `359b9a82`. It validates the exact descriptor, rejects target/header and
  response-mutation conflicts across control flow, preserves all policy bundle
  operands, and lowers to the owned RIR transform table. Independent review is
  `APPROVE`; Sol ran 9 focused tests with 189 checks.
- #259 public source-to-production-JIT evidence in `cd0893c1`, strengthened in
  `129b3c71`: exact clean target replacement, rebuilt Host, preserved ordinary
  headers, real Connection stripping, and normalization-sensitive rejection
  before backend activity. Independent review is `APPROVE`; Sol ran the wire
  test with 73 checks. #259 is closed and #258 is unblocked.
- #258 exact parser/model/golden lowering in `b1f84472`: only `/api/` paired
  with proxy URI `/` is added, while the legacy root/no-URI output remains
  byte-identical. Generated source reaches a verified owned RIR transform and
  preserves all policy operands. Independent review is `APPROVE`; Sol ran 17
  focused tests with 220 checks. Compatibility remains `PARTIAL` pending the
  converter-generated differential.
- #258 pinned clean-vector differential in `880b8786`, with bounded response
  deadlines in `f054cfc8`: converter-generated production RUT matches nginx
  1.29.7 for `/api/`, `/api/x`, and `/api/x?y=1` in exact upstream bytes and
  Date-normalized downstream bytes. Independent review is `APPROVE`; Sol's
  required serial Docker CTest passed all old and new phases in 1.26 seconds.
- #258 fail-closed boundary evidence in `76c92abb`, corrected by `05b1b4ec`:
  `/api`, repeated slash, dot segment, and percent escape each produce an exact
  local 400/EOF while a live recorder observes zero upstream side effects
  through a fixed settle and RUT shutdown. Independent review is `APPROVE`;
  Sol's complete pinned serial CTest passed in 1.82 seconds. #258 is closed;
  only its explicitly bounded clean request domain is `SUPPORTED`.
- #260 reconnaissance: official nginx 1.29.7 control flow confirms automatic
  redirect occurs before the proxy handler, preserves query, and has no
  upstream side effects. RUT inspection found that exact request-derived
  `Location` construction is not source-expressible; #261 now tracks the
  required general capability.
- #260 pinned nginx-only GET/close baseline in `413e210b`: exact absolute
  Location authority/port, query preservation, 169-byte response and EOF, with
  zero upstream activity through shutdown. Independent review is `APPROVE`;
  Sol's complete serial Docker CTest passed in 2.59 seconds.
- #261 design selection: use an owned generic redirect-policy table, dedicated
  Redirect action/terminator, actual-bound listener context, and atomic local
  serializer. The converter will retain one method-omitted `/api` route and use
  exact GET/raw-path guards before forwarding. Independent design review is
  `APPROVE_DESIGN`; no source/runtime capability is implemented yet.
- #261 metadata foundation in `810c8d05`: bounded generic redirect policies are
  validated in borrowed RIR and atomically deep-copied into RouteConfig-owned
  storage, with deterministic binary-safe printing and explicit duplicate/cap
  contracts. It remains unreachable from source, ABI, JIT, and runtime.
  Independent review is `APPROVE`; Sol ran 18 focused tests with 179 checks.
- #261 dedicated Redirect action in `b9df7ea2`, with non-vacuous fail-closed
  evidence corrected in `d3a278cf`: RIR, ABI, JIT, H1/H2 initial and timer
  resume, and simulator distinguish Redirect without pretending support. All
  current runtime paths reject locally before upstream side effects; successful
  serialization and source syntax are absent. Independent re-review is
  `APPROVE`; Sol ran 20 targeted/regression tests with 242 checks.
- #261 H1 success-path investigation selected a generic immutable actual-bound
  listener context copied by value into each connection. The serializer must
  atomically validate Host, raw origin-form path/query, capacity, and the
  pinned RouteConfig before publishing bytes; listener state must not enter
  hot-reloaded RouteConfig.
- #261 actual-bound listener context in `47187ebd`, with production-helper and
  reinitialization corrections in `751a2837`: port-zero multi-shard startup
  binds once, reuses and verifies the concrete kernel port, then copies the
  immutable context into legacy/epoll/io_uring connections after reset. Loop
  reinitialization clears stale state, request boundaries preserve it, and no
  listener data enters RouteConfig. Independent re-review is `APPROVE`; Sol
  ran 8 focused/regression tests with 127 checks.
- #261 bounded H1 serializer in `4d0bc31c`, hardened by `b29edbb5` and the
  timer-resume evidence correction `d74f485b`: policy-driven responses validate
  the cleartext connection, exact request/Host/framing domain, actual listener
  authority, raw query, capacity, and pinned config before atomically publishing
  bytes. Complete Date-normalized wire, close/epoch, hot-swap, and zero-upstream
  evidence passed independent review. Sol ran 30 targeted/regression tests with
  484 checks. H2/simulator execution remains unsupported; public source lowering
  is now compiler-supported, pending source-to-network evidence.
- #261 public-source design selection: use only an inline, fully explicit
  `return redirect({...})` policy inside one terminal `if ... else ...` in the
  existing method-omitted route. Stable 1-based policy IDs are validated at
  every compiler boundary; Redirect paths reject ignored response effects while
  the sibling Forward arm retains its target transform/policy bundle. Independent
  review is `APPROVE_DESIGN`; named policies and global declarations are absent.
- #261 inline source/compiler lowering in `c41b2442`, with parser-transaction,
  guard-failure conflict, and mandatory publication defenses in `5257452c`:
  owned binary bodies and stable policy IDs flow through AST/HIR/MIR into exact
  RetRedirect RIR and deep-owned RouteConfig, while the Forward sibling retains
  its target transform and full policy bundle. Independent re-review is
  `APPROVE`; Sol ran 46 compiler/config/loader tests with 549 checks.
- #261 public-source production evidence in `1fb576aa`, with synchronized
  backend-observation correction in `ee011ec4`: public source compiles through
  AST/HIR/MIR/RIR/JIT and runs on the real H1 loop, producing exact
  Date-normalized `/api` redirects with zero upstream activity while `/api/`
  and descendant requests retain the exact Forward branch behavior. Invalid
  raw targets fail locally. Independent re-review is `APPROVE`; Sol ran 19
  focused/regression tests with 521 checks. #261 is complete and no longer
  blocks converter issue #260.
- #260 converter lowering in `c695ccdc`: the accepted `/api/` + proxy URI `/`
  model now emits one method-omitted `/api` route with an exact GET/raw-path
  Redirect arm and the existing target-transform/request/response/failure
  Forward arm as its terminal sibling. Root lowering remains byte-identical and
  forged models remain fail closed. Independent review is `APPROVE`; Sol ran
  all 17 nginx frontend/converter tests with 242 checks. Converter-generated
  differential evidence is still required before a bounded `SUPPORTED` claim.
- #260 converter-generated differential in `42418f69`: two GET/close `/api`
  redirect vectors match pinned nginx 1.29.7 byte-for-byte after normalizing
  only Date, require EOF, and cause zero backend activity through process
  shutdown. The existing three clean Forward vectors remain exact and three
  normalization-boundary targets remain explicit RUT-only local rejections.
  Independent review is `APPROVE`; Sol's serial Docker CTest passed 1/1 in
  3.37 seconds. #260 is closed and only its exact bounded domain is `SUPPORTED`.
- #253 pinned nginx-only HEAD baseline in `5568b6b2`: an explicit-close HEAD is
  forwarded upstream as HEAD with the normalized upstream-authority Host. A
  strict upstream `Content-Length: 5` response produces exact nginx headers
  retaining that representation length, zero downstream body bytes, and EOF.
  Independent review is `APPROVE`; Sol's serial Docker CTest passed 1/1 in
  3.45 seconds. Current policy-bearing RUT Forward rejects HEAD before upstream
  connect, confirming a RUT capability gap rather than a converter task.
- #253 HEAD design selection: add only a generic response-policy disposition
  with legacy/default `Reject` and explicit future `SuppressBody`; keep policy
  IDs and Forward ABI unchanged. Metadata lands unreachable from source and
  fail closed before the strict H1 runtime increment. Failure-policy semantics
  wait for a separate pinned nginx gateway baseline. Independent review is
  `APPROVE_DESIGN`.
- #253 HEAD response metadata in `1a0fb56a`, with valid target-transform/bundle
  evidence in `dba9315b`: legacy/source policies remain `Reject`; reserved
  `SuppressBody` is validated, deduplicated, printed, deep-owned, and atomically
  published but remains source-unreachable and fails closed before upstream for
  every method. Independent re-review is `APPROVE`; Sol ran 36 focused tests
  with 1,088 checks across compiler/config, plain/transform dispatch, strict
  response/H2 regressions, and RIR printing.
- #253 strict HEAD success runtime in `1d908d2e`, hardened in `9452aebe` after
  review found original TE/Expect/Upgrade facts could be lost during request
  rewriting: internal `SuppressBody` now admits only the pinned cleartext-H1.1,
  bodyless, exact explicit-close domain, preserves representation Content-Length
  with zero downstream body, and abandons the nonpooled upstream after validated
  headers. Overlong coalesced bytes reject before commit; delayed bytes are not
  awaited; late events and completion are idempotent. Independent re-review is
  `APPROVE`; Sol ran 25 unique focused tests with 722 checks. Public source,
  failure policy, and converter selection remain absent.
- #253 pinned nginx-only HEAD gateway baseline in `b7d639e0`, with GCC/Clang
  target-link and line-scoped log correction in `4917575b`: one unavailable
  literal upstream yields exact header-only 502 with representation
  Content-Length 157, explicit close/EOF, and exactly one connection-failure
  record tied to the backend authority. Independent re-review is `APPROVE`;
  Sol's serial pinned Docker CTest passed 1/1 in 3.78 seconds.
- #253 failure-policy HEAD design: use one generic `ForwardPolicyHeadMode` with
  response/failure aliases and legacy `Reject` defaults. Preserve the existing
  response-only `SuppressBody` success path; only paired `SuppressBody` may later
  enter the bounded connect-failure path. Mismatches and failure-only suppression
  reject before body waiting, transform materialisation, or upstream effects.
  Selected-policy serialization is transactional and may never fall back to the
  legacy 11-byte 502. Independent review is `APPROVE_DESIGN`.
- #253 failure-policy HEAD metadata in `175337aa`, with ordinary direct/bundle
  HEAD and pre-bound publication evidence in `924b2d8a`: one shared enum now
  validates, deduplicates, prints, copies, and deep-owns response/failure HEAD
  dispositions. Existing source still creates `Reject`; every selected failure
  `SuppressBody` fails before body waiting, request rewriting, target
  materialisation, or upstream effects. Independent re-review is `APPROVE`;
  Sol ran 21 targeted tests with 436 checks across frontend, network,
  integration, legacy failure serialization, response-only HEAD, bad IDs, and
  H2 fail-closed regressions.
- #253 paired HEAD runtime in `f76f6f59`, hardened for fresh-connect/no-replay
  lifecycle in `bb343132` and with non-vacuous open-backend no-pool evidence in
  `d4a6dc29`: only paired response/failure `SuppressBody` can enter the bounded
  cleartext-H1.1, bodyless, exact-close HEAD domain. Original Host/Connection
  facts are validated before request-policy rewriting; strict success and
  connect failure retain representation Content-Length, emit no body, close the
  downstream, and abandon rather than pool the upstream. Mismatches and all
  declared out-of-domain inputs fail before upstream effects. Independent
  review is `APPROVE`; Sol ran 48 Clang tests with 689 checks, built both related
  GCC targets, and ran 8 GCC tests with 203 checks. Public source and converter
  selection remain absent.
- #253 public source design: add optional `head_mode: "reject" |
  "suppress_body"` to both policy objects, preserve omitted/explicit Reject,
  and expose only paired suppression. Central `analyze_term()` validation covers
  direct, if, match, and guard-failure Forward terms; response-only internal
  suppression remains available below the source boundary. No Forward ABI
  change is needed. Independent review is `APPROVE_DESIGN`.
- #253 public source/compiler selection in `0748c610`, with stale parser-negative
  and forged failure-policy evidence corrected in `5d09ae65`: optional omitted
  or explicit Reject remains legacy behavior, while source SuppressBody must be
  paired, request-derived-close capable, and transform-free. The common
  `analyze_term()` gate covers direct, if, match, and guard-failure terms without
  changing Forward/JIT ABI or internal response-only suppression. Independent
  re-review is `APPROVE`; Sol ran 23 focused frontend/converter tests with 513
  checks. Public-source production-JIT evidence and converter lowering remain
  absent.
- #253 public-source production-JIT evidence in `a0fddf74`, hardened for Date
  bounds, exact HIR/RIR branch mapping, deterministic failure short-circuit,
  default Reject, and exact dead address in `38766b44`: two fresh HEAD successes
  retain Content-Length with no body, a real GET sibling retains the strict
  body path, and connect refusal emits the configured header-only 502 rather
  than the legacy fallback. Exact rebuilt upstream wires, three fresh accepts,
  EOF, and source→JIT→owned-config registration are covered. Independent
  re-review is `APPROVE`; Sol ran 8 test executions with 195 checks across
  integration reruns and network lifecycle regressions. Converter lowering and
  the pinned nginx differential remain absent.
- #253 root/no-URI converter lowering in `3a3a9911`, with explicit no-plain-
  Forward evidence in `57b52685`: generated source selects paired SuppressBody
  only for HEAD and preserves the existing request/response/failure Reject
  bundle for every other method. `/api/` target-transform output is byte-
  identical and remains HEAD-unsupported. Independent re-review is `APPROVE`;
  Sol ran 14 focused frontend/converter tests with 386 checks, and the complete
  existing serial Docker differential passed 1/1 in 3.75 seconds.
- #253 converter-generated explicit-close HEAD differential in `50aa9abb`:
  pinned nginx 1.29.7 and production RUT each match the exact Date-normalized
  header-only 200 and unavailable-upstream 502 baselines, retain representation
  Content-Length, emit no body, and reach EOF. Success uses fresh recorders with
  exact-one accept/request/history and byte-exact rebuilt upstream HEAD wires;
  failure holds one reserved non-listening endpoint across both runs and retains
  nginx's line-scoped connect-failure evidence without claiming an unobserved
  RUT syscall count. Independent review is `APPROVE`; Sol ran the complete
  pinned serial Docker CTest twice (1/1 in 4.24 and 4.21 seconds), with no
  residual containers. The bounded slice is proven; #253 remains `PARTIAL`.
- #253 pinned keep-alive HEAD semantics in `0f405c0a`, built on the opt-in
  observation probe `2d439cd2` and review correction `e6497c27`: on one
  downstream socket, a default-keepalive HEAD receives an exact header-only
  200 and remains quiet/reusable, then a close-intent HEAD receives the exact
  header-only 200 and EOF. nginx opens two distinct upstream connections and
  sends exact rebuilt wires. With one held unavailable endpoint, nginx likewise
  emits an exact keep-alive header-only 502 followed by an exact close 502/EOF
  and two scoped connect-failure records. The observation was identical twice,
  independent review is `APPROVE`, and Sol's complete pinned serial Docker
  CTest passed twice (1/1 in 6.17 and 6.11 seconds) with no residual containers.
  Current RUT rejects this keep-alive HEAD domain, so #253 remains a RUT gap.
- #253 upstream-episode layout foundation in `d0456d1d`: internal pure helpers
  define non-colliding upstream 24-bit episode/8-bit aux and non-upstream full
  32-bit generation domains, `ConnectionBase` owns a persistent nonzero wrapping
  episode, and `IoEvent` has a neutral future transport field. No backend,
  dispatch, cancellation, serializer, or HEAD behavior changed. Independent
  review is `APPROVE`; Sol rebuilt `test_network` and ran 5 focused state tests
  with 291 checks. io_uring and epoll transport remain separate prerequisites.
- #253 io_uring upstream-episode transport in `eef1f114`, hardened by
  `c370c1e7` and `b8a333ea`, with evidence in `dca2b878` and `8c9e53cc`:
  valid nonzero episodes now cross connect/send/recv/pause/cancel SQEs and CQEs;
  stale or malformed receives are rejected before provided-buffer copy, stale
  partial sends cannot mutate or resubmit the live episode, and stale dispatch
  retires only final accounting without touching callbacks, armed state, or
  timers. Generic and specialized loop tests cover current/neutral controls,
  closed-slot reclaim, and two positive stale-buffer returns. Independent final
  review is `APPROVE`; Sol ran 12 focused tests with 396 checks. Episode
  advancement, cancellation/drain lifecycle, epoll #262, and HEAD admission are
  intentionally absent.
- #262 epoll pending-completion capacity prerequisite in `e50e1af6`, corrected
  by `d30ca1a7` and `5ece5ba6`: scoped send, TLS, connect, and upstream-recv
  producers now reserve the single-threaded fixed-stack capacity before any
  synchronous side effect, never overwrite an older completion, reject invalid
  connection IDs before syscalls/state mutation, and report partial-send
  registration failure through one exact terminal completion. Independent final
  review is `APPROVE`; Sol ran 9 focused tests with 321 checks. The wait boundary,
  fairness, episode identity, and stale pre-I/O fence remain open under #262.
- #262 epoll one-logical-completion prerequisite in `fd43876a`: each wait now
  returns at most one pending or kernel completion, pending work remains LIFO
  with a bounded burst quota of eight, kernel readiness gets a nonblocking
  fairness probe, accept consumes one backlog entry, and combined send/read
  readiness advances one operation at a time without ordinary-send starvation.
  The active-health regression exposed by the prerequisite was corrected in
  `a61bd6e9`: queued local upstream-send registration failures retain exact
  errno and teardown ownership without marking the backend down. Independent
  reviews are `APPROVE`; Sol built both focused targets and ran 45 integration
  tests with 17,438 checks plus 18 network tests with 140 checks.
- #262 C1 epoll episode transport in `7780b28d`, corrected by `cd2983f8`:
  registration and synthetic completions carry explicit episodes, raw stale or
  malformed tokens are rejected before fd-map/socket/TLS/state access, and
  concrete dispatch rejects them before timers, callbacks, or armed-state
  changes. Independent review is `APPROVE`; Sol ran 78 focused tests with
  17,697 checks and the complete 741-test network suite with 107,603 checks.
  The complete integration suite passed 378 tests and exposed one pre-existing
  io_uring TLS large-body failure, reproduced on isolated baseline `a1c71a56`
  and tracked in #263. Lifecycle advancement and real fd-reuse evidence remain
  open under #262.
- #262 C2a-0 epoll lifecycle foundation in `846b8216`, hardened by `cdbe0a08`:
  an epoll-owned active-token table now enforces strict begin and detached-retire
  transitions, checked generation advancement never wraps, and active, malformed,
  or max-token ownership is quarantined across connection-slot reuse. Independent
  re-review is `APPROVE`; Sol ran 24 focused epoll/io_uring episode tests with
  562 checks. Production owners and active-state fences are intentionally not
  wired yet, so this foundation alone adds no supported nginx behavior.
- #262 C2a production owner wiring in `6b034eaf`, with non-epoll async-accounting
  preservation in `9cd471c8`, fail-closed DEL handling in `69bf922a`, and sticky
  close-failure quarantine in `c58200e8`: fresh connect, retry, pool, health,
  timeout, and terminal owners now use centralized begin/detach/retire boundaries,
  while raw and concrete dispatch also require the active owner. Independent
  review is `APPROVE`. Sol ran 136 focused tests with 18,456 checks, the complete
  751-test network suite with 107,777 checks, and the complete integration suite
  with 378 passes, one failure, one skip, and 37,986 checks; its sole failure is
  the independently reproduced #263 baseline.
- #262 real reuse evidence in `cd4731d7`, made failure-safe by `1adc4070`, drives
  production fresh-connect/pool-return/pool-borrow on the same numeric fd and
  terminal-close/allocator reuse on the same Connection slot. Old raw readiness
  is rejected before socket I/O and old completion dispatch before timer/callback/
  state mutation; current-token controls still progress. Independent re-review is
  `APPROVE`; the worker repeated both tests ten times, and Sol ran 17 episode tests
  with 306 checks plus the complete 753-test network suite with 107,865 checks.
  Active-health production-transition evidence remains open under #262.
- #262 production failed-connect retry evidence in `6521d815` drives a complete
  request through an injected first `connect` failure, dispatches its real tagged
  completion through `on_upstream_connected`/`try_connect_next_backend`, and proves
  episode 1 is detached and retired before episode 2 begins on the same Connection
  and numeric fd. Replaying the old failure cannot alter retry, passive-health,
  callback, fd/map, owner, token, or response state; the current completion sends
  the exact original request to a real peer. Independent review is `APPROVE`; the
  worker repeated it ten times, and Sol ran 18 episode tests with 385 checks plus
  the complete 754-test network suite with 107,944 checks.
- #262 production active-health reprobe evidence in `65de05c0`, with the
  early-failure adoption correction in `050a8128`, drives two complete
  `sweep_health_probes` cycles through real connect, exact health-request send,
  HTTP 200 receive, and production terminal teardown. The second probe reuses the
  same Connection slot and numeric fd with the next episode; replaying the first
  real connect completion cannot mutate owner/map/token, callbacks, timer, health,
  pending/send/armed/buffer state, or peer bytes, while the current completion
  sends the exact request. Independent re-review is `APPROVE`; the worker repeated
  the final test ten times, and Sol ran 19 episode tests with 525 checks plus the
  complete 755-test network suite with 108,084 checks. A literal #262 audit found
  that active-health is extra confidence rather than a named acceptance item;
  non-vacuous upstream quiesce and raw stale send/connect transition evidence
  still block closure.
- #262 upstream quiesce evidence in `3a4d9aa7` begins a production-owned episode
  before registering a real socket, confirms peer-close HUP, and keeps owner,
  token, and fd map current while `ws_unpoll_upstream` removes the registration.
  A separate ready control fd proves `wait` executes, while no upstream event or
  deferred epoll wake remains; production detach then retires the episode before
  slot release. Independent review is `APPROVE`; the worker repeated the exact
  test ten times, and Sol ran the exact test with 30 checks, 13 quiesce/lifecycle
  tests with 467 checks, and the complete 755-test network suite with 108,103
  checks.
- #262 userspace-held raw connect evidence in `5bb74f75`, hardened in
  `9103a3f1`, adds a thread-local, exact-fd, one-shot test seam that captures an
  exact `epoll_event` returned by the real syscall, suppresses it, and later
  replays the by-value record through the unchanged backend. Exhaustive seam
  tests cover poison transitions, syscall-fault precedence, invalid operations,
  and RAII reset. The lifecycle test captures episode-1 kernel EPOLLOUT, performs
  production terminal detach/retire, reuses the same Connection slot and numeric
  fd for episode 2, then proves old raw replay returns no event or state mutation
  while the untouched current kernel completion dispatches exactly once. Two
  independent re-reviews are `APPROVE`; the worker repeated the connect proof ten
  times, and Sol ran 3 seam tests with 181 checks, all 29 fault tests with 273
  checks, 20 episode tests with 627 checks, and the complete 756-test network
  suite with 108,205 checks. The seam explicitly models a kernel-produced record
  already harvested into userspace, not kernel readiness surviving successful
  `EPOLL_CTL_DEL`.
- #262 userspace-held raw partial-send evidence in `20deb0e0`, strengthened in
  `33f493fb`, drives two real production EINPROGRESS connects and independently
  saturates each TCP send queue to kernel EAGAIN. It captures episode 1's exact
  nonterminal EPOLLOUT/UpstreamRecv record, performs production terminal
  detach/retire, and reuses the same Connection slot and numeric fd for episode
  2. Old raw replay returns no logical event and preserves all owner/map/token,
  callback, timer, pending, send, armed, buffer, connection, and wire state. The
  current kernel record then emits the exact UpstreamSend completion, sends one
  byte-exact payload, and dispatches one callback. FIN/EOF stream verification
  proves episode 1 emitted zero post-filler bytes and episode 2 emitted exactly
  one payload with no prefix, suffix, or duplicate. Independent re-reviews are
  `APPROVE`; the worker repeated the final test ten times, and Sol ran the exact
  test with 200 checks, 21 episode tests with 827 checks, and the complete
  757-test network suite with 108,405 checks. No direct raw-transition gap was
  known at that checkpoint; a fresh requirement-by-requirement closure audit
  then identified only the missing capture-before-transition receive proof.
- #262 userspace-held raw receive evidence in `0e0e3b7b` captures an exact
  kernel-produced episode-1 `EPOLLIN` record before production terminal
  teardown, then reuses the same Connection pointer/id and numeric fd for
  episode 2. Replaying the old record through unchanged `EpollBackend::wait()`
  returns zero and preserves current bytes, owner/map/token, send state,
  callbacks, timers, pending state, buffers, armed flags, and connection state;
  the untouched current record then reads the exact payload and dispatches once.
  Independent review is `APPROVE`; the worker repeated the test ten times for
  1,440 checks. Sol ran all 22 episode tests with 971 checks, the 15 lifecycle
  tests with 883 checks, 11 focused real integration tests with 402 checks, all
  29 fault tests with 273 checks, and the complete 758-test network suite with
  108,549 checks. Two independent closure audits and the final review prove all
  five required-capability and nine acceptance bullets under the corrected
  userspace-held record semantics. #262 is closed; this does not claim reusable
  converter-generated keep-alive `HEAD` support.
- #264 design investigation and independent adversarial audit classify reusable
  io_uring upstream abandonment as a real runtime capability gap, not missing
  converter lowering or test evidence. A strict HEAD header CQE may leave the
  multishot upstream recv live; current abandonment closes the fd and clears
  callbacks/armed flags without cancel, drain, or episode advancement, so a
  merely enabled request 2 could accept request-1 bytes before dispatch under
  the same token. The selected generic design uses serialized episode retirement,
  exact target/cancel ownership, checked advancement/quarantine, and deferred
  downstream admission. #264 is linked from #253 and explicitly separate from
  the #263 TLS large-body baseline. Existing focused evidence remains green:
  11 io_uring episode tests with 389 checks and 8 pause/cancel integration tests
  with 43 checks. C1 is deliberately recv-only and keeps reusable HEAD disabled.
- #264 C1 recv-only strict-abandon retirement in `0bb20537` advances the current
  io_uring episode before old provided-buffer copy, separately owns target and
  exact-token cancel finals, retries or collapses an unqueued cancel safely,
  quarantines exhausted tokens, and preserves tombstone/accounting/slices through
  deferred close and exact-one reclaim. Production `IoUringEventLoop` callback
  evidence proves the existing paired explicit-close HEAD wire, slot release,
  buffer return, and pre-copy fence. Two independent final reviews are `APPROVE`.
  Sol ran 10 retirement tests with 292 checks, 22 episode/reclaim tests with 447
  checks, 5 strict HEAD integrations with 96 checks, the complete 768-test network
  suite with 108,932 checks, and the complete integration suite with 378 passes,
  the sole independent #263 failure, one skip, and 37,986 checks. Reusable HEAD
  remains disabled; C2 must add deferred request-boundary resume and the remaining
  serialized lifecycle before #264 can close.
- #264 C2 design and adversarial audit select a bounded, generic HTTP/1 boundary
  rendezvous: finish request-1 bookkeeping once, park all request-2 parse/route/
  rearm work while retirement is active, preserve positive downstream bytes,
  close on EOF/error, and resume once after the current CQE batch. An inactive
  tombstone may pass only the exact current successor token while continuing to
  swallow retired/malformed tokens without accounting. C2 deliberately cannot
  clear/overwrite the tombstone, start a second strict retirement, enable reusable
  HEAD, or alter nginx converter/source/compiler behavior; those remain later
  reviewed batons.

## OPEN RUT CAPABILITY ISSUES

- #252 — upstream HTTP request policy for `forward`.
- #253 — proxied response header policy.
- #265 — all-upstream-I/O retirement before reusable failure responses.
- #266 — upstream HTTP/0.9/status-line-less close-delimited response semantics.

## KNOWN SEMANTIC RISKS

- nginx version must be pinned in differential evidence.
- The request-policy baseline is nginx 1.29.7 image digest
  `sha256:1854da86e82d5dfb49a8f3d78b099adcc7e36608b207146ed95cd47937938a40`.
- normalized routing URI and original forwarded request-target are distinct.
- With a URI in `proxy_pass`, nginx replaces the matching part of its normalized
  URI, not necessarily the raw request-target: repeated slashes and dot segments
  collapse, and `%7E` becomes `~` in the observed pinned vectors.
- A slash-terminated proxied location redirects the no-slash form (`/api` to
  `/api/`) before proxying. Current RUT routing erases that trailing-slash
  distinction; the #259 bounded slice must reject it rather than approximate it.
- A Redirect action alone cannot prove the nginx source-path condition because
  canonical routing collapses `/api` and `/api/`. Public lowering must execute
  an exact raw `req.pathOnly == "/api"` guard, and the bounded H1 serializer must
  independently revalidate the origin-form raw path before emitting bytes.
- `OPTIONS *` and authority-form CONNECT have no canonical origin-form path in
  current RUT, miss the converter's method-omitted `/` route, and fall through to
  unmatched default 200 before complete-buffering preflight. Internal admission
  rejection tests cannot establish production fail-closed behavior. Pin nginx
  location semantics before creating a routing/unmatched-policy capability issue.
- Complete response buffering currently requires downstream HTTP/1.1; accepted
  nginx configuration syntax does not itself exclude HTTP/1.0 clients.
- Redirect authority must use the actual port returned after `bind`/`getsockname`
  (including declared port `0`), not a listener declaration stored in the
  hot-reloaded RouteConfig.
- nginx and RUT path canonicalization and prefix boundaries differ.
- An invalid upstream status prefix is not an immediate nginx malformed-header
  502: pinned nginx 1.29.7 falls back to HTTP/0.9/close-delimited semantics,
  while RUT currently returns `ParseStatus::Error`. #266 tracks this broader
  gap. The completed malformed differential is intentionally limited to a valid
  HTTP/1.1 status line followed by an invalid empty-name header.
- Host, Connection, hop-by-hop fields, and framing cannot be compared by a
  permissive allowlist when declaring a feature `SUPPORTED`.
- nginx is not installed on the host; Docker provides the pinned nginx test
  dependency. The `%7E` success and explicit-close gateway vectors are committed
  and serialised; broader POST and keep-alive vectors remain manual evidence.
- The cross-thread io_uring stall is fixed and covered by a non-skipping
  real-socket regression, but full differential execution remains serial-only.
- The #263 io_uring downstream-TLS truncation is fixed for its bounded ordinary
  bodyless H1 proxy episode, with exact 256 KiB high/low-watermark evidence.
  nginx HTTPS-listener compatibility remains unsupported because converter and
  differential coverage do not exist; the runtime fix alone is not such a claim.
- nginx 1.29.7 uses HTTP/1.1 and omits upstream `Connection` by default. It uses
  a fixed header skip set rather than dynamic `Connection` token normalization:
  nominated ordinary fields, `Proxy-Connection`, and `Trailer` are forwarded.
- An absolute-form client target is normalized to origin-form by nginx 1.29.7;
  the first request-policy slice may reject it but must remain PARTIAL.
- The first #253 behavior increment supports only a bounded strict HTTP/1.1
  final-response/content-length serializer plus one explicit-close, bodyless
  HEAD slice. HTTP/1.0, HTTP/2, keep-alive HEAD, Upgrade, absolute-form,
  interim/no-body statuses, chunking, close-delimited framing, and response
  mutations remain intentional fail-closed `PARTIAL` limits; broader nginx
  response behavior is not claimed.
- The converter emits both bounded policies. One fixed-CL binary POST now has
  generated-RUT differential evidence, but the one-slice size bound and broader
  framing/request/response domains keep this `PARTIAL`, not general body support.
- nginx's default request buffering is observable at backend accept time. The
  #252 body increment must buffer before upstream allocation/connect; reusing
  transparent early-connect streaming would be a semantic mismatch.
- The first buffering slice is bounded by existing connection-buffer capacity;
  larger nginx request bodies remain unsupported until RUT gains a broader
  memory/file-backed body buffering primitive.
- #253 body admission distinguishes locally buffered completion from actual
  upstream upload completion. Response-before-send-CQE ordering is admitted
  only after a positive full-send completion; partial or failed writes remain
  fail closed.
- Pinned nginx consumes one upstream `Connection` field and synthesizes the
  downstream value without dynamically removing token-nominated ordinary
  fields. The bounded RUT serializer matches those tested vectors and rejects
  duplicate Connection fields; broader hop-by-hop response semantics are open.
- A connect failure has no upstream response, so #253 cannot serialize nginx's
  local gateway page. #256 must supply a separate general failure policy; the
  converter must not embed a hidden nginx-only runtime mode.
- Request-policy materialisation and downstream connection intent are separate:
  the converter now emits response `connection: "request"`, preserving explicit
  client close while the rebuilt upstream request remains reusable H1.1.
