# nginx converter status

## CURRENT

- Investigate the next smallest generic #271 capability increment after the
  bodyless known-non-HEAD exact/ANY public method matrix completed. Compare the
  remaining request-body, HEAD, and broader response framing/status/size domains;
  select one bounded primitive only if it moves a truthful converter-generated
  default-buffering differential closer without nginx-specific workarounds.

## NEXT

1. Independently review the #271 next-increment design and choose or reject it.
2. Implement the selected single bounded primitive, if approved.
3. Add its focused/public evidence before reassessing converter lowering.
4. Resume #270 only after #268/#271 admitted-domain and differential evidence,
   and only when every admitted method/body and default-buffering path
   is equivalent.

## BACKLOG

- General prefix, exact, `^~`, regex, and nested location selection.
- Multiple servers, listen/default-server selection, and `server_name`.
- Variables, rewrite phases, internal redirects, upstream groups, TLS, static
  files, error pages, and logging compatibility.

## BLOCKED

- broader nginx default upstream request behavior: #252.
- broader nginx default proxied response behavior and differential evidence: #253.
- upstream invalid-status/HTTP/0.9 fallback semantics: #266.
- timeout-cause-specific forward failure policy selection: #267.
- per-forward upstream read-timeout timing semantics: #268.
- proxied response buffering and downstream commit semantics: #271.
- nginx `proxy_read_timeout` parser/lowering/differential support: #270,
  lowering remains blocked by #268's missing all-method/body domain and #271's
  default-buffering mismatch; bounded parser recognition and fail-closed
  converter rejection are complete.
- broader reusable keep-alive `HEAD` failures: timeout behavior and the
  remaining #253 response semantics still depend on #265; invalid-status
  HTTP/0.9 fallback is separately #266. The epoll prerequisite #262 and
  recv-retirement capability #264 are closed. The incomplete-EOF converter
  differential is complete.

## RECENTLY COMPLETED

- #271 policy-1 ANY-route TRACE incomplete-inactivity public wire landed in
  `e41e628d` after independent `APPROVE`. A method-omitted ordinary RUT route
  compiles as ANY and admits one bodyless origin-form TRACE. The byte-exact
  rewritten request preserves target/query and X-Test, synthesizes backend Host,
  and strips client Host/Keep-Alive. Two permit-gated progress sends are about
  0.400s apart and each is followed by an open/zero-byte downstream probe; final
  progress to first downstream byte exceeds one second and first progress to
  publication exceeds 1.25s, proving refresh. Expiry emits exactly the pinned
  Date-normalized 200/CL12 keep-alive header, discards `hello`, emits no
  502/504/failure bytes, EOFs downstream, and retires the application-open origin
  with exactly one accept/request and no unused history. Sol passed TRACE 1/1
  (89 checks) and unchanged GET inactivity 1/1 (93); format/diff checks passed.
  The POST/TRACE public method evidence is complete, but converter equivalence
  remains absent and #268/#271/#270 stay open.
- CI found the closed route-method predicate added in `2d7d3157` lacked an
  explicit switch default under `bugprone-switch-missing-default-case`.
  `d216dd3c` adds a fail-closed default and removes the unreachable tail return;
  the admitted set remains exactly ANY/GET/POST/PUT/DELETE/PATCH/OPTIONS while
  HEAD/CONNECT/TRACE/invalid/future numeric values reject. Targeted clang-tidy,
  the 187-check frontend closed-method test, format, and diff checks passed.
- #271 policy-1 exact-POST fragmented-completion public wire landed in
  `d64b005b` after independent `APPROVE`. Ordinary RUT selects an exact POST
  route, `Http11FixedStrip`, the strict policy triple, one-second deadline, and
  complete-content-length buffering. Two byte-exact origin requests preserve
  POST target/query and X-Test, synthesize the dynamic backend Host, and strip
  client Host/Keep-Alive with no Content-Length or body. Origin 1 emits four
  permit-gated fragments at about 0.400s gaps across more than one second; three
  probes prove downstream open and byte-quiet before exact full completion.
  Origin retirement is observed before same-downstream request 2 opens a fresh
  origin episode; accept/request history settles exactly at 2/2 with unused
  slots empty. Sol passed the new wire 1/1 (102 checks) and the unchanged GET
  policy-1 neighbor 1/1 (102); format and diff checks passed. ANY-TRACE public
  evidence and converter equivalence remain absent, so #271/#270 stay open.
- #271 bodyless known non-HEAD complete buffering landed in `2d7d3157` after
  independent `APPROVE`. A shared closed route-method predicate admits exact
  GET/POST/PUT/DELETE/PATCH/OPTIONS and ANY, with TRACE admitted only through
  ANY; HEAD/CONNECT/Other/OPTIONS-star reject before upstream effects. Analyzer,
  lowering, verifier, preflight, active ownership, first-batch, post-commit, and
  D2 identities all revalidate the domain. Positive-CL classification remains
  deliberately split: only `CompleteContentLength` uses the broadened identity,
  while omitted-buffering streaming and its body pump remain GET-only. No ABI,
  config-layout, converter, or nginx-specific runtime field changed. Sol linked
  production `rut` and passed Frontend 977/977 (7,825 checks), JIT 487/487
  (6,026), and Network 903/903 (114,503); format and diff checks passed. Public
  POST/TRACE method wires and converter equivalence remain absent, so
  #268/#271/#270 stay open.
- #271 bodyless known non-HEAD buffering design received independent
  `APPROVE_DESIGN`. It reuses `BodylessNonHeadContentLengthZero` and existing
  parsed/route-method proofs: exact GET/POST/PUT/DELETE/PATCH/OPTIONS plus ANY,
  with TRACE admitted only through ANY; HEAD/CONNECT/Other/OPTIONS-star reject.
  No AST/HIR/MIR/RIR, HandlerResult, bundle, RouteConfig/RouteEntry, or Connection
  field is needed. One shared closed static-route predicate guards analyzer,
  lowering, verifier, preflight, and complete-buffering ownership. Positive-CL
  classification must remain split: buffering mode may use the closed non-HEAD
  identity, while omitted-buffering streaming and its body pump stay GET-only.
  Existing first-batch, ledger, expiry, retirement, D2, config, policy, and reset
  identities remain authoritative and require adversarial regression coverage.
  Converter behavior is unchanged and #268/#271/#270 stay open.
- Post-wire feasibility review concluded `NO`: no currently accepted nginx.conf
  slice can truthfully lower default buffering for its entire admitted domain.
  The converter emits one method-omitted route with a HEAD conditional; exact GET
  plus ANY fallback splitting is mechanically possible but would silently stream
  or fail closed for body-bearing GET, HEAD, write/other methods, and broader
  response shapes. Omitted nginx timeout also means 60s, whereas current lowering
  emits neither deadline nor buffering. No new issue is needed: #271 covers the
  buffering method/body/response gap, #268 the inactivity lifecycle breadth, and
  #270 the dependent explicit/default lowering. The selected next design extends
  the existing barrier to #268's bodyless known non-HEAD exact/ANY method identity;
  request bodies, HEAD, broader framing/status/size, and converter differential
  remain blocked afterward.
- #271 fixed-request-policy premature-clean-EOF public wire landed in `1b668c65`
  after independent `APPROVE`. One byte-exact rewritten origin request carries
  dynamic backend Host and X-Test with client Host/Keep-Alive stripped. Origin
  sends valid 200/CL12 plus `hello` and waits application-open behind an explicit
  close gate. A pre-gate probe and state recheck prove downstream open and byte-
  quiet; after the timestamped clean close, RUT promptly emits the exact Date-
  normalized pinned keep-alive header plus `hello`, no failure/502/504 bytes, then
  downstream EOF. Cleanup occurs only after gate close, tested EOF, and settled
  one accept/request with zero unused history. Sol passed policy-1 EOF 1/1 (91
  checks), policy-0 neighbor 1/1 (89), policy-1 completion 1/1 (102), and policy-1
  inactivity 1/1 (93). All three bounded policy-1 dispositions now have public
  wires; converter equivalence is still unproven and #271/#270 remain open.
- #271 fixed-request-policy incomplete-inactivity public wire landed in
  `3bc63a32` after independent `APPROVE`. Ordinary RUT reuses the policy-1 fixture;
  one byte-exact rewritten origin request carries dynamic backend Host and X-Test
  while stripping client Host/Keep-Alive. Origin sends valid 200/CL12 plus `hello`
  and remains open. A 100ms probe proves downstream open and byte-quiet; after
  more than one second of inactivity RUT emits exactly the Date-normalized pinned
  keep-alive success header, discards `hello`, emits no default/configured failure
  or 502/504 bytes, then EOFs downstream and retires origin before cleanup. A
  settle interval preserves exactly one accept/request with zero unused history.
  Sol passed policy-1 inactivity 1/1 (93 checks), policy-0 neighbor 1/1 (89), and
  policy-1 completion 1/1 (102). Clean-EOF policy-1 evidence remains; #271/#270
  stay open.
- #271 fixed-request-policy fragmented-completion public wire landed in
  `125de278` after independent `APPROVE`. Distinct ordinary RUT keeps the policy-0
  fixture byte-unchanged and selects exact GET, `Http11FixedStrip`, one-second
  deadline, strict policy triple, and complete-content-length buffering. Client
  Host/Keep-Alive/X-Test become two byte-exact origin wires with dynamic backend
  Host, preserved target/query and X-Test, and stripped client Host/Keep-Alive.
  Origin 1 emits four permit-gated fragments with about 0.400s gaps and a 1.201s
  span; three probes prove downstream open and byte-quiet. Final completion emits
  the exact full response, RUT retires origin 1, and same-downstream request 2
  uses a fresh exact rewritten origin episode. Sol passed the new wire 1/1 (102
  checks) and policy-0 neighbor 1/1 (95). Inactivity and clean-EOF policy-1 wires
  remain; #271/#270 stay open.
- #271 request-policy-compatible bounded buffering landed in `c4566ea6` after
  one `REQUEST_CHANGES` round and final `APPROVE`. A closed common predicate
  admits only omission or `Http11FixedStrip`; analyzer presence/value coherence,
  lowering, full-width RIR verification, pre-effect dispatch, active owner,
  first-batch, post-commit, and final D2 publication all revalidate it. The review
  caught a missing prebuilt D2 identity check; configured-timeout and CL0-success
  adversarial tests now prove forged current and stale captured policies reject
  transactionally with zero peer bytes or send/resource effects. No packed ABI,
  ForwardPolicyBundle, RIR/config/route layout, method/body domain, or converter
  behavior changed. Sol linked production `rut` and passed Frontend 976/976
  (7,628 checks), JIT 487/487 (5,992), and Network 901/901 (114,138). Policy-1
  public wires remain absent; #271/#270 stay open.
- #271 request-policy-compatible buffering design received independent
  `APPROVE_DESIGN`. The selected Option A adds a closed profile predicate that
  accepts only omission or `Http11FixedStrip`, then applies it at analyzer,
  lowering, verifier, pre-effect runtime dispatch, deadline-owner, and post-
  commit stability boundaries. Existing HandlerResult fields already carry the
  request-policy and bundle IDs separately, request rewrite completes before
  upstream/resource effects, and response buffering owns independent storage;
  no RIR/config/route/ForwardPolicyBundle layout change or preflight identity pin
  is needed. Implementation must reject presence/value mismatches, dynamic or
  forged IDs, and stale runtime policy state before copy/commit. Public policy-1
  wires for all three dispositions are a separate next increment. #271/#270
  remain open and converter rejection is unchanged.
- Pinned nginx 1.29.7 default-buffering premature-clean-EOF baseline landed in
  `6449ecce` after independent `APPROVE`. One origin emits a valid 200/CL12
  header plus `hello`, then remains application-open behind an explicit close
  gate. A 400ms probe proves downstream open and byte-quiet. The gate timestamps
  clean origin shutdown; nginx then publishes exactly the Date-normalized pinned
  keep-alive header plus `hello` and downstream EOF in about 0.001s, with no
  502/504, retry, or reconnect. Sol passed the fixed-Docker monolithic
  differential executable with 18 PASS markers. Together with `6ea51e0c` and
  `4980446b`, all three bounded nginx dispositions now have committed semantic
  baselines; converter equivalence is still absent and #271 stays open.
- Read-only feasibility review found no honest converter-generated default-
  buffering differential in the current admitted domain. The converter emits a
  method-omitted route with HEAD handling and a mandatory nginx-compatible
  upstream request policy, while `complete_content_length` currently requires an
  effect-free exact GET with no request policy. Runtime is additionally limited
  to bodyless GET and bounded positive CL. A GET-only test cannot prove the
  method/body domain of the nginx location, and absence of an explicit directive
  still carries nginx's implicit timeout and buffering defaults. Primary blockers
  remain #271/#268; #270 correctly rejects before RUT emission. The smallest
  useful RUT increment is request-policy-compatible buffering for exact bodyless
  GET, but it alone must not unblock converter lowering.
- Pinned nginx 1.29.7 default-buffering fragmented-completion baseline landed in
  `4980446b` after independent `APPROVE`. A minimal root proxy deliberately omits
  `proxy_buffering` and uses `proxy_read_timeout 1s`. One origin emits an exact
  200/CL12 response in four permit-gated fragments with observed gaps of about
  0.401s and a total span of 1.204s. Three pre-final probes prove downstream
  open and byte-quiet. The final byte publishes the exact Date-normalized full
  response in about 0.001s; nginx promptly retires origin, keeps downstream
  reusable, and performs exactly one origin request with no reconnect. Sol
  passed the fixed-Docker monolithic differential executable with 17 PASS
  markers. This is semantic evidence, not converter equivalence; #271 stays open.
- Pinned nginx 1.29.7 default-buffering incomplete-inactivity baseline landed in
  `6ea51e0c` after independent `APPROVE`. Minimal root proxy config contains
  `proxy_read_timeout 1s` and deliberately omits `proxy_buffering`. One origin
  sends a valid 200/CL12 header plus `hello`, remains open, and receives the exact
  dynamic-Host request once. A 500ms capture proves downstream open and byte-
  quiet; first downstream byte, EOF, and nginx-attributed origin close occur at
  about 1.001s. The Date-normalized wire is exactly the pinned keep-alive 200
  header, with no prefix, 502, or 504, and a further settle excludes retry. Sol
  passed the fixed-Docker monolithic differential executable with 16 PASS markers.
  This is a semantic baseline, not converter equivalence; #271 stays open.
- #271 premature-EOF public wire landed in `ecfe2431` after independent
  `APPROVE`. One exact origin sends a valid 200/CL12 header plus `hello`, then
  waits behind a close gate. Gate state plus a 100ms probe prove origin and
  downstream remain open while downstream is byte-quiet. The explicit monotonic
  close timestamp precedes clean origin EOF; RUT then emits the byte-exact pinned
  keep-alive 200 header plus exactly `hello`, no 502/504 bytes, and downstream
  EOF within one second. Exactly one accept/request and settled history exclude
  reconnect; cleanup occurs after the tested EOF. Sol passed the wire and its
  inactivity neighbor 2/2 (178 checks). This is not nginx equivalence evidence;
  #271 remains open.
- #271 incomplete-inactivity public wire landed in `83565177` after independent
  `APPROVE`. Ordinary RUT pins exact GET/strict policy/one-second timeout/
  CompleteContentLength metadata. One origin sends a valid 200/CL12 header plus
  `hello`, remains open and silent, and receives no reconnect. A 100ms probe
  proves downstream open with zero bytes; more than one second after origin
  progress, RUT emits only the byte-exact pinned 200 header retaining
  `Connection: keep-alive`, discards `hello`, emits no 502/504 bytes, then EOFs
  downstream and retires origin before cleanup. Sol passed the wire 1/1 (89
  checks) and completed-buffer neighbor 1/1 (95). This is not yet nginx
  differential evidence; #271 stays open.
- #271 complete-response public wire landed in `eb19ff10` after independent
  `APPROVE`. Ordinary RUT carries `CompleteContentLength` plus one-second timeout
  through RIR/JIT/RouteConfig. Origin 1 sends an exact positive-CL response in
  four permit-gated application fragments; every gap is 350--650ms and total
  exceeds one second. Three pre-final probes prove downstream remains open with
  zero bytes. Final completion emits the exact pinned header/body, then RUT
  retires the still-open origin. Request 2 reuses the same downstream socket,
  waits for a fresh origin episode, and returns a second exact positive-CL
  success; exactly two accepts/requests exclude retry. Sol passed the new wire
  1/1 (95 checks) and immediate-streaming neighbor 1/1 (94). This does not claim
  kernel-CQE boundaries or nginx equivalence; #271 remains open.
- #271 bounded runtime commit barrier landed in `74d7c37f` after two
  `REQUEST_CHANGES` findings and final `APPROVE`. For the exact cleartext
  io_uring/bodyless GET/strict 200-positive-CL/16 KiB profile, a valid response
  pins the serialized success header and commits zero downstream bytes until a
  terminal disposition. Full completion sends the exact header/body and gates
  request 2 on send drain plus pre-send origin retirement; inactivity sends
  pinned header only and discards prefix; clean EOF sends pinned header plus
  prefix; other errors and unsupported/surplus/capacity shapes fail before
  commit. Batch progress/completion wins matching Timeout, and late old-episode
  CQEs are fenced. Sol built production `rut`, passed focused 78/78 (3,056
  checks), Network 896/896 (114,010), and Integration 394 pass / one known #263
  failure / one skip. Public wires and converter differential remain absent;
  #271 stays open and the matrix remains `BLOCKED_BY_RUT`.
- #271 compiler/config metadata landed in `2aece7ca` after independent
  `APPROVE`. Ordinary RUT accepts only
  `response_buffering: "complete_content_length"`; the enum propagates through
  AST/HIR/MIR/RIR bundle dedup, verifier, RouteConfig, registration, and JIT
  without changing the 64-bit HandlerResult or 8-byte bundle ABI. Exact GET,
  positive timeout, and the complete strict policy triple are statically
  required; forged values fail at every trust boundary. Until runtime support
  lands, nonzero mode closes at route preflight before handler, body wait,
  connect, or output, so there is no streaming fallback. Sol built the actual
  `rut` executable and passed Frontend 976/976 (7,593 checks), JIT 487/487
  (5,989), and Network 882/882 (113,595). #271 and #270 remain blocked.
- Production executable link coverage exposed the template visibility gap from
  `d2db00ed`; `943f59d1` adds one explicit io_uring body-pump instantiation plus
  an extern declaration. Independent review is `APPROVE`; default JIT and
  no-JIT `rut` plus `test_network` all link, with no runtime behavior change.
- #271 selected `response_buffering: "complete_content_length"` and received
  independent `APPROVE_DESIGN`. The enum lives in Forward policy metadata;
  omission preserves immediate streaming and the packed handler ABI. The first
  bounded runtime contract is an entire-response commit barrier for cleartext
  io_uring, exact bodyless GET, strict 200/positive CL within 16 KiB. Header
  acceptance snapshots strict success bytes once. Full completion sends the
  pinned header and full body; incomplete inactivity sends pinned header only
  and discards prefix; observed premature EOF sends pinned header plus prefix;
  other recv errors stay explicit zero-byte unsupported. Progress/completion
  wins same-batch Timeout, and surplus rejects before commit. Implementation is
  staged metadata first, then runtime; #271 and #270 remain blocked.
- Pinned nginx 1.29.7 post-commit investigation established two distinct
  baselines for one 200/CL12 header plus `hello` followed by origin silence.
  Default `proxy_buffering on` waits about 1.002s, emits only the 117-byte 200
  header, omits the buffered prefix, then closes downstream and origin without
  502/504. Explicit `proxy_buffering off` immediately emits the 122-byte header
  plus prefix, then closes both sides at about 1.001s. Both use one exact origin
  request/accept; only Date needs normalization. Current RUT matches only the
  explicit-off control, so using it as evidence for the default config would be
  a silent semantic downgrade. #271 now tracks the required general buffering/
  commit primitive and blocks #270 alongside #268. The experiment is not yet a
  committed regression.
- #268 public bounded positive-CL GET progress-refresh wire landed in
  `cdd91d43`. Ordinary RUT source carries exact GET, strict policy bundle, and
  one-second timeout through RIR/JIT/RouteConfig into production cleartext
  io_uring. One origin sends a 140-byte exact HTTP/1.1 200/CL48 response in nine
  recorded 16-byte application fragments; every adjacent gap is 100--650ms,
  while first fragment to exact downstream completion exceeds 1.10s. The
  origin remains open after its last send, then only RUT retirement supplies
  peer close; exactly one accept/request and a settle interval exclude retry.
  The wire exposed a fragmented-header transition bug: retained header-prefix
  progress conflicted with the new body owner when the completing batch also
  carried body bytes. `8e729400` now atomically retires only the exact current
  pre-header proof and seeds post-commit progress from authoritative coalesced
  body bytes. Independent reviews approved both changes. Sol passed deadline
  63/63 (2,627 checks), the new public wire 1/1 (94), and its CL0 neighbor 1/1
  (64). This is application-send evidence, not a kernel-CQE fragmentation claim
  or nginx differential; #270 remains blocked and `NOT_IMPLEMENTED`.
- #268 bounded positive-CL GET response streaming landed in `d2db00ed` after
  two independent `REQUEST_CHANGES` rounds and final `APPROVE`. The internal
  runtime profile admits only cleartext io_uring bodyless exact/ANY GET and an
  exact HTTP/1.1 200 with one positive Content-Length whose header plus body fit
  the existing 16 KiB origin slice. Exact generation/episode/send/cancel owners,
  tombstones, whole-batch progress settlement, deferred pumping, and origin-
  retirement rendezvous make progress/Timeout and completion order independent.
  Pre-commit timeout may emit the configured 504; post-commit timeout, invalid
  framing, short/error, surplus, or SQ pressure closes/truncates without a new
  502/504. Sol passed deadline 62/62 (2,497 checks), Network 880/880 (113,435),
  Frontend 974/974, JIT 486/486, and HTTP/2 75/75. Integration was 393 pass,
  one known #263 failure, and one skip. Public wires, pinned-nginx evidence,
  converter lowering, other methods, larger/chunked/close-delimited bodies, and
  other transports remain open; #270 stays blocked and `NOT_IMPLEMENTED`.
- #268 bounded positive-CL response-streaming design received independent
  `APPROVE_DESIGN`. It generalizes the existing bodyless exact/ANY GET request
  profile and selects a post-commit phase only after validating exact HTTP/1.1
  200 with one positive CL, no TE, and overflow-safe total response size within
  the existing 16 KiB origin slice. Existing streaming callbacks remain the
  data plane; a same-generation post-commit proof, monotonic origin-body count,
  whole-batch ledger, and deferred body pump make progress/Timeout and Send/
  Timeout order-independent across buffer compaction and send-inflight callback
  gaps. Pre-header inactivity retains configured 504; post-commit inactivity
  retires origin and closes/truncates downstream without any replacement
  failure response. Full origin-body receipt removes the timer immediately but
  retains ownership until downstream drain and origin retirement. Review also
  requires overflow-safe capacity checks, full-body-plus-terminal same-batch
  success, and explicit proof that a stalled committed wire contains one 200/
  body prefix followed by EOF with no 502/504 bytes. No compiler/source/API/ABI
  change is planned; #270 stays blocked.
- #268 public fixed-CL POST progress-then-stall wire in `24368e13`: ordinary
  RUT source preserves exact POST, fixed request-policy, strict bundle, and
  one-second timeout metadata through RIR/JIT/RouteConfig into production
  cleartext io_uring. One fresh origin records the exact rewritten request and
  byte-identical 12-byte binary body, sends one nonempty incomplete response-
  header fragment, then stays open and silent. Fragment-to-client completion
  exceeds one second but remains below the unrelated global timeout; the client
  receives only the exact configured full 504 including its 20-byte body, with
  no leaked origin fragment or extra bytes. RUT retires the origin after the
  stall; one accept/request plus a settle interval exclude reconnect.
  Independent review is `APPROVE`. Sol passed progress-stall 1/1 (75 checks),
  fragmented success 1/1 (75), and upload-boundary 1/1 (310). Successful
  response bodies, converter lowering, and nginx differential evidence remain
  open; #270 stays blocked.
- #268 public fixed-CL POST fragmented-success wire in `2e34ffc1`: ordinary
  RUT source preserves exact POST, fixed request-policy, strict bundle, and
  one-second timeout metadata through RIR/JIT/RouteConfig into production
  cleartext io_uring. One fresh origin receives the exact rewritten request
  with a byte-identical 12-byte binary body, then emits exact HTTP/1.1 200 with
  unique CL0 in seven positive fragments. Six recorded adjacent gaps are
  constrained to 100--650ms while first-fragment-to-client completion exceeds
  1.10s; the client receives the exact configured CL0 success rather than the
  timeout response. The origin stays open after its final fragment, and only
  RUT retirement supplies the later close; one accept/request and a settle
  interval exclude reconnect. Independent review is `APPROVE`. Sol passed the
  new wire 1/1 (75 checks), upload-boundary neighbor 1/1 (310), and fragmented
  GET baseline 1/1 (64). Fixed-upload progress-stall, successful response
  bodies, converter lowering, and nginx differential evidence remain open;
  #270 stays blocked.
- #268 public fixed-CL POST upload-boundary wire in `29b47e9c`: ordinary RUT
  source preserves exact POST, request-policy, policy-bundle, and one-second
  timeout metadata through RIR/JIT/RouteConfig into a production cleartext
  io_uring shard. A 12-byte binary body is split 5/7; the incomplete upload is
  held for at least 1.2 seconds plus a 100ms downstream quiet/open probe while
  origin accepts and requests remain exactly zero. After the final fragment,
  one fresh origin records the exact rewritten header and byte-identical body,
  stays open, and sends zero bytes. The exact configured full 504 completes
  more than one second after final-body send and after origin request
  observation, below the unrelated global timeout, then RUT retires the
  origin. Review added a second origin-zero assertion after the quiet probe;
  final review is `APPROVE`. Sol passed the wire 1/1 (310 checks) and its
  bodyless POST neighbor 1/1 (77 checks). Fragmented response progress,
  progress-stall, successful response bodies, converter lowering, and nginx
  differential evidence remain open; #270 stays blocked.
- #268 bounded fixed-CL request-upload runtime in `54545566`: cleartext
  io_uring HTTP/1.1 exact/ANY POST/PUT/PATCH routes now admit one positive
  unique Content-Length body within the existing 16 KiB request-policy slice.
  The handler runs once; pinned config, route/function, bundle, raw/rewritten
  body boundaries, binary bytes, upstream identity, episode, and full-send
  proof are revalidated across body continuation, upload, first-batch copy,
  expiry, D1/D2, and reset. No explicit response-read timer exists during body
  wait, connect, or upload; only exact full upload plus a successfully queued
  first response Recv arms it. Surplus, forged/stale ownership, short/error/
  over-reported sends, and every excluded shape fail closed. Success remains
  exact HTTP/1.1 200/unique-CL0. Two review rounds corrected initial handler
  identity admission and isolated short/error/stale tests; final review is
  `APPROVE`. Sol passed deadline 48/48 (1,854 checks), Network 871/871
  (113,088), Frontend 974/974, JIT 486/486, and Http2Conn 75/75. Integration
  passed 390 tests and failed only the independent #263 baseline (38,705
  checks, one skip). Public ordinary-source upload timing, response bodies,
  converter lowering, and nginx differential evidence remain open; #270 stays
  blocked.
- #268 fixed-CL request-upload design received independent `APPROVE_DESIGN`.
  The selected general-RUT slice admits bounded positive unique-CL POST/PUT/
  PATCH on exact or ANY routes, reuses the existing request-policy body
  continuation, invokes the handler once, pins the authoritative policy bundle
  and exact raw/rewritten body boundaries, and rejects surplus or unsupported
  framing before upstream effects. The explicit response-read timer remains
  absent during client-body wait, connect, and upload; only an exact full-send
  terminal followed by a successfully queued first response Recv may arm it.
  Success remains exact HTTP/1.1 200/unique-CL0. Buffered positive-CL responses
  were rejected as the next slice because nginx may already commit response
  bytes before a later inactivity close, so private buffering plus a replacement
  504 would not be behavior-equivalent. #270 stays blocked.
- #268 public ANY-route TRACE fragmented-success wire in `c6edca67`: ordinary
  method-omitted RUT source preserves exact `kRouteMethodAny` through RIR and
  registered RouteConfig, then a production cleartext io_uring shard receives
  a strict bodyless TRACE. One fresh origin records the exact rewritten TRACE
  wire and emits exact HTTP/1.1 200/unique-CL0 in eight positive fragments; all
  adjacent gaps are 100--650ms while first-fragment-to-client completion
  exceeds 1.10s. The exact downstream CL0 response has no extra bytes, the
  fixture keeps origin open, and RUT-attributed retirement occurs after the
  final fragment with one accept/request only. Independent review is `APPROVE`;
  Sol passed TRACE plus its GET fragmented baseline 2/2 with 130 checks.
  Request/response bodies, converter lowering, and nginx differential evidence
  remain open; #270 stays blocked.
- #268 public bodyless POST wire in `7092cbfd`: an explicit POST ordinary-RUT
  route preserves exact `kRouteMethodPost` through RIR and registered
  RouteConfig, then runs on a production cleartext io_uring shard. Origin 1
  receives an exact POST request with no CL/TE/body, sends zero bytes while
  staying open, and observes RUT retire it; the client receives the exact full
  configured 504 including its 20-byte body after the one-second interval and
  below the unrelated global timeout. Only afterward, the same downstream
  socket sends an exact bodyless POST through a fresh origin connection; origin
  2 stays open after exact HTTP/1.1 200/unique-CL0 and the exact downstream CL0
  response remains quiet/open. An initial review correctly rejected missing
  exact-route assertions; the minimal correction was re-reviewed `APPROVE`.
  Sol passed POST plus its GET baseline 2/2 with 151 checks. TRACE public
  evidence, request/response bodies, converter lowering, and nginx differential
  evidence remain open; #270 stays blocked.
- #268 generic bodyless non-HEAD/CL0 runtime in `54dd54ad`: the deadline
  profile now admits exact known GET, POST, PUT, DELETE, PATCH, OPTIONS, and
  TRACE methods, with TRACE restricted to a method-omitted/ANY route; HEAD
  remains separate and CONNECT/extension methods reject before effects. Every
  admitted request is strict origin-form with exactly one Host and no CL/TE/
  body/pipeline. Exact method identity is captured and revalidated through the
  active owner, first-batch snapshot, backend copy witness, batch ledger,
  expiry, retained D2, send completion, disarm, and reset. Existing progress,
  race, exact configured full-timeout, exact 200/unique-CL0 success, and origin
  retirement machinery is reused. Independent review is `APPROVE`. Sol passed
  Network 863/863 (112,790 checks), Frontend/JIT/Http2Conn, while Integration
  passed 388 tests and failed only the independent #263 baseline (38,562
  checks, one skip). Public POST/TRACE wires, request/response bodies, converter
  lowering, and nginx differential evidence remain open; #270 stays blocked.
- #268 next-increment design comparison selected a generic bodyless origin-form
  non-HEAD/CL0 profile over bounded buffered CL>0 responses and received
  independent `APPROVE_DESIGN`. The selected runtime domain admits known GET,
  POST, PUT, DELETE, PATCH, OPTIONS, and TRACE methods with no CL/TE/body;
  TRACE enters only through a method-omitted/ANY route. HEAD remains on its
  separate suppress profile; CONNECT, extension methods, `OPTIONS *`, explicit
  request CL0, and every other excluded shape reject before upstream effects.
  Exact parsed method identity must be captured and revalidated through active
  ownership, copy witness, batch ledger, expiry, retained D2, and reset. Timer,
  progress, race, full-timeout D1/D2, CL0 success, and retirement machinery are
  otherwise reused. Buffered response bodies remain a later, materially larger
  design; #270 stays blocked.
- #268 public GET/CL0 positive-progress-then-stall wire in `5876280e`: ordinary
  RUT source reaches production cleartext io_uring; origin 1 receives the exact
  bodyless GET, sends one recorded non-empty incomplete header fragment, then
  stays open and silent. Both fragment-to-client timeout completion and
  fragment-to-origin-retirement exceed one second while staying below the
  unrelated global-timeout class. The client receives the exact configured full
  504 including its 20-byte body. Only after RUT-attributed origin retirement,
  the same downstream socket sends request 2 to a fresh origin, which stays
  open after an exact HTTP/1.1 200/unique-CL0 response; the exact downstream CL0
  success remains quiet/open. Independent review is `APPROVE`; Sol passed this
  vector and its HEAD neighbor 2/2 with 138 checks. Broader methods/bodies,
  converter lowering, and nginx differential evidence remain open; #270 stays
  blocked.
- #268 public fragmented GET/CL0 success wire in `34c7e156`: ordinary RUT
  source reaches a production cleartext io_uring shard and one fresh origin
  receives the exact bodyless GET. The exact HTTP/1.1 200/unique-CL0 header is
  emitted in eight recorded positive fragments; every adjacent send gap is
  100--650ms while first-fragment-to-client completion exceeds 1.10s. The
  client receives the exact configured CL0 response, the origin stays open
  after sending, and only RUT retirement supplies the observed close; no
  reconnect occurs. Independent review is `APPROVE`; Sol passed this vector and
  its neighboring fragmented HEAD regression 2/2 with 115 checks. GET
  progress-then-stall, broader methods/bodies, converter lowering, and nginx
  differential evidence remain open; #270 stays blocked.
- #268 public bodyless-GET/CL0 wire in `045412ab`: ordinary RUT source compiles
  through lexer/parser, HIR/MIR/RIR, JIT, RouteConfig, and a production
  cleartext io_uring shard. Origin 1 receives the exact bodyless GET, sends zero
  bytes while staying open, then observes RUT retire it; the client receives the
  exact configured full 504 response including its 20-byte body below the
  unrelated global-timeout class. Only after retirement, the same downstream
  socket sends request 2 through a fresh origin connection; origin 2 returns
  exact HTTP/1.1 200 with one `Content-Length: 0` while staying open, and the
  client receives the exact configured CL0 success and remains quiet/open.
  Independent review is `APPROVE`; Sol passed this vector plus three neighboring
  deadline wires 4/4 with 241 checks. Fragmented GET progress, broader methods/
  bodies, converter lowering, and nginx differential evidence remain open;
  #270 stays blocked.
- #268 bodyless-GET/CL0 runtime slice in `0e58e97b`: one authoritative captured
  deadline profile and shared owner validator now cover the existing strict
  HEAD domain plus cleartext io_uring H1.1 bodyless default-keepalive GET with a
  fresh single IPv4 upstream. GET success accepts only exact HTTP/1.1 200 with
  one `Content-Length: 0`; every other status/framing/parser shape retires the
  origin and zero-byte closes before downstream mutation. A distinct immutable
  full-Content-Length prebuilt purpose validates the complete configured timeout
  body, including empty, binary, and exact-capacity bodies. Both timeout and
  success retire the fresh origin, and a two-owner send/retirement rendezvous
  gates request 2. Independent review is `APPROVE`. Sol passed Network 856/856
  (112,329 checks), Frontend, JIT, and Http2Conn; Integration passed 385 tests
  and failed only the independent #263 baseline (38,343 checks, one skip).
  Public ordinary-source GET wire evidence, broader methods/bodies, converter
  lowering, and nginx differential evidence remain open; #270 stays blocked.
- #270 bounded parser/model recognition in `1e3b1477`: the independent nginx
  parser accepts exactly one explicit raw root-location `proxy_read_timeout Ns`,
  `N=1..63`, stores nginx-semantic milliseconds plus directive/value spans, and
  gives source-located diagnostics for duplicate/arity/context errors, invalid
  time values, and nginx-valid but excluded aliases/quoted/escaped forms. The
  converter independently validates present/absent values, root path, and span
  invariants before any output allocation; every valid present directive returns
  `lowering is not implemented`, while forged models fail closed. Absence keeps
  legacy generated RUT byte-identical. Independent review is `APPROVE`; Sol
  passed `test_nginx_parser` 23/23 with 518 checks. No RUT lowering, golden
  timeout output, differential, or support claim exists; #270 remains
  `NOT_IMPLEMENTED` and its lowering is blocked by #268's broader method/body
  domain.
- #268 public fragmented timing evidence in `4b40e2cb`: ordinary RUT source
  compiles through lexer/parser, HIR/MIR/RIR, JIT, RouteConfig, and a production
  cleartext io_uring shard. Six actual positive header fragments have measured
  100--650ms gaps while first-fragment-to-client-completion exceeds 1.10s, and
  the exact strict HEAD success completes without reconnect or EOF. A separate
  vector sends one positive incomplete fragment, stays open/quiet beyond 1s,
  receives the exact configured header-only 504 below the global-timeout class,
  observes RUT close origin 1, then reuses the same downstream socket against a
  fresh exact origin request. Independent review is `APPROVE`; Sol passed the
  two vectors plus the zero-byte deadline wire 3/3 with 167 checks. This proves
  only the narrow general-RUT domain, not nginx equivalence; #268 remains open
  for broader lifecycle/domain acceptance while #270 can resume bounded work.
- #268 fragmented strict-HEAD runtime progress in `60abda7b`: the io_uring
  provided-buffer producer emits all-or-nothing copy witnesses, and a bounded
  pointer-free whole-batch ledger validates generation, episode, byte ranges,
  every matching Recv terminal, downstream EOF/error vetoes, and slot pinning
  before mutation. Matching positive progress wins a same-batch Timeout; live
  `F_MORE` refreshes the explicit per-forward deadline without another SQE;
  terminal incomplete progress queues exactly one continuation Recv before
  refresh. Later expiry authoritatively revalidates the complete admitted domain
  and retained generation/episode/exact-prefix proof before consuming private
  bytes and entering the configured D1/D2 response. Three review rounds ended
  in `APPROVE`. Sol passed Network 846/846 (111,904 checks), Frontend, JIT, and
  Http2Conn, while Integration passed 383 tests and failed only the independent
  #263 baseline (38,235 checks, one skip). The admitted domain remains narrow;
  public fragmented timing evidence, converter lowering, and nginx differential
  evidence remain open, so #270 stays blocked.
- #268 fragmented strict-HEAD progress design has independent
  `APPROVE_DESIGN` after one `REQUEST_CHANGES` round. The selected general-RUT
  primitive adds all-or-nothing backend copy witnesses and a bounded batch
  ledger keyed only by connection ID, deadline generation, and upstream
  episode. Every owner is validated before timer/state mutation; matching
  progress wins same-batch Timeout, earlier fragments are accounting-only, and
  the parser runs once at the final relevant CQE. Live `F_MORE` refreshes
  without another SQE, while terminal incomplete progress must queue one exact
  continuation before refresh. Invalid copy/cancel/terminal ordering or same-
  batch client close zero-byte closes and pins slot reuse until batch-end
  reclaim. The admitted source/request/upstream/policy domain remains unchanged;
  nginx issue #270 stays blocked by #268.
- #268 public ordinary-RUT deadline wire in `e30cbe47`: an effect-free direct
  HEAD Forward with `response_read_timeout: 1s` compiles through lexer/parser,
  HIR/MIR/RIR, JIT, RouteConfig, and production cleartext io_uring serving.
  A real origin receives the complete request, sends zero response bytes, and
  observes RUT retire it; the client receives the exact configured header-only
  `504 First Response Deadline` within eight seconds while the global upstream
  timeout remains its default greater than eight. The same downstream
  connection then completes a second HEAD against a fresh origin. Independent
  review is `APPROVE`; Sol passed the new and nearest #267 wires 2/2 with 116
  checks. This still does not prove positive-progress refresh, general
  `proxy_read_timeout`, converter lowering, or nginx differential equivalence.
- #268 narrow first-response runtime deadline in `7228a7b4`: cleartext io_uring
  H1.1 bodyless default-keepalive HEAD with one fresh IPv4 upstream and the
  complete paired strict policy bundle now pins the immutable route bundle,
  submits one exact fresh response Recv after upload, owns the TimerWheel node,
  and resolves same-batch Timeout/Recv deterministically before reclamation.
  An unresolved expiry can call only the proven #267 configured timeout path;
  every unsupported or forged shape closes with zero bytes. A review found and
  a different Luna fixed the sole local-400 materialization leak; final review
  is `APPROVE`. Sol passed network 835/835 (111,501 checks), frontend 974/974
  (7,511), JIT 486/486 (5,965), and Http2Conn 75/75 (232). Integration passed
  382 tests and failed only the independent #263 TLS watermark baseline. No
  public ordinary-RUT timing wire, progress rearm/refresh, converter lowering,
  or nginx differential exists yet.
- #268 narrow first-response deadline design has independent
  `APPROVE_DESIGN`. It admits only cleartext io_uring H1.1 bodyless HEAD, one
  IPv4 fresh upstream, and the complete paired strict policy bundle. A
  dedicated fresh-Recv primitive replaces ambiguous idempotent submission;
  the pinned route bundle remains authoritative; Timer expiry latches as
  `ExpiryPending` until the whole CQE batch is accounted, with a matching Recv
  deterministically winning. Batch-end expiry calls only the proven #267
  strict timeout path and otherwise closes with zero bytes. Body/body-wait,
  reuse/retry, TLS/H2/epoll, transforms/mutations/decorators, incomplete first
  response batches, and unpaired policies remain explicitly rejected. This
  slice still excludes positive-progress rearm/refresh, partial responses,
  converter lowering, and nginx differential evidence.
- #268 ordinary-RUT metadata and preflight foundation in `8b541a5a`:
  `forward(..., response_read_timeout: <duration>)` accepts only exact whole
  seconds 1..63 and carries the scalar through AST/HIR/MIR/RIR, the immutable
  policy bundle, RouteConfig, and JIT dispatch without widening the packed
  handler ABI. This first slice deliberately admits only an effect-free static
  direct Forward; branch/yield/body-dependent/effectful shapes and request/
  response mutation combinations reject explicitly. A route stores only the
  authoritative bundle ID, so H1 closes before rate limiting, body wait,
  handler execution, or upstream work, while H2 uses a sticky process-batch
  close fence and reclaims only after `Http2Conn::process()` returns. RIR
  verification is bidirectional, and JIT route registration validates and
  stages the complete fixed registration before commit. Two final reviews are
  `APPROVE`. Sol's full affected suites passed frontend 974/974 (7,511 checks),
  JIT 486/486 (5,965), network 823/823 (111,234), and Http2Conn 75/75 (232).
  This is metadata/preflight evidence only: no timer arm, refresh, timeout
  response, converter lowering, or nginx differential exists yet.
- PR #269 CI hygiene in `12581aeb` and `c5a87303`: the simulator reconstructs
  its non-owning RIR module in place instead of triggering GNU 13's aggregate-
  assignment ICE, and the held-epoll invalid-output regression now uses the
  defined nonnull/`maxevents == 0` EINVAL path instead of violating glibc's
  nonnull contract under UBSan. Both changes have independent `APPROVE`
  reviews and focused local tests. PR CI run `32539599547` confirmed GNU 13
  compilation and the sanitizer job; remaining integration failures are the
  independent #263 TLS watermark baseline.
- #268 general RUT design investigation and independent `APPROVE_DESIGN`:
  `response_read_timeout` is a per-forward upstream response-read inactivity
  interval, not a total request deadline. Exact duration literals representing
  1..63 whole seconds fit the current wheel; zero is absence and explicit
  values never inherit the global 30-second timeout. The scalar extends the
  existing policy bundle without changing the packed handler ABI. Runtime work
  must pin request/config/episode ownership, refresh only on exact positive
  upstream progress, settle same-batch Recv/Timeout races explicitly, and keep
  every unproven phase zero-byte fail-closed. Converter lowering remains later.
- #267 public ordinary-RUT Recv-timeout wire in `a1cd9745`: the source declares
  distinct response, default-502, and timeout-504 policies, compiles through
  HIR/MIR/RIR/JIT/RouteConfig, and runs on a real production io_uring shard.
  A test-only pre-spawn global clock acceleration triggers the real
  timerfd/TimerWheel path without fabricating request policy IDs, transport
  ownership, pending counts, callbacks, timeout events, or CQEs. The origin
  sends zero response bytes and proves RUT closes it; the same downstream
  connection then reaches a fresh origin and completes an exact header-only
  200/close response. The first response is the exact configured header-only
  504/CL167. Independent review is `APPROVE`; Sol's affected public set passed
  6 tests with 241 checks. This proves policy selection, not #268/source-level
  duration semantics, converter lowering, or nginx differential equivalence.
- PR #269 GNU 13.3 build compatibility repair in `f4346acc`: the RIR module
  reset now uses an explicitly typed value, preserving destroy/init/source-name
  semantics while avoiding the compiler's untyped-brace assignment failure.
  Independent review is `APPROVE`; Sol's focused frontend set passed 5 tests
  with 82 checks. PR CI remains responsible for the exact GNU 13.3 rerun.
- #267 bounded Recv-only runtime selection in `77542eeb`: a configured timeout
  policy now serializes transactionally only for cleartext H1.1, bodyless,
  default-keepalive paired `HEAD` with the exact live upstream Recv owner and
  composes with #265 D1/D2. Every unsupported phase/layout remains zero-byte
  fail-closed; epoll stays explicitly unsupported, the default policy ID is not
  mutated, and the successful timeout-origin rendezvous rearms a bounded timer.
  Production-transition evidence reaches admission through the real JIT,
  Connect and full Send callbacks, backend completion dispatch, and TimerWheel;
  fake CQEs replace only kernel delivery. Two independent reviews are
  `APPROVE`. Sol's focused network set passed 54 tests with 2,257 checks.
  Public ordinary-source wire behavior, #268 duration semantics, converter
  lowering, and pinned differential evidence remain open.
- #267 ordinary-RUT timeout-policy metadata/compiler foundation in `b41b1aac`:
  additive `timeout_failure_policy` carries a complete 400..599 immutable policy
  beside the exact-502 default policy through AST/HIR/MIR/RIR, a triple policy
  bundle, RouteConfig/JIT, fixed-body pending state, current request metadata,
  and reset without widening the packed handler ABI. Invalid roles/IDs, missing
  peers, HEAD mismatches, caps, forged outcomes, and config swaps are covered
  before upstream effects. Until runtime selection lands, an explicit timeout
  policy zero-byte fails closed rather than falling into legacy 504/default 502;
  no-policy behavior is unchanged. Two independent reviews are `APPROVE`.
  Sol's focused frontend/JIT/network set passed 8 tests with 194 checks. Runtime
  serialization/D1-D2, public-source behavior, #268 duration semantics,
  converter lowering, and pinned differential remain open.
- Converter-generated pinned incomplete-header/EOF differential in `61bedb27`:
  the independent converter parses/lowers the same minimal nginx fragment to
  ordinary RUT. Fresh pinned-nginx 1.29.7 and generated-RUT recorders both prove
  that exact positive incomplete header bytes stay quiet/open before the
  test-authorized origin EOF, then yield the exact reusable header-only
  502/CL157. Request 2 uses the same downstream connection, a fresh origin
  connection, the exact byte-identical upstream wire, and ends with the exact
  200 response plus downstream EOF. All request-wire/connection assertions run
  after recorder join; failure cleanup waits for recorder-thread Abort
  acknowledgement and cannot manufacture EOF. Two independent reviews are
  `APPROVE`; required serial Docker CTest passed 1/1 in 13.65s with no skip or
  residue. This proves observable EOF behavior, while focused runtime tests
  retain zero-owner/CQE evidence. Timeout/504 remains open under #265 and
  invalid-status HTTP/0.9 fallback remains #266.
- Converter-generated pinned malformed-header differential in `4c844be2`: the
  independent converter parses/lowers the same minimal nginx fragment to
  ordinary RUT, and pinned nginx 1.29.7 matches production generated RUT for a
  valid HTTP/1.1 status followed by the empty-name header `:`. Both yield the
  exact header-only reusable 502/CL157, quiet window, proxy-attributed origin-1
  close, fresh exact request-2 origin, exact 200 close response, and EOF. Fresh
  recorder instances prove two exact accepts/connection IDs and byte-identical
  origin wires. Independent review is `APPROVE`; required serial Docker CTest
  passed 1/1 in 10.33s with no skip or residue. The initially proposed
  `not-http` wire was correctly rejected: nginx treats it as HTTP/0.9 fallback,
  a real broader gap now tracked in #266. At this checkpoint incomplete EOF and
  timeout remained open; the former is now completed by `61bedb27`.
- #265 public incomplete-header/EOF wire evidence in `5c7668dc`: an atomic
  origin-close gate proves non-empty incomplete response bytes alone leave the
  downstream quiet/open, then authorized EOF yields the exact configured
  header-only keep-alive 502/CL157. The same downstream socket sends request 2
  only afterward, using a fresh exact origin wire and ending with the exact
  header-only 200 close response and EOF. Failure, premature-close, unexpected-
  data, authorized-close, and teardown states cannot manufacture the terminal
  event needed to pass. Independent review is `APPROVE`. Sol's public parser-
  failure pair passed 83 checks; full integration retained only #263 (381
  passed, 1 failed, 1 skipped; 38,115 checks). Exact zero-owner accounting stays
  in focused tests, and converter-generated differential evidence is still
  absent.
- #265 public RUT invalid-status-prefix/open-peer wire evidence in `fecec54a`:
  ordinary public
  RUT source compiles through HIR/MIR/RIR and JIT into the production io_uring
  loop. The RUT-specific `not-http` parser error yields the exact configured
  header-only keep-alive 502/CL157 and a quiet reusable boundary; the origin
  observes RUT close request 1 before the same downstream socket sends request
  2, which uses a fresh exact upstream wire and ends with the exact header-only
  200 close response and EOF. The test deliberately does not claim to observe
  kernel `CQE_F_MORE`; focused tests separately prove the exact live-Recv mask.
  Independent review is `APPROVE`. Sol's targeted pair passed 69 checks and full
  integration retained only #263 (380 passed, 1 failed, 1 skipped; 38,077
  checks). This wire is not nginx-equivalent because nginx falls back to
  HTTP/0.9; #266 now records that distinction.
- #265 parser-only production admission in `ba39832a`: non-empty malformed
  positive reads now rendezvous the configured transactional header-only 502
  with exact live-Recv retirement, while buffered incomplete-header terminal
  EOF/error uses the zero-owner handoff. Cause, policy, request, upload, and
  ownership predicates remain exhaustive; every excluded cause fails closed
  without bytes or fallback. Two independent reviews are `APPROVE`. Sol's
  focused suite passed 70 tests with 2,772 checks, the full network suite passed
  807 tests with 110,654 checks, and fresh integration retained only #263 (379
  passed, 1 failed, 1 skipped; 38,032 checks). Public wire and converter
  differential evidence are still absent, so #265 remains open.
- #265 D2 internal prebuilt-header/request-epoch rendezvous in `cf22e100`:
  HeaderSend and exact upstream retirement now race under separate wait owners,
  but request 2 is published only at batch end after phase-bound buffer
  normalization and exact-once epoch release. Zero-owner terminal handoff,
  max-token quarantine, real single-wait composition, duplicate HeaderSend
  fencing, close/reset slice pinning, and reachable Connect/Send/Recv layouts are
  covered. Two review rounds found and closed three HIGH/phase blockers; final
  reviews are `APPROVE`. Sol's full network suite passed 804 tests with 110,534
  checks, and fresh integration retained only #263 (379 passed, 1 failed, 1
  skipped; 38,032 checks). At that checkpoint there was no production failure
  caller.
- #265 D2 reconciled design: a transactional prebuilt header may race exact
  upstream retirement, but both sides publish request-2 readiness only at batch
  end. Compact wait/layout state preserves Connect/fresh-Send prefixes,
  retry-Send snapshots, existing pipeline ordering, and the old config epoch;
  a zero-owner terminal path advances the episode without inventing ownership.
  An independent adversarial re-review is `APPROVE_DESIGN`. No code or public
  behavior changed; the selected implementation remains internal-only.
- #265 D1 exact-owner transport foundation in `2bdbfe1d`: the io_uring
  retirement ledger now owns Connect/Send/Recv target, cancel, and retry bits
  independently, atomically detaches selected live authority, fences retired
  Send mutation, and preserves masks/tombstones through close/reset to exact-one
  reclaim. The production strict entry remains recv-only and no public failure
  response was enabled. Two independent reviews are `APPROVE`; Sol's full
  network suite passed 794 tests with 110,246 checks, and fresh integration kept
  only the #263 baseline (379 passed, 1 failed, 1 skipped; 38,032 checks). #265
  remains open for the header/epoch rendezvous and public behavior.
- Converter-generated reusable paired `HEAD` differential in `5a334df6`: the
  same minimal nginx fragment is parsed/lowered to ordinary RUT source, and
  pinned nginx 1.29.7 matches production RUT for two-request header-only success
  and connect-establishment 502 behavior, including quiet windows, final EOF,
  fresh exact upstream wires, and request-derived persistence. Independent
  reviews are `APPROVE`; Sol's required serial Docker CTest passed 1/1 in 7.89s
  without skips or resource residue. Broader failures remain #265/`PARTIAL`.
- #264 bounded public reusable paired `HEAD` admission in `67411a67`: default
  HTTP/1.1 keep-alive success and connect-establishment 502 now preserve body
  suppression independently from request-derived persistence, while broader
  failures terminate before bytes under #265. Public source-to-real-io_uring
  evidence proves two header-only responses, fresh exact upstream wires, C1-C3
  retirement, and final EOF. Independent reviews are `APPROVE`; Sol's full
  network suite passed 789 tests with 109,994 checks, and full integration had
  only the #263 baseline (379 passed, 1 failed, 1 skipped; 38,032 checks). #264
  is closed; converter-generated differential evidence remains CURRENT.
- A three-way public-admission audit found the next bounded split. #264 may close
  after real reusable success and connect-establishment failure evidence, while
  timeout, send/recv failure, and malformed/incomplete/excess response paths must
  emit zero bytes and terminate until #265 supplies generic all-upstream-I/O
  abandonment. Pinned nginx 1.29.7 proves those broader paths instead emit
  reusable header-only 502/504 responses, so #265 tracks the real compatibility
  gap and #253 remains `PARTIAL`.
- #264 C3 repeated strict recv retirement in `ef6e5d73`: a persistent latest
  tombstone is atomically replaced only after exact prior ownership drains, so
  two production strict callbacks can retire E1 and E2 before request 3 resumes.
  Old records remain inert across reset/reuse, close/reclaim composes with the
  current successor ledger, and exhaustion quarantines. Independent reviews are
  `APPROVE`; Sol's full network suite passed 786 tests with 109,913 checks, and
  the freshly rebuilt integration suite retained only the #263 baseline
  (378 passed, 1 failed, 1 skipped; 37,991 checks). Public reusable `HEAD`
  remains disabled, so #264 stays open.
- #264 C2 generic io_uring HTTP/1 request-boundary rendezvous in `3bb907ac`:
  request-1 bookkeeping completes once, request-2 bytes remain inert until C1
  retirement drains, and batch-end resume occurs exactly once. Exact successor
  ownership now fences backend recv/send mutation and survives close through a
  per-type target/cancel ledger. Independent reviews are `APPROVE`; Sol's full
  network suite passed 779 tests with 109,339 checks, while the freshly rebuilt
  full integration suite had only the independently tracked #263 failure
  (378 passed, 1 failed, 1 skipped; 37,991 checks). Reusable `HEAD` and a second
  strict retirement remain disabled, so #264 stays open.
- Repository architecture and RUT capability reconnaissance.
- Existing test infrastructure and differential harness reconnaissance.
- Official nginx semantic analysis for the minimal proxy example.
- Compatibility matrix v0 and converter architecture proposal.
- Minimal nginx fragment lexer/parser, semantic model, and targeted tests.
- Any-method route design investigation for #251; method omission was selected
  over an `ANY` keyword.
- Any-method source route implementation for #251, including compiler/runtime
  registration and precedence tests.
- Independent review and production registration validation for #251; the RUT
  capability issue is closed.
- First general source listener capability for #250, including source/CLI
  resolution, owned loader metadata, independent review, targeted tests, and
  production request-through-route acceptance. #250 is closed.
- Production io_uring cross-thread submission repair for #255, including fatal
  control-plane propagation, two review rounds, and real one/two-shard HTTP
  process acceptance. #255 is closed.
- Upstream request-policy design investigation for #252; the recommended source
  surface is per-forward opt-in backed by an immutable general policy table.
- First strict #252 request-policy slice, including exact H1 request
  reconstruction, u16 policy ABI, compiler/runtime conflict guards, independent
  review, and 10 Sol-run targeted tests (139 checks). The capability remains
  `PARTIAL` and #252 remains open.
- Proxied response-policy design investigation for #253; the existing transparent
  response path is explicitly insufficient for nginx equivalence.
- Response-policy metadata/ABI foundation and bounded strict H1 serializer for
  #253, including config-owned strings, independent request/response policy IDs,
  exact Content-Length output, H1/H2 fail-closed limits, and targeted tests.
  Broader response compatibility remains `PARTIAL`.
- Independent strict-serializer reviews and Sol validation: 19 targeted tests
  passed with 507 checks. A real RUT process matched the pinned nginx 1.29.7
  downstream response after normalizing only dynamic `Date`, and emitted the
  exact expected upstream request. #253 remains open and `PARTIAL`.
- Deterministic minimal-model lowering in `e6e1ff9b`, followed by independent
  review and the RIR-depth test correction in `4508e306`. Sol reran the focused
  target: 13 tests, 125 checks.
- First converter-generated process loop: the same fragment was included by
  pinned nginx 1.29.7 and parsed/lowered through the converter API for RUT.
  Both matched the bounded GET response vector after only Date normalization,
  and the recording backend received byte-identical requests. #254 remains
  open because its POST/body and gateway-error acceptance cases are unmet.
- Pinned nginx POST baseline: one fixed-CL binary body is forwarded byte for
  byte with the expected rebuilt headers. A 15-second half-body timing probe
  showed zero backend accepts until the final body bytes arrived, rejecting an
  early-connect streaming design. A reviewed Option-B design uses bounded
  pre-upstream staging and keeps #253 body admission as a separate dependency.
- Bounded #252 request-body staging in `0e13413a`, exact-cap/fail-closed
  hardening in `1b051cb1`, and the header-only semantic correction in
  `b0ac05cf`. Independent review is `APPROVE`; Sol reran 7 targeted tests with
  16,495 checks. Complete body arrival now precedes upstream slot/connect, and
  full upload completion is tracked separately from local buffering.
- Bounded #253 fixed-body admission in `642a6085`, its non-vacuous slot test in
  `ce226557`, and response-before-send-completion repairs in `501db5ba` and
  `8e023bfa`. The strict serializer consumes one nginx-compatible upstream
  `Connection` field via `1193a043`, with token/duplicate evidence in
  `303e9a22`; all reviews are `APPROVE`.
- Pinned converter-generated POST process loop: a split four-byte binary
  fixed-CL request produced byte-identical upstream bytes and exact normalized
  downstream bytes versus nginx 1.29.7. Neither implementation connected to
  the backend during the 350 ms half-body window. Sol reran 5 integration tests
  with 319 checks and 2 network tests with 49 checks.
- Pinned unavailable-upstream evidence: nginx emits a 157-byte HTML 502 with
  Server/Date/Content-Type and request-derived keep-alive after the complete
  request body; current RUT emits its hard-coded 11-byte close response. This is
  classified as a RUT capability gap and tracked in #256.
- Request-derived downstream connection mode in `216672d2`, followed by the
  drain/header lifecycle correction and syntax documentation in `45df3f41`.
  Independent review is `APPROVE`; Sol reran 12 targeted tests with 346 checks.
  A pinned converter-generated `Connection: close` success differential matched
  normalized downstream bytes, exact upstream bytes, and EOF after the body.
- The #256 immutable failure-policy/bundle/JIT-RIR foundation in `13877fa1` and
  `5d1bb789`, with fail-closed simulator/runtime regressions in `165c4a2b` and a
  clean-build test correction in `1105897c`. Independent review is `APPROVE`;
  Sol ran 8 targeted tests with 143 checks. Runtime serialization remains open.
- The bounded #256 runtime serializer and connection-establishment failure wiring
  in `32d05244`, with deterministic endpoint/injection and combined-success
  evidence in `7cb6a883`. Independent review is `APPROVE`; Sol ran 11 targeted
  tests with 202 checks. Converter lowering and the real differential remain.
- Converter failure-policy lowering in `f45c767c`, corrected to the pinned
  157-byte CRLF body in `63f635c9`; independent review is `APPROVE` and Sol ran
  4 converter tests with 90 checks.
- Pinned unavailable-upstream process differential: converter-generated RUT and
  nginx 1.29.7 matched complete Date-normalized bytes for two keep-alive GETs,
  explicit close plus EOF, and a split fixed-CL POST. Neither emitted the POST
  502 during the 350 ms half-body window. #256 acceptance is complete.
- Pinned percent-encoded success differential: `/encoded/%7Euser?tag=unreserved`
  produced identical Date-normalized downstream bytes and byte-identical
  upstream requests; both preserved `%7E` in the raw forwarded target. The
  explicit #254 vector set is complete for the bounded minimal fragment.
- The first committed serial differential harness case in `b6b1814c`, hardened
  by `3257feda` and `58c12bc6`: it exercises the converter API, production RUT,
  and the pinned no-pull nginx image for the `%7E` success vector. Independent
  review is `APPROVE`; Sol's real Docker CTest passed with no resource residue.
- The second committed harness case in `989a969f` reserves a non-listening
  upstream port and proves the explicit-close 502 response and EOF against the
  complete pinned CRLF baseline. Independent review is `APPROVE`; Sol's real
  two-case Docker CTest passed and #257 acceptance is complete.
- Pinned `proxy_pass` URI-replacement reconnaissance for #258: `/api/`,
  `/api/x`, and `/api/x?y=1` become `/`, `/x`, and `/x?y=1`; `/api` redirects;
  repeated slash, dot segment, and `%7E` inputs are normalized. Current RUT has
  only literal full-path `set_path`, so #259 tracks the required general
  transform and #258 is `BLOCKED_BY_RUT`.
- #259 design selection: use an independent immutable `target_transform` policy
  recorded by a pre-return RIR effect and resolved through pinned config before
  all upstream side effects. Preserve old literal `set_path`; the first slice is
  cleartext header-only H1 and explicitly rejects normalization-sensitive input.
- #259 metadata-only foundation in `17d3a1e9`, corrected by `e1fc2ee4`: validated
  borrowed RIR specs are atomically deep-copied into RouteConfig-owned storage.
  Independent review is `APPROVE`; Sol built JIT production RUT and ran 9 tests
  with 88 checks. The metadata is intentionally not executable yet.
- #259 executable fail-closed effect plumbing in `9c07a91f`, with full printer,
  H1 keep-alive, and real H2 initial/timer-resume evidence through `9c3a94c5`,
  `97f51929`, `c6a57343`, `2ac9dda0`, and `2bfbed2e` (intermediate review
  corrections retained in history). Independent reviews are `APPROVE`; Sol
  ran 19 targeted tests with 262 checks. Recorded transforms now fail before
  request-policy and upstream side effects, while the simulator reports
  `Unsupported`; target materialisation and source syntax were absent at that
  checkpoint.
- #259 bounded clean H1 materialisation in `05a1e655`, hardened by `54e48807`,
  with real compiled-JIT/request-policy/backend-wire evidence in `99c362e9`.
  The reviewed slice performs an atomic prefix splice before upstream side
  effects and rejects normalization-sensitive or out-of-domain inputs. Sol ran
  26 targeted tests with 654 checks.
- #259 public compiler syntax in `3351f7a8`, with adversarial coverage corrected
  in `359b9a82`. It validates the exact descriptor, rejects target/header and
  response-mutation conflicts across control flow, preserves all policy bundle
  operands, and lowers to the owned RIR transform table. Independent review is
  `APPROVE`; Sol ran 9 focused tests with 189 checks.
- #259 public source-to-production-JIT evidence in `cd0893c1`, strengthened in
  `129b3c71`: exact clean target replacement, rebuilt Host, preserved ordinary
  headers, real Connection stripping, and normalization-sensitive rejection
  before backend activity. Independent review is `APPROVE`; Sol ran the wire
  test with 73 checks. #259 is closed and #258 is unblocked.
- #258 exact parser/model/golden lowering in `b1f84472`: only `/api/` paired
  with proxy URI `/` is added, while the legacy root/no-URI output remains
  byte-identical. Generated source reaches a verified owned RIR transform and
  preserves all policy operands. Independent review is `APPROVE`; Sol ran 17
  focused tests with 220 checks. Compatibility remains `PARTIAL` pending the
  converter-generated differential.
- #258 pinned clean-vector differential in `880b8786`, with bounded response
  deadlines in `f054cfc8`: converter-generated production RUT matches nginx
  1.29.7 for `/api/`, `/api/x`, and `/api/x?y=1` in exact upstream bytes and
  Date-normalized downstream bytes. Independent review is `APPROVE`; Sol's
  required serial Docker CTest passed all old and new phases in 1.26 seconds.
- #258 fail-closed boundary evidence in `76c92abb`, corrected by `05b1b4ec`:
  `/api`, repeated slash, dot segment, and percent escape each produce an exact
  local 400/EOF while a live recorder observes zero upstream side effects
  through a fixed settle and RUT shutdown. Independent review is `APPROVE`;
  Sol's complete pinned serial CTest passed in 1.82 seconds. #258 is closed;
  only its explicitly bounded clean request domain is `SUPPORTED`.
- #260 reconnaissance: official nginx 1.29.7 control flow confirms automatic
  redirect occurs before the proxy handler, preserves query, and has no
  upstream side effects. RUT inspection found that exact request-derived
  `Location` construction is not source-expressible; #261 now tracks the
  required general capability.
- #260 pinned nginx-only GET/close baseline in `413e210b`: exact absolute
  Location authority/port, query preservation, 169-byte response and EOF, with
  zero upstream activity through shutdown. Independent review is `APPROVE`;
  Sol's complete serial Docker CTest passed in 2.59 seconds.
- #261 design selection: use an owned generic redirect-policy table, dedicated
  Redirect action/terminator, actual-bound listener context, and atomic local
  serializer. The converter will retain one method-omitted `/api` route and use
  exact GET/raw-path guards before forwarding. Independent design review is
  `APPROVE_DESIGN`; no source/runtime capability is implemented yet.
- #261 metadata foundation in `810c8d05`: bounded generic redirect policies are
  validated in borrowed RIR and atomically deep-copied into RouteConfig-owned
  storage, with deterministic binary-safe printing and explicit duplicate/cap
  contracts. It remains unreachable from source, ABI, JIT, and runtime.
  Independent review is `APPROVE`; Sol ran 18 focused tests with 179 checks.
- #261 dedicated Redirect action in `b9df7ea2`, with non-vacuous fail-closed
  evidence corrected in `d3a278cf`: RIR, ABI, JIT, H1/H2 initial and timer
  resume, and simulator distinguish Redirect without pretending support. All
  current runtime paths reject locally before upstream side effects; successful
  serialization and source syntax are absent. Independent re-review is
  `APPROVE`; Sol ran 20 targeted/regression tests with 242 checks.
- #261 H1 success-path investigation selected a generic immutable actual-bound
  listener context copied by value into each connection. The serializer must
  atomically validate Host, raw origin-form path/query, capacity, and the
  pinned RouteConfig before publishing bytes; listener state must not enter
  hot-reloaded RouteConfig.
- #261 actual-bound listener context in `47187ebd`, with production-helper and
  reinitialization corrections in `751a2837`: port-zero multi-shard startup
  binds once, reuses and verifies the concrete kernel port, then copies the
  immutable context into legacy/epoll/io_uring connections after reset. Loop
  reinitialization clears stale state, request boundaries preserve it, and no
  listener data enters RouteConfig. Independent re-review is `APPROVE`; Sol
  ran 8 focused/regression tests with 127 checks.
- #261 bounded H1 serializer in `4d0bc31c`, hardened by `b29edbb5` and the
  timer-resume evidence correction `d74f485b`: policy-driven responses validate
  the cleartext connection, exact request/Host/framing domain, actual listener
  authority, raw query, capacity, and pinned config before atomically publishing
  bytes. Complete Date-normalized wire, close/epoch, hot-swap, and zero-upstream
  evidence passed independent review. Sol ran 30 targeted/regression tests with
  484 checks. H2/simulator execution remains unsupported; public source lowering
  is now compiler-supported, pending source-to-network evidence.
- #261 public-source design selection: use only an inline, fully explicit
  `return redirect({...})` policy inside one terminal `if ... else ...` in the
  existing method-omitted route. Stable 1-based policy IDs are validated at
  every compiler boundary; Redirect paths reject ignored response effects while
  the sibling Forward arm retains its target transform/policy bundle. Independent
  review is `APPROVE_DESIGN`; named policies and global declarations are absent.
- #261 inline source/compiler lowering in `c41b2442`, with parser-transaction,
  guard-failure conflict, and mandatory publication defenses in `5257452c`:
  owned binary bodies and stable policy IDs flow through AST/HIR/MIR into exact
  RetRedirect RIR and deep-owned RouteConfig, while the Forward sibling retains
  its target transform and full policy bundle. Independent re-review is
  `APPROVE`; Sol ran 46 compiler/config/loader tests with 549 checks.
- #261 public-source production evidence in `1fb576aa`, with synchronized
  backend-observation correction in `ee011ec4`: public source compiles through
  AST/HIR/MIR/RIR/JIT and runs on the real H1 loop, producing exact
  Date-normalized `/api` redirects with zero upstream activity while `/api/`
  and descendant requests retain the exact Forward branch behavior. Invalid
  raw targets fail locally. Independent re-review is `APPROVE`; Sol ran 19
  focused/regression tests with 521 checks. #261 is complete and no longer
  blocks converter issue #260.
- #260 converter lowering in `c695ccdc`: the accepted `/api/` + proxy URI `/`
  model now emits one method-omitted `/api` route with an exact GET/raw-path
  Redirect arm and the existing target-transform/request/response/failure
  Forward arm as its terminal sibling. Root lowering remains byte-identical and
  forged models remain fail closed. Independent review is `APPROVE`; Sol ran
  all 17 nginx frontend/converter tests with 242 checks. Converter-generated
  differential evidence is still required before a bounded `SUPPORTED` claim.
- #260 converter-generated differential in `42418f69`: two GET/close `/api`
  redirect vectors match pinned nginx 1.29.7 byte-for-byte after normalizing
  only Date, require EOF, and cause zero backend activity through process
  shutdown. The existing three clean Forward vectors remain exact and three
  normalization-boundary targets remain explicit RUT-only local rejections.
  Independent review is `APPROVE`; Sol's serial Docker CTest passed 1/1 in
  3.37 seconds. #260 is closed and only its exact bounded domain is `SUPPORTED`.
- #253 pinned nginx-only HEAD baseline in `5568b6b2`: an explicit-close HEAD is
  forwarded upstream as HEAD with the normalized upstream-authority Host. A
  strict upstream `Content-Length: 5` response produces exact nginx headers
  retaining that representation length, zero downstream body bytes, and EOF.
  Independent review is `APPROVE`; Sol's serial Docker CTest passed 1/1 in
  3.45 seconds. Current policy-bearing RUT Forward rejects HEAD before upstream
  connect, confirming a RUT capability gap rather than a converter task.
- #253 HEAD design selection: add only a generic response-policy disposition
  with legacy/default `Reject` and explicit future `SuppressBody`; keep policy
  IDs and Forward ABI unchanged. Metadata lands unreachable from source and
  fail closed before the strict H1 runtime increment. Failure-policy semantics
  wait for a separate pinned nginx gateway baseline. Independent review is
  `APPROVE_DESIGN`.
- #253 HEAD response metadata in `1a0fb56a`, with valid target-transform/bundle
  evidence in `dba9315b`: legacy/source policies remain `Reject`; reserved
  `SuppressBody` is validated, deduplicated, printed, deep-owned, and atomically
  published but remains source-unreachable and fails closed before upstream for
  every method. Independent re-review is `APPROVE`; Sol ran 36 focused tests
  with 1,088 checks across compiler/config, plain/transform dispatch, strict
  response/H2 regressions, and RIR printing.
- #253 strict HEAD success runtime in `1d908d2e`, hardened in `9452aebe` after
  review found original TE/Expect/Upgrade facts could be lost during request
  rewriting: internal `SuppressBody` now admits only the pinned cleartext-H1.1,
  bodyless, exact explicit-close domain, preserves representation Content-Length
  with zero downstream body, and abandons the nonpooled upstream after validated
  headers. Overlong coalesced bytes reject before commit; delayed bytes are not
  awaited; late events and completion are idempotent. Independent re-review is
  `APPROVE`; Sol ran 25 unique focused tests with 722 checks. Public source,
  failure policy, and converter selection remain absent.
- #253 pinned nginx-only HEAD gateway baseline in `b7d639e0`, with GCC/Clang
  target-link and line-scoped log correction in `4917575b`: one unavailable
  literal upstream yields exact header-only 502 with representation
  Content-Length 157, explicit close/EOF, and exactly one connection-failure
  record tied to the backend authority. Independent re-review is `APPROVE`;
  Sol's serial pinned Docker CTest passed 1/1 in 3.78 seconds.
- #253 failure-policy HEAD design: use one generic `ForwardPolicyHeadMode` with
  response/failure aliases and legacy `Reject` defaults. Preserve the existing
  response-only `SuppressBody` success path; only paired `SuppressBody` may later
  enter the bounded connect-failure path. Mismatches and failure-only suppression
  reject before body waiting, transform materialisation, or upstream effects.
  Selected-policy serialization is transactional and may never fall back to the
  legacy 11-byte 502. Independent review is `APPROVE_DESIGN`.
- #253 failure-policy HEAD metadata in `175337aa`, with ordinary direct/bundle
  HEAD and pre-bound publication evidence in `924b2d8a`: one shared enum now
  validates, deduplicates, prints, copies, and deep-owns response/failure HEAD
  dispositions. Existing source still creates `Reject`; every selected failure
  `SuppressBody` fails before body waiting, request rewriting, target
  materialisation, or upstream effects. Independent re-review is `APPROVE`;
  Sol ran 21 targeted tests with 436 checks across frontend, network,
  integration, legacy failure serialization, response-only HEAD, bad IDs, and
  H2 fail-closed regressions.
- #253 paired HEAD runtime in `f76f6f59`, hardened for fresh-connect/no-replay
  lifecycle in `bb343132` and with non-vacuous open-backend no-pool evidence in
  `d4a6dc29`: only paired response/failure `SuppressBody` can enter the bounded
  cleartext-H1.1, bodyless, exact-close HEAD domain. Original Host/Connection
  facts are validated before request-policy rewriting; strict success and
  connect failure retain representation Content-Length, emit no body, close the
  downstream, and abandon rather than pool the upstream. Mismatches and all
  declared out-of-domain inputs fail before upstream effects. Independent
  review is `APPROVE`; Sol ran 48 Clang tests with 689 checks, built both related
  GCC targets, and ran 8 GCC tests with 203 checks. Public source and converter
  selection remain absent.
- #253 public source design: add optional `head_mode: "reject" |
  "suppress_body"` to both policy objects, preserve omitted/explicit Reject,
  and expose only paired suppression. Central `analyze_term()` validation covers
  direct, if, match, and guard-failure Forward terms; response-only internal
  suppression remains available below the source boundary. No Forward ABI
  change is needed. Independent review is `APPROVE_DESIGN`.
- #253 public source/compiler selection in `0748c610`, with stale parser-negative
  and forged failure-policy evidence corrected in `5d09ae65`: optional omitted
  or explicit Reject remains legacy behavior, while source SuppressBody must be
  paired, request-derived-close capable, and transform-free. The common
  `analyze_term()` gate covers direct, if, match, and guard-failure terms without
  changing Forward/JIT ABI or internal response-only suppression. Independent
  re-review is `APPROVE`; Sol ran 23 focused frontend/converter tests with 513
  checks. Public-source production-JIT evidence and converter lowering remain
  absent.
- #253 public-source production-JIT evidence in `a0fddf74`, hardened for Date
  bounds, exact HIR/RIR branch mapping, deterministic failure short-circuit,
  default Reject, and exact dead address in `38766b44`: two fresh HEAD successes
  retain Content-Length with no body, a real GET sibling retains the strict
  body path, and connect refusal emits the configured header-only 502 rather
  than the legacy fallback. Exact rebuilt upstream wires, three fresh accepts,
  EOF, and source→JIT→owned-config registration are covered. Independent
  re-review is `APPROVE`; Sol ran 8 test executions with 195 checks across
  integration reruns and network lifecycle regressions. Converter lowering and
  the pinned nginx differential remain absent.
- #253 root/no-URI converter lowering in `3a3a9911`, with explicit no-plain-
  Forward evidence in `57b52685`: generated source selects paired SuppressBody
  only for HEAD and preserves the existing request/response/failure Reject
  bundle for every other method. `/api/` target-transform output is byte-
  identical and remains HEAD-unsupported. Independent re-review is `APPROVE`;
  Sol ran 14 focused frontend/converter tests with 386 checks, and the complete
  existing serial Docker differential passed 1/1 in 3.75 seconds.
- #253 converter-generated explicit-close HEAD differential in `50aa9abb`:
  pinned nginx 1.29.7 and production RUT each match the exact Date-normalized
  header-only 200 and unavailable-upstream 502 baselines, retain representation
  Content-Length, emit no body, and reach EOF. Success uses fresh recorders with
  exact-one accept/request/history and byte-exact rebuilt upstream HEAD wires;
  failure holds one reserved non-listening endpoint across both runs and retains
  nginx's line-scoped connect-failure evidence without claiming an unobserved
  RUT syscall count. Independent review is `APPROVE`; Sol ran the complete
  pinned serial Docker CTest twice (1/1 in 4.24 and 4.21 seconds), with no
  residual containers. The bounded slice is proven; #253 remains `PARTIAL`.
- #253 pinned keep-alive HEAD semantics in `0f405c0a`, built on the opt-in
  observation probe `2d439cd2` and review correction `e6497c27`: on one
  downstream socket, a default-keepalive HEAD receives an exact header-only
  200 and remains quiet/reusable, then a close-intent HEAD receives the exact
  header-only 200 and EOF. nginx opens two distinct upstream connections and
  sends exact rebuilt wires. With one held unavailable endpoint, nginx likewise
  emits an exact keep-alive header-only 502 followed by an exact close 502/EOF
  and two scoped connect-failure records. The observation was identical twice,
  independent review is `APPROVE`, and Sol's complete pinned serial Docker
  CTest passed twice (1/1 in 6.17 and 6.11 seconds) with no residual containers.
  Current RUT rejects this keep-alive HEAD domain, so #253 remains a RUT gap.
- #253 upstream-episode layout foundation in `d0456d1d`: internal pure helpers
  define non-colliding upstream 24-bit episode/8-bit aux and non-upstream full
  32-bit generation domains, `ConnectionBase` owns a persistent nonzero wrapping
  episode, and `IoEvent` has a neutral future transport field. No backend,
  dispatch, cancellation, serializer, or HEAD behavior changed. Independent
  review is `APPROVE`; Sol rebuilt `test_network` and ran 5 focused state tests
  with 291 checks. io_uring and epoll transport remain separate prerequisites.
- #253 io_uring upstream-episode transport in `eef1f114`, hardened by
  `c370c1e7` and `b8a333ea`, with evidence in `dca2b878` and `8c9e53cc`:
  valid nonzero episodes now cross connect/send/recv/pause/cancel SQEs and CQEs;
  stale or malformed receives are rejected before provided-buffer copy, stale
  partial sends cannot mutate or resubmit the live episode, and stale dispatch
  retires only final accounting without touching callbacks, armed state, or
  timers. Generic and specialized loop tests cover current/neutral controls,
  closed-slot reclaim, and two positive stale-buffer returns. Independent final
  review is `APPROVE`; Sol ran 12 focused tests with 396 checks. Episode
  advancement, cancellation/drain lifecycle, epoll #262, and HEAD admission are
  intentionally absent.
- #262 epoll pending-completion capacity prerequisite in `e50e1af6`, corrected
  by `d30ca1a7` and `5ece5ba6`: scoped send, TLS, connect, and upstream-recv
  producers now reserve the single-threaded fixed-stack capacity before any
  synchronous side effect, never overwrite an older completion, reject invalid
  connection IDs before syscalls/state mutation, and report partial-send
  registration failure through one exact terminal completion. Independent final
  review is `APPROVE`; Sol ran 9 focused tests with 321 checks. The wait boundary,
  fairness, episode identity, and stale pre-I/O fence remain open under #262.
- #262 epoll one-logical-completion prerequisite in `fd43876a`: each wait now
  returns at most one pending or kernel completion, pending work remains LIFO
  with a bounded burst quota of eight, kernel readiness gets a nonblocking
  fairness probe, accept consumes one backlog entry, and combined send/read
  readiness advances one operation at a time without ordinary-send starvation.
  The active-health regression exposed by the prerequisite was corrected in
  `a61bd6e9`: queued local upstream-send registration failures retain exact
  errno and teardown ownership without marking the backend down. Independent
  reviews are `APPROVE`; Sol built both focused targets and ran 45 integration
  tests with 17,438 checks plus 18 network tests with 140 checks.
- #262 C1 epoll episode transport in `7780b28d`, corrected by `cd2983f8`:
  registration and synthetic completions carry explicit episodes, raw stale or
  malformed tokens are rejected before fd-map/socket/TLS/state access, and
  concrete dispatch rejects them before timers, callbacks, or armed-state
  changes. Independent review is `APPROVE`; Sol ran 78 focused tests with
  17,697 checks and the complete 741-test network suite with 107,603 checks.
  The complete integration suite passed 378 tests and exposed one pre-existing
  io_uring TLS large-body failure, reproduced on isolated baseline `a1c71a56`
  and tracked in #263. Lifecycle advancement and real fd-reuse evidence remain
  open under #262.
- #262 C2a-0 epoll lifecycle foundation in `846b8216`, hardened by `cdbe0a08`:
  an epoll-owned active-token table now enforces strict begin and detached-retire
  transitions, checked generation advancement never wraps, and active, malformed,
  or max-token ownership is quarantined across connection-slot reuse. Independent
  re-review is `APPROVE`; Sol ran 24 focused epoll/io_uring episode tests with
  562 checks. Production owners and active-state fences are intentionally not
  wired yet, so this foundation alone adds no supported nginx behavior.
- #262 C2a production owner wiring in `6b034eaf`, with non-epoll async-accounting
  preservation in `9cd471c8`, fail-closed DEL handling in `69bf922a`, and sticky
  close-failure quarantine in `c58200e8`: fresh connect, retry, pool, health,
  timeout, and terminal owners now use centralized begin/detach/retire boundaries,
  while raw and concrete dispatch also require the active owner. Independent
  review is `APPROVE`. Sol ran 136 focused tests with 18,456 checks, the complete
  751-test network suite with 107,777 checks, and the complete integration suite
  with 378 passes, one failure, one skip, and 37,986 checks; its sole failure is
  the independently reproduced #263 baseline.
- #262 real reuse evidence in `cd4731d7`, made failure-safe by `1adc4070`, drives
  production fresh-connect/pool-return/pool-borrow on the same numeric fd and
  terminal-close/allocator reuse on the same Connection slot. Old raw readiness
  is rejected before socket I/O and old completion dispatch before timer/callback/
  state mutation; current-token controls still progress. Independent re-review is
  `APPROVE`; the worker repeated both tests ten times, and Sol ran 17 episode tests
  with 306 checks plus the complete 753-test network suite with 107,865 checks.
  Active-health production-transition evidence remains open under #262.
- #262 production failed-connect retry evidence in `6521d815` drives a complete
  request through an injected first `connect` failure, dispatches its real tagged
  completion through `on_upstream_connected`/`try_connect_next_backend`, and proves
  episode 1 is detached and retired before episode 2 begins on the same Connection
  and numeric fd. Replaying the old failure cannot alter retry, passive-health,
  callback, fd/map, owner, token, or response state; the current completion sends
  the exact original request to a real peer. Independent review is `APPROVE`; the
  worker repeated it ten times, and Sol ran 18 episode tests with 385 checks plus
  the complete 754-test network suite with 107,944 checks.
- #262 production active-health reprobe evidence in `65de05c0`, with the
  early-failure adoption correction in `050a8128`, drives two complete
  `sweep_health_probes` cycles through real connect, exact health-request send,
  HTTP 200 receive, and production terminal teardown. The second probe reuses the
  same Connection slot and numeric fd with the next episode; replaying the first
  real connect completion cannot mutate owner/map/token, callbacks, timer, health,
  pending/send/armed/buffer state, or peer bytes, while the current completion
  sends the exact request. Independent re-review is `APPROVE`; the worker repeated
  the final test ten times, and Sol ran 19 episode tests with 525 checks plus the
  complete 755-test network suite with 108,084 checks. A literal #262 audit found
  that active-health is extra confidence rather than a named acceptance item;
  non-vacuous upstream quiesce and raw stale send/connect transition evidence
  still block closure.
- #262 upstream quiesce evidence in `3a4d9aa7` begins a production-owned episode
  before registering a real socket, confirms peer-close HUP, and keeps owner,
  token, and fd map current while `ws_unpoll_upstream` removes the registration.
  A separate ready control fd proves `wait` executes, while no upstream event or
  deferred epoll wake remains; production detach then retires the episode before
  slot release. Independent review is `APPROVE`; the worker repeated the exact
  test ten times, and Sol ran the exact test with 30 checks, 13 quiesce/lifecycle
  tests with 467 checks, and the complete 755-test network suite with 108,103
  checks.
- #262 userspace-held raw connect evidence in `5bb74f75`, hardened in
  `9103a3f1`, adds a thread-local, exact-fd, one-shot test seam that captures an
  exact `epoll_event` returned by the real syscall, suppresses it, and later
  replays the by-value record through the unchanged backend. Exhaustive seam
  tests cover poison transitions, syscall-fault precedence, invalid operations,
  and RAII reset. The lifecycle test captures episode-1 kernel EPOLLOUT, performs
  production terminal detach/retire, reuses the same Connection slot and numeric
  fd for episode 2, then proves old raw replay returns no event or state mutation
  while the untouched current kernel completion dispatches exactly once. Two
  independent re-reviews are `APPROVE`; the worker repeated the connect proof ten
  times, and Sol ran 3 seam tests with 181 checks, all 29 fault tests with 273
  checks, 20 episode tests with 627 checks, and the complete 756-test network
  suite with 108,205 checks. The seam explicitly models a kernel-produced record
  already harvested into userspace, not kernel readiness surviving successful
  `EPOLL_CTL_DEL`.
- #262 userspace-held raw partial-send evidence in `20deb0e0`, strengthened in
  `33f493fb`, drives two real production EINPROGRESS connects and independently
  saturates each TCP send queue to kernel EAGAIN. It captures episode 1's exact
  nonterminal EPOLLOUT/UpstreamRecv record, performs production terminal
  detach/retire, and reuses the same Connection slot and numeric fd for episode
  2. Old raw replay returns no logical event and preserves all owner/map/token,
  callback, timer, pending, send, armed, buffer, connection, and wire state. The
  current kernel record then emits the exact UpstreamSend completion, sends one
  byte-exact payload, and dispatches one callback. FIN/EOF stream verification
  proves episode 1 emitted zero post-filler bytes and episode 2 emitted exactly
  one payload with no prefix, suffix, or duplicate. Independent re-reviews are
  `APPROVE`; the worker repeated the final test ten times, and Sol ran the exact
  test with 200 checks, 21 episode tests with 827 checks, and the complete
  757-test network suite with 108,405 checks. No direct raw-transition gap was
  known at that checkpoint; a fresh requirement-by-requirement closure audit
  then identified only the missing capture-before-transition receive proof.
- #262 userspace-held raw receive evidence in `0e0e3b7b` captures an exact
  kernel-produced episode-1 `EPOLLIN` record before production terminal
  teardown, then reuses the same Connection pointer/id and numeric fd for
  episode 2. Replaying the old record through unchanged `EpollBackend::wait()`
  returns zero and preserves current bytes, owner/map/token, send state,
  callbacks, timers, pending state, buffers, armed flags, and connection state;
  the untouched current record then reads the exact payload and dispatches once.
  Independent review is `APPROVE`; the worker repeated the test ten times for
  1,440 checks. Sol ran all 22 episode tests with 971 checks, the 15 lifecycle
  tests with 883 checks, 11 focused real integration tests with 402 checks, all
  29 fault tests with 273 checks, and the complete 758-test network suite with
  108,549 checks. Two independent closure audits and the final review prove all
  five required-capability and nine acceptance bullets under the corrected
  userspace-held record semantics. #262 is closed; this does not claim reusable
  converter-generated keep-alive `HEAD` support.
- #264 design investigation and independent adversarial audit classify reusable
  io_uring upstream abandonment as a real runtime capability gap, not missing
  converter lowering or test evidence. A strict HEAD header CQE may leave the
  multishot upstream recv live; current abandonment closes the fd and clears
  callbacks/armed flags without cancel, drain, or episode advancement, so a
  merely enabled request 2 could accept request-1 bytes before dispatch under
  the same token. The selected generic design uses serialized episode retirement,
  exact target/cancel ownership, checked advancement/quarantine, and deferred
  downstream admission. #264 is linked from #253 and explicitly separate from
  the #263 TLS large-body baseline. Existing focused evidence remains green:
  11 io_uring episode tests with 389 checks and 8 pause/cancel integration tests
  with 43 checks. C1 is deliberately recv-only and keeps reusable HEAD disabled.
- #264 C1 recv-only strict-abandon retirement in `0bb20537` advances the current
  io_uring episode before old provided-buffer copy, separately owns target and
  exact-token cancel finals, retries or collapses an unqueued cancel safely,
  quarantines exhausted tokens, and preserves tombstone/accounting/slices through
  deferred close and exact-one reclaim. Production `IoUringEventLoop` callback
  evidence proves the existing paired explicit-close HEAD wire, slot release,
  buffer return, and pre-copy fence. Two independent final reviews are `APPROVE`.
  Sol ran 10 retirement tests with 292 checks, 22 episode/reclaim tests with 447
  checks, 5 strict HEAD integrations with 96 checks, the complete 768-test network
  suite with 108,932 checks, and the complete integration suite with 378 passes,
  the sole independent #263 failure, one skip, and 37,986 checks. Reusable HEAD
  remains disabled; C2 must add deferred request-boundary resume and the remaining
  serialized lifecycle before #264 can close.
- #264 C2 design and adversarial audit select a bounded, generic HTTP/1 boundary
  rendezvous: finish request-1 bookkeeping once, park all request-2 parse/route/
  rearm work while retirement is active, preserve positive downstream bytes,
  close on EOF/error, and resume once after the current CQE batch. An inactive
  tombstone may pass only the exact current successor token while continuing to
  swallow retired/malformed tokens without accounting. C2 deliberately cannot
  clear/overwrite the tombstone, start a second strict retirement, enable reusable
  HEAD, or alter nginx converter/source/compiler behavior; those remain later
  reviewed batons.

## OPEN RUT CAPABILITY ISSUES

- #252 — upstream HTTP request policy for `forward`.
- #253 — proxied response header policy.
- #263 — io_uring TLS large proxied-body watermark lifecycle.
- #265 — all-upstream-I/O retirement before reusable failure responses.
- #266 — upstream HTTP/0.9/status-line-less close-delimited response semantics.

## KNOWN SEMANTIC RISKS

- nginx version must be pinned in differential evidence.
- The request-policy baseline is nginx 1.29.7 image digest
  `sha256:1854da86e82d5dfb49a8f3d78b099adcc7e36608b207146ed95cd47937938a40`.
- normalized routing URI and original forwarded request-target are distinct.
- With a URI in `proxy_pass`, nginx replaces the matching part of its normalized
  URI, not necessarily the raw request-target: repeated slashes and dot segments
  collapse, and `%7E` becomes `~` in the observed pinned vectors.
- A slash-terminated proxied location redirects the no-slash form (`/api` to
  `/api/`) before proxying. Current RUT routing erases that trailing-slash
  distinction; the #259 bounded slice must reject it rather than approximate it.
- A Redirect action alone cannot prove the nginx source-path condition because
  canonical routing collapses `/api` and `/api/`. Public lowering must execute
  an exact raw `req.pathOnly == "/api"` guard, and the bounded H1 serializer must
  independently revalidate the origin-form raw path before emitting bytes.
- Redirect authority must use the actual port returned after `bind`/`getsockname`
  (including declared port `0`), not a listener declaration stored in the
  hot-reloaded RouteConfig.
- nginx and RUT path canonicalization and prefix boundaries differ.
- An invalid upstream status prefix is not an immediate nginx malformed-header
  502: pinned nginx 1.29.7 falls back to HTTP/0.9/close-delimited semantics,
  while RUT currently returns `ParseStatus::Error`. #266 tracks this broader
  gap. The completed malformed differential is intentionally limited to a valid
  HTTP/1.1 status line followed by an invalid empty-name header.
- Host, Connection, hop-by-hop fields, and framing cannot be compared by a
  permissive allowlist when declaring a feature `SUPPORTED`.
- nginx is not installed on the host; Docker provides the pinned nginx test
  dependency. The `%7E` success and explicit-close gateway vectors are committed
  and serialised; broader POST and keep-alive vectors remain manual evidence.
- The cross-thread io_uring stall is fixed and covered by a non-skipping
  real-socket regression, but full differential execution remains serial-only.
- io_uring TLS proxying can terminate after zero or one 8192-byte body chunk
  while streaming a 256 KiB fixed-length response through the TLS output
  watermark. This pre-existing failure is tracked in #263; future nginx TLS
  compatibility must remain unsupported until its repeated acceptance passes.
- nginx 1.29.7 uses HTTP/1.1 and omits upstream `Connection` by default. It uses
  a fixed header skip set rather than dynamic `Connection` token normalization:
  nominated ordinary fields, `Proxy-Connection`, and `Trailer` are forwarded.
- An absolute-form client target is normalized to origin-form by nginx 1.29.7;
  the first request-policy slice may reject it but must remain PARTIAL.
- The first #253 behavior increment supports only a bounded strict HTTP/1.1
  final-response/content-length serializer plus one explicit-close, bodyless
  HEAD slice. HTTP/1.0, HTTP/2, keep-alive HEAD, Upgrade, absolute-form,
  interim/no-body statuses, chunking, close-delimited framing, and response
  mutations remain intentional fail-closed `PARTIAL` limits; broader nginx
  response behavior is not claimed.
- The converter emits both bounded policies. One fixed-CL binary POST now has
  generated-RUT differential evidence, but the one-slice size bound and broader
  framing/request/response domains keep this `PARTIAL`, not general body support.
- nginx's default request buffering is observable at backend accept time. The
  #252 body increment must buffer before upstream allocation/connect; reusing
  transparent early-connect streaming would be a semantic mismatch.
- The first buffering slice is bounded by existing connection-buffer capacity;
  larger nginx request bodies remain unsupported until RUT gains a broader
  memory/file-backed body buffering primitive.
- #253 body admission distinguishes locally buffered completion from actual
  upstream upload completion. Response-before-send-CQE ordering is admitted
  only after a positive full-send completion; partial or failed writes remain
  fail closed.
- Pinned nginx consumes one upstream `Connection` field and synthesizes the
  downstream value without dynamically removing token-nominated ordinary
  fields. The bounded RUT serializer matches those tested vectors and rejects
  duplicate Connection fields; broader hop-by-hop response semantics are open.
- A connect failure has no upstream response, so #253 cannot serialize nginx's
  local gateway page. #256 must supply a separate general failure policy; the
  converter must not embed a hidden nginx-only runtime mode.
- Request-policy materialisation and downstream connection intent are separate:
  the converter now emits response `connection: "request"`, preserving explicit
  client close while the rebuilt upstream request remains reusable H1.1.
