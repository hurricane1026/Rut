# nginx converter status

## CURRENT

- Implement the minimal nginx server-fragment lexer/parser and semantic model.
- Scope: one `server`, one `listen <port>`, one `location /`, and one literal
  IPv4 HTTP `proxy_pass` without a URI suffix.
- Unknown or out-of-scope syntax must fail with a source-located diagnostic.
- No RUT lowering in this increment.

## NEXT

1. Investigate the minimal general RUT design for any-method source routes
   (#251); do not implement before the design is reviewed.
2. Resolve the listener representation contract (#250).
3. Add minimal-model capability validation and deterministic RUT lowering only
   after dependencies are explicit.

## BACKLOG

- Dedicated serial nginx-vs-RUT process differential harness.
- General prefix, exact, `^~`, regex, and nested location selection.
- `proxy_pass` URI replacement and trailing-slash semantics.
- Multiple servers, listen/default-server selection, and `server_name`.
- Variables, rewrite phases, internal redirects, upstream groups, TLS, static
  files, error pages, and logging compatibility.

## BLOCKED

- Listener lowering: #250.
- All-method location lowering: #251.
- nginx default upstream request behavior: #252.
- nginx default proxied response header behavior: #253.

## RECENTLY COMPLETED

- Repository architecture and RUT capability reconnaissance.
- Existing test infrastructure and differential harness reconnaissance.
- Official nginx semantic analysis for the minimal proxy example.
- Compatibility matrix v0 and converter architecture proposal.

## OPEN RUT CAPABILITY ISSUES

- #250 — downstream listeners in RUT source/config.
- #251 — any-method source routes.
- #252 — upstream HTTP request policy for `forward`.
- #253 — proxied response header policy.

## KNOWN SEMANTIC RISKS

- nginx version must be pinned in differential evidence.
- normalized routing URI and original forwarded request-target are distinct.
- nginx and RUT path canonicalization and prefix boundaries differ.
- Host, Connection, hop-by-hop fields, and framing cannot be compared by a
  permissive allowlist when declaring a feature `SUPPORTED`.
- nginx is not installed on the host; Docker is available for a pinned test
  dependency, but no differential test has run yet.
