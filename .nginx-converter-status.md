# nginx converter status

Historical relay evidence is preserved byte-for-byte in
[nginx-converter-status-2026-09.md](docs/archive/nginx-converter-status-2026-09.md).

## CURRENT

- #638 nginx-only oracle `6772e375` pushed but independently REQUEST_CHANGES:
  missing shared actual acceptance/negative controls, explicit client cleanup,
  and a clear frozen live-evidence versus post-join teardown boundary. Astra
  resolves the minimal correction design, including separation of observed
  natural peer retirement from subsequent recorder-local shutdown/close.
  No build/runtime tests ran; off remains rejected by converter and its exact
  ordinary-RUT capability is still unproven. #630 closure docs were APPROVED
  by Astra and #630 is closed for its exact witness. Earlier CURRENT entries
  below are historical; CURRENT remains #638 oracle correction, not admission.
- #637 merged as `a9ebd7b9` into converter integration, not main. Final head
  `367631ef` CI `34698186843` passed all 15 ordinary jobs; required nginx
  `103565197203` passed 160/160, zero skips, 643.59s, new HEAD pair 7.45s.
  Log: `/tmp/rut-637-3676-nginx-ci.log`. #630's exact declared bodyless HEAD
  witness is complete; only its precise matrix row is SUPPORTED. Broad #270
  PARTIAL/#271 BLOCKED_BY_RUT remain unchanged. CURRENT moves to the explicit-
  off single-prefix nginx oracle on `nginx-off-oracle`, starting from
  `a9ebd7b9`. Source audit found candidate ordinary-RUT None streaming paths,
  but exact capability still needs a separate ordinary-source live witness
  before any off parser/lowering admission. Prior CURRENT entries are history.
- Complete #630 source `cd690657` independently APPROVED, including actual
  selected-HEAD timeout/head-mode/buffering controls. Root Clang Release build
  passed; ten targeted regressions passed 10/10, zero skips, in 61.66s. Logs:
  `/tmp/rut-630-cd69-controls-build.log` and
  `/tmp/rut-630-cd69-controls-targeted.log`. CURRENT is complete-candidate
  PR/normal-CI validation into `agent/nginx-to-rut-converter`, not main.
  All code is pushed; earlier rejected attempts and failures remain recorded.
  Full CI/integration and exact compatibility closure remain pending; broad
  #270 PARTIAL and #271 BLOCKED_BY_RUT remain unchanged.
- #630 same-file wiring source `2ef3f7c9` independently APPROVED and root
  Clang Release build passed. First behavioral validation passed 2/2, zero
  skips, 12.07s: original HEAD oracle 4.36s and actual same-file nginx/generated-
  RUT pair 7.71s. Logs: `/tmp/rut-630-2ef3-pair-build.log` and
  `/tmp/rut-630-2ef3-pair-first.log`. CURRENT is Luna
  `luna_630_selected_head_controls`: default-off actual-selected-HEAD policy
  mutations 2s->1s, SuppressBody->Reject, None->CompleteContentLength, with
  independent rejection/identity checks and restored positive invocation after
  each. Existing runtime episode is unchanged. Independent review, focused
  regressions and final complete-candidate PR/CI remain; no support promotion.
- #630 wiring `37cf3bd1`/`c255b6cb` is pushed and independently reviewed
  REQUEST_CHANGES: Date normalization incorrectly erased upstream requests,
  and its empty-on-failure comparator could equate invalid downstream wires;
  formatting also failed. Root appended `2ef3f7c9`, requiring both downstream
  normalizations to succeed and comparing raw upstream bytes, plus mechanical
  formatting. Re-review is CURRENT; no build/behavioral tests yet for wiring.
  Existing default Date/Server/X-Pad plus custom hide inventory was confirmed
  in converter source by Astra, so the exact nginx config is unchanged.
- Luna `luna_630_wire_shared_pair` now owns one immutable-config/two-frontend
  HEAD pair atop reviewed and locally validated `8ff77274`. It must call the
  same capture helper for nginx and generated RUT, retain identical endpoints
  and configured sink path, archive the nginx ledger, authenticate actual
  converter stdout with existing O2 validation at 2s, and compare independent
  live results. No helper behavior changes or ad-hoc runtime episode. Astra
  keeps selected-HEAD mutation controls as the following separate commit;
  full #630 acceptance and normal final-candidate CI require both batons.
  Extraction-only full CI is not duplicated before the requested pair exists.
- #630 extraction `8ff77274` independently APPROVED after one-line failed-
  acceptance client cleanup and mechanical formatting corrected `95651dfc`.
  Root Clang Release build passed; nine targeted tests passed 9/9 with zero
  skips in 51.85s, including original HEAD oracle, adjacent #270/#621/#627
  real-nginx/generated-RUT regressions and #618 observer self-check. Logs:
  `/tmp/rut-630-8ff7-extraction-build.log` and
  `/tmp/rut-630-8ff7-extraction-targeted.log`. Shared HEAD capture is now
  locally validated with unchanged acceptance gates. Astra defines the next
  bounded same-file/two-frontend wiring baton using this helper. Full #630
  differential, final required CI and any support promotion remain pending.
- #631 integrated as `bc3c770b` after final `4c38c945` CI `34695407252`
  passed 15/15 ordinary jobs and required nginx 159/159 zero skips (634.82s).
  #634/#635 are integrated and closed; #632 historical Docker cause remains
  recorded, not a current blocker. #630 full same-file generated-RUT HEAD pair
  remains the objective. Attempts `4e5c052f` and `d81b6ebd` are preserved on
  `issue630-head-cli-pair` but independently REQUEST_CHANGES; no tests ran.
  They substitute admission/separate abbreviated runtime probes for the pair,
  retain wrong timeout/CLI admission, and include unsafe live history reads.
  Astra selected CURRENT: extract the proven nginx HEAD live episode from
  `bc3c770b` on `issue630-head-episode-extraction`, with unchanged acceptance,
  pre-cleanup frozen snapshot and post-join history. No new CLI/test/policy in
  this extraction baton. Different-Luna review and original-oracle validation
  precede the next same-config/two-frontend wiring baton. Earlier CURRENT
  entries below are historical; no HEAD support promotion is claimed.
- #635 code `75db77fa` independently APPROVED; mechanical formatting produced
  `f34e9590`. Root Release build passed, shared observer self-check passed 1/1
  (0.01s), and all eight targeted real-nginx/generated-RUT regressions passed
  with zero skips in 51.55s, including the previously failing #270/#621 expiry
  episodes, #627 2s pairs and #630 nginx-only HEAD oracle. Logs:
  `/tmp/rut-635-f34e-clang-build.log`, `/tmp/rut-635-f34e-selfcheck.log`, and
  `/tmp/rut-635-f34e-targeted.log`. CURRENT is combined corrective-candidate
  PR/normal-CI integration for separately committed #634/#635. Historical #633
  failures remain preserved; no old-head retry or support promotion. NEXT is
  still the same-file generated-RUT HEAD pair after the stack passes its gates.
- #635 candidate `1311dbe4` is pushed but REQUEST_CHANGES; no build/tests run.
  Different Luna `luna_635_snapshot_fix` now implements Astra's correction:
  acquire the closure flag in a separate first statement, read committed
  count/time afterward, use one owned snapshot per sample, remove later cleanup
  stores from readiness, and freeze successful live evidence before cleanup.
  Actual publisher ordering is timestamp, count, release flag; the contrary
  initial review claim was corrected. Stability custody checks remain required.
  Shared controls must cover publication-in-progress and the unchanged timing,
  regression and sticky-failure contracts. Review evidence is preserved in #635.
- #635 is CURRENT on `issue635-joint-expiry-observation`, based on reviewed
  #634 head `bbfbe368` (already pushed). Original #633 run `34691750670`
  finished failed: 13/15 ordinary jobs passed; required nginx 157/159 with zero
  skips in 653.23s. #270 expiry oracle and #621 explicit-on CLI nginx episode
  failed in addition to #634 provisioning. Log: `/tmp/rut-633-13f9-nginx-ci.log`.
  Astra identified a frozen retirement-readiness snapshot that irreversibly
  fails the ledger observer before its existing window can observe publication.
  Luna `luna_635_joint_observer` implements joint retirement/ledger acceptance
  within unchanged EOF+250ms, followed by unchanged 175ms stability and genuine
  sticky custody failures. No retrospective acceptance, deadline relaxation,
  production change or old-run retry. Independent review and focused tests
  precede combined candidate CI; #630 HEAD pair remains NEXT after integration.
- #634 candidate `23cf3db2` independently APPROVED and root-scheduled
  deterministic real-curl tests passed 7/7 with zero skips in 19.060s.
  Log: `/tmp/rut-634-23cf-acquisition-tests.log`. Tests observe TLS error 35
  with one old-policy attempt versus four new-policy attempts, partial retry
  recovery, wrong-hash rejection, destination preservation and timeout 28.
  Earlier `9b061e6a` test failed (1 failure, 4 errors) because loopback traffic
  was not isolated from proxy environment; log and review corrections are
  preserved in #634. Production proxy behavior is unchanged. CURRENT is final
  candidate PR/normal-CI integration after original run `34691750670` finishes;
  #633/#631 are not waived, and #630 HEAD equivalence remains NEXT.
- #634 is the current integration prerequisite: #633 candidate `13f92e4e`
  normal CI run `34691750670` failed its Runtime state model job
  `103548110103` while downloading pinned TLA+ tools (curl 35, connection
  reset); checksum and model execution were not reached. Other jobs remain
  running at this handoff. No rerun, skip, or merge waiver is authorized.
  Astra selected bounded immutable-artifact acquisition with the same official
  version/hash and retry count, explicit transport-error retry classification,
  private staging, checksum gating, and atomic publication. Luna
  `luna_634_acquisition` implements only that script, deterministic real-curl
  loopback controls, and workflow wiring on `issue634-pinned-tla-acquisition`.
  Independent review and root-scheduled tests follow. Historical reset cause
  remains unknown. PR #633 is stacked on #631; #630 generated-RUT HEAD pair
  stays NEXT. No compatibility row is promoted by infrastructure evidence.
- #632 diagnostic mode `5bd24396` independently APPROVED. Root Release build
  and Docker-free self-check passed (1/1, zero skips, 0.26s); invalid extra
  arguments reject before probing. Single manual diagnostic run `34691231681`
  completed successfully at that exact source head: Docker info exited 0 in
  2.320741006s within the unchanged 10s bound, with three ordered launch records,
  channel closure before cleanup, no integrity/ownership errors, and a nonempty
  client/server snapshot. Artifact `10297278732` preserves the evidence.
  This is NOT acceptance CI and does not explain the historical timeout.
  CURRENT is the reviewed instrumentation PR into `issue630-head-oracle`, then
  normal acceptance for the materially changed candidate; no rerun of frozen
  #631 or support promotion. NEXT remains #630 generated-RUT HEAD differential.
  Earlier CURRENT entries below are historical relay evidence.
- #632 lifecycle candidate `884f42de` independently APPROVED and integrated
  append-only as `bd3ad3c2` (test source byte-identical). Root Release build
  and expanded Docker-free self-check passed 1/1, zero skips (0.25s).
  Logs: `/tmp/rut-632-bd3a-clang-build.log` and
  `/tmp/rut-632-bd3a-selfcheck.log`. Astra is defining the next bounded
  real-Docker diagnostic/integration step; no historical cause or timeout fix
  is claimed, and PR #631 remains frozen pending that decision.
- #632 protocol code `5d984c3e` independently APPROVED. Root Release build
  passed, then Docker-free preflight self-check passed 1/1 with zero skips
  (0.17s). Logs: `/tmp/rut-632-5d98-clang-build.log` and
  `/tmp/rut-632-5d98-selfcheck.log`. Different worker `luna_632_lifecycle`
  now owns silent post-launch versus FIFO evidence and isolated standard-FD
  collision/cleanup checks. Original Docker timeout remains unexplained;
  PR #631 remains frozen, with no acceptance-CI retry authorized.
- #632 revision `061fedf9` remains REQUEST_CHANGES: bounded field decoding
  improved, but valid two-record launch errors still fail final-prefix checks.
  Different worker `luna_632_protocol` now owns exactly the shared byte decoder
  and actual-parser positive/negative controls. Silent post-launch and descriptor
  lifecycle evidence remain follow-up requirements, not waived acceptance.
- #632 diagnostic implementation `1f0c03e3` is pushed but independently
  REQUEST_CHANGES: error-record enum/decoder mismatch, incomplete protocol
  validation and missing actual-parser negative controls. Luna corrects the
  shared bounded decoder and frozen launch evidence; no build/runtime test
  has been scheduled. This does not explain or fix the historical Docker stall.
- #632 is CURRENT: PR #631 exact-head CI `34688539296` failed with 14/15
  successful jobs. Required nginx passed 158/159 in 667.03s; the new HEAD
  oracle passed (3.90s), but the first Docker-info preflight timed out at 10s
  and was killed/reaped cleanly. No retry or merge waiver. Preserved log:
  `/tmp/rut-631-7e87-nginx-ci.log`. Astra recommends dedicated bounded
  launch-stage diagnostics to distinguish fixture launch from Docker execution;
  work proceeds on `issue632-docker-launch-evidence`, leaving PR #631 frozen.
  NEXT remains #630 same-file generated-RUT HEAD differential after integration.
- #630 oracle code `9640547d` independently APPROVED. Root Clang Release
  build passed; ten targeted tests passed with zero skips (67.61s), including
  the new pinned-nginx HEAD oracle (3.81s). Logs:
  `/tmp/rut-630-9640-clang-build.log` and `/tmp/rut-630-9640-targeted.log`.
  CURRENT is exact-head PR/CI integration of this nginx-only oracle; NEXT is
  the same-file generated-RUT HEAD differential. This is not RUT equivalence
  evidence and does not promote HEAD or close #630. Earlier failures remain
  recorded below and in the issue.
- #630 `0620d6cd` passed independent source review, but its first root-scheduled
  Clang Release build failed: vector expected wire was passed to a C-string
  validator, and the Docker mount expression concatenated character arrays.
  Evidence: `/tmp/rut-630-0620-clang-build.log`, terminal exit 2. Luna owns
  only these type corrections; no runtime tests have run. Earlier review
  entries below are historical, not the current blocker set.
- #630 frozen `4ce41620` remains REQUEST_CHANGES after independent review.
  Shared acceptance and the missing Docker TempDir mount are fixed; actual
  lifecycle evidence and distinct wire mutations still need correction. Astra
  selected a bounded design: real access/quiet timestamps, unconditional
  post-ACK custody, measured HEAD closure count, pre-cleanup verdict, and
  actual header/body mutations. `luna_630_finish_controls` implements this
  design; no production change or behavioral test result is claimed.
- #630 `e437682b` is also pushed and independently REQUEST_CHANGES:
  fresh probe and shared timing checks now exist, but ACK-wait custody and
  required non-timing negative controls remain incomplete. A different Luna,
  `luna_630_finish_controls`, now owns only those corrections and a final live
  ledger read. No build/test has been scheduled; previous reviews remain
  preserved in issue #630. HEAD status is unchanged.
- #630 revision `82a57fb1` is pushed but independently reviewed
  REQUEST_CHANGES. Post-join history, pre-cleanup retirement, exact publication
  and repeated live-ledger checks improved. Remaining: fresh near-permit peer
  probe/ACK and synthetic controls sharing actual acceptance predicates (the
  current synthetic timing lambda remains separate). Luna owns the correction;
  root has not scheduled compilation or behavioral tests for this revision.
  No HEAD compatibility promotion or production defect is established.
- #630 is CURRENT: pinned-nginx delayed complete-header bodyless HEAD oracle
  for explicit-on/X-Powered-By/2s. Luna implements on `issue630-head-oracle`,
  reusing the initialized isolated directory `Rut_issue627_two_second_oracle`.
  NEXT is its same-file generated-RUT HEAD differential. Do not reuse GET body
  predicates or fake an EOF anchor for the live header-completion ledger.
- #627 completed through PR #629 merged at `afe50e5b` from `04ae2bf7`.
  CI `34686463457` passed 15/15 jobs; required nginx passed 158/158 with zero
  skips (671.73s), new 2s CLI 13.24s. Issue #627 is closed for its representative
  GET episodes only; broad #270/#271 remain unchanged. Earlier entries below
  preserve prior baton evidence, not active CI waits.

- #627 CURRENT is now the same-file CLI-generated RUT 2s expiry/completion
  differential, assigned to Luna on `issue627-two-second-cli`. The initialized
  isolated directory `Rut_issue627_two_second_oracle` is reused on this new branch;
  root remains sole build/test scheduler. Expected selected-bundle duration must
  come from the fixture, with a genuine 2s-to-1s selected-policy rejection control.
- CLI code `ac4b1571` is independently Luna-approved. Root scheduled Clang
  Release build passed, then nine targeted tests passed with zero skips (63.79s),
  including the new same-file nginx/generated-RUT 2s expiry/completion pairs
  (13.35s), oracle and existing 1s default/explicit/boundary/ledger regressions.
  The selected GET mutation preserves route identity, rejects 1s, and validates
  restored 2s. Exact-head CI/integration remains pending; no promotion or closure.
  A worker-started build was interrupted and is not counted as validation;
  root verified no residual process before its sole scheduled build.
- Oracle PR #628 merged at `205aa878` from final `ebff78d1` after CI
  `34685162795` passed all 15 jobs; required nginx passed 157/157 with zero skips
  (620.45s), new 2s oracle 6.50s. #627 remains open pending generated-RUT proof.
  The earlier oracle-baton entries below are historical evidence.

- #627 is CURRENT: first establish a pinned-nginx 2s explicit-on/X-Powered-By
  GET expiry/completion oracle with gaps above 1s and below 2s, plus discriminating
  timing controls. Luna implements in `issue627-two-second-oracle`; root schedules
  tests. NEXT is the same-file generated-RUT 2s witness with selected-bundle checks,
  then one separately designed HEAD wire scenario. No production gap is proven.
- #627 oracle code `1d4e536b` is independently Luna-approved. Root fresh isolated
  Clang Release build passed; eight targeted tests passed with zero skips in
  50.33s, including the new nginx-only 2s expiry/completion oracle (6.55s) and
  existing 1s default/explicit/boundary CLI and ledger regressions. Earlier reviews
  required causal timing mutants and restoration of original 1s predicates;
  append-only fixes precede these results. Exact-head CI/integration is pending.
  This does not prove generated-RUT 2s equivalence or close #627.
- #621 completed through PR #626, merged at `132a0d35` from source `a0c847a6`.
  CI `34326645381` passed all 15 jobs; required nginx passed 156/156 with zero
  skips (619.28s), new explicit-on CLI witness 7.73s. Issue #621 is closed only
  for its bounded component acceptance and representative X-Powered-By/1s GET
  expiry/completion network evidence. Broad #270/#271 statuses remain unchanged.
  Entries below preserve preceding baton evidence, not active CI waits.

- PR #625 is integrated at `6c127bb362e4968a7257897182f569fa991a6726`.
  Final source `810d15d4` passed independent Luna review and CI `34324359742`
  (15/15 jobs); required pinned nginx passed 155/155 with zero skips in 611.54s.
  The final direct `<algorithm>` include fixes the preserved earlier CI compile
  failure; final GCC16 Debug component tests passed 2/2. CURRENT now advances
  to the dedicated #621 explicit-on same-file CLI-generated RUT expiry/completion
  differential, assigned to Luna in `issue621-explicit-on-cli-differential`.
  Network code `1f547c57` is independently Luna-approved; root Clang Release
  build passed and seven targeted real-nginx tests passed with zero skips in
  47.39s, including the new explicit-on CLI expiry/completion test (8.08s).
  Initial `e9634830` review found missing CLI admission/path/help wiring; the
  append-only correction precedes these passing results. Exact-head CI and
  integration remain pending; #621 stays open and no support row is promoted.
  The lowering evidence below records the preceding baton, not a live CI wait.

- #621 explicit `proxy_buffering on` + custom hide-header + timeout composition
  is CURRENT. Astra identified a converter admission/source-inventory gap;
  existing ordinary RUT emission appears sufficient, pending pinned behavior
  evidence. Scope remains exact-loopback/root/no-URI, one custom name and the
  existing bounded timeout grammar. Oracle prerequisite `aac11d14` is independently
  Luna-approved; root Clang Release build and six targeted tests passed with zero
  skips (45.95s), including four-run explicit/default-on nginx oracle (9.88s).
  PR #622 merged at `233a0321` after final source `9533b340` passed all 15 CI jobs
  in `34318669602`; required nginx passed 155/155 (610.99s), new oracle 7.74s.
  Oracle-only evidence does not establish generated-RUT composition support.
- CURRENT baton: #621 authenticated explicit-on/custom-hide/timeout lowering,
  preserving byte-identical ordinary RUT output versus omitted buffering and all
  bounded admission constraints. Generated-RUT network proof follows separately;
  oracle and model-authentication prerequisites are integrated. Code `41487c0f`
  is independently Luna-approved; root Clang Release component build and tests
  passed 2/2 (4.83s; parser 188/188, CLI 12/12), and no-JIT build/tests passed
  2/2 (4.24s). All 24 directive orders, 3-/46-byte names, 1s/63s values,
  complete-profile equality/IR verification and located real CLI negatives are
  covered at component level. Earlier `b44f0aab` passed CLI but failed 14 parser
  diagnostic regressions; the scoped validation-order correction restores the
  existing expectations unchanged. Exact-head CI/integration remains pending,
  with generated-RUT network proof still a separate next baton. No promotion.
- #623 is completed: trusted-source hide metadata
  erasure was reproduced and preserved in the issue. Fix `04a7fc49` passed
  independent Luna review, root Clang Release build and parser/converter tests
  2/2 (4.17s); the unchanged reproducer now rejects erased metadata. Initial
  `d58708d0` failed to compile its test lambda; the one-line capture correction
  is append-only. PR #624 merged at `2bba609a` from final source `8669dc83`
  after CI `34320265023` passed all 15 jobs and required nginx 155/155 (643.77s).
  Final-head Release and no-JIT component tests passed 2/2 each (4.13s/4.05s).
  Issue #623 is closed. Intact #621 composition still rejects at this baseline;
  no runtime or emission changes were made by the fix.
- #617 completed through PR #620, merged at `9034ca51` from source
  `57e0b8252f014de9612145f0514386443983eb55`. Independent Luna review approved
  code `e74c5366`; root Clang Release build and six targeted tests passed with
  zero skips (32.93s). CI `34316694133` passed all 15 jobs; required pinned
  nginx passed 154/154 (603.53s), including boundary CLI 15.39s. Issue #617 is
  closed; broad #270 remains PARTIAL. Historical entries below describe their
  original relay state, not current blockers.
- PR #616 merged at `8759156d` from reviewed source `d9a11d49` after final
  CI `34314855669` passed all 15 jobs (required nginx 153/153), plus root's
  eight targeted tests, zero skips, 17.38s. #487, #618 and #619 are closed with
  their scoped evidence. #487 also passed 50 focused Clang Release spikes and
  20 serial six-gate suites, followed by source-head CI `34313564732` 15/15.
  The original Docker preflight cause remains unknown; #619 supplies diagnostics,
  not a claimed Docker root-cause fix. Historical failures below remain evidence.

- Historical PR #616 loaded/JIT and semantic-mutant gate at `20b6d52f`: root build and the five
  required targeted tests pass in 16.74s (CLI path in about 8s). CI `34307836036`
  is 14/15: the required nginx suite is 151/152, with only the new CLI differential
  failing its live RUT expiry access-ledger observation. CI `34309417741` retained the
  same 14/15 result while showing the RUT ledger reached exact `60\n` before cleanup;
  the initial read was empty and is not treated as an instantaneous requirement.
  #618's subsequently reviewed bounded observer correction uses EOF-anchored live publication,
  sticky read/custody failures, and shared 175ms stability validation are test-harness
  only; no runtime gap or promotion is claimed. Follow-up `4b5d81e9` passed the six
  targeted tests in 16.64s; final `ac76525a` results are recorded above.

- PR #615 combined hide/timeout lowering merged at `4b77709b` after all 15 CI
  checks passed. PR #616's same-file nginx/RUT expiry and completion pairs,
  loaded-bundle binding, and targeted regressions passed; final integration
  evidence is recorded above. Broad #270 remains PARTIAL.
- [#583](https://github.com/hurricane1026/Rut/issues/583) is closed through
  [#593](https://github.com/hurricane1026/Rut/pull/593); its
  parser prerequisite [#585](https://github.com/hurricane1026/Rut/pull/585)
  merged at `370f9452` after all 15 CI checks passed in `34257358077`.
  Lowering PR [#586](https://github.com/hurricane1026/Rut/pull/586)
  merged at `df0387fb` after review, local tests and all 15 checks in
  CI `34258705363` (attempt 2). The earlier failed/canceled jobs remain recorded
  under #588; the passing retry is not a fixture-race fix.
- CLI/same-file PR [#589](https://github.com/hurricane1026/Rut/pull/589)
  merged at `3230df6f` after all 15 checks in CI `34261094678`, plus root's
  five targeted tests and no-JIT CLI validation.
- Supporting fixture PR [#590](https://github.com/hurricane1026/Rut/pull/590)
  merged at `931374a1` from `4baab2e2` and addressed
  [#588](https://github.com/hurricane1026/Rut/issues/588). Its review and
  seven targeted tests passed, and CI `34261958820` passed all 15 checks.
  #588 is now closed, but the original CI failure's exact predicate remains
  historically unknown; this is not a demonstrated RUT semantic gap.
- Parser PR [#595](https://github.com/hurricane1026/Rut/pull/595) merged at
  `fa4acd4f` after all 15 checks in CI `34267670102`; it adds no
  support-matrix promotion.
- Authenticated lowering PR [#597](https://github.com/hurricane1026/Rut/pull/597)
  merged at `4776dd86` after all 15 checks in CI `34269326102`.
- Oracle PR [#598](https://github.com/hurricane1026/Rut/pull/598) merged at
  `780af3e6` after all 15 checks in CI `34272105956`; generated-RUT
  differential PR [#599](https://github.com/hurricane1026/Rut/pull/599) merged
  at `d3bc0b86` after all 15 checks in CI `34273814610`. Together they prove
  only the bounded explicit-Off retained-header row below; no general logging
  or nginx.conf support is claimed.
- Current #600 integration is still gated. #606 (`4f7862f9`) merged into
  parent `baac4c53` after all 15 checks in CI `34284374201`; its self-check
  Docker-dispatch defect is fixed, while the underlying #487 diagnosis remains
  open. #602 merged into parent `e9c2b8fc` after all 15 checks in CI
  `34287590928`; its original `RECV_OWNER` failure (`34276997536`) and
  superseded/canceled runs remain historical evidence, not passes. Parser PR
  #603 (`b90f58ac`) merged after all 15 checks in CI `34290079024`. Lowering
  PR #604 (`43091626`) merged after all 15 checks in CI `34292137712`; its
  authenticated dynamic-name lowering remains bounded and does not expand
  parser grammar. #612's diagnostic increment (`40bf6557`) is merged, while
  #611 remains open. CLI PR #607 (refreshed source union `2bac6902`) merged
  after all 15 checks in CI `34293936209`; its same-file JIT/runtime proof
  remains bounded. Boundary PR #608 source head `76272ba5` merged at
  `72acb7fc` after all 15 checks in CI `34295400826`; the reviewed local
  boundary/combined suite passed 28/28
  with zero skips in 48.18s. The #600 documentation closure merged through
  [#613](https://github.com/hurricane1026/Rut/pull/613); #487 and #611 remain
  diagnostics, not fixes or compatibility promotion. #614 is merged after its
  pinned-nginx expiry/completion oracle and CI; current #270 work is the
  authenticated converter lowering slice for one custom hide name composed with
  literal `proxy_read_timeout` 1..63s. Generated-RUT behavior remains unproven.
- The parser/lowering grammar admits the bounded unquoted ASCII `X-` name
  range, but runtime evidence is representative rather than every admitted
  spelling: existing `X-Powered-By` plus the 3-byte and 46-byte boundaries.
  Complete-file admission covers one empty `events {}` wrapper followed by the
  bounded HTTP profile; it does not claim arbitrary directive placement or
  general nginx.conf support.
- [#580](https://github.com/hurricane1026/Rut/pull/580) is merged and closes
  [#577](https://github.com/hurricane1026/Rut/issues/577). [#582](https://github.com/hurricane1026/Rut/pull/582)
  is merged and closes [#574](https://github.com/hurricane1026/Rut/issues/574).
- Parent integration remains Draft [#269](https://github.com/hurricane1026/Rut/pull/269).
- The workflow remains `nginx.conf` → converter → ordinary RUT; there is no
  direct runtime nginx configuration mode.
- Matrix counts remain 89 `SUPPORTED`, 11 `PARTIAL`, 2 `BLOCKED_BY_RUT`, 0
  `NOT_IMPLEMENTED`, and 4 `NOT_PLANNED` (106 total); the new SUPPORTED row is
  the bounded explicit HTTP `access_log off` slice only.

## NEXT

1. Independently review and root-validate the explicit-off single-prefix oracle.
2. Prove the same bounded behavior through ordinary RUT None buffering and
   production admission; record any demonstrated runtime gap before workaround.
3. Only after capability evidence, model/lower literal off and build its actual
   same-file converter differential. No generalized buffering support claim.

## BACKLOG

- Broader nginx configuration, server/location selection, variables, rewrites,
  TLS/H2, and general logging semantics remain outside the bounded workflow.
- Broader hide-plus-timeout composition semantics under
  [#270](https://github.com/hurricane1026/Rut/issues/270) remain backlog, not
  promoted here. The precise nginx-only oracle merged in #614; same-file
  CLI/generated-RUT comparison and name boundaries merged in #616/#620.

## BLOCKED

- Broad upstream request policy remains `PARTIAL` under [#252](https://github.com/hurricane1026/Rut/issues/252);
  bounded request slices already supported remain unchanged.
- Broad proxied response policy remains `PARTIAL` under [#253](https://github.com/hurricane1026/Rut/issues/253);
  bounded response slices already supported remain unchanged.
- Response-read inactivity compatibility remains `PARTIAL` under
  [#270](https://github.com/hurricane1026/Rut/issues/270), with broader RUT
  timeout capability tracked by [#268](https://github.com/hurricane1026/Rut/issues/268).
- Broad buffering/commit compatibility remains `BLOCKED_BY_RUT` under
  [#271](https://github.com/hurricane1026/Rut/issues/271).

## RECENT

- #594: fixture predicate-diagnostic follow-up merged after all 15 CI checks in
  `34264392174`; #588 closed with its original predicate uncertainty retained.
- #595: typed `access_log off;` parser merged after all 15 CI checks in
  `34267670102`; #597 lowering, #598 oracle and #599 generated-RUT differential
  all merged with their recorded 15/15 CI runs. The bounded row is now
  SUPPORTED; broader logging remains outside scope.
- #585: bounded complete-file parser merged after all 15 source CI checks.
- #587/#584: exact history archive and lightweight current status merged/closed.
- #580/#577: standalone converter command merged and closed.
- #613: #600 matrix/evidence closure merged; #270 timeout-plus-custom-hide
  remains a separate nginx-only oracle sequence.
- #582/#574: pinned-nginx preload ABI/preflight merged and closed.
- #606 merged into the accepted parent; #602 merged as `e9c2b8fc` after its
  final 15-check CI; #603/#604/#607 merged after their final 15-check CIs;
  and #608 source head `76272ba5` merged at `72acb7fc` after CI `34295400826`.
  #612's diagnostic
  increment is merged; #487/#611 remain open diagnostics.
- #362 and #363 are closed; their bounded request-length/access-log work must
  not be described as an unresolved current blocker or as broad nginx support.

## OPEN RUT ISSUES

- [#279](https://github.com/hurricane1026/Rut/issues/279),
  [#281](https://github.com/hurricane1026/Rut/issues/281),
  [#282](https://github.com/hurricane1026/Rut/issues/282),
  [#283](https://github.com/hurricane1026/Rut/issues/283), and
  [#284](https://github.com/hurricane1026/Rut/issues/284): bounded local-response,
  HTTP-version, no-content, and framing capabilities.
- [#352](https://github.com/hurricane1026/Rut/issues/352): generic access-record
  endpoint/status and Host exposure.
- [#252](https://github.com/hurricane1026/Rut/issues/252),
  [#253](https://github.com/hurricane1026/Rut/issues/253),
  [#268](https://github.com/hurricane1026/Rut/issues/268), and
  [#271](https://github.com/hurricane1026/Rut/issues/271): broader upstream,
  response, timeout, and buffering domains.

## KNOWN RISKS

- #588 tracks a concrete fixture publication-order defect and the original
  CI's unresolved exact predicate. Passing retries alone do not fix that risk;
  #590 is merged; the original predicate remains historically unknown. This
  is not a demonstrated RUT semantic gap.
- Pinned-nginx evidence is version/digest-specific and does not establish
  general nginx.conf, process/resource, or network-equivalence support.
- URI normalization, trailing-slash routing, broader methods/framing, retries,
  reuse, pipeline expansion, TLS/H2, and wider timeout/buffering domains remain
  excluded unless separately evidenced.
- Parser acceptance, lowering output, and same-file differential evidence are
  separate gates; no one gate should be presented as completing the others.
