# nginx compatibility matrix

This matrix records behavior claims, not syntax coverage. `SUPPORTED` requires
behavioral equivalence evidence. Golden output alone is insufficient.

Allowed states are `SUPPORTED`, `PARTIAL`, `BLOCKED_BY_RUT`,
`NOT_IMPLEMENTED`, and `NOT_PLANNED`.

| nginx feature | parser | converter | RUT capability | behavior test | status |
| --- | --- | --- | --- | --- | --- |
| exact fixed-upload HEAD success boundary: root `proxy_read_timeout 1s`, default downstream keep-alive cleartext H1.1, one literal positive `Content-Length: 12` upload split 5+7, fresh single IPv4 origin, strict positive-CL 200 response, no retry/reuse | yes: the bounded root timeout and proxy model preserves the exact directive and request-independent route shape | yes: merged #475 selects request policy ID2 on `req.hasContentLength` and ID1 on absence with one identical timeout/response/failure bundle; CL0 still selects ID2 and fails closed, rather than entering this positive-length row | yes for this exact success boundary: verified JIT framing selection, positive fixed-upload HEAD admission, binary-safe complete materialization, buffering `None`, strict header-only response, retirement and request-local ownership are implemented. Zero-response timeout expiry and exactly-one-fragment incomplete-header timeout are supported only by the separate exact rows below; repeated or otherwise different progress, multi-fragment progress and broader responses remain excluded | PR #469 accepted head `9f01f9f3`, merge `f896924c`: pinned nginx 1.29.7 and converter-generated ordinary RUT both remain quiet until the 5+7 binary upload completes, emit exact Host → reconstructed Content-Length → retained headers → body upstream wire, return the normalized 187-byte header-only 200 with downstream keep-alive, and retire one origin without retry. Independent APPROVE; CI run `33952237545` passed 15/15 | SUPPORTED |
| exact fixed-upload HEAD zero-response initial deadline: root `proxy_read_timeout 1s`, default downstream keep-alive cleartext H1.1, exact HEAD with one positive `Content-Length: 12` upload split 5+7, request policy ID2, response buffering `None`, fresh single IPv4 origin, depth 0 and no retry/reuse | yes: the bounded root timeout and proxy model preserves the exact directive and request-independent route shape | yes: the exact HEAD route emits ordinary RUT `req.hasContentLength` selection, choosing ID2 for the present Content-Length and the same one-second response/failure/timeout bundle; CL0 fails closed before upstream effects and does not enter this row | yes for this exact zero-response profile: after the complete binary upload is sent, a generation-owned precise io_uring response-header timer starts from the initial response-read origin, publishes the configured header-only keep-alive 504 on silent-origin expiry, retires origin 1 without retry, and isolates a same-fd successor through a fresh origin | PR #489 accepted head `906f05c1`, merge `14a12bae`, and CI `33964724191` prove runtime timer/custody; PR #490 accepted head `1c8d0814`, merge `f54761c7`, and CI `33965711502` prove public ordinary-RUT/JIT/io_uring behavior. PR #491 accepted head `7f6df8cb`, merge `ed3354c8384e077ad5596ee5c9d244dfec9c0eff`: pinned nginx 1.29.7 and independently converter-generated ordinary RUT remain quiet until upload completion, send the exact upstream wires, return strictly equal Date-normalized 162-byte header-only keep-alive 504s near 1.001s, retire origin 1, then use the same downstream fd and a fresh origin for the exact normalized 121-byte HEAD 200 control. Independent APPROVE; CI `33967413028` passed 15/15 | SUPPORTED |
| exact fixed-upload HEAD one-fragment initial deadline: exact loopback listener, root no-URI `proxy_pass`, `proxy_read_timeout 1s`, nginx-default request buffering, default downstream keep-alive, cleartext H1.1 exact HEAD with one positive `Content-Length: 12` binary upload split 5+7, request policy ID2, generated response buffering `None`, fresh single IPv4 origin, attempt 1/depth 0 and no upstream retry/reuse or pipeline; exactly one valid 41-byte incomplete response-header fragment about 400ms after upload completion | yes: the bounded exact-listener root timeout and proxy model preserves the directive, no-URI route and request-independent shape | yes: the exact HEAD route emits ordinary RUT `req.hasContentLength` selection, chooses ID2 for the present positive Content-Length and emits the same one-second response/failure/timeout bundle with buffering `None`; unsupported shapes fail closed | yes only for this exact one-fragment profile: the generation-owned precise io_uring response-header timer accepts the single incomplete fragment without refreshing its initial deadline, publishes the exact normalized 162-byte header-only keep-alive 504, retires origin 1, and isolates a same-fd successor through a fresh origin for the exact normalized 121-byte HEAD 200 control | PR #493 accepted head `e1ccdfc4`, merge `ad9f1a1d`, CI run `33975779221` proves runtime timer/custody; PR #494 accepted head `8b351c58`, merge `b3932fed`, CI run `33976404460` proves public ordinary-RUT/JIT/io_uring behavior. PR #495 accepted head `9db3a948`, merge `e84a503e`, CI run `33978473866` (15/15) proves pinned nginx 1.29.7 and independently converter-generated ordinary RUT preserve the initial deadline across the one 41-byte fragment, return the strictly equal timeout/control wires, retire origin 1 and use a fresh origin without retry. nginx.conf is converter input; runtime consumes ordinary RUT | SUPPORTED |
| exact fixed-upload HEAD two-fragment initial deadline: the same exact loopback/root no-URI `proxy_pass`/`proxy_read_timeout 1s`/positive CL12 binary 5+7 upload/ID2/generated buffering `None`/default keep-alive/fresh IPv4 attempt-1 depth-0 profile, with exactly two valid still-incomplete application writes: 41-byte `HTTP/1.1 200 OK\r\nX-Progress: fixed-head\r\n` about +300ms and 28-byte `X-Second: still-incomplete\r\n` about +700ms from completed upload | yes: identical bounded exact-listener root timeout and proxy model; fragment schedule is origin behavior, not nginx syntax | yes: identical generated ordinary-RUT framing selector and shared one-second response/failure/timeout bundle; no schedule-specific converter branch or runtime nginx mode | yes only for this exact two-write schedule within #493's closed profile: retained cumulative incomplete-header progress keeps the generation-owned initial deadline, emits the normalized 162-byte header-only keep-alive 504 without fragment leakage, retires origin 1 without retry, and isolates the same-fd successor through a fresh origin for the normalized 121-byte HEAD 200 | PR #493 accepted head `e1ccdfc4`, merge `ad9f1a1d`, CI `33975779221` proves focused internal cross-batch timer/custody behavior. PR #498 accepted head `724ff9fe`, merge `3e890d49`, CI `33982305303` proves public ordinary-RUT/O2-JIT/config/real-io_uring behavior. PR #499 accepted head `5c08f7c1`, merge `6a01be7f`, CI `33984219762` (15/15) proves pinned nginx 1.29.7 and independently converter-generated ordinary RUT match the exact F1/F2 timing, 504/control wires, upload gate, origin retirement/no retry and fresh-origin successor. Other fragment bytes, timings or counts remain outside this claim; runtime consumes generated ordinary RUT, never nginx.conf | SUPPORTED |
| exact GET initial response-header deadline: root `proxy_read_timeout 1s`, exact cleartext H1.1 GET without request Content-Length, default downstream keep-alive, fresh single IPv4 origin, response buffering `CompleteContentLength`, depth 0 and no retry/reuse | yes: the bounded root timeout and proxy model preserves the exact directive and request-independent route shape | yes: the exact GET branch emits ordinary RUT request policy ID1 and the common one-second response/failure/timeout bundle; unsupported or forged shapes fail closed | yes for this exact profile: a generation-owned precise io_uring timer retains its initial origin across accepted incomplete response-header progress, publishes the configured keep-alive 504, retires the origin, and isolates a sequential successor on the same downstream fd | PR #486 accepted head `f9b80d2f`, merge `e4095818d5e8f50190c80c2bab09d4c85bd067c3`: pinned nginx 1.29.7 and converter-generated ordinary RUT both return the exact normalized 329-byte keep-alive 504 at the initial deadline after one incomplete header fragment, retire origin 1 without retry, then use a fresh origin on the same downstream fd for the exact normalized 121-byte CL0 200 control. CI run `33960095244` attempt 2 passed 15/15 | SUPPORTED |
| exact omitted-default-buffering bodyful positive-GET completion: exact-loopback listener, root no-URI `proxy_pass`, omitted `proxy_read_timeout`/`proxy_buffering`/`proxy_request_buffering`/`proxy_http_version`/header overrides, default downstream keep-alive, one cleartext H1.1 GET with binary positive CL12 exact 143-byte request delivered as 136 bytes then 7 bytes after 1.2s, one fresh IPv4 origin and no retry/reuse/pipeline | yes: the bounded exact-listener root proxy model preserves the no-URI upstream and all omitted directives; the exact request-independent route and nginx defaults are converter input only | yes only for this exact schedule: genuine parser/model/lowering emits owned ordinary source whose lexer/AST validates the GET `Http11FixedStrip` request policy, default `response_read_timeout: 60s`, `response_buffering: "complete_content_length"`, endpoints and response/failure policies; the public JIT CLI consumes that generated source and, after PR #512, must log exact `Backend: io_uring\n` before the request. Runtime never reads `nginx.conf`; this row claims no HIR/MIR/RIR/O2/`RouteConfig` introspection | yes for this exact complete-body vector: neither side accepts or reads at the origin before the 136+7 request completes; each then receives the exact 138-byte four-digit-port upstream request with rewritten `Host`, omitted `Connection` and the same binary body. The origin makes four exact 33-byte application publications totaling the strict 132-byte `HTTP/1.1 200 OK`/CL36/body `0123456789abcdefghijklmnopqrstuvwxyz` response, with every adjacent gap 350–650ms and W1-to-W4 greater than one second; W3 completes the header and includes the first three body bytes while downstream remains quiet, and W4 completes the body | PR #431 accepted head `46cc210a60b17b3c01701630b8e2ea789068b342`, merge `ec398dc6476a607906fee37283d11024e3460bdf`, CI `33702601281`; PR #512 accepted head `3e437a034d5003ab2e22cc2104f5cb33e041c27f`, merge `950a8ab6ddeb6e2fabedc900e9c21ce494f4400b`, independent APPROVE and CI `34007569105` (15/15 applicable). Pinned nginx 1.29.7 and independently generated ordinary RUT return the strictly equal Date-normalized 158-byte complete bodyful keep-alive response, then prove only 200ms downstream-open quiet before the client closes; there is no real-EOF or successor claim. Each side has exactly one accept/request, four publications and one active-origin retirement, a bounded 500ms no-retry interval, and exactly one `143\n` access record after client close (not a live/horizon/shutdown claim). This proves completion, not default-60s expiry or inactivity refresh; application-publication timestamps are not TCP/read/CQE boundaries. Other request/response bodies, splits, fragments, gaps, Content-Lengths, statuses or framing; incomplete EOF/reset/error; retry/reuse/pipeline/successor; TLS/H2/epoll; and broad #253/#268/#270/#271 remain excluded | SUPPORTED |
| exact default-buffered incomplete-positive-CL clean-EOF GET: exact loopback listener, root no-URI `proxy_pass`, explicit `proxy_read_timeout 1s`, omitted `proxy_buffering`/`proxy_request_buffering`/`proxy_http_version`/header overrides, default downstream keep-alive, one fresh bodyless cleartext H1.1 `GET /buffered-eof?q=1` request of exactly 56 bytes, one fresh IPv4 origin and no retry/reuse | yes: the bounded exact-listener root proxy model preserves the explicit one-second timeout, no-URI upstream, omitted buffering/request-buffering/http-version/header directives and the exact request-independent route shape | yes: the profile is semantically parsed and lowered to owned ordinary RUT; the exact GET route selects request policy ID1, `response_read_timeout: 1s` and `response_buffering: "complete_content_length"`, then the public CLI consumes only that generated source through lexer/AST, HIR/MIR/verified RIR, O2 JIT/config and real io_uring, never `nginx.conf` | yes only for this exact terminal vector after closed #502/PR #503: each origin performs one 103-byte application write containing a complete `HTTP/1.1 200 OK`/`Content-Length: 12` header and only `hello`, remains open while the proxy withholds downstream bytes for at least 400ms, and closes only after test authorization. The verified fully drained close-after-drain path publishes completion exactly once before forced close, preserves explicit-close rendezvous and never dispatches a default-persistent successor | PR #504 accepted head `58b016b224c8be74997dfc6fe2917cadf16e8b97`, merge `70ca82754bd4459839ad0e6464349b40d4d94ca0`, CI `33994351943` (15/15 applicable): pinned nginx 1.29.7 and independently converter-generated ordinary RUT match one authority-rewritten upstream request, one accept/request/application write and no retry; after authorized origin clean EOF they emit the exact equal Date-normalized 127-byte partial 200/CL12/keep-alive prefix plus real downstream EOF. Both live ledgers are exactly `56\n`, remain exact across the timeout horizon and after shutdown. The clean EOF is authorized before one second and therefore proves no response-body inactivity-expiry or refresh semantics; #253/#268/#270 remain open/`PARTIAL`, broad #271 remains `BLOCKED_BY_RUT`, and reset/chunked/close-delimited, other methods/statuses, reuse, TLS/H2/epoll are excluded | SUPPORTED |
| exact default-buffered incomplete-positive-CL single-prefix inactivity-expiry GET: exact loopback listener, root no-URI `proxy_pass`, explicit `proxy_read_timeout 1s`, omitted `proxy_buffering`/`proxy_request_buffering`/`proxy_http_version`/header overrides, default downstream keep-alive, one fresh bodyless cleartext H1.1 `GET /buffered-timeout?q=1` request of exactly 60 bytes, one fresh IPv4 origin and no retry/reuse/pipeline | yes: the same bounded exact-listener root proxy model preserves the explicit one-second timeout, no-URI upstream, omitted buffering/request-buffering/http-version/header directives and exact request-independent route shape | yes: the profile is parsed and lowered to owned ordinary RUT; the exact GET route selects request policy ID1, `response_read_timeout: 1s` and `response_buffering: "complete_content_length"`. PR #508 proves generated source through lexer/AST, HIR, MIR, verified RIR, O2 JIT/config and the public CLI with real io_uring; runtime consumes ordinary RUT and never `nginx.conf` | yes only for this exact open-origin expiry vector after closed #506/PR #507: the admitted depth-zero non-pipeline GET retains its precise generation/episode timer after one 103-byte origin application write containing a complete `HTTP/1.1 200 OK`/`Content-Length: 12` header plus only `hello`; with no further progress or EOF it withholds downstream bytes for at least 800ms, expires from the validated progress clock near one second, drops the incomplete body prefix, sends only the pinned response header and then closes downstream and origin | PR #508 accepted head `c73cc32905220a4eb32e0d80dcec448db8d69f6d`, merge `283162526d6f66a0a6c40b25d7c6a44cba703784`, CI `34001263664` (15/15): pinned nginx 1.29.7 and independently converter-generated ordinary RUT match one authority-rewritten upstream request, one accept/request/application write and no retry; both emit the strictly equal Date-normalized 122-byte header-only 200 wire without `hello`, followed by real downstream EOF and active origin close. First byte is `[750ms,1700ms)`, EOF/origin close `[750ms,2000ms)`, cross-side first-byte delta is at most 350ms, and live/horizon/shutdown ledgers are exactly `60\n`. This excludes a second/later progress fragment and refresh timing, clean EOF (the separate preceding row), other prefix/CL/status/method/framing schedules, retry/reuse/pipeline, TLS/H2/epoll and broader default buffering; #253/#268/#270 stay open/`PARTIAL` and broad #271 stays `BLOCKED_BY_RUT` | SUPPORTED |
| exact default-buffered incomplete-positive-CL second-body-progress refresh GET: the preceding exact loopback/root no-URI/explicit `proxy_read_timeout 1s`/omitted-default directives/default-keep-alive/fresh-origin profile and exact bodyless H1.1 60-byte `GET /buffered-timeout?q=1`, with W1 exactly 103 bytes (98-byte complete `200`/CL12 header plus `hello`) and W2 exactly one byte `!` about 600ms later, leaving 6/12 body bytes and an application-open stall | yes: the same bounded parser/model retains the exact directive, omitted defaults and request-independent route; W1/W2 are origin scheduling evidence, not new nginx syntax | yes only for this exact schedule: genuine parser/lowering produces ordinary source whose exact GET AST/HIR/MIR/verified RIR selects ID1, `response_read_timeout: 1s`, `CompleteContentLength` and the pinned policy bundle; owned config and O2 JIT execute through the public CLI and real io_uring, with the exact `Backend: io_uring\n` log required before the request. Runtime never reads `nginx.conf` | yes only for the closed-#506 precise depth-zero/no-stash/no-pipeline profile: independently timestamped W1/W2 publications advance the validated progress clock at W2; both sides stay downstream-open and zero-byte through at least W2+700ms and W1+1.25s, then expire from W2 around 1.001s, send no incomplete body bytes and actively retire the origin | PR #510 commits `2d0d8951`, `a6ad1c0b` and accepted head `02b5ae0f64339dde1ca1581b0c3a1b05b88b93c9`, merge `5e2b5f0491c3c29ff519760d73084223128d089b`: pinned oracle 3/3, paired direct 5/5, registered 1/1, adjacent single-prefix/clean-EOF 2/2 and focused precise-timer runtime 3/3 passed after independent `REQUEST_CHANGES` then `APPROVE`; CI `34005174611` passed 15/15. Both sides require W2-relative first byte `[750ms,1700ms)` and real EOF/active peer close `[750ms,2000ms)`, cross-side first-byte delta at most 350ms, strict equal Date-normalized 122-byte header-only 200/CL12/keep-alive wire with no `hello`, `!`, tail, 502, 504 or failure/timeout body, exactly one accept/request/two publications, one exact authority-rewritten 60-byte upstream request with no original Host/Connection, no retry, and exact `60\n` live/horizon/shutdown ledgers. Other fragment bytes/timings/counts, third/repeated progress, completion/EOF/reset/error variants, other status/method/framing, request bodies, retry/reuse/pipeline, TLS/H2/epoll and broad buffering remain excluded; #253/#268/#270 remain open/`PARTIAL` and #271 remains open/`BLOCKED_BY_RUT` | SUPPORTED |
| exact default-buffered positive-CL three-publication completion GET: the preceding exact loopback/root no-URI/explicit `proxy_read_timeout 1s`/omitted-default directives/default-keep-alive/fresh-origin profile and exact bodyless H1.1 60-byte request, with W1 exactly 103 bytes (98-byte complete 200/CL12 header plus `hello`, 5/12), W2 exactly one byte `!` about +600ms (6/12), and W3 exactly six bytes `world!` another about +600ms later, completing 12/12 | yes: the same bounded parser/model retains the explicit timeout, omitted defaults and request-independent route; W1/W2/W3 are origin application-publication evidence, not nginx syntax or TCP/read/CQE boundaries | yes only for this exact schedule: genuine parser/lowering produces ordinary source whose lexer/AST/HIR/MIR/verified RIR selects the GET ID1, `response_read_timeout: 1s`, `CompleteContentLength` and exact policy bundle; owned config and O2 JIT execute through the public CLI and real io_uring, with exact `Backend: io_uring\n` required before the request. `nginx.conf` is converter input only; runtime consumes generated ordinary RUT | yes only for this exact completion schedule: each W1→W2 and W2→W3 gap is `[550ms,750ms)`, and both sides remain downstream-open and zero-byte before W3 through W1+1.10s and W2+550ms. Completion yields the exact Date-normalized 134-byte `hello!world!` keep-alive response within W3+750ms, with paired W3-relative delta at most 350ms; only 200ms downstream-open quiet follows before client close, with no EOF/reuse/successor claim | PR #514 accepted head `7e3187cd64407969595b94a98472728e581f7c23`, merge `f2685b94e025e10e9a2184557ce519aaeca45541`: pinned nginx oracle direct 3/3 plus registered/adjacent checks and independent approval; CI `34010356587` passed 15/15. PR #515 accepted head `95267271e0489c629c4f3e2abbf0e1690bdf5efc`, merge `e2781697dac169e378591d7c2ed1c482e3c9bc0f`: paired direct 5/5, registered, oracle/adjacent, focused runtime 5/5 (1268 checks), independent approval and rebased direct pass; CI `34011913768` passed 15/15. Both sides prove one canonical authority-rewritten 60-byte upstream request, one accept/request/three publications, active origin close, 175ms no retry and exact `60\n` live/after-client-close/after-shutdown ledgers. Other bytes/timings/counts, third-still-incomplete progress, EOF/reset/error, request bodies, statuses/framing/methods, retry/reuse/pipeline, TLS/H2/epoll and broad buffering/timeout semantics remain excluded; #253/#268/#270 remain open/`PARTIAL` and #271 remains open/`BLOCKED_BY_RUT` | SUPPORTED |
| exact default-buffered positive-CL third-still-incomplete progress expiry GET: the preceding exact loopback/root no-URI/explicit `proxy_read_timeout 1s`/omitted-default directives/default-keep-alive/fresh-origin profile and exact bodyless H1.1 60-byte request, with W1 exactly 103 bytes (98-byte complete 200/CL12 header plus `hello`, 5/12), W2 exactly one byte `!` about +600ms (6/12), and W3 exactly five bytes `world` another about +600ms later, leaving 11/12 while the origin remains application-open | yes: the same bounded parser/model retains the explicit timeout, omitted defaults and request-independent route; W1/W2/W3 are origin application-publication evidence, not nginx syntax or TCP/read/CQE boundaries | yes only for this exact schedule: genuine parser/lowering produces owned ordinary source whose lexer/AST/HIR/MIR/verified RIR selects GET ID1, `response_read_timeout: 1s`, `CompleteContentLength` and the exact policy bundle; O2 JIT/owned config execute through the public CLI and real io_uring, with exact `Backend: io_uring\n` required before the request. `nginx.conf` is converter input only; runtime consumes generated ordinary RUT | yes only for the closed-#506 precise depth-zero/no-stash/no-pipeline profile and this schedule: each independently timestamped W1→W2 and W2→W3 gap is `[550ms,750ms)`; pre-W3 downstream-open/zero-byte quiet crosses W1+1.10s and W2+550ms, and post-W3 quiet crosses W3+700ms and W2+1.25s. The timer expires from W3 near one second, drops all 11 incomplete body bytes, sends only the pinned header and closes downstream and origin | PR #517 accepted head `9090d62e6c763a240e30bcf707ac54024fc8e44e`, merge `3d6f6ce09aaac37ea623f0711986582e1deff80c`, independently approved nginx-only oracle and CI `34014203391` (15/15). PR #518 accepted head `be053557585d662daea2ecb206de59d09657d33c`, merge `2aea437273afb7aa00d57ff213b2da6899b567dd`, independently approved paired differential and CI `34015624034` (15/15). Both sides emit the strictly equal Date-normalized 122-byte header-only 200/CL12/keep-alive wire without body/failure leakage, followed by real EOF and active origin close without ordering those terminal observations: first byte is W3-relative `[750ms,1700ms)`, EOF/close `[750ms,2000ms)`, with cross-side first-byte delta at most 350ms. Each proves one exact authority-rewritten 60-byte upstream request with no original Host/Connection, one accept/request, three publications, 175ms no retry and exact `60\n` live/horizon/client-close/shutdown ledgers. Other third/later/repeated progress bytes/timings/counts, completion/EOF/reset/error variants, other status/method/framing, request bodies, retry/reuse/pipeline, TLS/H2/epoll and broad buffering/timeout semantics remain excluded; #253/#268/#270 remain open/`PARTIAL` and #271 remains open/`BLOCKED_BY_RUT` | SUPPORTED |
| exact default-buffered incomplete-positive-CL `201 Created` single-prefix inactivity-expiry GET: exact loopback listener, root no-URI `proxy_pass`, explicit `proxy_read_timeout 1s`, omitted `proxy_buffering`/`proxy_request_buffering`/`proxy_http_version`/header overrides, default downstream keep-alive, one fresh bodyless cleartext H1.1 60-byte `GET /buffered-timeout?q=1`, and one exact 108-byte origin publication comprising the complete 103-byte 201/Created/CL12 header plus `hello` (5/12), followed by an application-open stall | yes: the bounded parser/model retains the exact directive, omitted defaults and request-independent route; the 201 origin publication is runtime evidence, not nginx configuration syntax | yes only for this exact schedule: genuine parser/lowering produces owned ordinary source whose lexer/AST/HIR/MIR/verified RIR selects GET ID1, `response_read_timeout: 1s`, `CompleteContentLength` and the exact policy bundle; O2 JIT/owned `RouteConfig` execute through the public CLI and real io_uring, with exact `Backend: io_uring\n` required before either request. `nginx.conf` is converter input only; runtime consumes generated ordinary RUT | yes only for this exact admitted 201 open-origin expiry vector: both sides remain downstream-open and zero-byte through at least W1+800ms, preserve the unique 201 status/reason and pinned header while dropping the incomplete five-byte body, then emit the strictly equal Date-normalized 127-byte header-only 201/CL12/keep-alive wire near W1+1s, followed by real EOF and active origin close without ordering those terminal observations | PR #520 accepted head `c3ea403efe23711f5eda77746720b79b45730050`, merge `c070d0e9d5f90b0d07cb37b703ced480f0366422`, independently approved nginx-only oracle and CI `34018162597` (15/15). PR #521 accepted head `27d6972c32178f5226e9176ee3315a00e21917d1`, merge `ca97a4dffcfb0ea3c944b6bf6af8bbd6e92e0c18`, independently approved paired differential and CI `34019254901` (15/15). Both sides prove the exact authority-rewritten 60-byte upstream request and cross-side equality, one accept/request/publication/peer-close, 175ms listener-live no retry and exact `60\n` ledgers through their independent W1+2.1s horizons, client close and frontend shutdown. The wire contains exactly one 201 status and no 200, incomplete body or failure tail. Application-publication timestamps do not establish TCP, frontend-read or CQE boundaries. Other statuses, bodies/schedules/framing/methods, retry/reuse/pipeline, TLS/H2/epoll and broad buffering/timeout semantics remain excluded; #253/#268/#270 remain open/`PARTIAL` and #271 remains open/`BLOCKED_BY_RUT` | SUPPORTED |
| exact minimal fragment: one wildcard listener, root location, literal IPv4 no-URI proxy, bounded H1 domain | yes | yes | yes for declared bounds | committed pinned `%7E` success and explicit-close gateway differentials; pinned fixed-CL POST and keep-alive evidence | SUPPORTED |
| server fragment, exactly one server | yes | yes | partial: exact single-server model only; no server selection model | bounded pinned generated-RUT GET and POST differentials | PARTIAL |
| `listen <port>` IPv4 wildcard | yes | yes | yes: one source `listen :<port>` declaration | pinned nginx/generated-RUT bind and request | SUPPORTED |
| exact explicit IPv4-wildcard spelling `listen 0.0.0.0:<port>` under the accepted single-server minimal root-proxy profile; #342 closed | yes: exact ASCII prefix plus existing decimal 1..65535 grammar, whole directive span, both declaration orders and strict address/option/overflow exclusions; `5363bc4c` | yes: retains the existing semantic port and emits byte-identical ordinary RUT `listen :<port>` to port-only spelling; full golden, structural mutation guards and AST→HIR→MIR→RIR→owned listener/config lifetime pass; `5363bc4c` | yes: ordinary RUT listener metadata and runtime bind IPv4 `INADDR_ANY`; no new primitive or nginx-specific mode | pinned oracle `e67eb8ba` proves both declaration orders independently. Four-side differential `e8b2e723` drives both exact nginx fragments through genuine parser/provenance/lowering and two independent ordinary-RUT public CLI/io_uring sides; all four sides match exact one-episode upstream Host/Connection policy, Date-normalized 200/CL2/`ok` close/zero-tail/EOF wire, strict real access logs and no retry with eight held-port handoffs, owned public load state and post-readiness source overwrite. Different-Luna review approved after two mutation-gate fixes; Sol focused 3/3 and complete nginx-labelled 56/56 pass | SUPPORTED |
| exact asterisk IPv4-wildcard spelling `listen *:<port>` under the accepted single-server minimal root-proxy profile; #343 closed | yes: exact ASCII prefix plus existing decimal 1..65535 grammar, whole directive span, both declaration orders and strict address/option/overflow exclusions; `02a3946a` | yes: retains the existing semantic port and emits byte-identical ordinary RUT `listen :<port>` to port-only and `0.0.0.0:<port>` spellings; six-way golden, structural mutation guards and AST→HIR→MIR→RIR→owned listener/config lifetime pass; `02a3946a` | yes: pinned nginx confirms IPv4-wildcard behavior and the existing ordinary RUT listener binds `INADDR_ANY`; no nginx-specific mode needed | pinned oracle `89beb2a3` proves both declaration orders independently. Four-side differential `18bfd8d3` drives both exact asterisk fragments through genuine parser/provenance/lowering and two independent ordinary-RUT public CLI/io_uring sides; all four sides match exact one-episode upstream Host/Connection policy, Date-normalized 200/CL2/`ok` close/zero-tail/EOF wire, strict real access logs and no retry with eight held-port handoffs, owned public load state and post-readiness source overwrite. Different-Luna reviews approved; Sol focused 5/5 and complete nginx-labelled 58/58 pass | SUPPORTED |
| exact loopback address spelling `listen 127.0.0.1:<port>` under the accepted single-server minimal root-proxy profile; #344 closed, unblocked by closed #345 | yes: `78ce38a2` retains generic address kind, host-order `0x7f000001`, complete endpoint borrow and exact value/directive spans for both declaration orders; only exact ASCII loopback plus historical decimal 1..65535 is added, with other addresses/options fail-closed | yes: `78ce38a2` centrally revalidates parser-produced wildcard/exact provenance and endpoint bytes, permits exact loopback only in the minimal root proxy, and emits canonical ordinary RUT `listen 127.0.0.1:<port>`; full golden, forged-model, AST/HIR/RIR separation and owned loader reuse pass | yes: generic exact-IPv4 metadata, resolver, socket/context bind and strict ordinary RUT `listen a.b.c.d:PORT` AST→HIR→owned loader propagation are complete. Public CLI/io_uring evidence `e594e568` proves same-port positional inheritance, `.1` success, retained `.2` refusal, source ownership, exact lifecycle and a wildcard mutation failing `EADDRINUSE`; no nginx-specific mode exists | pinned-nginx-only oracle `7315aae7` proves both declaration orders and address scope. Four-side differential `b7fde2b3` drives both fragments through genuine parser/provenance/lowering into two independent ordinary-RUT public CLI/io_uring sides; all four sides prove exact `.1` reachability, retained same-port non-listening `.2` guards and refusal before/after the positive request with zero extra effects, one byte-exact upstream episode/no retry, Date-normalized 200/CL2/`ok` close/EOF wire, exact access/lifecycle inventories, owned public load state and post-readiness source overwrite. Different-Luna review approved after three causal/fail-closed fixes; Sol focused listener 3/3 and complete nginx-labelled 60/60 pass. Generic access field 5 now preserves downstream request size under closed #347; nginx `$request_length` equivalence is explicitly outside this row and tracked by #362 | SUPPORTED |
| canonical assigned non-loopback numeric IPv4 `listen a.b.c.d:<port>` under the minimal root-proxy profile; #398 and #358 closed | yes in `139e9165`: canonical four-octet decimal, host-order exact address, endpoint/source/span provenance and both directive orders; `0.0.0.0` remains wildcard, while malformed, hostname, IPv6, leading-zero-octet and option forms fail closed | yes in `139e9165`: the existing minimal root/no-URI proxy profile and bounded access-log wrapper emit canonical ordinary RUT `listen a.b.c.d:<port>` with owned frontend/loader state; non-loopback prefixes, actions, redirects, hide-header and timeout remain rejected | yes: ordinary RUT already carries and binds generic exact IPv4; no RUT capability gap is identified | PR #424 (`af5c04f9`, merge `6274711b`) composes the pinned nginx 1.29.7 oracle and genuinely parsed/lowered ordinary-RUT public CLI/JIT side inside one exact-input rotation. Both bind the same assigned positive IPv4 at fixed port 41857, receive the exact 59-byte normal GET and target unavailable `127.0.0.1:9000`; the comparator requires endpoint identity, valid-Date-only normalized exact 309-byte 502 wire, ordered headers, 157-byte body, EOF, pre/post refusal evidence, nonzero process/listener identity, complete independent lifecycle and terminal cleanup. Different-Luna review approved after the first guarded run exposed and corrected a parser-version self-check literal. Exact-head standard CI `33668450721` passes 15/15; guarded direct/CTest differential `33670045263` passes with final `residual-count=0 audit-error=0`. RUT upstream attempt count and broader proxy/listener/request semantics remain explicitly outside this row | SUPPORTED |
| exact `listen 127.0.0.1:<port>` composed with the bounded exact bodyless `location = /static { return 204; }` and minimal root proxy; #348 closed | yes: combined parser/model evidence `26c08956` covers exactly listen/exact/root and listen/root/exact with common-source listener/root/action provenance, precise spans, cross-source/scalar forgeries, retained metadata lifetime and the 62-byte path plus 65535 listener/upstream boundary | yes: `df6e1a23` admits only this composition and emits canonical ordinary RUT; full-byte goldens cover both pinned orders and differ from wildcard output only in listener bytes, with isolated mutation, capacity, AST/HIR and loaded-program source-destruction/reuse evidence. Bodyful local returns, redirects, non-root proxies, multiple actions and malformed inventories remain fail-closed | yes: ordinary exact-IPv4 listener, normalized exact NoContent204 route and root forward primitives already exist independently; no new RUT capability is identified | pinned oracle `2c3b6d88` and four-side differential `8341cc0c` cover both declaration orders through two pinned nginx 1.29.7 and two genuine converter-generated ordinary-RUT public CLI/io_uring sides. Across eight unique ports all sides prove causal exact `.1` scope/retained `.2` refusal, exact 105-byte local 204/EOF with live zero upstream, one byte-exact root-forward episode/no retry, matching normalized 118-byte response/EOF, two ordered access records, canonical source equality, ownership/source overwrite and clean lifecycle. Independent review approved after isolated structural/access self-check fixes; Sol focused 3/3 and complete nginx-labelled 62/62 pass. #362 remains explicitly excluded | SUPPORTED |
| exact `listen 127.0.0.1:<port>` composed with the bounded bodyful normalized exact `return 200` action and minimal root proxy; #349 closed | yes: combined parser/model evidence `20d82363` covers exactly listen/exact/root and listen/root/exact with complete listener/root/action/body spans, common-source provenance, cross-source/scalar/body/status forgeries, retained scalar/span lifetime and maximum clean P62+B64 plus listener/upstream 65535 | yes: `5f162e47` admits only this composition and emits canonical ordinary RUT; full-byte exact/wildcard/order goldens differ only in listener bytes, with isolated structural/hook mutations, maximum capacity, binding-linked AST/HIR policy and loaded-program source-destruction/same-owner wildcard-reuse evidence. Redirects, non-root proxies, malformed/multiple actions and broader listener shapes remain fail-closed | yes: ordinary exact-IPv4 listener, normalized strict Representation200 exact route, owned bounded body and root forward primitives exist independently; no RUT gap is identified | pinned oracle `046e5cb0` and four-side differential `0bf80312` cover both declaration orders through two pinned nginx 1.29.7 and two genuinely parsed/lowered ordinary-RUT public CLI/JIT/io_uring sides. Across eight unique ports all sides prove causal exact `.1` scope/retained `.2` refusal, an exact normalized 159-byte 200/CL16/`successor-static`/EOF local response with live zero upstream, one byte-exact root-forward episode/no retry and normalized 118-byte response/EOF, two ordered strict access records, canonical source equality, source ownership/overwrite and clean lifecycle. Independent review approved; Sol focused 4/4 and complete nginx-labelled 64/64 pass. #279 and #362 remain explicitly excluded | SUPPORTED |
| exact `listen 127.0.0.1:<port>` composed with the bounded fixed absolute `return 302` action and minimal root proxy; #350 closed | yes: combined model evidence `c5b967ab` covers both declaration orders with complete listener/root/proxy/exact/return/status/target spans and represented common-source provenance, cross-source/span/scalar forgeries, lifetime boundaries and mechanical capacity projection; proxy endpoint address/nonzero port remain semantic scalars without token borrow | yes: `44808079` admits only the fully validated status-302/root-without-URI shape after complete redirect validation; status 301 is now independently admitted under open #351. Full-byte exact/wildcard/order goldens have representative lengths 5913/5904 and maximum 5921/5912, differing only in listener bytes with 25 bytes strict headroom under capacity 5946; isolated structural/hook, binding-linked AST/HIR and owned public-loader destruction/same-owner stale-policy-clearing evidence pass | yes: ordinary exact-IPv4 listener, fixed-authority 302 redirect with query discard and root forward primitives are independently supported; no RUT gap is identified | pinned oracle `bb7b8d60` and four-side differential `68d2e75a` cover both declaration orders through two pinned nginx 1.29.7 and two genuinely parsed/lowered ordinary-RUT public CLI/JIT/io_uring sides. Across eight unique ports all sides prove causal exact `.1` scope/retained `.2` refusal; three ordered Date-normalized 342-byte fixed-Location 302/CL145/full-body/EOF responses with live and settled zero upstream; two byte-exact root-forward episodes/no retry and normalized 118-byte responses; five ordered access records, canonical source equality, source ownership/overwrite and clean lifecycle. Independent review approved; Sol focused 4/4 and complete nginx-labelled 66/66 pass. Generic access field 5 preserves downstream request size under closed #347, but nginx request-length formatting remains #362, and Host/upstream endpoint/status are independently observed rather than claimed as access fields under #352; #279/#352/#362 remain explicitly excluded | SUPPORTED |
| exact `listen 127.0.0.1:<port>` composed with the bounded fixed absolute `return 301` action and minimal root proxy; #351 | yes: the existing model independently represents both declaration orders with the same bounded listener/root/redirect provenance already pinned beside #350 | yes at converter/component level in `39ccb0b4`: only the fully validated `{301,302}` exact-root composition is admitted. Both orders emit identical 5937-byte ordinary RUT versus 5928 wildcard; genuine max endpoints emit 5945 versus 5936, differ only in listener bytes and retain one NUL byte under capacity 5946. Full 301 tuple/root partition, hook mutations, AST/HIR/MIR/RIR linkage and genuine public-loader ownership/same-owner clearing pass | yes: ordinary exact-IPv4 listener, fixed 301 redirect and root forward primitives exist; the former converter-owned source-capacity/API gap was resolved by `7924bdd6` without a runtime primitive or nginx-specific mode | pinned oracle and four-side differential `9875b62e` cover both declaration orders through two pinned nginx 1.29.7 and two independently parsed/lowered ordinary-RUT public CLI/JIT/io_uring sides across eight unique ports. All sides prove causal exact `.1` scope/retained `.2` refusal; three exact Date-normalized 366-byte fixed-Location 301/CL169/full-body/close/EOF responses with Host-independent Location, query discard and live/settled zero upstream; two ordered byte-exact root-forward episodes with rebuilt Host, omitted Connection and no retry; five scoped access records, source overwrite/ownership and clean lifecycle. The shared `{301,302}` harness retains #350, and an independently generated 8080/9000 representative plus unique dynamic-endpoint normalization proves canonical 5937-byte status-301 source. Different-Luna review approved after requiring that length/full-byte gate; Sol forced self-checks, focused #350/#351 4/4 and complete nginx-labelled 75/75 pass. #279/#352/#362 and broader redirects/listeners/requests remain excluded | SUPPORTED |
| bounded nginx `log_format`/`access_log` `$request_length`, exact fresh bodyless explicit-close H1.1 GET witness; #362 closed | yes for the exact profile in `e30d7cc4`: a distinct bounded http wrapper preserves the bare-server API and accepts fixed-order one exact format declaration, one clean absolute path binding and one existing server with absolute common-source spans; 255/256, provenance/lifetime and adjacent logging/root/server/location mutations fail closed | yes in independently approved `b917e78d`: a separate owned 6275-byte HttpProfile output reparses and completely compares the common-source model without dereferencing supplied child borrows, validates the shared sink grammar, emits one canonical ordinary-RUT live downstream-request-bytes access declaration before byte-identical existing Server lowering, and proves exact maximum capacity/NUL, both orders, forgery/mutation rejection and public-loader ownership. Existing Server API/output remain unchanged | yes: closed #347 preserves downstream request size distinctly from rebuilt upstream size; closed #363/#364/#365/#366 provide the generic exact decimal-newline formatter, source-owned regular-file sink metadata/opening and reliable live publication with public success/fatal evidence | pinned oracle `552181f0` and converter-generated differential `2ffd13de` run isolated pinned nginx 1.29.7 and genuine parsed/lowered ordinary-RUT public CLI sides. Both prove the same exact downstream102 request, distinct exact rebuilt-upstream66 wire, one accept/request/send with no retry, Date-normalized118 response/EOF, exact prefix-only then live-stable/final `102\n`, owner destruction and clean lifecycle. Different-Luna review approved after exact five-line runtime-log hardening; Sol focused 2/2 and complete required nginx 85/85 in 426.23s pass. Positive-body behavior is separately #367; other variables/formats/destinations/options, incomplete/aborted requests, reuse and broader protocols remain excluded | SUPPORTED |
| same bounded `$request_length` profile, exact fresh explicit-close H1.1 POST with one complete literal `Content-Length: 12` body delivered by one application send; #367 closed | yes: the configuration uses the unchanged exact HttpProfile parser/model from `e30d7cc4`; this row adds no syntax | yes: unchanged `b917e78d` emits genuine ordinary RUT with HEAD/GET/Any; POST selects Any, retains downstream135 and rebuilds exact upstream99. Final tests-only composition is `9f353d15` | yes after #368 runtime fix `fcbeb980`: strict one-shot and streamed success establish exact io_uring recv retirement before fd detach, so the origin observes FIN while RUT remains live without adding `Connection` or changing the exact request wire. Retry/target, episode/quarantine, retired-F_MORE and unowned-armed mutation coverage is independently approved | pinned oracle `46edceb5` and final converter-generated public-process differential `9f353d15` both prove exact downstream header123/body12/total135, rebuilt upstream header87/body12/total99 with body once and no Connection, normalized118 response/EOF, one accept/request/send/no retry, prompt live origin peer close/no late bytes and prefix-only/live-stable/final `135\n`. The RUT side is only `parse_http_profile`→`lower_to_rut(HttpProfile)`→owned ordinary source→public CLI with exact five-line lifecycle. Sol Network2/2 (71 checks), Integration4/4 (228 checks), #362/#367 differentials2/2 and complete nginx-labelled87/87 in430.59s pass. TCP recv boundaries, split application delivery (#369), incomplete/aborted bodies, other lengths/framing and reuse remain excluded | SUPPORTED |
| same exact #367 POST/CL12 request delivered by two bounded application sends: header123 + body prefix `hello` (first total128), then delayed suffix `-body12` (7); #369 closed | yes: unchanged exact HttpProfile parser/model; no new syntax | yes: the same genuine HEAD/GET/Any ordinary RUT is produced only through parse/lower; POST selects Any with request policy1 and response/failure policy2. Final tests-only public composition is `a11c8ea8` | yes: after exact128 the fixed-CL policy saves an equivalent direct Forward and returns before response allocation, Proxying/slot/attempt/idle/socket/connect/send/access; suffix7 accounts once to downstream135 without handler re-entry, rebuilds99 while retaining135, then reaches #368 strict retirement and SourceLive | pinned `5ef506b6` and final generated differential `a11c8ea8` both prove exact128, measured >=500ms zero origin/access/client effects with a mandatory post-poll final snapshot, exact7 completion, downstream135→upstream99, normalized118 response/EOF, one/no-retry origin, prompt live peer close/no late bytes and live/final `135\n`. Generated source is genuinely parsed/lowered/persisted with source accessLog, owner destruction, public CLI and exact lifecycle. Independent final review approved after removing the final quiet-window escape and live vector race; Sol focused3/3 and complete nginx-labelled89/89 in434.61s pass. TCP/read/CQE boundaries, other splits, fragmented headers (#370), incomplete/aborted bodies, other framing/length and reuse remain excluded | SUPPORTED |
| same exact #362 bodyless GET delivered by two bounded application sends across one incomplete-header split inside Host: exact45, delay >=500ms, exact57; #370 closed | yes: unchanged exact HttpProfile parser/model; no new syntax | yes in tests-only `558aae9b`: the unchanged lowering is exercised only through `parse_http_profile()` -> `lower_to_rut(HttpProfile)` -> owned/persisted ordinary source -> public CLI/JIT/io_uring. The retained #362 branch remains one exact102 send without source poison; split mode survives post-readiness source poison, and all Recorder request/history reads occur only after join | yes after closed #371: `b170efee` retains/rearms initial `Incomplete` headers before request effects; `d735eb75` preserves one-positive-per-token CQ order plus selected-buffer/terminal/cancel/new-owner isolation; generic public-process `8d1085e6` proves the hand-authored ordinary-RUT exact45/500ms/57 capability with downstream102/upstream66/response116/EOF/access102, prompt FIN and clean lifecycle. Sol raw13/13, Network1074/1074 and Integration434/434 pass | pinned `27140133` and converter-generated `558aae9b` both prove the unchanged downstream102 request split at `client.exam|ple.with.a.long.name`: mandatory final-snapshot-bound >=500ms quiet with zero origin/access/client effects, exact57 completion, rebuilt upstream66, normalized118 response/EOF, one/no-retry origin, prompt live FIN/no late bytes, live/final `102\n`, owned public source and exact five-line lifecycle. Independent design/implementation reviews approve; Sol focused5/5 and complete nginx-labelled91/91 in438.94s pass. TCP/read/CQE boundaries, arbitrary/multiple splits, incomplete/aborted terminal requests, other framing and reuse remain excluded | SUPPORTED |
| nginx access-log variables for downstream Host and actual upstream endpoint/status history (`$http_host`, `$upstream_addr`, `$upstream_status`); #352 | no: `access_log`/`log_format` syntax is outside the current parser | no | no: current generic access entry/text has no bounded downstream Host or actual upstream endpoint/status history; independent wire/backend observations are not access-field support | no converter claim; #350 documents the omission and proves routing behavior independently | BLOCKED_BY_RUT |
| exact `listen 127.0.0.1:<port>` composed with bounded clean proxy prefix P length 3..63 and explicit proxy URI exactly `/`; #353 closed | yes: combined model evidence `c2002750` covers both declaration orders with complete listener/location/proxy/URI spans and common-source provenance, cross-source/scalar/span/timeout/directive forgeries, safe lifetime boundary and P63/max ports/IP capacity projection; valid non-`/` and true no-URI forms remain distinct #354/#355 | yes: `8f5e5af9` admits only the validated `PrefixWithUri` profile whose explicit proxy URI is byte-exact `/` and has no exact action. Both orders have canonical 3358-byte output versus 3349 wildcard; P63/max ports/IP is 3582 versus 3573 with 2364 bytes strict headroom under capacity 5946. Structural, binding-linked AST/HIR and public-loader ownership/reuse evidence pass; non-`/`, configured-query and true no-URI forms remain fail-closed | yes: ordinary exact-IPv4 listener, prefix route, query-preserving automatic slash redirect, strip/replace-to-root transform and fixed forward primitives exist independently; no RUT gap is identified | pinned oracle `75e49edb`, representative four-side differential `0113b765` and exact-address P63 differential `c61d8dae` cover both declaration orders and the full P3..63 boundary. Representative evidence uses two pinned nginx 1.29.7 and two independently parsed/lowered ordinary-RUT public CLI/JIT/io_uring sides across eight unique ports; P63 adds one isolated pinned/generated exact-address pair reusing the closed #335 five-vector semantics. All prove causal exact `.1` scope/retained `.2` refusal; exact Date-normalized wires/EOF; transformed upstream histories with rebuilt Host/omitted Connection/no retry; query-preserving automatic 301s with zero extra upstream; scoped access, source ownership and clean lifecycle. Reachable loopback P63 is canonical 3576 bytes and genuine max-IP/max-port lowering is 3582 by one unique six-byte address expansion. Independent review and Sol focused 5/5 plus complete nginx-labelled 69/69 pass. #352/#362 access fields and #354/#355 semantics remain excluded | SUPPORTED |
| exact `listen 127.0.0.1:<port>` composed with the fixed representative non-`/` replacement `/api/`→`/v1/`; #354 closed | yes: combined model evidence `9fa52e16` covers both directive orders with complete listener/location/path/proxy/URI spans and fail-fast common-source provenance, isolated cross-source/span/scalar/timeout/action/directive/delimiter forgeries, safe copied-metadata lifetime boundary and configured-query/#355 separation | yes: `e07fb776` admits only fully validated exact-loopback + fixed `/api/`→`/v1/` with no exact action. Both orders emit canonical 3345-byte ordinary RUT versus 3336 wildcard; fixed-shape max endpoints are 3353/3344 with 2593 bytes strict headroom under capacity 5946. Independent policy/hook mutations, binding-linked AST/HIR and public-loader response/failure/redirect/transform ownership plus same-owner stale clearing pass. Other P/U, query, actions, P63/U128 and #355 remain fail-closed | yes: ordinary exact-IPv4 listener, fixed `/api/` prefix selection, query-preserving automatic slash redirect, `/api/`→`/v1/` strip/replace transform and fixed forward primitives exist independently; no RUT gap is identified | pinned oracle `dad39cb7` and four-side differential `cd902378` cover both directive orders through two pinned nginx 1.29.7 and two independently parsed/lowered ordinary-RUT public CLI/JIT/io_uring sides across eight unique ports. All sides prove causal exact `.1` scope/retained `.2` refusal; exact Date-normalized five-vector wires/EOF; transformed `/v1/`, `/v1/x`, `/v1/x?y=1` upstream histories with rebuilt Host/omitted Connection/no retry; query-preserving automatic `/api/` 301s with zero extra upstream; five scoped access records, canonical source/public policy ownership and clean lifecycle. Independent review and Sol focused 6/6 plus complete nginx-labelled 71/71 pass. #352/#362 access fields and all other P/U/query/P63/U128/#355 semantics remain excluded | SUPPORTED |
| non-root `proxy_pass` without a URI, including fixed exact-listener `/api/` composition; #355 closed | yes: `366e8263` admits the existing bounded clean P3..63 non-root path grammar and retains `ProxyPass.has_uri == false`, null/zero URI and default URI span as distinct from explicit `/`. `PrefixWithoutUri` proves both exact-loopback orders, P3/P63, complete source/span/position/delimiter/endpoint/path invariants, cross-source/URI-state/timeout/action forgeries and safe copied metadata; a pre-read forged-span ordering defect found by Sol was fixed and independently re-approved | yes only for the fixed exact-loopback `/api/` representative: `fd429532` emits the ordinary `/api` prefix/automatic-redirect route and no-transform forward. Both orders are canonical 3244 bytes; max endpoints are 3252 with 2694 bytes strict headroom under capacity 5946. Source structure/policy/hook mutations, binding-linked AST/HIR/MIR/RIR zero-transform evidence and public loader ownership/reuse pass. Wildcard and every other P3..63 no-URI prefix remain explicitly unsupported; explicit `/` and `/v1/` outputs are unchanged | yes for the selected clean domain: ordinary exact listener, `/api` prefix selection, query-preserving automatic slash redirect and no-transform `Http11FixedStrip` forwarding preserve the untouched raw target; no new RUT primitive or nginx-specific mode is required | pinned oracle `60ffca04` and four-side differential `68f07984` cover both orders through two pinned nginx 1.29.7 and two independently parsed/lowered ordinary-RUT public CLI/JIT/io_uring sides across eight unique ports. All prove causal exact `.1` scope/retained `.2` refusal; exact five-vector wires/EOF; unchanged `/api/`, `/api/x`, `/api/x?y=1` upstream histories with rebuilt Host/omitted Connection/no retry; query-preserving automatic 301s with zero extra upstream; scoped access, zero transforms through source/AST/HIR/MIR/RIR/config, binding-linked policy ownership and clean lifecycle. Independent review, Sol focused 6/6 and complete nginx-labelled 73/73 pass. Normalization/rewrite-sensitive targets, wildcard/other prefixes/P63, configured query and #352/#362 access fields remain excluded | SUPPORTED |
| complete clean trailing-slash P3..63 prefix class for exact-loopback `proxy_pass` without a URI; #356 closed | yes: the existing parser/model accepts `P := "/" SEG ("/" SEG)* "/"` over non-dot ASCII unreserved segments, retains complete path/source/span provenance and distinct `has_uri=false`/null URI/default URI-span state; P3/P63 and excluded-shape evidence already exists under #355 | yes in `f9c5dfe0`: after the complete existing provenance/path/endpoint/timeout/action validators, only the fixed `/api/` literal gate is removed. Both orders and P3/P63 emit ordinary no-transform RUT with exact formula `3244 + 3*(len(P)-5)`; genuine P63/max endpoints are 3426 bytes under capacity 5946 with 2520 bytes strict headroom. Full-byte mechanical relation to retained #355, complete policy/binding mutations, AST/HIR/MIR/RIR zero transforms and public-loader ownership/same-owner clearing pass. Wildcard no-URI and every excluded profile remain fail-closed | yes: ordinary exact listener, prefix/slash-redirect selectors up to the declared bounds, query-preserving automatic 301 and no-transform forward already exist; no RUT capability gap was found | pinned oracle `3cdbc58c`, representative four-side differential `32a2afd5` and genuine P63 boundary differential `5e0c6012` cover both `/service/` declaration orders and the complete P3..63 boundary. Representative evidence uses two pinned nginx 1.29.7 and two independently parsed/lowered ordinary-RUT public CLI/JIT/io_uring sides across eight unique ports; P63 adds one isolated pinned/generated exact-address pair. All prove causal exact `.1` scope/retained `.2` refusal; exact Date-normalized wires/EOF; unchanged P/Px/Px?query upstream targets with rebuilt Host, omitted Connection and no retry; query-preserving automatic 301s with zero extra upstream; scoped access, zero transforms, source ownership and clean lifecycle. P63 genuine max endpoints emit 3426 bytes with NUL reserve and 2520 bytes headroom; P64 remains fail-closed. Different-Luna review approved after requiring direct profile-structural mutation rejection. Sol focused P63 2/2 and complete nginx-labelled 78/78 in 401.55s pass. Wildcard no-URI, configured URI/query, normalization-sensitive/absolute-form targets, broader requests/protocols and #352/#362 remain excluded | SUPPORTED |
| complete clean trailing-slash P3..63 prefix class for wildcard IPv4 `proxy_pass` without a URI; #357 closed | yes: the same existing `PrefixWithoutUri` parser/model retains P3..63 path/source/span provenance and true URI absence for the accepted wildcard listen spellings | yes in `3a719cc1`: the sole post-validation wildcard no-URI rejection is removed. Port-only, `0.0.0.0:` and `*:` × both orders emit byte-identical ordinary `listen :PORT` no-transform RUT across P3/P63/max. Genuine exact siblings differ by one unique nine-byte listener-address deletion; lengths are P3 3229, `/api/` 3235, `/service/` 3247, P63 3409 and max 3417 under capacity 5946, leaving 2528 additional payload bytes with NUL reserve. Complete source/policy/binding mutations, AST/HIR/MIR/RIR/config zero transforms and public-loader teardown/same-owner clearing pass; excluded forms remain fail-closed | yes: ordinary wildcard IPv4 listener, prefix/slash-redirect selectors, query-preserving automatic 301 and no-transform forward already exist; no RUT capability gap is identified | pinned oracle `b9e91499`, representative four-side differential `1c78da34` and genuine P63 boundary `b90523e0` cover both port-only wildcard `/service/` declaration orders and the complete P3..63 boundary. Representative evidence uses two pinned nginx 1.29.7 and two independently parsed/lowered ordinary-RUT public CLI/O2-JIT/io_uring sides across eight simultaneously held-and-handed-off ports; P63 adds one isolated pinned/generated wildcard pair with four causal ports. All prove exact Date-normalized wires/EOF; unchanged P/Px/Px?query upstream targets with rebuilt Host, omitted Connection, duplicate headers and no retry; query-preserving 301s with live/settled zero upstream; side-specific ordered access, zero transforms, RIR-derived owned policies after JIT/RIR/source teardown and clean lifecycle. Six P63 spelling/order inputs emit identical ordinary RUT. Baseline is 3409 bytes; max endpoints emit 3417 under capacity 5946 with delta 2529, 2528 payload headroom and NUL. P64 remains fail-closed only as the converter boundary. Independent reviews approved; Sol focused 4/4 and complete nginx-labelled 81/81 in 414.66s pass. Configured URI/query, normalization-sensitive/absolute targets, broader requests/protocols and #352/#362 remain excluded | SUPPORTED |
| ordinary prefix `location /` | yes | yes | partial: root catch-all exists | bounded pinned generated-RUT GET differential | PARTIAL |
| location applies to every method | yes | yes | yes: method-omitted route source form | RUT route tests; pinned GET and POST differentials | PARTIAL |
| `OPTIONS *` under the accepted minimal root proxy config | implicit | yes; emits exact OPTIONS 400 plus bounded ANY fail-closed 400 through ordinary RUT `unmatched` declarations | yes for the bounded strict H1.1 exact-method local-response slice; invalid/partial fails closed and configured H2 misses close; #273 closed | converter golden and source-to-RouteConfig ownership/mapping tests; pinned nginx 1.29.7 exact-close 400/157-byte-body/EOF, matching ordinary-RUT source/JIT/io_uring wire, and converter-generated nginx-vs-RUT differential, all with zero upstream effects | SUPPORTED |
| authority-form CONNECT under the accepted minimal root proxy config | implicit | yes; emits exact CONNECT 405 plus bounded ANY fail-closed 400 through ordinary RUT `unmatched` declarations | yes for the bounded strict H1.1 exact-method local-response slice; invalid/partial fails closed and configured H2 misses close; #273 closed | converter golden and source-to-RouteConfig ownership/mapping tests; pinned nginx 1.29.7 exact-close 405/157-byte-body/EOF, matching ordinary-RUT source/JIT/io_uring wire, and converter-generated nginx-vs-RUT differential, all with zero upstream effects | SUPPORTED |
| fixed IPv4 HTTP `proxy_pass`, no URI suffix | yes | yes | partial: fixed `forward` plus bounded policies | pinned generated-RUT GET/query/header and fixed-CL POST differentials | PARTIAL |
| preserve raw request-target and query | implicit | yes | partial: origin-form forward sends original bytes | pinned query differential; broader normalization untested | PARTIAL |
| preserve request method and body | implicit | yes | partial: fixed-CL body is staged before connect within one 16 KiB composite slice | RUT exact binary/segmented/boundary tests and pinned generated-RUT binary POST differential | PARTIAL |
| nginx default upstream HTTP version and request headers | implicit | yes | partial: explicit fixed HTTP/1.1 policy with bounded fixed-CL buffering; #252 | exact RUT tests plus pinned generated-RUT GET and POST differentials | PARTIAL |
| proxied response status and body | implicit | yes, including method-isolated root HEAD lowering | partial: strict final H1.1 exact-Content-Length streaming plus bounded reusable paired HEAD success/connect-failure serialization; #265 admits malformed/incomplete parser failures through D1/D2; #267 has bounded configured-timeout Recv-only execution and a public ordinary-RUT wire but no converter differential; #268 has internal bounded positive-CL GET response streaming and one fragmented-progress public wire; #266 tracks nginx HTTP/0.9 fallback | exact converter-generated valid-status/invalid-header and incomplete-header/EOF reusable differentials; public RUT invalid-prefix/open-peer, incomplete/EOF, timeout, and positive-CL fragmented-progress wires; reusable success/connect-failure differentials; internal parser/lifecycle and generated-RUT GET/POST evidence | PARTIAL |
| nginx default proxied response header policy | implicit | yes, including method-isolated root HEAD lowering | partial: bounded strict H1.1 final-response/content-length serializer and reusable paired HEAD success/connect-failure policy; #265 has narrow malformed/incomplete production admission; #267 can execute a configured timeout response only in the exact io_uring Recv-only paired-HEAD domain and otherwise fails closed; #268 tracks timing; #266 tracks invalid-status fallback | exact converter-generated valid-status/invalid-header and incomplete-header/EOF reusable differentials; public RUT parser-failure and configured-timeout wires; reusable success/failure/no-pool differentials; generated-RUT GET/POST/close evidence | PARTIAL |
| exactly one literal `proxy_hide_header X-Compat-Hidden` on the minimal exact-loopback root proxy, fresh bodyless explicit-close cleartext H1.1 GET; #373 | yes in `bede03a2`: one exact unquoted borrowed literal with directive/name spans is modeled compositionally across current proxy profiles and both hide/timeout orders. Complete arity/context/default inventory, common-source/span/line-column, null/address-1/overflow, cross-source, source-shell, adjacency and HttpProfile comparison evidence passes. Any hide inventory takes the dedicated validated branch; fully absent inventory retains legacy diagnostics/output | yes in `738b493e` for the exact validated profile: fixed ordinary-RUT `X-Compat-Hidden` is appended only to root HEAD/GET/Any upstream-success `hide_headers`. Four orders equal an independent 5366-byte golden; max Server5374/HttpProfile5703, AST/HIR/MIR/RIR/config ownership, case-insensitive matching and same-owner 84→54 clearing pass. Timeout/wildcard/prefix/URI/action/forged compositions remain explicit fail-closed; all no-hide outputs stay byte-identical | yes: existing generic response-policy `hide_headers` supports eight case-insensitive owned names; the generated config uses four and introduces no nginx-specific runtime mode. Broader default/control-header behavior remains #253 | pinned nginx `97f15d8f`, source/model `a3ef5f60`, live side `987373db`, generated pair `a9c5e434`, and final four-side `6996ed93` are independently reviewed. Two nginx 1.29.7 and two genuine generated ordinary-RUT sides use eight held P4 ports/four isolated resources and match exact85→Host-normalized66/219→Date-normalized176 wires/EOF, hidden removal, visible+cookie order, one/no-retry, exactly one prompt FIN/no late bytes, poison ownership, and side-specific access/lifecycle. Sol focused7/7 and complete nginx-labelled98/98 pass | SUPPORTED |
| nginx default proxied response buffering/commit | implicit | no broad lowering; #271 remains open. Bounded generated profiles deliberately choose explicit RUT buffering modes and do not claim a general translation of nginx's omitted/default buffering | partial: ordinary RUT supports bounded complete-Content-Length buffering for selected cleartext H1.1 profiles and buffering `None` for the narrowly admitted positive fixed-upload HEAD profile. Closed #506 adds precise body inactivity timing only to the exact depth-zero non-pipeline bodyless GET profile. Broader bodies/framing/status/size, transforms, protocols, transports, reuse/retry, mutations and deeper pipelines remain excluded | deterministic ownership/race/retirement evidence and bounded differentials exist for selected GET/POST/PUT/PATCH/OPTIONS profiles. PR #450/#451 prove fixed-upload HEAD runtime and ordinary-source behavior; PR #469 proves its exact fixed-CL12 success boundary. PR #491/#495/#499 prove only the separate zero-response, exactly-one-incomplete-fragment and exact two-incomplete-fragment header-suppressing HEAD timeout boundaries with generated RUT buffering `None`. PR #504 proves one exact default-buffered incomplete-CL clean EOF, PR #507/#508 prove one exact single-prefix open-origin inactivity expiry, PR #510 proves the adjacent exact W2=`!` at about +600ms refresh schedule, PR #514/#515 prove only its exact W3=`world!` completion sibling, and PR #517/#518 prove only its exact W3=`world` still-incomplete expiry sibling. PR #431/#512 separately prove one exact omitted-default-buffering complete CL36 body in four 33-byte publications with generated `CompleteContentLength`; other bodies, other third/later/repeated progress, and general bodyful/default-buffering commit equivalence remain unproven | BLOCKED_BY_RUT |
| nginx default buffering for bounded root bodyless origin-form GET, cleartext H1.1, sequential downstream requests only, no CL/TE/Expect/Upgrade, strict 200 with one positive bounded CL | implicit | yes; exact GET lowering restored in `caa9fce3`, with HEAD/ANY and `/api/` isolated | yes for this exact sequential RUT domain: initial same-batch complete+EOF settlement, validated SocketCreate/ConnectSubmit/async ConnectCompletion 502, canonical sequential response neutrality, and the ordinary unavailable-upstream wire are implemented. #276 closes one late-arrival 502-to-ordinary-local pipeline slice, but this row remains explicitly sequential; #277 and broader pipelining are excluded, while the separate exact #278 late-successor row is declared below | golden plus AST/HIR/MIR/RIR/RouteConfig ownership passes. Converter-generated pinned-nginx differentials prove complete fixed-CL response plus immediate origin EOF, exact downstream response/EOF and live no-retry settle, and strictly sequential same-downstream unavailable-upstream 502 keep-alive then 502 close/EOF. Deterministic and ordinary-RUT production evidence proves the corresponding settlement and two failure/health episodes | SUPPORTED |
| bounded late-arrival strict successor: same root bodyless origin-form GET route, cleartext H1.1, request 1 default keep-alive and unavailable single IPv4 upstream, complete request 2 arrives after request 1 failure is staged but before its first response byte, depth exactly 1, request 2 has one normalized `Connection: close` | implicit in the accepted exact-root fragment | yes; existing exact GET lowering generates ordinary RUT with FixedStrip request policy, request-bound nginx-shaped 502/504 policies, 60s read deadline, and complete-content-length buffering | yes for this exact generation-bound fresh-connect domain; #278 closed. Both live and terminal-consumed downstream Recv ownership are covered; retry/reuse, mutations, bodies, other methods, protocols, transports, targets, and greater depth remain excluded | deterministic normal/ASan ownership and forgery matrices; ordinary-source raw-CQ production wire with two independent failure episodes and exact accounting; converter-generated pinned-nginx causal-gate differential proves byte-identical 502 keep-alive then 502 close and real EOF, with exactly two scoped upstream attempts on each side | SUPPORTED |
| bounded pre-admission coalesced successor: one application `send(R1\|\|R2)` where R1 is a root bodyless origin-form GET with default keep-alive and an unavailable fresh single-IPv4 upstream, and R2 is an exact `/static` bodyless GET with normalized `Connection: close`, cleartext H1.1, depth 0→1 | implicit in the accepted root-proxy plus exact-local-return fragment | yes; the fragment lowers to an ordinary root forward plus method-omitted exact strict-local route | yes for this exact request-bound, no-body/no-CL/TE, no-retry/reuse/mutation domain; #277 closed. Other methods, bodies, fragmentation patterns, protocols, transports, targets, and greater depth remain excluded | deterministic ownership/exclusion matrices and ordinary-source production wire; converter-generated pinned-nginx E1/E2/E3 causal gates prove the identical complete wire exists before nginx parsing and RUT dispatch, then Date-only-equal 502 keep-alive → exact-local 200 close/EOF, zero tail, and exactly one scoped upstream attempt per side | SUPPORTED |
| `proxy_read_timeout` response-read inactivity timing | yes for explicit raw root-location `1s..63s`; all excluded forms/contexts reject explicitly | yes for the accepted root proxy lowering: preserves explicit `1s..63s` on generated HEAD/GET/method-omitted-ANY forward bundles and accepted exact-redirect GET fallback, with owned policy metadata and fail-closed unsupported/forged models; no broad behavioral promotion | partial: ordinary RUT carries exact 1..63s metadata. The precise io_uring timer covers the bounded bodyless `HeaderOnlyHead` profiles using generated buffering `None`, a fresh single IPv4 upstream, one episode and no retry: PR #460/#462 prove explicit downstream close and PR #465/#467 prove default downstream keep-alive; PR #463 preserves configured timeout failure after accepted incomplete headers. PR #484/#485/#486 additionally prove the exact cleartext H1.1 GET profile with absent request Content-Length, response buffering `CompleteContentLength`, default downstream keep-alive and the initial response-header deadline across incomplete progress. PR #489/#490/#491 prove the separate exact ID2 positive fixed-upload HEAD silent-origin deadline after upload completion; PR #493/#494/#495 prove that profile's exact single valid 41-byte incomplete-header fragment, and PR #493/#498/#499 prove the separate exact 41-byte-at-+300ms plus 28-byte-at-+700ms two-fragment schedule while retaining the initial deadline. PR #507 closes #506 for the admitted depth-zero non-pipeline GET `CompleteContentLength` precise body clock, PR #508 proves one exact single-prefix body inactivity expiry, PR #510 proves exactly one later W2=`!` at about +600ms refresh schedule, and PR #517/#518 prove exactly one W3=`world` still-incomplete expiry about +600ms later. Literal request CL0, other third/later/repeated body progress, other fragment contents/timings/counts, chunked/close-delimited responses, broader methods/bodies, upstream retry/reuse, pipeline expansion, wider nginx default bodyful buffering, TLS/H2/epoll and wider lifecycles remain open under #268/#271 | PR #462 proves pinned-nginx phase timing for explicit-close bodyless HEAD; PR #467 proves the default-keep-alive two-request bodyless HEAD timeout and successor boundary; PR #486 proves the exact absent-request-Content-Length GET initial-deadline and same-downstream fresh-origin successor boundary; PR #469 proves the positive fixed-CL12 HEAD success boundary; PR #491 proves the matching zero-origin-response initial deadline, PR #493/#494/#495 prove its exact one-fragment initial deadline, and PR #493/#498/#499 prove its exact two-fragment initial deadline plus same-fd fresh-origin control. PR #508 proves the separate exact default-buffered header-plus-`hello` open-origin expiry, PR #510 proves the adjacent exact W2 refresh schedule, PR #517/#518 prove the exact W3 still-incomplete refresh/expiry schedule with generated `CompleteContentLength`, and the clean-EOF disposition is PR #504. Pinned nginx omits `proxy_buffering`; generated HEAD uses buffering `None`, while these exact GET profiles use `CompleteContentLength`. #253/#268/#270/#271 remain open and the broad directive stays `PARTIAL` | PARTIAL |
| single unavailable upstream gateway error | implicit | yes | yes for bounded H1 single-IPv4 connect failures; #256 | committed pinned close/EOF differential plus pinned keep-alive and split-POST evidence | SUPPORTED |
| one exact literal `location = /static` returning status 200 with a 1..64-byte token-safe quoted ASCII body beside the accepted root proxy; bounded cleartext H1.1 bodyless origin-form GET, fresh explicit-close HEAD, and fresh depth-zero header-absent explicit-close POST | yes for this exact two-location model; #286 closed | yes: emits a method-omitted ordinary RUT exact strict-local route beside the unchanged root fallback | yes for the declared literal exact strict-local GET/HEAD/header-absent POST domain; #287 and #288 closed. POST explicitly excludes CL including CL0, Transfer-Encoding/TE, Expect, Upgrade, body/tail, and pipeline; #281/#284 remain open | golden plus AST/HIR/MIR/RIR/RouteConfig ownership; converter-generated explicit-close GET, HEAD, and header-absent POST `/static` pinned-nginx differential proves Date-only-equal ordered wires, exact body or HEAD suppression, close/EOF, and zero upstream effects; causal one-send root GET → exact `/static` GET differential proves 502 keep-alive → 200 close/EOF, zero tail, and exactly one upstream attempt per side | SUPPORTED |
| the same bounded exact local return with body `"hello world"`, containing exactly one non-edge ASCII space, fresh bodyless explicit-close H1.1 GET only | yes: contextual raw quote scan only after accepted `return 200`; exact borrowed interior/body span accepts old safe grammar plus one non-edge SP and rejects all broader quoting; `3946c555` | yes: quote-delimiter/common-source validation then unchanged ordinary-RUT byte-literal emission; dual-order full golden, legacy isolation, exact 5664/5681 capacity and post-lifetime owned RouteConfig pass; `32a13f91` | yes: ordinary RUT `b"hello world"` compiles, owns and serves the exact wire through the public CLI/io_uring; no RUT capability issue is needed | independently approved pinned oracle `c70c8c32` and converter-generated public-CLI/io_uring differential `9ffd4c4f` prove both real declaration orders and four isolated nginx/RUT sides: exact Date-normalized 154-byte 200/CL11/`hello world`/close/EOF wire with no newline/tail/Location, live and settled zero upstream, and one raw access record. Sol focused 5/5 and complete nginx 34/34 pass | SUPPORTED |
| the same bounded exact local return with any number of non-edge internal ASCII spaces within the 1..64-byte safe body grammar, fresh bodyless explicit-close H1.1 GET only; #322 closed | yes: contextual raw/no-decode scanner and borrowed model accept zero or more internal SP while retaining non-space endpoints and all old exclusions; `466200bf` | yes: provenance-first quote validation then unchanged ordinary-RUT byte-literal emission; dual-body/order full golden, independent legacy evidence, dense 5664/5681 capacity, forgeries and owned RouteConfig lifetime pass; `458a6c86` | yes: ordinary public RUT compiles and serves both consecutive and separated multi-space byte literals exactly through the real io_uring CLI; no RUT capability issue is needed | approved oracle `187e2ff8` and converter-generated public-CLI/io_uring differential `fdeb6ae9` prove both bodies and declaration orders across eight isolated nginx/RUT sides: exact pairwise Date-normalized CL12/CL17 wires, close/EOF, strong zero upstream and scoped access/log evidence. Sol final repeated/focused gates and complete nginx 36/36 pass | SUPPORTED |
| exact bodyless `location = /static { return 204; }` beside the accepted root proxy, fresh bodyless explicit-close H1.1 literal `GET /static` only; #324 closed | yes for the literal bounded action: distinct borrowed `ExactNoContentReturnLocation`, exact `/static`, literal status 204 with complete spans/provenance and explicit exclusions; `a0427b82` | yes: provenance-first complete lexical-shell validation emits one canonical ordinary public `route exact slash_normalized GET "/static"` with the complete strict no-content tuple; dual-order/format full goldens, exact 5556/5564 capacity, forgeries and source-destruction owned RouteConfig pass; `184dbb25`, `90a62541` | yes for the bounded target: closed #323 stages through public activation and real CLI/io_uring proof (`935255f6`, `920a4747`, `c858ee44`, `4b6a03a7`, `d3abaa6b`, `fad6be75`) preserve the exact 105-byte no-content profile, ordinary-204 CL0 distinction, zero upstream and selector/fallback isolation | approved oracle `97ed1750` and converter-generated four-side differential `62dcbea4` prove both declaration orders over eight unique ports: genuine parse/lower, public CLI/io_uring source destruction, pairwise exact Date-normalized 105-byte local wires/EOF, live/settled zero upstream, exact 118-byte fallback wires and one byte-exact episode/no retry, exactly two ordered response-size access records and clean lifecycle logs per side. The normalized-selector update retains these wires/effects under Sol focused gates and complete nginx 45/45. Only the declared literal target is claimed | SUPPORTED |
| the same exact bodyless `return 204;` config for one fresh explicit-close H1.1 literal `GET /static?x=1`; #325 closed | implicit in the accepted distinct action; request query does not alter nginx config parsing | yes: uniform canonical `slash_normalized` exact GET lowering remains query-free and byte-identical across declaration orders after verified port-only normalization; validators reject Raw, query- and Host-specific source mutations; `f2c7c504`, `90a62541` | yes: generic normalized exact selection removes query before comparing the path and ordinary public NoContent204 runs through real CLI/io_uring; no new RUT primitive or issue is needed | approved pinned oracle `bddf01c3` and final converter-generated four-side differential `f2c7c504` prove both declaration orders over eight unique ports: genuine parse/lower, source destruction, pairwise exact Date-normalized 105-byte wire/EOF, live/settled zero upstream, exact 118-byte fallback/one episode/no retry, two ordered overflow-safe access records and clean five-line RUT lifecycles. The selector update retains this behavior under Sol focused gates and complete nginx 45/45 | SUPPORTED |
| bounded clean exact bodyless `location = /healthz { return 204; }` beside the accepted root proxy; fresh bodyless explicit-close H1.1 GET domain; #328 closed | yes: literal bodyless 204 reuses the existing bounded clean 62-byte exact-path grammar with complete borrowed path/location/directive/status spans and provenance in either order; clean multi-segment/trailing-slash, 62/63 boundaries, dirty/root/variable/non-ASCII exclusions, `/old` action isolation and `/static` regression pass; `9c3c67f6` | yes: provenance-first validator admits only the modeled 2..62 clean path and now uniformly emits one ordinary public SlashNormalized exact GET with the unchanged strict NoContent204 tuple; `/static`, `/x`, `/healthz`, multi-segment and both 62-byte shapes have exact goldens/capacity, forgeries and source-destruction AST→HIR→MIR→RIR→owned RouteConfig evidence; `bc846bee`, `90a62541` | yes in generic production: ordinary public normalized exact GET plus strict NoContent204 and root forwarding compile, own and execute through real CLI/io_uring; no new RUT capability issue | approved pinned-nginx oracle `85749c79` plus mutation hardening `7cb099d0` and final converter-generated four-side differential `8d073885` prove both declaration orders across eight unique ports/four isolated nginx/RUT sides. Genuine parse/lower and public CLI/io_uring execution match five exact Date-normalized wires/EOF: 105-byte local `/healthz` and query bodyless 204 with live/settled zero upstream, plus 118-byte slash/sibling/root fallbacks and three ordered byte-exact upstream histories/no fourth. Five ordered response-size access records, exact lifecycle logs, source destruction/ownership and fail-closed source/access/resource/port mutations pass. The normalized-selector update retains these effects under Sol focused gates and complete nginx 45/45; only the declared five-vector slice remains claimed | SUPPORTED |
| exact trailing-slash bodyless `location = /healthz/ { return 204; }` beside the accepted root proxy; fresh bodyless explicit-close H1.1 GET five-vector slice; #329 closed | yes in the existing bounded model: trailing slash is preserved with complete borrowed path/location/directive/status spans and provenance; no broader normalization syntax is claimed | yes in the existing lowering: the same provenance-first 2..62-byte clean-path validator emits ordinary public `route exact slash_normalized GET "/healthz/"` with the unchanged strict NoContent204 tuple; no nginx-specific runtime mode | yes: generic checked `slash_normalized` exact selection from #319/#320 composes with generic strict NoContent204 from #323; query exclusion and slash-run collapse are ordinary RUT primitives | independently approved required oracle `3975fff1` and final four-side converter-generated differential `b211e2af` prove both declaration orders across eight bind-reserved ports/four isolated nginx/RUT sides. Genuine parser/provenance/lowering and public CLI/io_uring execution match five exact Date-normalized wires/EOF: `/healthz/`, its query and `/healthz//` emit 105-byte strict bodyless 204 with live/settled zero upstream; `/healthz` has no redirect and it plus `/` emit exact fallback wires, two ordered byte-exact histories/no third. Generated source is canonical ordinary `slash_normalized` RUT and remains owned after source destruction/overwrite; exact access/lifecycle logs, global resource isolation and fail-closed mutations pass. Sol focused gates and complete nginx 44/44 in 188.80s pass | SUPPORTED |
| complete existing 2..62-byte clean non-root exact-path class excluding reserved exact `/old`, with literal bodyless `return 204;`, optional meaningful trailing slash, accepted root proxy and the same fresh bodyless explicit-close H1.1 GET domain; #330 closed | yes structurally: one generic bounded grammar with complete borrowed provenance, `/x`, mixed-alphabet multi-segment and trailing-slash examples, 62/63-byte boundary, empty/dot-segment/dirty/root/variable/control/non-ASCII exclusions and action isolation | yes in `90a62541`: every validated key emits exactly one ordinary public SlashNormalized exact GET with the unchanged strict NoContent204 tuple. Parser/model and RUT runtime are unchanged; exact goldens, 5551..5563 sample sizes, 5619 maximum, Raw-rejection mutations and AST→HIR→MIR→RIR→owned view/source-destruction tests pass. A genuine 62-byte non-trailing key deterministically matches literal/query/embedded-`//` views and misses slash extension/neighbor/root | yes for the declared target domain: generic checked SlashNormalized selection from #319 and strict NoContent204 from #323 compose with closed #332's bounded owned access-target publication. Targets through 128 bytes publish exact Complete bytes; the #330 62-byte key plus `?x=1` is preserved as all 66 raw bytes across public CLI/io_uring completion and asynchronous ring ownership, with no nginx-specific mode | approved oracle `b3095fe6`, lowering `90a62541`, generic public epoll/io_uring access proof `4ad1c673`, and the retained current-HEAD four-side differential prove all 22 observations across both declaration orders: exact Date-normalized 105-byte local and 118-byte fallback wires/EOF, complete raw access targets including `<NONTRAILING62>?x=1`, live/settled zero local upstream, exactly three/two ordered fallback histories with no retry, eight reserved ports, isolated resources, genuine parse/provenance/lowering, public CLI/io_uring load and post-destruction source ownership. Independent closure audit is `CLOSE_BOTH`; Sol complete Network 1049/1049, Integration 432/432 and pinned-nginx 46/46 pass | SUPPORTED |
| one bounded clean exact local-return path `/healthz` beside the accepted root proxy, fresh bodyless explicit-close H1.1 GET domain | yes: non-root absolute clean ASCII-unreserved segments through the 62-byte exact-binding limit with optional single trailing slash, exact borrowed path/body/location/response spans and redirect-action isolation; `af7ff351` | yes: generic ordinary-RUT normalized exact binding with provenance-first three-way borrowed-source validation, full dual-order golden, exact 5681-byte maximum capacity and post-lifetime owned `RouteConfig`; `880a729a`, `50aed5d7` | yes: generic exact strict-local binding supports clean paths up to 62 bytes | independently approved pinned-nginx oracle `59870bb5` and converter-generated public-CLI differential `1eeee017` prove both declaration orders and five vectors: exact local `/healthz` plus query with live/settled zero upstream, exact root forwarding for `/healthz/`, `/health`, `/`, complete Date-normalized wires/EOF, three ordered backend histories with no fourth, and five scoped access records per order; Sol focused 3/3 and complete nginx 30/30 pass; the normalized selector update retains this gate | SUPPORTED |
| nginx default slash-normalized exact selection for a currently accepted bodyful clean multi-segment key, minimally `location = /a/b { return 200 "ok"; }`, when the raw request target is `/a//b` or the single combined literal `/a//b?x=1`; #331 and #337 closed | yes: the existing bodyful exact-return model already represents `/a/b`, its borrowed provenance and unchanged 1..64-byte body grammar | yes in `50aed5d7`: every validated bodyful clean exact key emits exactly one method-omitted ordinary public `route exact slash_normalized` binding with the unchanged strict 200 tuple. Exact goldens/capacity, AST→HIR→MIR→RIR→owned view/lifetime and source validators pass; Raw, duplicate/method/query/Host/nginx-marker/workaround mutations reject | yes for the declared bounded response profile: the checked normalizer stops at the first query separator and collapses path slash runs in one production helper while access retains the complete raw target. Generic #333 `846e024d` preserves positive-CL `{200,201}` status/reason through complete buffering and #334 `aab5d066` preserves zero or one validated 1..128-byte upstream Content-Type. Broader query/slash classes, CL0/streaming 201, status 202+, other forbidden headers, framing/protocols and nginx buffering semantics remain fail-closed | independently approved oracle `e899a3ca`, retained contract `d48a11a0`/`dfd0f0a4`, public-loader capabilities `846e024d`/`aab5d066`, and #337's reviewed tests-only extension `ddd95395` prove both declaration orders across four isolated pinned-nginx/generated-RUT sides and all 28 observations over seven fresh explicit-close H1.1 GET vectors. `/a//b?x=1` has the exact 144-byte local 200/CL2/`ok` wire/EOF, live/settled zero upstream and byte-exact raw access target; the three fallback vectors retain exact 175-byte 201/Content-Type/CL8/`fallback` wires, ordered backend histories/no retry and clean lifecycles. Generated source remains one query-free ordinary SlashNormalized route and mutation guards reject combined-specific workarounds. Sol focused 1/1 and complete pinned-nginx 50/50 pass | SUPPORTED |
| complete cross-product of the existing 2..62-byte clean non-root exact-path class (excluding reserved exact `/old`, optional meaningful trailing slash) and the accepted 1..64-byte raw `return 200 "B"` body class, beside the accepted root proxy with nginx default `merge_slashes on`, for one fresh bodyless explicit-close cleartext H1.1 GET | yes structurally: `P := "/" SEG ("/" SEG)* ["/"]` with non-dot ASCII alnum/`-._~` segments; B has non-space endpoints, permits any internal ASCII spaces, and otherwise uses printable `0x21..0x7e` excluding `"`, `\`, `$`, `#`, `{`, `}`, `;`. Complete common-source path/body/location/response provenance, P 2/62 acceptance and B 1/64 acceptance, boundary rejection, dirty/variable/control/non-ASCII exclusions and `/old` action isolation are covered | yes generically in `50aed5d7`: validated P and B bytes are independently copied without literal, length, trailing-slash or space-count branching into exactly one method-omitted ordinary public `route exact slash_normalized` binding with the fixed Representation200 tuple. Both 62-byte path shapes plus a 64-byte body are exactly 5681/5937 bytes with 256 bytes headroom and compile to owned RouteConfig after source destruction | yes for the declared bounded composition: generic #319 SlashNormalized selection, linear owned Representation200 body/Content-Length serialization, #332 complete access-target ownership through 128 bytes, #333 positive-CL `{200,201}` complete-buffered fallback status and #334 zero/one typed Content-Type. The 64-byte body remains in the existing two-digit Content-Length branch and yields a 207-byte local wire, far below the send slice; no P/B cross-coupled runtime branch exists | class-level consolidation of independently reviewed evidence: #286/#321/#322 prove the accepted body axis at fixed `/static`, including one/consecutive/separated internal spaces and exact byte/CL wires; #318/#320/#331 prove non-trailing/trailing/multi-segment normalized P selection and fallback with fixed `ok`; #330/#332 prove both 62-byte selector shapes and 66-byte raw access boundary. Exact combined maximum source/lexer/parser/ownership tests and source audit find no new capacity, digit-width, selector, policy, send or access seam, so a duplicate max-P+max-B Docker differential is not required. Broader methods, escapes, edge spaces, framing/reuse/protocols and normalization remain excluded | SUPPORTED |
| the complete accepted clean exact P × raw B `return 200` class above for exactly one fresh depth-zero cleartext H1.1 origin-form explicit-close header-absent `OPTIONS P` selected hit | implicit in the same method-free P/B semantic model and common-source provenance | yes: the same method-omitted ordinary public SlashNormalized exact route is emitted for every validated P/B; there is no OPTIONS, path, body or cross-product branch | yes for this exact hit: concrete OPTIONS route lookup explicitly falls back to ANY, fresh header-absent admission includes OPTIONS, exact selection precedes unmatched OPTIONS handling, and the Representation200 materializer suppresses bodies only for HEAD. Maximum P+B source remains 5681/5937, maximum B64 response is 207 bytes, and maximum request/target sizes remain within #330/#332 ownership bounds | independently reviewed composition of #294's real pinned-nginx/generated-RUT `OPTIONS /static` proof (full body, exact CL/wire/EOF, no Allow/CORS, live/settled zero upstream) with the complete P×B selector/body/ownership evidence above. Source audit finds no method×path/body coupling, so no duplicate max-P+max-B OPTIONS Docker differential is needed. OPTIONS misses, OPTIONS-star, query/slash-run variants, framing/body/tail, additional headers, reuse/protocols and broader semantics remain excluded | SUPPORTED |
| the complete accepted clean exact P × raw B `return 200` class above for exactly one fresh depth-zero cleartext H1.1 origin-form explicit-close header-absent `DELETE P` selected hit | implicit in the same method-free P/B semantic model and common-source provenance | yes: every validated P/B emits the same method-omitted ordinary public SlashNormalized exact route; #296's validators reject DELETE-specific routes and nginx workarounds | yes for this exact hit: concrete DELETE lookup falls back to ANY, #295's fresh admission and saturating per-transport completion witness are independent of route-key/body/policy bytes, and only HEAD suppresses the Representation200 body. P62+B64 stays 5681/5937; the fixed maximum DELETE request is 128 bytes, target 62, and response 207, all within #330/#332 and existing capture/send bounds | independently reviewed composition of #295/#296's real generic and converter-generated `/static` DELETE proof (full body/CL, close/EOF, exact-once completion/access, zero upstream) with the complete P×B selector/body/ownership evidence. No method×P/B or new request/send/access boundary exists, so no duplicate max-P+max-B DELETE Docker differential is needed. Query/slash runs, CL including CL0, framing/body/tail, extra headers, reuse/pipeline, proxy DELETE, misses and broader protocols/semantics remain excluded | SUPPORTED |
| the complete accepted clean exact P × raw B `return 200` class above for exactly one fresh depth-zero cleartext H1.1 origin-form explicit-close header-absent `PUT P` selected hit | implicit in the same method-free P/B semantic model and common-source provenance | yes: every validated P/B emits the same method-omitted ordinary public SlashNormalized exact route; #298's validators reject PUT-specific routes/tokens and nginx workarounds | yes for this exact hit: concrete PUT lookup falls back to ANY, #299's independently validated closed method-pair admission/completion path does not read route-key/body/policy bytes, and only HEAD suppresses the Representation200 body. P62+B64 stays 5681/5937; the fixed maximum PUT request is 125 bytes, target 62, and response 207, within #330/#332 and existing capture/send bounds | independently reviewed composition of #298/#299's real pinned-nginx/generated-RUT `/static` PUT proof (full body/CL, close/EOF, exact-once completion/access, zero upstream) with complete P×B selector/body/ownership evidence. No PUT×P/B or new request/send/access boundary exists, so no duplicate max-P+max-B PUT Docker differential is needed. Query/slash runs, CL including CL0, framing/body/tail, extra headers, reuse/pipeline, proxy PUT, misses and broader protocols/semantics remain excluded | SUPPORTED |
| the complete accepted clean exact P × raw B `return 200` class above for exactly one fresh depth-zero cleartext H1.1 origin-form explicit-close header-absent `PATCH P` selected hit | implicit in the same method-free P/B semantic model and common-source provenance | yes: every validated P/B emits the same method-omitted ordinary public SlashNormalized exact route; #300's validators reject PATCH-specific routes/tokens and nginx workarounds | yes for this exact hit: concrete PATCH lookup falls back to ANY, #301 independently proves the exact PATCH metadata/raw-method pair, full request reparse, freshness/ownership fences and 3×3 DELETE/PUT/PATCH mismatch rejection; completion and Representation200 serialization do not branch on PATCH. P62+B64 stays 5681/5937; the fixed maximum PATCH request is 127 bytes, target 62, and response 207, within #330/#332 and existing bounds | independently reviewed composition of #300/#301's pinned-nginx/generated-RUT `/static` PATCH proof (full body/CL, close/EOF, exact-once completion/access, zero upstream) with complete P×B selector/body/ownership evidence. No PATCH×P/B or new request/send/access boundary exists, so no duplicate max-P+max-B PATCH Docker differential is needed. Query/slash runs, CL including CL0, framing/body/tail, extra headers, reuse/pipeline, proxy PATCH, misses and broader protocols/semantics remain excluded | SUPPORTED |
| the complete accepted clean exact P × raw B `return 200` class above for exactly one fresh depth-zero cleartext H1.1 origin-form explicit-close header-absent `POST P` selected hit | implicit in the same method-free P/B semantic model and common-source provenance | yes: every validated P/B emits the same method-omitted ordinary public SlashNormalized exact route; #290's guards reject POST-specific routes, Raw/query aliases and nginx workarounds | yes for this exact hit: concrete POST lookup falls back to ANY and the fresh common admission explicitly requires no Content-Length header/count, no framing/body/tail and neutral ownership. The separate #291/#292 literal-CL0 branch is not used. Only HEAD suppresses the Representation200 body. P62+B64 stays 5681/5937; the fixed maximum POST request is 126 bytes, target 62, and response 207, within #330/#332 and existing bounds | independently reviewed composition of #290's real pinned-nginx/generated-RUT header-absent `/static` POST proof (full body/CL, close/EOF, zero upstream) with complete P×B selector/body/ownership evidence. No POST×P/B or new request/send/access boundary exists, so no duplicate max-P+max-B POST Docker differential is needed. Every Content-Length including CL0 remains outside this row and only in the separate #291/#292 slice; query/framing/body/tail/extra headers/reuse/proxy/miss/protocol domains remain excluded | SUPPORTED |
| the complete accepted clean exact P × raw B `return 200` class above for exactly one fresh depth-zero cleartext H1.1 origin-form explicit-close literal `Content-Length: 0` `POST P` selected hit | implicit in the same method-free P/B semantic model and common-source provenance; the request CL field is not nginx configuration syntax | yes: every validated P/B emits the same method-omitted ordinary public SlashNormalized exact route; #292 rejects CL0-, POST-, query-, Host- and nginx-specific generated-source workarounds | yes for exactly one byte-literal `Content-Length: 0`: the separate #291 CL0 admission requires POST, field presence/count one, numeric zero, exact close and fresh depth-zero ownership, then uses the same ANY route and complete Representation200 serializer. P62+B64 stays 5681/5937; the fixed maximum request is 145 bytes, target 62, and response/access size is 207 bytes, below existing receive/capture/send/access bounds | independently reviewed composition of #291's generic fail-closed CL0 admission/completion/access/capture and public io_uring proof, #292's pinned-nginx/generated-RUT `/static` full-wire/EOF/zero-upstream differential, and complete P×B selector/body/ownership evidence. No CL0×P/B branch or new threshold exists, so no duplicate max-P+max-B Docker differential is needed. Header-absent POST remains the preceding separate row; alternate zero spelling/case/OWS, duplicate/conflicting/malformed/positive CL, TE, request body/tail, query/slash runs, misses/proxy, extra headers, reuse/pipeline and broader protocols remain excluded | SUPPORTED |
| the complete accepted clean exact P × raw B `return 200` class above for exactly one fresh depth-zero cleartext H1.1 origin-form explicit-close header-absent `HEAD P` selected hit | implicit in the same method-free P/B semantic model and common-source provenance | yes: every validated P/B emits the same method-omitted ordinary public SlashNormalized exact route with `head_mode: "suppress_body"`; #289 rejects HEAD-specific routes, exact-GET specialization and response workarounds | yes for this exact hit: concrete HEAD lookup falls back to ANY, common fresh admission includes HEAD, Content-Length is derived from the complete B length and only the body copy is suppressed. P62+B64 stays 5681/5937; the fixed maximum HEAD request is 126 bytes, target 62, and header-only response/access size is 143 bytes, within #330/#332 and existing capture/send bounds | independently reviewed composition of #289's real pinned-nginx/generated-RUT `/static` HEAD proof (200, CL16, zero body bytes, close/EOF and zero upstream), #287's exact-once HEAD completion/access/capture proof, and the complete P×B selector/body/ownership evidence. No HEAD×P/B or new capacity/digit-width/access seam exists, so no duplicate max-P+max-B HEAD Docker differential is needed. Misses, proxy HEAD, query/slash runs, every request CL including CL0, framing/body/tail, reuse/pipeline and broader protocols remain excluded | SUPPORTED |
| the complete accepted clean exact P × raw B `return 200` class above for exactly one fresh bodyless explicit-close cleartext H1.1 literal `GET P?x=1` selected hit | implicit in the same query-free P/B semantic model; the literal query belongs only to the request observation | yes: every validated P/B emits the same query-free method-omitted ordinary public SlashNormalized exact route; #293 rejects query-specific routes, exact-GET specialization and response workarounds | yes for this exact literal suffix: generic checked normalization terminates the path at the first `?`, retains a meaningful trailing slash, selects the P key and then serializes complete B. P62+B64 remains 5681/5937; both P62 shapes plus `?x=1` produce a 66-byte raw target, 129-byte request and 207-byte response, below established capture/send/access bounds | independently reviewed composition of #293's real pinned-nginx/generated-RUT `/static?x=1` full-wire/EOF/zero-upstream proof, #330's genuine four-side non-trailing and trailing P62+`?x=1` selector/wire/raw-access boundary differential, #332's public epoll/io_uring Complete 66-byte target ownership, and complete P×B body/source evidence. No query×P/B branch or new threshold exists, so no duplicate max-P+max-B differential is needed. Only literal `?x=1` is claimed; empty/arbitrary/repeated/encoded query forms, variables/rewrites, raw slash runs, misses/proxy forwarding, other methods, framing/reuse and broader protocols remain excluded | SUPPORTED |
| exactly one fresh bodyless explicit-close cleartext H1.1 query-bearing exact-location miss `GET /a/c?x=1` under the accepted `/a/b` local return plus root no-URI proxy model | implicit in the existing exact-local-plus-root model; the query is request data, not configuration syntax | yes: both relevant accepted configurations lower through the same ordinary root GET forward policy: HTTP/1.1, upstream-authority Host, omitted client Connection, 60s read timeout and complete-Content-Length buffering. Runtime copies the complete checked raw target without path/query literal branches | yes for this exact fixed slice: selection ignores the query and misses `/a/b`; the root no-URI forward preserves `/a/c?x=1` byte-for-byte in the upstream request, rebuilds Host from the fixed backend authority, and uses the already-closed 201/Content-Type/CL8 response profile. The target is shorter than the existing query-bearing production witness and crosses no buffer/access boundary | independently reviewed composition of #325's four-side real pinned-nginx/generated-RUT `/fallback?q=1` proof (byte-exact upstream target/Host, omitted Connection, one episode/no retry, raw access) with #331's `/a/c` exact miss (three ordered fallbacks, exact 175-byte 201/CL8/`fallback` wire/EOF and access/lifecycle). Both use the identical canonical root GET forward path, so no duplicate literal differential is needed. Arbitrary query forms/misses, proxy_pass URI suffix/replacement, variables, bodies/methods, extra request/response policy shapes, retries/reuse and broader protocols remain excluded by #252/#253 | SUPPORTED |
| exact local-return path `/health/check/` with nginx slash-normalized selection for raw `/health/check//`, beside the accepted root proxy | yes: the bounded clean exact-path model represents `/health/check/` with borrowed spans/provenance; no broader normalized selection model is claimed | yes: validated exact paths uniformly emit the generic ordinary-RUT `slash_normalized` exact view; `50aed5d7` retains the approved trailing behavior while removing the non-trailing Raw seam, with dual-order full goldens, exact 5681-byte capacity and post-lifetime owned RouteConfig/view matching | yes for the bounded generic H1 exact strict-local slice; #319 closed. Ordinary `slash_normalized` metadata crosses compiler/owned config and uses a checked full raw-target witness, allocation-free slash-only view, explicit Raw/normalized method precedence and H2 fail-close. Broader normalization/protocols/actions remain excluded | independently approved pinned-nginx oracle `7c29dd53`, ordinary-source RUT proof `3a8c194c`, and converter-generated public-CLI/io_uring differential `7cfd5ca5` prove both declaration orders and five vectors: exact local `/health/check/`, query and raw double slash with live/settled zero upstream; no-redirect raw fallback for `/health/check` and `/`; exact Date-normalized wires/EOF, two ordered histories/no third and five raw access records per side/order. Sol focused 4/4 and two post-#311-fix complete nginx suites 32/32 pass; `50aed5d7` retains these gates | SUPPORTED |
| the same exact local return for one fresh cleartext H1.1 origin-form explicit-close literal `GET /static?x=1`, with the query ignored only for this exact-location decision | implicit in the accepted exact model | yes: the existing method-omitted exact route is used; generated source contains no query-specific route or workaround; #293 closed | yes for the literal `?x=1` target boundary: exact matching accepts the path when the next byte is `?`; no query normalization or variable semantics are claimed | pinned nginx baseline under both declaration orders, generic ordinary-RUT production and hand-authored differential evidence, plus the converter-generated pinned-nginx differential independently prove the exact 200/CL16/body/close/EOF wire and zero upstream effects; focused required mode and full pinned-Docker suite pass | SUPPORTED |
| the same exact local return for one fresh depth-zero cleartext H1.1 origin-form explicit-close header-absent `OPTIONS /static` | implicit in the accepted exact model | yes: the existing method-omitted exact route is used; generated source contains no OPTIONS-specific route or workaround; #294 closed | yes for this exact OPTIONS request: the ANY exact selector admits it and the strict serializer emits the full representation body; no general OPTIONS handler or Allow/CORS semantics are claimed | pinned nginx baseline under both declaration orders, generic ordinary-RUT production and hand-authored differential evidence, plus the converter-generated pinned-nginx differential independently prove the exact 200/CL16/full-body/close/EOF wire, absence of extra headers, and zero upstream effects; focused required mode and full pinned-Docker suite pass | SUPPORTED |
| the same exact local return for one fresh depth-zero cleartext H1.1 origin-form explicit-close header-absent `DELETE /static` | implicit in the accepted exact model | yes: existing method-omitted exact lowering is sufficient and generated source contains no DELETE-specific route or workaround; #296 closed | yes for this exact fresh generic request slice: the saturating per-transport completion witness, bounded DELETE admission, deterministic lifecycle/exclusion evidence, and ordinary-source frontend/JIT/production-io_uring wire are complete; #295 closed. Query, framing including CL0, body/tail, reuse/pipeline, TLS/H2, and proxy DELETE remain excluded | pinned nginx 1.29.7 baseline under both declaration orders, ordinary-RUT production, and the converter-generated pinned-nginx differential independently prove the exact 200/CL16/full-body/close/EOF wire and zero upstream; focused required mode, adjacent baselines, and full pinned-Docker suite pass | SUPPORTED |
| the same exact local return for one fresh depth-zero cleartext H1.1 origin-form explicit-close header-absent `PUT /static` | implicit in the accepted exact model | yes: existing method-omitted exact lowering is sufficient and generated source contains no PUT-specific route or workaround; #298 closed | yes for this exact fresh generic request slice: closed DELETE/PUT pair proof, deterministic lifecycle/exclusion evidence, and ordinary-source frontend/JIT/production-io_uring wire are complete; #299 closed. Query, framing including CL0, body/tail, reuse/pipeline, TLS/H2, proxy PUT, and PATCH remain excluded | pinned nginx 1.29.7 baseline under both declaration orders, ordinary-RUT production, and converter-generated pinned-nginx differential independently prove exact 200/CL16/full-body/close/EOF and zero upstream; focused required mode, adjacent baselines, and full pinned-Docker suite pass | SUPPORTED |
| the same exact local return for one fresh depth-zero cleartext H1.1 origin-form explicit-close header-absent `PATCH /static` | implicit in the accepted exact model | yes: existing method-omitted exact lowering is sufficient and generated source contains no PATCH-specific route or workaround; #300 closed | yes for this exact fresh generic request slice: the closed DELETE/PUT/PATCH proof, deterministic lifecycle/exclusion evidence, and ordinary-source frontend/JIT/production-io_uring wire are complete; #301 closed. Query, framing including CL0, body/tail, reuse/pipeline, TLS/H2, and proxy PATCH remain excluded | pinned nginx 1.29.7 baseline under both declaration orders, ordinary-RUT production, and converter-generated pinned-nginx differential independently prove exact 200/CL16/full-body/close/EOF and zero upstream; focused required mode, adjacent baselines, and full pinned-Docker suite pass | SUPPORTED |
| nginx pre-location rejection for one fresh depth-zero cleartext H1.1 origin-form explicit-close header-absent `TRACE /static` with the accepted root proxy plus exact local-return configuration | implicit closed semantic profile in the accepted exact-root model | yes; #302 closed. Emits ordinary concrete `pre_route TRACE` before unmatched/routes; a TRACE-specific route, unmatched fallback, or runtime workaround is forbidden | yes for this bounded generic source/runtime slice; #303 closed. Concrete-only `pre_route METHOD { return local_response(...) }` crosses parser/AST/HIR/MIR/RIR/verifier/printer, transactionally populates RouteConfig, and executes before exact/prefix/unmatched selection in the real io_uring frontend | pinned nginx 1.29.7 baseline under both declaration orders, independent ordinary-RUT source production, converter golden/owned RouteConfig evidence, and converter-generated pinned-nginx differential independently prove the fixed ordered 405/CL157/full-nginx-body/close/EOF wire and live/settled zero upstream | SUPPORTED |
| nginx pre-location rejection for one fresh depth-zero cleartext H1.1 origin-form explicit-close header-absent `TRACE /` with the accepted root-only proxy configuration | yes: closed server-level semantic profile; #305 closed | yes: emits the same ordinary concrete `pre_route TRACE` before unmatched/routes | yes: closed generic bounded `pre_route TRACE` capability #303 | pinned nginx-only oracle, converter golden/post-lifetime owned RouteConfig evidence, and converter-generated pinned-nginx differential independently prove the exact ordered 405/CL157/full-nginx-body/close/EOF wire, one scoped access record, and live/settled zero upstream | SUPPORTED |
| nginx pre-location rejection for one fresh depth-zero cleartext H1.1 origin-form explicit-close header-absent `TRACE /api/x` with the accepted `/api/` proxy-URI replacement configuration | yes: closed server-level semantic profile; #306 closed | yes: emits the canonical ordinary concrete `pre_route TRACE` before unmatched/routes while retaining redirect and URI transform | yes: closed generic bounded `pre_route TRACE` capability #303 | pinned nginx-only oracle, converter golden/owned transform evidence, and converter-generated pinned-nginx differential independently prove the exact ordered 405/CL157/full-nginx-body/close/EOF wire and live/settled zero upstream | SUPPORTED |
| one exact `location = /old { return 301 http://redirect.example/new; }` beside the accepted root proxy, for one bounded fresh explicit-close GET | yes: distinct bounded semantic action with source provenance; `6fa09648` | yes for the exact bounded model: byte-stable ordinary-RUT conditional lowering, strict forged provenance/capacity checks, canonical RIR and owned RouteConfig; `18d04baf` | yes for the exact generic composition: fixed redirect #308, 768-slot lexer #309, metadata `77847b51`, atomic activation `ceb05091`, and independently reviewed ordinary-source real-io_uring proof `8b314151`; H2 and broader conditional forwarding remain excluded | pinned nginx oracle `da414a71` and converter-generated differential `538b87d0` independently prove both declaration orders and five fresh explicit-close GET vectors: exact 301/EOF/Host independence/query discard with live zero upstream, exact `/old/` and `/` forward wires/histories, and exactly five scoped access records per order; focused required mode and full pinned-nginx suite pass | SUPPORTED |
| the same bounded exact/root-proxy shape with `return 302 http://redirect.example/new;`, for one fresh explicit-close GET | yes: strict literal `{301,302}` bounded model with genuine borrowed status lexeme/span/provenance, both declaration orders and forged/unsupported-shape rejection; `86144bfa` | yes for deterministic ordinary-RUT lowering: coherent status/provenance dispatch, full-partition dual-order golden, exact 302 reason/body, 5912/5936/5937 capacity and owned RouteConfig; `c5078468` | yes for the generic bounded fixed-target policy: closed `{301,302}` validator, frontend/JIT/loader/wire/real-io_uring proof and full acceptance are complete in `427ba484`; legacy request-host and all wider status/tuple shapes are unchanged; #312 closed | pinned oracle `e9d67fff` and converter-generated differential `a3522654` independently prove both declaration orders and five fresh explicit-close GET vectors: exact 342-byte 302/EOF/Host/query behavior, live/settled zero redirect upstream, exact forward wires/histories, and five scoped nginx access records per order; focused required mode and full pinned-nginx 24/24 pass | SUPPORTED |
| one exact literal `/static` local return for a fresh depth-zero cleartext H1.1 origin-form explicit-close POST with exactly one valid `Content-Length: 0`, no other framing/body/tail | implicit in the accepted exact model | yes: the existing method-omitted exact route is sufficient; no CL-specific source or converter workaround; #292 closed | yes for this exact generic request slice: field-count identity, single validated numeric-zero POST admission, deterministic lifecycle/forgery coverage, and ordinary-source frontend/JIT/production-io_uring wire are complete; #291 closed. Duplicate/conflicting/positive/malformed CL, CL+TE, other methods, bodies/tails, reuse/pipeline, TLS/H2, and alternate-zero compatibility claims remain excluded | pinned nginx 1.29.7 baseline and ordinary-RUT production wire plus the converter-generated pinned-nginx differential independently prove the exact 200/CL16/body/close/EOF wire and zero upstream effects; focused required-mode, adjacent E3, and full pinned-Docker suites pass | SUPPORTED |
| broader exact, `^~`, regex, or nested locations | no | no | no broader nginx selection semantics | no | NOT_PLANNED |
| exact `/api/` + proxy URI `/`, clean bounded H1 request domain | yes | yes | yes: bounded prefix replacement; #259 closed | pinned converter-generated `/api/`, `/api/x`, and query differentials; four out-of-domain targets fail before upstream | SUPPORTED |
| exact `/api/` + clean non-root proxy URI `/v1/`, fresh bodyless explicit-close H1.1 GET domain | yes: absolute trailing-slash ASCII-unreserved clean prefix up to 128 bytes with exact borrowed URI/span, boundary and rejection coverage; `55e394f6` | yes: provenance-first clean URI validation, byte-stable ordinary-RUT `replace_prefix`, full golden, 3336/3468/5937 capacity and owned RouteConfig; `a21336bc` | yes: generic bounded clean strip/replace prefix and runtime suffix+query composition already exist | pinned nginx-only oracle `6bd34f28` and converter-generated differential `ed3f0865` independently prove three exact `/v1/` replacement/query upstream histories, two automatic-slash redirects with live/settled zero upstream, exactly five scoped access records, and public-CLI wire/EOF equivalence; focused tests and full pinned-nginx 26/26 pass | SUPPORTED |
| complete accepted clean `proxy_pass` replacement-URI set for fixed location `/api/`: distinct root `/` or a non-root trailing-slash URI of 3..128 bytes with non-dot ASCII alnum/`._~-` segments, limited to the five fresh bodyless explicit-close cleartext H1.1 GET targets `/api/`, `/api/x`, `/api/x?y=1`, `/api`, `/api?x=1`; #336 closed | yes: generic borrowed URI/span/provenance, exact `/` inventory, 3-byte non-root minimum, 128 acceptance/129 rejection, and empty/repeated/dot/dirty/variable/query/fragment/non-ASCII exclusions; `55e394f6` | yes: validated U is copied byte-for-byte into one ordinary-RUT `replace_prefix` while strip and redirect remain `/api/`; maximum ports/IP output is exactly 3468/5937 and passes the public loader, with complete source and owned transform evidence; `a21336bc`, `5b13b43b` | yes for the bounded class: the generic transform validates the complete recv-buffer target and implements both overlap directions atomically. Closed concrete `/` and `/v1/` cover shrink deltas -4/-1; #336 directly proves the opposite maximum `delta=+123` growth path, 128/129/133-byte rebuilt targets and post-load ownership through the public CLI/io_uring. Original raw access snapshots remain pre-transform and Complete | closed #259 and #316 establish root and representative non-root nginx semantics; #336 `5b13b43b` drives exact 128-byte U through genuine parser/lowering and matches pinned nginx for all five Date-normalized wires/EOF, three ordered full-length backend requests/no retry, exact rebuilt Host and duplicate headers, two query-preserving redirects with live/settled zero upstream, real original-target nginx/RUT access records and isolated lifecycles. Independent review approved after mutation-path corrections; Sol focused 1/1 and complete nginx 50/50 pass. Access request-size formats remain side-specific and are not claimed equivalent | SUPPORTED |
| exactly one fresh bodyless explicit-close cleartext H1.1 non-root replacement request `GET /api/users?x=1` for accepted `location /api/ { proxy_pass http://127.0.0.1:<backend>/v1/; }`, yielding upstream `/v1/users?x=1` | implicit in the existing clean location/replacement model; the suffix/query are request data | yes: the same ordinary `strip_prefix: "/api/"` plus `replace_prefix: "/v1/"` policy is emitted without suffix/query literals or length branches | yes for this exact request: the generic transform splits at the first `?`, moves the complete suffix+query region and writes the replacement prefix. This is the same query length and `delta=-1` shrink branch as #316's direct `/api/x?y=1 → /v1/x?y=1` witness; `users` changes no branch, and #336 covers substantially larger growth/capacity/ownership boundaries. Original access target is captured before transform | independently reviewed composition of #316's real pinned-nginx/generated-RUT query-bearing replacement proof (byte-exact upstream target/authority Host/duplicate headers, exact 118-byte 200/CL2/`ok` wire/EOF, one episode/no retry) with the content-independent production transform and #336 boundary evidence. No duplicate literal differential is needed. Arbitrary query classes, config-time query/variables, normalization-sensitive targets, other methods/bodies/framing, retries/reuse and broader #252/#253/protocol semantics remain excluded | SUPPORTED |
| bounded literal query in a configured `proxy_pass` replacement URI, minimally `/v1/?fixed=1`, for exactly the two fresh bodyless explicit-close cleartext H1.1 GET targets `/api/users?x=1` and `/api/users`, including nginx concatenation order `/api/users?x=1 → /v1/?fixed=1users?x=1`; #339 closed | yes for one bounded nonempty static `[A-Za-z0-9._~-=&]+` query in the existing complete borrowed `ProxyPass::uri/uri_span`; shared parser/lowering validation, exact boundary and provenance-forgery evidence are integrated in `5eddf443` and `a283c91a` | yes: after provenance checks the converter emits the complete URI verbatim into one ordinary public #338 `replace_prefix`; dual-order goldens, canonical-source guards, 128-byte URI/3700-byte source boundary and post-source-destruction owned RouteConfig pass; `a283c91a` | yes for the generic bounded primitive: #338 extends only replacement-prefix validation with one nonempty static `[A-Za-z0-9._~-=&]+` query while strip/path-only validation, layouts and runtime concatenation stay unchanged. Public source owns the prefix after source/RIR destruction and real io_uring emits configured-prefix + unmatched-suffix + original-query exactly; invalid grammar and capacity overflow fail atomically | pinned-nginx oracle `b935cdad` independently proves both declaration orders and exact upstream/downstream/access/no-retry behavior. Four-side differential `5879922c` takes both exact nginx fragments through genuine parser/provenance/lowering and independent ordinary-RUT public CLI/io_uring sides; both generated sides match both nginx 1.29.7 sides for `/v1/?fixed=1users?x=1` and `/v1/?fixed=1users`, rebuilt Host/omitted Connection, Date-normalized 200/CL2/`ok` close/zero-tail/EOF wires, two ordered clean episodes and original raw access targets. Different-Luna review approved after mutation-gate corrections; Sol focused 2/2 and complete pinned-nginx 52/52 pass. Other queries, empty configured query, variables, percent encoding/fragments, methods/bodies/framing/reuse/retries/failures and H2/TLS remain excluded | SUPPORTED |
| the same accepted `/api/` + `/v1/?fixed=1` configuration at exactly the zero-length unmatched-suffix boundary, for fresh bodyless explicit-close cleartext H1.1 GET `/api/?x=1` and `/api/`, producing `/v1/?fixed=1?x=1` and `/v1/?fixed=1`; #341 closed | yes: identical already accepted #339 parser/model/provenance; request suffix/query are runtime input | yes: identical ordinary #338 `strip_prefix: "/api/"` plus `replace_prefix: "/v1/?fixed=1"`; no boundary-specific source branch | yes: #338 deterministic production Network evidence directly proves both exact empty-suffix target materializations and upstream Send bytes | pinned-nginx oracle `c0f85cff` independently proves both declaration orders, exact Host-rebuilt/Connection-omitted upstream histories, Date-normalized 200/CL2/`ok` close/zero-tail/EOF wires, two strict raw access records and no retry with held port handoff. Four-side differential `452c8524` drives both declaration orders through genuine parser/provenance/lowering and independent ordinary-RUT public CLI/io_uring sides; all four sides match exact histories/wires/access/lifecycle. Different-Luna review approved, Sol #341/#339 focused 4/4 and complete pinned-nginx 54/54 pass. Empty configured query, other queries, variables/encoding/normalization, redirect, methods/bodies/framing/reuse/retries/failures and H1.0/H2/TLS remain excluded | SUPPORTED |
| explicitly empty configured `proxy_pass` query delimiter, exact `/api/` + `/v1/?` four-forwarded-target and two-redirect fresh bodyless explicit-close cleartext H1.1 GET slice; #360 closed | yes in `f43f0935`: both directive orders retain `has_uri=true`, exact borrowed five-byte URI/span/line/column/common-source provenance, distinction from `/v1/` and URI absence, and fail-closed delimiter/pointer/span/cross-source/action/timeout forgeries | yes in `f43f0935`: both orders emit a canonical 3337-byte ordinary-RUT source with exactly one `/api/` strip plus `/v1/?` replacement and 2608 bytes headroom; full golden/source-shape gates and AST/HIR/MIR/RIR→10-byte owned-config lifetime evidence pass | yes: closed #359 `80f433bd` generically admits one explicit terminal query delimiter with no new field/runtime branch. `/?`, `/v1/?`, 128/129 boundaries, AST/HIR/MIR/RIR/config ownership, exact four-target direct-send and public io_uring behavior, retained nonempty query and atomic rejection evidence pass | pinned oracle `ed376930` and four-side differential `f0912b27` cover both declaration orders through two pinned nginx 1.29.7 and two independently parsed/lowered ordinary-RUT public CLI/O2-JIT/io_uring sides with eight held-and-handed-off ports. All sides match six exact Date-normalized wires/EOF, four ordered `/v1/?`, `/v1/?x`, `/v1/?x?y=1`, `/v1/??y=1` upstream histories with rebuilt Host/omitted Connection/duplicate headers/no retry, and two query-preserving 301s with zero added upstream. Runtime-specific ordered raw-target access, transform-ID-1 ownership after source/RIR teardown, exact source capacity, disjoint resources and clean lifecycle pass. Initial review changes were fixed by a different Luna and re-approved. Sol focused 6/6, generic #359 regressions, parser/converter 129/129 and complete nginx-labelled 83/83 in 423.12s pass. `/?`, other queries, variables/encoding/normalization, methods/bodies/framing/reuse/retry/failure, H1.0/H2/TLS and #352/#362 remain excluded | SUPPORTED |
| configured root replacement with an explicitly empty query delimiter, exact `/api/` + `/?` four-forwarded-target and two-redirect fresh bodyless explicit-close cleartext H1.1 GET slice; #372 closed | yes in `8e4d35d0`: a parser-private exact-len2 admission retains `has_uri=true`, the borrowed `/?`, absolute URI/directive/location/listener spans and common-source provenance for both orders while keeping `/`, `/v1/?`, `/?fixed=1`, no URI, lexer gap/comment forms and invalid adjacent grammar distinct. Copied-model lifetime and pre-dereference forgery evidence pass; no layout/API change | yes in `0ab64bf1`: a two-stage converter-private gate first validates path/URI provenance and then exact-loopback listener provenance, admitting only `/api/` + `/?` while wildcard/unpinned compositions remain closed. Both orders emit canonical3343-byte ordinary RUT with one generic strip `/api/` + replace `/?` transform; max endpoints3351 fit capacity5946/NUL. Independent fixed golden, source coherence, AST/HIR/MIR/RIR/config seven-byte ownership, teardown survival and same-owner clearing pass; the public validator and generic #359 primitive are unchanged | yes: closed #359 already proves generic ordinary-RUT `replace_prefix: "/?"` through AST/HIR/MIR/RIR/config ownership, direct-send and public io_uring behavior; no nginx-specific runtime mode exists | pinned `c215dab0` and final four-side `b812a2ff` cover both declaration orders through two nginx 1.29.7 and two independently parsed/lowered ordinary-RUT public CLI/O2/JIT/io_uring sides with eight held unique ports. All four sides match exact `/api/` -> `/?`, `/api/x` -> `/?x`, `/api/x?y=1` -> `/?x?y=1`, `/api/?y=1` -> `/??y=1` upstream histories, P4 Host rewrite, duplicate headers, omitted Connection/no body, normalized118 forward responses/EOF, queryless/query-preserving automatic 301s with zero fifth upstream, side-specific raw access plus bounded common projection, source ownership/poison survival and clean lifecycle. Independent reviews approve; Sol focused behavior8/8, #359 generic1/1, #372 components6/6 and complete nginx-labelled93/93 in448.44s pass. Other prefixes/replacements, variables/encoding/normalization, methods/bodies/framing/reuse/retry/failure, broader protocols and nginx access-format equivalence remain excluded | SUPPORTED |
| complete non-empty 3..63-byte clean trailing-slash proxy location-prefix class + exact proxy URI `/`, limited to the five fresh bodyless explicit-close cleartext H1.1 GET vectors represented by `P`, `P+x`, `P+x?y=1`, `R`, and `R?x=1` where `R` removes the final slash; #335 closed | yes: `P := "/" SEG ("/" SEG)* "/"`, non-dot segments over ASCII alnum/`._~-`, exact borrowed path/span/provenance, 63/64-byte boundary and dirty/variable/normalization-sensitive rejection; `1ba9ac8e`. The earlier 2-byte shorthand has no member; `/a/` is the 3-byte minimum | yes: provenance-first generic path validation and byte-independent ordinary-RUT route/redirect/strip lowering, exact 3573/5937 maximum for replacement `/`, general 3700/5937 maximum, full lexer/parser/loader and owned RouteConfig evidence; `e7be6890`, `dddf024f` | yes for this bounded composition: generic prefix selection preserves the 63-byte trailing segment boundary across the request-path mirror clamp, target transform reparses the complete recv buffer, redirect preserves raw query, and #332 publishes Complete owned access targets through 128 bytes. No nginx-specific runtime mode | closed #317's pinned oracle `22bd50e1` and generated `/service/` proof `97727e9d` establish the five semantics; #335 `dddf024f` proves the exact 63-byte `P`, 62-byte `R`, and 64/68/66-byte raw-target boundary through genuine parse/lower and the public CLI. Both sides match all five Date-normalized wires/EOF, three ordered `/`, `/x`, `/x?y=1` backend histories/no retry, two query-preserving redirects with live/settled zero upstream, exact nginx and RUT access records, source destruction and isolated lifecycles. Independent review approved after removing a synthetic access-count assertion; Sol focused 1/1 and complete nginx 49/49 pass | SUPPORTED |
| broader `proxy_pass` URI replacement and nginx URI normalization | partial | no | partial: clean raw-target transform only | nginx baselines only for repeated slash/dot/percent forms | PARTIAL |
| automatic slash redirect for exact accepted `/api/` + proxy URI `/`, bounded GET/cleartext-H1/close domain (`/api` → `/api/`) | implicit in accepted location/proxy model | yes | yes for declared bounds; #261 closed | pinned converter-generated two-vector full-wire/EOF differential with zero upstream activity; #260 closed | SUPPORTED |
| multiple servers / `server_name` / `default_server` | no | no | no virtual-server selection | no | NOT_PLANNED |
| variables, rewrite, or internal redirects | no | no | insufficient nginx phase semantics | no | NOT_PLANNED |
| HTTPS/DNS/IPv6/Unix-socket upstreams | no | no | fixed IPv4 HTTP only | no | NOT_PLANNED |

## Internal evidence notes

- PR #520 (accepted head `c3ea403efe23711f5eda77746720b79b45730050`,
  merge `c070d0e9d5f90b0d07cb37b703ced480f0366422`) pins nginx 1.29.7 for
  the exact 201 incomplete-body expiry schedule declared above. PR #521
  (accepted head `27d6972c32178f5226e9176ee3315a00e21917d1`, merge
  `ca97a4dffcfb0ea3c944b6bf6af8bbd6e92e0c18`) adds the independently
  converter-generated ordinary-RUT side with source/lexer/AST/HIR/MIR/verified-
  RIR/O2-JIT/owned-`RouteConfig`/public-CLI provenance and exact pre-request
  `Backend: io_uring\n`. Each side
  receives the exact 60-byte bodyless GET and independently publishes one exact
  108-byte W1: the complete 103-byte 201/Created/CL12 header plus `hello` (5/12),
  then remains application-open. Both remain downstream-open and byte-quiet
  through W1+800ms. Application-publication timestamps do not establish TCP,
  frontend-read or CQE boundaries. Both sides then emit the equal normalized
  127-byte header-only 201/CL12/keep-alive wire near W1+1s, containing exactly
  one 201 and no 200, incomplete body or failure tail, followed by real EOF and
  active origin close without ordering those observations. Each records the
  exact equal authority-rewritten 60-byte upstream request, one accept/request/
  publication/peer-close, 175ms no retry and exact `60\n` ledgers through its
  independent W1+2.1s horizon, client close and frontend shutdown. Oracle direct
  3/3 before rebase and 1/1 after, registered 1/1 and adjacent 2/2 passed; paired
  direct 5/5 before review and 1/1 after the independent-horizon fix, registered
  1/1, oracle/adjacent 3/3 and focused runtime 8/8 (442 checks) passed.
  Independent reviews approved. CI `34018162597` for #520 and `34019254901`
  for #521 each passed 15/15. This proves only the exact 201 schedule;
  other statuses, bodies/schedules/framing/methods/lifecycles and broad
  #253/#268/#270/#271 remain open. Runtime consumes generated ordinary RUT,
  never `nginx.conf`.

- PR #517 (accepted head `9090d62e6c763a240e30bcf707ac54024fc8e44e`,
  merge `3d6f6ce09aaac37ea623f0711986582e1deff80c`) pins nginx 1.29.7 for
  the exact third-still-incomplete progress schedule declared above. PR #518
  (accepted head `be053557585d662daea2ecb206de59d09657d33c`, merge
  `2aea437273afb7aa00d57ff213b2da6899b567dd`) adds the independently
  converter-generated ordinary-RUT side with source/lexer/AST/HIR/MIR/
  verified-RIR/O2-JIT/owned-config/public-CLI provenance and exact pre-request
  `Backend: io_uring\n`. Each side receives the exact 60-byte bodyless GET and
  independently publishes the exact 103-byte header-plus-`hello` W1, one-byte
  W2=`!` about +600ms later, and five-byte W3=`world` another about +600ms later,
  leaving 11/12 while the origin remains application-open. Both gaps are
  `[550ms,750ms)`; pre-W3 quiet crosses W1+1.10s/W2+550ms and post-W3 quiet
  crosses W3+700ms/W2+1.25s. Application timestamps do not establish TCP,
  frontend-read or CQE boundaries. Both sides then emit the equal normalized
  122-byte header-only 200/CL12/keep-alive wire near W3+1s, real EOF and active
  origin close without ordering those terminal observations, with one exact
  authority-rewritten 60-byte upstream request,
  one accept/request, three publications, 175ms no retry and exact `60\n`
  live/horizon/client-close/shutdown ledgers. Oracle direct 3/3 before rebase and
  1/1 after, registered 1/1 and adjacent 3/3 passed; paired direct 5/5,
  registered 1/1, oracle/adjacent 3/3 and focused runtime 5/5 (224 checks)
  passed. Independent reviews approved, and CI `34014203391` and `34015624034`
  each passed 15/15. This proves only the exact schedule; other progress/status/
  method/framing/lifecycle variants and broad #253/#268/#270/#271 remain open.
  Runtime consumes generated ordinary RUT, never `nginx.conf`.

- PR #514 (accepted head `7e3187cd64407969595b94a98472728e581f7c23`,
  merge `f2685b94e025e10e9a2184557ce519aaeca45541`) pins nginx 1.29.7 for
  the exact three-publication completion schedule declared above. PR #515
  (accepted head `95267271e0489c629c4f3e2abbf0e1690bdf5efc`, merge
  `e2781697dac169e378591d7c2ed1c482e3c9bc0f`) adds the independently
  converter-generated ordinary-RUT side with complete source/lexer/AST/HIR/MIR/
  verified-RIR/O2-JIT/owned-config/public-CLI provenance and exact pre-request
  `Backend: io_uring\n` selection. For the exact 60-byte bodyless GET, W1 is the
  98-byte complete 200/CL12 header plus `hello`, W2 is `!` about +600ms, and W3
  is `world!` another about +600ms later. Each application-publication gap is
  `[550ms,750ms)`; the pre-W3 zero-byte/open observation crosses W1+1.10s and
  W2+550ms, without claiming TCP/read/CQE segmentation. Both sides then emit
  the strictly equal Date-normalized 134-byte complete `hello!world!`
  keep-alive response within W3+750ms and within 350ms of each other, remain
  open/quiet for only 200ms, and record one exact canonical authority-rewritten
  upstream request, one accept/request, three publications, active origin close,
  175ms no retry and exact `60\n` live/after-client-close/after-shutdown ledgers.
  There is no downstream EOF, reuse or successor claim. Oracle direct 3/3 plus
  registered/adjacent checks, paired direct 5/5 plus registered/oracle/adjacent,
  focused runtime 5/5 (1268 checks), independent approvals and rebased direct
  passes succeeded. CI `34010356587` and `34011913768` each passed 15/15. This
  proves only exact completion; third-still-incomplete progress and broad
  #253/#268/#270/#271 remain open. The runtime consumes generated ordinary RUT,
  never `nginx.conf`.

- PR #510 (commits `2d0d8951`, `a6ad1c0b`, accepted head
  `02b5ae0f64339dde1ca1581b0c3a1b05b88b93c9`, merge
  `5e2b5f0491c3c29ff519760d73084223128d089b`) proves only the exact
  default-buffered second-body-progress schedule declared above. Independently
  scheduled W1 is the 98-byte complete 200/CL12 header plus `hello`; W2 is one
  `!` about 600ms later. Both pinned nginx 1.29.7 and converter-generated
  ordinary RUT remain quiet beyond W2+700ms and W1+1.25s, then terminate from
  W2 around 1.001s with the equal normalized 122-byte header-only wire, real
  downstream EOF, active origin close, one exact authority-rewritten upstream
  request, no retry and stable `60\n` access ledgers. Pinned oracle 3/3, paired
  direct 5/5, registered 1/1, adjacent 2/2 and focused runtime 3/3 passed; an
  independent review first requested the exact pre-request
  `Backend: io_uring\n` proof and then approved it. CI `34005174611` passed
  15/15. Other progress schedules and broad #253/#268/#270/#271 remain open.
  This is translation evidence: the runtime consumes generated ordinary RUT,
  never `nginx.conf`.

- PR #469 (accepted head `9f01f9f3`, merge `f896924c`, final CI run
  `33952237545`, 15/15 green) closes #468 with the exact fixed-CL12 HEAD success
  differential. Pinned nginx 1.29.7 and converter-generated ordinary RUT both
  withhold origin effects until the split 5+7 binary upload completes, then
  produce the exact Host → reconstructed Content-Length → retained headers →
  body upstream wire, normalized header-only 200, downstream keep-alive and
  prompt one-origin retirement without retry. Independent review approved. The
  nginx side retains omitted/default buffering while generated RUT uses
  buffering `None`; PR #491 separately proves only the exact silent-origin
  timeout boundary, and PR #493/#494/#495 separately prove only the exact
  one-fragment boundary described below, while PR #493/#498/#499 prove the
  separate exact two-fragment schedule. Other fragment contents, timings or
  counts and broader #268/#270/#271 semantics remain open. This is translation
  evidence: RUT never reads nginx.conf directly.

- PR #493 (accepted head `e1ccdfc4`, merge `ad9f1a1d`, CI `33975779221`), PR
  #494 (accepted head `8b351c58`, merge `b3932fed`, CI `33976404460`) and PR
  #495 (accepted head `9db3a948`, merge `e84a503e`, CI `33978473866`, 15/15
  green) form the runtime → public ordinary-RUT → pinned-nginx differential
  chain for the exact fixed-upload HEAD ID2 one-fragment boundary. After the
  5+7 binary CL12 upload, exactly one valid 41-byte incomplete response header
  arrives about 400ms later; both nginx 1.29.7 and converter-generated ordinary
  RUT retain the initial one-second deadline, emit the exact normalized 162-byte
  header-only keep-alive 504, retire origin 1, then serve the same-fd successor
  through a fresh origin with the exact normalized 121-byte HEAD 200. This does
  not support repeated/different or multi-fragment progress, and it does not
  generalize nginx's default buffering: generated RUT uses buffering `None`.

- PR #493 (accepted head `e1ccdfc4`, merge `ad9f1a1d`, CI `33975779221`), PR
  #498 (accepted head `724ff9fe`, merge `3e890d49`, CI `33982305303`) and PR
  #499 (accepted head `5c08f7c1`, merge `6a01be7f`, CI `33984219762`, 15/15
  green) form the internal runtime → public ordinary-RUT → pinned-nginx
  differential chain for one separate exact two-fragment schedule in the same
  fixed-upload HEAD ID2 profile. After the completed 5+7 binary CL12 upload, the
  origin writes exactly the valid 41-byte
  `HTTP/1.1 200 OK\r\nX-Progress: fixed-head\r\n` prefix around +300ms and the
  valid 28-byte `X-Second: still-incomplete\r\n` continuation around +700ms.
  Both nginx 1.29.7 and converter-generated ordinary RUT retain the initial
  one-second deadline, emit the strictly equal normalized 162-byte header-only
  keep-alive 504, retire origin 1 without retry, and serve the same-fd successor
  through a fresh origin with the normalized 121-byte HEAD 200. This does not
  support other fragment bytes, timings or counts and does not generalize
  nginx's default buffering; generated RUT uses buffering `None`.

- PR #450 (accepted head `86464c9a8afd83ebdc03da7c76076ec1ff57f8b3`,
  merge `e61b0390b99724ac63e3af29b552b1bc8be9f2be`, exact-head CI
  `33863884622`, 15/15 green) activates the bounded fixed-upload HEAD profile in
  normal production dispatch for exact HEAD and method-omitted ANY. Partial and
  initially complete positive fixed-Content-Length uploads retain exact route,
  bundle, handler, policy and byte ownership, invoke the handler once, arm the
  deadline only after the rebuilt upload completes, and reuse the staged strict
  success/configured failure/timeout/same-batch proofs. Unsupported framing,
  protocol, mutation, pipeline and identity shapes remain fail closed. This does
  not add public ordinary-source or converter evidence; matrix statuses therefore
  remain unchanged and #268/#271/#270 stay open.

- PR #452 (merge `8908f2e2d1eea58abede24e16c3207d73921604d`, accepted head
  `09b045abc2178cf3738319fe3a1dc89fe330b6b4`) now adds bounded converter
  lowering for explicit root `proxy_read_timeout 1s..63s` across the
  already-supported HEAD/GET/method-omitted-ANY bundles, with focused ownership,
  policy mapping, capacity and fail-closed mutation coverage. CI run
  `33876573639` passes the 15 standard jobs (privileged broker skipped). At that
  relay no pinned-nginx/generated-RUT timeout differential existed; PR #462 now
  supplies one for the narrower declared HEAD profile, while the row remains
  `PARTIAL`, not `SUPPORTED`.

- PR #453 (accepted head `06d71e52`, merge `2315c18e`, exact-head CI 15/15) is
  complete historical test-only infrastructure/profile evidence: zero-response
  Recorder stall mode and an explicit-timeout generated-source/profile
  self-check. Its initial no-upstream limitation was later resolved by #455/#457;
  no compatibility promotion followed.

- PR #455 (commit `e605f7f1`) proves the generated ordinary-RUT episode with
  exactly one 58-byte upstream HEAD request and no origin sends or retry.

- PR #457 (commit `f9b58ddc`) publishes the live HeaderOnlyHead explicit-close
  zero-stall 504 path with exact header-only 504/EOF behavior. Together with
  #455, this establishes the bounded admission/ownership and wire episode.

- PR #460 (merge `b7cfa699`) connects the precise response-read io_uring timer
  to the narrow cleartext H1.1 bodyless `HeaderOnlyHead` + `Http11FixedStrip`,
  generated ordinary-RUT response buffering `None`, fresh-single-IPv4-upstream,
  explicit-downstream-close profile. The matching pinned-nginx configuration
  omits `proxy_buffering` and uses nginx's default buffering.
  It retains generation-owned timer/CQE custody, response/timeout batch ordering,
  exact configured 504 publication and fail-closed exclusions without adding an
  nginx-specific runtime mode.

- PR #463 (merge `3d009441`) closes #461 for that same profile: accepted
  incomplete upstream response-header progress retains the proof required for a
  later due timer to publish the configured header-only 504, while preserving
  the pinned initial deadline origin, timer custody and response precedence.

- PR #462 (merge `46815019`) adds the real pinned-nginx 1.29.7 versus
  converter-generated ordinary-RUT phase differential. The pinned nginx
  configuration omits `proxy_buffering` and uses nginx's default buffering;
  the generated ordinary-RUT response policy uses buffering `None`. Zero
  progress measures about 1.00140s/1.00082s (nginx/RUT), and the delayed
  incomplete-header phase about 1.00145s/1.00081s from complete upstream
  request; both produce the exact Date-normalized header-only 504/zero-body/EOF.
  A complete HEAD response is the exact 200 control before the timeout floor.
  All phases prove one exact upstream episode/no retry and only the declared
  origin sends. This closes the historical near-2s precision gap only for the
  declared profile; #268/#270/#271 remain open and the matrix row remains
  `PARTIAL`.

- PR #458 (merge `61b80933`) historically introduced the precise response-read
  io_uring timer primitive as inert scaffolding. PR #460 subsequently connected
  it to the narrow live proxy profile, and PR #462 now supplies the phase-varied
  behavior evidence described above.

- PR #451 (accepted head `7228f3a0d4d4ff55a0bd1bb395bf7475587ef6ee`, merge
  `b0eb92dc725a799a5aba51fdcf47588f9081bcfc`, exact-head CI `33869863683`,
  15/15 green) adds tests-only public ordinary-source evidence for the bounded
  fixed-upload HEAD profile: genuine source reaches parser/HIR/MIR/RIR/JIT/
  io_uring normal dispatch for exact HEAD and method-omitted ANY, with partial
  and initially complete positive fixed-Content-Length uploads. It proves
  exactly-once dispatch/materialization/upload and deadline arming only after
  upload completion. No converter lowering or differential was added by #451;
  #452 subsequently supplied the bounded lowering. PR #462 later supplied the
  bodyless explicit-close HEAD differential, while the fixed-upload and broader
  #268/#271 domains remain open.

- PR #449 (accepted head `18985dd9a94501610b3667a48a17eb53c8c2962b`, merge
  `499bc405d02b859a0465b8ccc2d462a7b698f6a7`, exact-head CI `33857715293`,
  15/15 green) adds tests-only Stage 2C2b3 evidence that a current fixed-upload
  HEAD/ANY response wins a same-batch expiry independent of event array order.
  Live and terminal positive Recv cover strict 200 and valid-status configured
  header-only 502; incomplete live progress refreshes, incomplete/invalid terminal
  input fails closed, and timeout-only controls retain configured header-only 504.
  The existing batch ledger required no production change and no new RUT issue.
  This does not activate the profile, prove public ordinary-source behavior or add
  converter lowering, so all compatibility statuses remain unchanged.

- PR #448 (accepted head `0b23e428b7dab71c20ac9a87768e157721b9ff22`, merge
  `7343364d0aa9863b2033dcc45c7c80211407a944`, exact-head CI `33835342238`,
  15/15 green) records terminal-positive Stage 2C2b2 evidence for the classifier-
  unreachable fixed-upload HEAD profile. An exact per-dispatch witness admits
  zero-target D2 custody for current terminal Recv: strict HTTP/1.1 200 with one
  positive Content-Length produces header-only success, while complete valid
  HTTP/1.x parse/unsupported witnesses may produce the pinned configured header-
  only 502. Invalid/statusless/HTTP/0.9, incomplete, body-overrun, transport-error
  and mutated witnesses fail closed; same-batch live-plus-terminal fragments,
  late CQEs, successor custody and downstream send failures are covered. The
  initial sanitizer-only fixture double cleanup was corrected without production
  changes and the exact-head rerun is fully green. This internal staging evidence
  does not activate the profile, provide public ordinary-source behavior or add
  converter lowering. #452 subsequently supplies bounded lowering; timeout
  behavior differential evidence remains absent. The buffering row therefore
  remains `BLOCKED_BY_RUT`, proxied response behavior remains `PARTIAL`, and
  `proxy_read_timeout` remains `PARTIAL`.

- PR #447 (accepted head `97fadbfe04609e74c045512678d412a0318439ce`, merge
  `6d0ab94c17e0f6f8e2fde875a2616f8f5b109b23`, exact-head CI `33830389147`,
  15/15 green) records live-only Stage 2C2b1 evidence for the classifier-
  unreachable fixed-upload HEAD profile. A complete valid HTTP/1.x status-line
  witness permits valid-status parse/unsupported domains, including unsupported
  framing, to map to configured header-only 502; strict 200 positive-
  Content-Length success wins. Invalid or status-line-less HTTP/0.9, terminal
  configured responses, and proof/event/output identity mutations do not publish
  configured 502, while fragmented live partial input safely continues. This
  internal staging evidence does not prove terminal Recv handling or
  timeout-vs-response same-batch precedence, and does not activate the profile
  or add converter/public ordinary-source lowering. #452 subsequently supplies
  bounded converter lowering; timeout behavior differential evidence remains
  absent. The `nginx default proxied response buffering/commit` row therefore
  remains `BLOCKED_BY_RUT`; proxied response status/body and header policy remain
  `PARTIAL`, and `proxy_read_timeout` remains `PARTIAL`.

## Semantic risks already observed

- nginx route matching normalizes percent escapes, dot segments, and (by
  default) repeated slashes. RUT routing has different canonicalization rules.
- Non-origin-form request targets receive no RUT canonical path and miss the
  generated root route. Generated programs now install bounded generic unmatched
  policies instead of reaching the legacy fixed `200 OK`. Pinned nginx 1.29.7
  rejects `OPTIONS *` with an exact local 400 and authority-form CONNECT with an
  exact local 405, both with zero upstream effects. #273 now has an independently
  approved generic method-keyed
  strict-local-response design with legacy-default safeguards. Compiler metadata,
  deep-owned config, H1 dispatch, and configured-H2 fail-close are active. The
  OPTIONS-star and authority-form CONNECT ordinary-source production wires and
  converter lowering/golden/RouteConfig ownership tests are now committed. The
  OPTIONS-star converter-generated differential is also committed, so that
  exact OPTIONS-star and CONNECT converter-generated differentials are also
  committed, so both bounded rows are `SUPPORTED` and #273 is closed.
- With `location /api/` and a `/` URI in `proxy_pass`, pinned nginx sends `/`,
  `/x`, and `/x?y=1` upstream for `/api/`, `/api/x`, and `/api/x?y=1`.
  It redirects `/api`, collapses repeated slash/dot-segment inputs, and decodes
  `%7E` to `~` before replacement. Closed #259 provides only a clean,
  fail-closed first transform slice; normalization and slash redirect behavior
  remain outside it, so #258 cannot become broadly `SUPPORTED` from clean-vector
  evidence alone.
- The automatic slash-redirect `SUPPORTED` row is limited to GET, cleartext
  H1, explicit close, and the exact accepted `/api/` model. It does not cover
  HEAD/POST, keep-alive Redirect, TLS/H2, arbitrary prefixes, or broader nginx
  URI normalization.
- nginx prefix matching is byte-prefix based; RUT routing is segment-aware.
  The root `/` case overlaps, but broader prefix support cannot reuse that fact.
- PR #455/#457 establish the narrow `HeaderOnlyHead`/`Http11FixedStrip`
  admission, downstream-close ownership and zero-stall wire episode. PR
  #460/#463 connect and complete the precise live timer path for this profile,
  and PR #462 proves its zero-progress, delayed-incomplete-header and complete-200
  phases against pinned nginx. PR #465/#467 extend and prove the exact reusable
  downstream HEAD slice; PR #469 separately proves the fixed-CL12 HEAD success
  boundary without proving timeout expiry. Pinned nginx omits `proxy_buffering`
  and uses its default, while generated ordinary RUT uses response buffering
  `None`; none of these bounded claims extends to broader methods, bodies,
  buffering, reuse/retry, transports or lifecycles.
- `OPTIONS *` and ordinary authority-form CONNECT do not have a canonical
  origin-form path in current RUT. They miss the converter's method-omitted `/`
  route, then select the generated exact OPTIONS/CONNECT policy before the
  bounded ANY fail-closed policy. Separate converter-generated differentials now
  prove both exact bounded rows. This does not make CONNECT tunneling or generic
  request-target handling supported.
- Complete response buffering currently admits downstream HTTP/1.1 only. The
  accepted nginx configuration syntax does not exclude HTTP/1.0 clients, so the
  omitted-buffering row remains blocked for that version domain as well.
- Pipelined validated-failure support has three distinct tracks. Closed #276
  owns successor bytes that arrive after request 1 is admitted but before its
  502 Send completes, with deterministic and raw-SQ/CQ production evidence into
  ordinary JIT dispatch. Closed #277 admits one bodyless exact strict GET
  already coalesced with one exact-local successor and proves the bounded slice
  through deterministic, ordinary-source production, and converter-generated
  causal nginx/RUT evidence; broader coalesced domains remain excluded. Closed
  #278 owns one exact strict-policy admission and
  execution slice after a preserved successor is dispatched at depth one; its
  separate bounded `SUPPORTED` row claims only the causal late-arrival GET
  vector. The earlier sequential `SUPPORTED` GET row claims none of these cases.
  Closed #285 now enforces RUT's declared maximum HTTP/1 pipeline depth: exactly
  16 successor transitions are admitted and the attempted 17th fails closed
  before dispatch or request-side effects. This generic runtime invariant does
  not broaden #278's exact depth-one compatibility claim.
- The #277 E3 raw-CQ gate failed closed once with an unclassified `ERROR_CQ`
  before its stricter RUT side was scheduled first on the freshly allocated
  ports. No response mismatch or false positive occurred; the final focused
  differential passed 10 consecutive Sol runs and the full differential passed.
  Treat recurrence as a harness diagnostic risk and preserve the offending CQE
  identity before changing any fail-closed rule.
- Pinned nginx 1.29.7 treats one valid `Content-Length: 0` on the bounded fresh
  exact-local POST as the same zero-length request behavior as an absent
  Content-Length. RUT now preserves field-count identity and admits exactly one
  validated numeric-zero POST under the strict fresh/close/framing fences; an
  ordinary-source production wire proves the generic capability, so #291 is
  closed. #292 closes the separate converter-generated differential for the
  literal single-CL0 slice.
  Duplicate/conflicting CL, CL+TE, positive bodies, extra bytes,
  Expect/Upgrade, reuse, pipelines, and alternate-zero compatibility claims
  remain excluded.
- The literal query-bearing exact-local claim is only `GET /static?x=1` (#293).
  It proves that this query does not alter the exact `/static` location choice
  or static return wire. Empty, multiple, repeated, or differently spelled
  queries; encoded delimiters; URI normalization; fragments/absolute form;
  `$args`, `$request_uri`, and other variables; rewrite/internal redirects;
  proxy query forwarding; reuse/pipeline; and broader locations remain outside
  that `SUPPORTED` row.
- The exact-local OPTIONS claim is only the fresh header-absent explicit-close
  origin-form `OPTIONS /static` vector (#294). It proves the static return emits
  its full body and no extra header. OPTIONS-star, CORS/preflight, general
  `Allow` synthesis, proxy or query-bearing OPTIONS, request framing/body,
  reuse/pipeline, other paths/methods, TLS/H2, and broader method semantics are
  excluded.
- Ordinary JIT local responses now use one parser-proven HTTP/1 request boundary
  and client persistence decision for their wire framing and lifecycle (#280).
  Their text body literal still retains escape spellings such as `\\n` instead
  of providing an exact decoded-byte form (#279). Converter lowering does not
  hide that remaining limitation; the current #276 wire uses an unescaped body.
- The post-#273 audit identified an exact-HEAD streaming, exact-bodyless-GET
  complete-buffering, method-omitted streaming split. Its first lowering attempt
  compiled correctly but exposed two generic runtime gaps: an initially complete
  fixed-CL response plus same-batch clean EOF originally closed before settlement
  (#271), and validated connect failures bypassed the configured default 502
  (#267). Both gaps now have deterministic and ordinary-RUT public evidence;
  canonical request-boundary response neutrality and a sequential async
  unavailable-upstream wire closed #275. The exact split was restored in
  `caa9fce3`. Its converter-generated completion and strictly sequential gateway
  differentials are committed, so the exact bounded row is now `SUPPORTED`.
  Broader pipelined behavior remains excluded; closed #276 proves its late-
  arrival 502-to-ordinary-local slice, closed #277 proves only its narrow
  pre-admission root-GET-to-exact-local-GET phase, and closed #278 proves only the separate
  exact depth-one same-strict-policy row. `/api/` remains separately blocked
  because complete buffering rejects target transforms.
- A `proxy_pass` without a URI suffix must not accidentally forward the
  normalized routing path instead of the original request target.
- nginx proxy request defaults are version-dependent beginning at 1.29.7.
- Default request and response hop-by-hop/header behavior is not transparent
  byte forwarding.
- Pinned nginx 1.29.7 forwards an explicit-close HEAD upstream as HEAD and
  retains an upstream `Content-Length: 5` in the downstream headers while
  emitting no body bytes and closing the client. Public RUT source can now
  select only a paired success/failure disposition for this bounded slice, and
  source→JIT→production success/failure wires are proven, and the root converter
  now emits the method-isolated pair. The pinned nginx-vs-generated-RUT
  differential proves the exact Date-normalized downstream response, EOF, and
  rebuilt upstream HEAD request for this explicit-close slice. #253 remains
  `PARTIAL` because RUT keep-alive HEAD support and broader response domains are
  absent.
- Pinned nginx also suppresses the 157-byte generated 502 body for an
  explicit-close HEAD when the single upstream connect fails, while retaining
  `Content-Length: 157`. RUT now carries a public paired HEAD disposition in
  response and failure metadata. Legacy policies remain
  `Reject`; only paired `SuppressBody` enters a bounded fresh-connect path that
  emits header-only strict success or configured 502, forces EOF, and never
  pools/replays the upstream. Mismatches fail before upstream effects. Public
  source selection enforces the pairing, its production path is proven, and the
  root converter emits it only on HEAD. The pinned generated-RUT differential
  now matches the exact header-only 502/EOF baseline while a single reserved
  non-listening endpoint is held across both implementations; it does not claim
  an unobserved RUT syscall count.
- Pinned nginx keep-alive HEAD behavior is now exact: a default-keepalive HEAD
  emits a header-only 200 retaining `Content-Length: 5` and keeps the downstream
  quiet/reusable; a second close-intent HEAD emits the exact close headers and
  EOF. nginx uses two distinct upstream connections and sends one exact rebuilt
  HEAD wire on each. With the upstream unavailable, the first header-only 502
  retains `Content-Length: 157` and downstream keep-alive, while the second
  close-intent request emits the exact close 502/EOF and produces the second
  scoped connect-failure record. Public RUT now admits this bounded reusable
  success/connect-establishment domain after #264. The committed pinned
  converter-generated differential now matches both two-request success and
  unavailable-upstream response vectors, fresh upstream wires, quiet window,
  request-derived persistence, and final EOF. The row remains `PARTIAL` because
  the dynamic failure domain below is not yet expressible.
- Pinned nginx 1.29.7 also keeps a default-keepalive HEAD reusable after a read
  timeout, malformed upstream response header, or incomplete-header EOF. It
  emits a header-only 504 with `Content-Length: 167`, or a header-only 502 with
  `Content-Length: 157`, then accepts a second close-intent HEAD on a fresh
  upstream connection. Closing before bytes is fail-closed but not equivalent.
  #265 now has D1 exact-owner Connect/Send/Recv retirement, the D2 internal
  prebuilt-header/request-epoch rendezvous, and one narrow production caller for
  non-empty malformed reads or incomplete-header terminal EOF/error. Internal
  tests prove transactional failure-header construction, live-Recv and
  zero-owner handoff, both completion orders, batch-end-only request-2
  publication, phase-bound buffer normalization, epoch/config pinning, and
  close/reclaim behavior. A public-source production-io_uring real-socket test
  proves RUT's invalid-status-prefix/open-peer wire: exact header-only keep-alive
  502, quiet boundary, old-origin closure, fresh request-2 origin and exact close
  response/EOF. It is runtime evidence, not nginx equivalence: nginx applies the
  #266 HTTP/0.9 fallback to that wire. The test also does not claim to observe
  kernel `CQE_F_MORE`; focused tests supply exact live-Recv ownership evidence.
  A second gated public test proves incomplete positive bytes remain quiet until
  origin EOF, then exercises the exact reusable 502 and fresh request-2 close
  wire; focused tests supply exact zero-owner evidence. The first
  converter-generated parser-failure differential now matches pinned nginx for
  a valid HTTP/1.1 status line followed by an invalid empty-name header, including
  exact response/request wires, quietness, origin closure, freshness, and EOF.
  A second converter-generated differential matches pinned nginx for an exact
  positive incomplete HTTP/1.1 header held open through a quiet window and then
  closed only by a test-authorized origin gate. Both sides emit the exact
  reusable header-only 502, preserve the same downstream connection for request
  2, use a fresh origin connection with byte-identical request wire, and finish
  with the exact 200 response plus downstream EOF. Recorder history is compared
  only after join, and abort acknowledgement precedes proxy teardown, so cleanup
  cannot manufacture the tested EOF.
  An invalid status prefix is different: nginx falls back to HTTP/0.9-style
  close-delimited behavior while RUT rejects immediately, so #266 tracks that
  real gap. #267 now has a deliberately narrow runtime selection path: an
  ordinary-RUT timeout policy survives compiler/config/JIT and request pinning,
  then the exact live io_uring Recv owner may transactionally emit it only for
  cleartext H1.1 bodyless default-keepalive paired `HEAD`, using #265 D1/D2.
  Focused production-transition tests reach that boundary through real JIT,
  Connect, Send, backend dispatch, and TimerWheel state. A public ordinary-RUT
  real-socket test now proves exact configured header-only 504/CL167 output,
  RUT-owned origin closure, downstream reuse, a fresh request-2 origin, and the
  exact 200/close completion. It accelerates the global runtime clock only
  before shard spawn and therefore does not prove #268 precise source-level
  duration semantics; #452 supplies bounded converter lowering, and PR
  #465/#467 later replace that public-clock limitation with the precise pinned-
  nginx timing differential for the reusable paired-HEAD profile. PR #462
  separately proves the explicit-close profile.
  Every excluded phase/layout,
  including epoll, remains zero-byte fail-closed. #268 separately tracks
  per-forward read-timeout timing semantics. The broader
  failure domain stays unsupported and does not
  upgrade
  #264's narrower normal-success/connect-establishment slice.
- The reusable HEAD gap previously depended on the generic epoll transport
  capability tracked in #262. Epoll now exposes at most one logical completion
  per wait
  with bounded pending-versus-kernel fairness, removing the former whole-batch
  pre-dispatch I/O window. It now also transports explicit upstream episodes and
  rejects stale or malformed records before fd-map/socket/TLS/state access and
  before concrete callback dispatch. Centralized production owners now begin,
  detach, retire, or quarantine fresh, retry, pooled, health-probe, timeout, and
  terminal episodes. Real tests prove pool return/borrow with the same numeric fd
  and terminal close/allocation with the same Connection slot: old readiness does
  no socket I/O and old completion dispatch does no timer/callback/state mutation,
  while current-token controls progress. A production failed-connect retry also
  proves episode 1 is detached/retired before episode 2 begins on the same
  Connection and numeric fd; replaying its old failure cannot mutate the replacement,
  while the current completion sends the exact request to a real peer. Two real
  active-health probes additionally prove production begin/terminal retirement,
  same-slot/same-fd reprobe, stale-completion isolation, and a current-token wire
  control. A production-owned active-episode regression now also proves upstream
  quiesce removes genuine HUP readiness while owner/token/map remain current, then
  retires cleanly. A kernel-produced raw connect record, held in the test harness,
  is also rejected before I/O after production same-slot/same-fd reuse while the
  current completion progresses. A second captured kernel record proves the same
  boundary for receive: the episode-2 bytes remain unread after stale replay,
  then the current record reads the exact payload and dispatches once. A third
  captured kernel record proves the boundary for a genuinely backpressured
  partial send, including zero old wire bytes and exact current payload through
  FIN/EOF. The requirement-by-requirement audit proves all five capability and
  nine acceptance bullets, so #262 is closed. These tests explicitly hold raw
  records already harvested into userspace and do not claim kernel retention
  after successful `EPOLL_CTL_DEL`. This transport work alone does not imply
  keep-alive HEAD support by itself. The io_uring episode cancellation/drain and
  deferred request admission work tracked in #264 first protected the existing
  explicit-close strict-abandon path, including cancel retry, token exhaustion,
  provided-buffer isolation, and deferred close/reclaim. Its second bounded
  runtime slice now completes request-1 bookkeeping before parking the generic
  HTTP/1 boundary, buffers request-2 bytes without parse/route/timer/callback
  effects, and resumes exactly once after the retirement-final CQE batch. Exact
  successor ownership also fences provided-buffer/partial-send mutation and
  survives close through a per-type target/cancel ledger. A third bounded runtime
  slice persists and
  atomically replaces the latest fully drained tombstone, proving two production
  strict callbacks, two batch-end boundary resumes, older-record isolation,
  close/reclaim composition, and token-exhaustion quarantine. The final bounded
  public slice now admits default-keepalive paired HEAD, proves a real two-request
  io_uring success path plus reusable connect-establishment 502, and fails broader
  paths closed before bytes. #264 is closed, and converter-generated nginx
  differentials now prove its bounded success/connect-failure domain. The
  remaining #265 response semantics stay under #253.
- The first request-policy slice rejects body/framing inputs (including
  `Content-Length` and every `Transfer-Encoding` value), HTTP/2, and
  non-origin-form targets before upstream connect. These are intentional
  fail-closed `PARTIAL` limits; nginx absolute-form/body normalization remains
  a follow-up.
- Pinned nginx 1.29.7 waits for a fixed-Content-Length request body to complete
  before accepting the upstream connection under the minimal default config.
  The planned RUT body slice must therefore stage the complete body before
  upstream slot/connect; transparent early-connect streaming is not equivalent.
- The first RUT body increment now stages a complete fixed-CL request within the
  existing 16 KiB connection slice, preserves a pipeline suffix, and tracks
  local buffering separately from successful upstream upload. Exact-cap and
  cap+1 behavior are tested. Larger bodies and file-backed buffering remain
  unsupported, so the feature stays `PARTIAL`.
- A converter-generated split binary POST now matches pinned nginx in exact
  upstream bytes and Date-normalized downstream bytes. Both delay backend
  accept until the complete body is locally available. This proves only the
  bounded fixed-CL vector, not larger or alternate-framing bodies.
- Valid downstream Upgrade requests are intentionally rejected before upstream
  connect in this header-only slice; nginx 1.29.7 strips and proxies Upgrade,
  so that behavior remains PARTIAL.
- The converter-generated differentials cover an origin-form header-only GET
  and a split fixed-CL binary POST with a query, custom Host, duplicate ordinary
  request headers, one final
  exact-Content-Length upstream response, custom reason, hidden/synthesized
  headers, duplicate response headers, and preserved trailing value whitespace.
  Dynamic Date is the only normalized response field. The `%7E`
  encoded-unreserved vector is now a committed serial pinned-nginx CTest; it
  preserves the complete raw upstream target and matches the downstream
  response. Encoded slash/dot/malformed cases remain open.
- Pinned nginx consumes an upstream `Connection` header for absent,
  `keep-alive`, `close`, and token-list vectors, synthesizes one downstream
  connection field, and preserves token-nominated ordinary response headers.
  The bounded strict serializer matches one such field and rejects duplicates;
  other hop-by-hop response behavior remains unclaimed.
- Pinned nginx success responses follow the downstream HTTP/1.1 request intent:
  default/explicit keep-alive remains reusable, while explicit close emits
  `Connection: close` and EOF after the body. Converter-generated RUT now matches
  the close vector and continues stripping the client Connection upstream.
- For one unavailable fixed upstream, pinned nginx waits for the complete
  fixed-CL request and emits a 157-byte HTML 502 with Server, Date,
  Content-Type, and request-derived keep-alive. Before #256, RUT emitted an
  11-byte close response; converter-generated RUT now matches the complete
  response after normalizing only Date, preserves keep-alive for a second
  request, closes on explicit client intent, and waits through the split-body
  window. The explicit-close 502/EOF vector is now a committed serial CTest.
  The claim remains limited to the bounded H1 single-IPv4 connect-failure
  domain.
- Response policy is intentionally limited to bounded non-HEAD HTTP/1.1
  requests plus one bodyless explicit-close HEAD slice, and one final HTTP/1.1
  response with exact Content-Length. Keep-alive HEAD, Upgrade, absolute-form
  targets, HTTP/1.0, HTTP/2, interim/no-body statuses, chunking, close-delimited
  framing, and response mutations remain fail-closed PARTIAL limits.
- RUT's current unmatched-route response is not nginx's normal 404 behavior.
